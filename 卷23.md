原则：
- 保留前卷硬约束与结论，不改变任何已锁数字与逻辑。
- **活动优先，记录忠实**：$n_k$ 是交汇翻转在支集上的事件计数记录，不是先验整数表；每一步先还原「走到哪、为何必须多走」，再认符号。
- 本卷一条路走完：支集 $D_k$ → 三约束分刀 → $n_1=4$ → $n_2=13$ → $n_3=30$。逐步统计，不跳算。
- 与第四卷分座：本卷只闭事件计数；$S_1$、$q$、轻子 MeV 仍在第四、五卷。
- 禁止：$\binom{3}{k}$ 当 $n_k$；$\{0,1,2\}$ 读成三态；把 $17/9$ 写成夸克比；把「分类相加」读成凑整数。

---

# 第二十三卷　事件计数：从支集到 $n_k=4,13,30$

**本卷要解决的问题：**  
第四卷已钉 $n_k=4,13,30$，但读者最容易停在「$9+4$」「$27+3$」两句硬贴上，说不清每一刀在数什么活动。本卷**单独走完事件计数的完整统计过程**：支集是位置格、事件不是类型、三条约束如何分刀启动、$k=1,2,3$ 各自付哪些步、哪些假计数必须排除。

本卷只问一句：**一次完整交汇翻转，在支集上要付多少事件——怎么一刀一刀数出来。**

走到尽头：

$$
\boxed{n_{1}=\mathrm{L1}+\cdots+\mathrm{L4}=4;\quad
n_{2}=\mathrm{C1\text{–}C9}+\mathrm{B1\text{–}B4}=13;\quad
n_{3}=\mathrm{V1\text{–}V27}+\mathrm{K1\text{–}K3}=30}
$$

每一个整数背后都有**点名标记**：L＝线上四步，C＝九宫位置覆盖，B＝面边界四边，V＝体位置覆盖，K＝色循环三步。抬杠只许指着某一个标记。

四件不要并：

| 本卷有 | 本卷没有 |
|:---|:---|
| 位置格 $D_k=\{0,1,2\}^k$；事件＝覆盖/闭链/色锁步数 | $\{0,1,2\}$＝三态；$\binom{3}{k}$＝$n_k$ |
| $n_1=4$ 最短闭覆盖逐步列出 | $n_1=3$ 开链；$n_1=\binom{3}{1}$ |
| $n_2=9+4$：覆盖与边界分付 | $9+1$；$2\times 4=8$ 双重边界 |
| $n_3=27+3$：满维不再 $+4$；色循环 $+3$ | $27+4$；把 $+3$ 抄到 $k=1,2$ |

---

```text
翻转只在交汇；k = 参与管数
        │
        ▼
支集 D_k = {0,1,2}^k   （位置，不是三态）
        │
        ▼
三条约束分刀
  ├─ 约束0 全覆盖：至少 |D_k| = 3^k
  ├─ 约束1 闭链：k<3 才启动边界修正
  └─ 约束2 色锁定：仅 k=3 付 +3
        │
        ▼
k=1：线 P_3 → 最短闭覆盖 4 步
k=2：九宫 9 + 嵌 n_1=4 → 13
k=3：体 27 + 色循环 3 → 30（不再 +4）
        │
        ▼
n_k = 4, 13, 30
```

## 读法与指引

**记录与活动：** $D_k$、$n_k$、闭链、色锁定都是记录。检验标准：能否还原到「在哪些位置上走了哪些步、为何少一步就漏 holonomy 或拆锁定」。

| 这里的词 | 读成 | 不要读成 |
|:---|:---|:---|
| 事件 | 支集上的一步覆盖／折返／色循环 | 粒子碰撞次数；类型数 $\binom{3}{k}$ |
| $\{0,1,2\}$ | 沿管的位置档位 | 不在／在／翻了 |
| 全覆盖 | 走遍 $D_k$ 每个位置 | 只翻子集也算完整 |
| 闭链 | 边界 holonomy 不漏到外部 | 任意回到起点的散步 |
| 色锁定 | 三管轮替净排列归零（仅 $k=3$） | 又一条线闭链 $+4$ |
| $n_k$ | 上述约束下的最小事件总数 | $3^k$；PDG 代编号 |
| 分类相加 | 同一完整翻转的几张必付账单（合取） | 先看好目标整数再拆项凑数 |

读完请能说出 boxed，并复述三行对照表。不回头改第四卷已锁的 $S_1$、$q$。

---

# 第一部　数什么：事件，不是类型

---

## 第一章　事件计数的对象

第四卷：翻转只在交汇；$k=$ 参与管数 $=$ 代（$k=1$ 最重）。本卷收录，不重证。

本卷统计的对象是：

> **一次完整翻转，在交汇支集上必须发生的最少步数。**

记为 $n_k$。

**不是**下面这些：

| 假对象 | 数出来是 | 为何不是 $n_k$ |
|:---|:---:|:---|
| 从三管选 $k$ 根的方式 | $\binom{3}{k}$ | 类型计数；$\binom{3}{1}=3\neq 4$ |
| 每个格点的「状态组合」 | $2^{3^k}$ 一类 | 格子是位置，不是三态 |
| 各管各自付一条线闭链再相加 | $k\times 4$ | 面／体是共同支集，边界不双重计数 |

$$
\boxed{\text{事件计数}\neq\text{类型计数}\neq\text{状态组合}}
$$

**严格度：算出（分座）。**

**断在哪：** $\binom{3}{k}$ 写进 $n_k$ 表。

---

## 第二章　支集 $D_k$：位置格

每根参与翻转的管，交汇支集沿该管是 **3 个相邻格点** $\{0,1,2\}$。横向厚度 $3$（面向种数的一维切片），不是纵向四步的 $4$。

$$
D_{k}=\{0,1,2\}^{k}\times\{0\}^{3-k}.
$$

| $k$ | 几何 | 位置清单（示意） | $\lvert D_k\rvert=3^k$ |
|:---:|:---|:---|:---:|
| 1 | 线 | $0,1,2$ | 3 |
| 2 | 面 | $(x,y)$，$x,y\in\{0,1,2\}$ | 9 |
| 3 | 体 | $(x,y,z)$，$x,y,z\in\{0,1,2\}$ | 27 |

未参与的管坐标冻在 $0$。原点 $p=0$ 属所有支集，是 $k=0$，六步链不进——本卷不用。

**禁止。** 不要把每点读成「不在／在不翻／在且翻」。坐标是位置档位；$n_k$ 数的是这些位置上的覆盖与闭合事件。

**严格度：算出（$\lvert D_k\rvert=3^k$）。**

**断在哪：** $\{0,1,2\}$ 读成三态 → 事件数变成状态组合，闭链那一刀没有座位。

---

# 第二部　三条约束：分刀，不并句

---

## 第三章　约束 0：全覆盖

锁定 ⇒ 完整事件。必须走遍 $D_k$，不能只翻子集。

$$
\text{代价下界}=\lvert D_k\rvert=3^{k}.
$$

| $k$ | 全覆盖下界 |
|:---:|:---:|
| 1 | 3 |
| 2 | 9 |
| 3 | 27 |

这一刀只保证「每个位置被走到」。**不保证**路径闭合，**不保证**三管轮替归零。

**断在哪：** 只数 $3^k$ 就宣称完整翻转——开链 holonomy 可漏到外部。

---

## 第四章　约束 1：闭链

第二卷：$H_P=-1$ 是拓扑不变量。第一卷：无外部可漏。翻转路径边界必须为零——开链不够。

**哪一刀启动：**

| $k$ | 闭链修正是否启动 | 原因 |
|:---:|:---:|:---|
| 1 | 启动 | 线开链无法闭合；必须折返 |
| 2 | 启动 | 面覆盖可开链漏到面外；须嵌最小面边界 |
| 3 | **不启动** | 体的边界已是闭合的面；满维无「外面的线边界」 |

满维再 $+4$，等于假装体还有一条要嵌的线边界——断。

**断在哪：** $k=3$ 仍抄 $+4$；或把闭链与全覆盖揉成一个数。

---

## 第五章　约束 2：色锁定

仅当 $k=3$（三管全参与）启动。管间循环

$$
e_{1}\to e_{2}\to e_{3}\to e_{1}
$$

不能产生净排列；否则锁定被拆成「某根管多转了一圈」。消掉净排列，最小是沿三色循环各补一步，共 $+3$。

| $k$ | 色锁定 | 原因 |
|:---:|:---:|:---|
| 1 | 0 | 另两管冻在 $0$，循环不闭合启动 |
| 2 | 0 | 仍有一根管未参与 |
| 3 | $+3$ | 三管全在，付色循环归零 |

$27$ 数的是格点走到没有；$+3$ 数的是**管与管之间**循环是否归零。两口不并。

**断在哪：** 把 $+3$ 抄到 $k=1,2$；或把 $+3$ 当成又一条线闭链 $+4$。

---

## 第六章　统计总账公式（先钉结构）

### 6.1 为何要「分类」再「相加」——不是凑数

有人一看 $9+4$、$27+3$，会说：「你们先分成两堆，再加起来凑出想要的整数。」必须先钉死：**分类与相加都不是修辞，是同一完整翻转的不同必要条件。**

**$n_k$ 在记什么。**  
$n_k$＝一次**完整**交汇翻转，在支集上必须付出的最少事件总数。  
「完整」＝三条约束**同时**满足（第二部）：全覆盖、闭链（该启动时）、色锁定（该启动时）。少一条，翻转就不完整——不是「少付一点也行」。

**为何要分类。**  
三条约束检查的是**不同的活动面**：

| 类 | 检查的活动面 | 问的问题 |
|:---|:---|:---|
| 覆盖（C／V／线的位置） | 支集上的**位置** | 每个位置走到了没有？ |
| 边界／闭链（B／L 的闭合） | 路径的**边界** | holonomy 漏到外部没有？ |
| 色锁定（K） | **管与管之间**的轮替 | 三色循环净排列归零没有？ |

分座是为了：**不许用「走遍格子」冒充「边界闭合」，也不许用「边界闭合」冒充「色循环归零」。** 分类＝防并句，不是为了好凑数。

**为何要相加。**  
完整翻转必须**同时**通过所有已启动的约束。每一类标记是「这一面必须付的步」；总事件数是各类的并——在计数上就是求和：

$$
n_{k}=\text{（覆盖类）}+\text{（闭链类，若启动）}+\text{（色锁类，若启动）}.
$$

| 读成 | 不要读成 |
|:---|:---|
| 同一翻转的几张必付账单，缺一张事就没做完 | 先看好目标整数，再拆成两项去凑 |
| 缺一类 → 翻转不完整（有明确失败模式） | 「多加几步比较好看」 |
| 哪一类不启动就记 $0$（如 $k=3$ 无 B） | 每一类都硬塞一点进去 |

**凑数检验（反证）。**  
若相加是凑数，则任意拆法都行，且缺一类也无所谓。事实相反：

- 只有覆盖、无闭链 → 开链，holonomy 可漏（失败模式钉死）；  
- 只有闭链、无覆盖 → 边界转圈但子集未翻（锁定不完整）；  
- $k=3$ 有覆盖、无 K → 色循环净余，锁定被拆。  

每一类都有**独立的失败模式**；相加是「全部失败模式都关掉」的总代价，不是往 $13$、$30$ 上靠。

### 6.2 结构式

三约束分刀后，事件总数的结构是：

$$
n_{k}=\lvert D_{k}\rvert+\Delta_{\text{闭链}}(k)+\Delta_{\text{色}}(k),
$$

其中

$$
\Delta_{\text{闭链}}(k)=\begin{cases}
1 & (k=1;\ \text{折返，使 }3+1=4)\\
n_{1}=4 & (k=2;\ \text{整条线闭链嵌进面})\\
0 & (k=3;\ \text{满维边界已闭})
\end{cases}
\qquad
\Delta_{\text{色}}(k)=\begin{cases}
0 & (k\lt 3)\\
3 & (k=3)
\end{cases}
$$

$\Delta=0$ 不是「这一项没凑上」，而是**该约束在本维已自动满足，账单为空**——与「硬塞一项」相反。

下面三部把每一刀的**逐步统计**写开，并在 $k=2,3$ 再次回答「为何 C+B、V+K 必须相加」。

---

# 第三部　$k=1$：$n_1=4$ 逐步统计

---

## 第七章　支集图：$P_3$

$k=1$ 支集是路径图 $P_3$：

```text
位置：  0 —— 1 —— 2
边：    e01    e12
```

只有两条边。全覆盖下界 $=3$（三个位置）。

---

## 第八章　排除：哪些走法不算

| 候选 | 步数 | 覆盖？ | 闭链？ | 判决 |
|:---|:---:|:---:|:---:|:---|
| $0\to 1\to 2$ | 2 边／3 点 | 是 | 否（开链） | 排除：holonomy 可漏 |
| $0\to 1\to 2\to 0$ | — | — | — | 非法：$2$ 与 $0$ 不相邻 |
| $0\to 1\to 0\to 1\to 2$ | 更长 | 可 | 视终点 | 非最短；且末仍开 |
| $0\to 1\to 2\to 1\to 0$ | **4** | 是 | 是 | **接纳：最短闭覆盖** |
| 更长闭走（如往返多次） | $\ge 5$ | 是 | 是 | 非最小，不取 |

---

## 第九章　接纳：最短闭覆盖四步

唯一最短合法路径——**4 个标记，每一记写明在数什么**：

| # | 标记 | 计数的是什么 |
|:---:|:---:|:---|
| 1 | L1 | 边步 $0\to 1$（离开起点） |
| 2 | L2 | 边步 $1\to 2$（走到远端） |
| 3 | L3 | 边步 $2\to 1$（折返：换成另一面回来） |
| 4 | L4 | 边步 $1\to 0$（回到起点，闭链完成） |

```text
L1：0 → 1
L2：1 → 2
L3：2 → 1   （折返）
L4：1 → 0
```

统计：

| 项目 | 标记 | 计数 |
|:---|:---|:---:|
| 位置覆盖（约束 0） | 走到 $0,1,2$（由 L1–L4 覆盖到） | 3 个位置 |
| 闭链折返（约束 1） | L3 使开链变闭链 | 总长成 4 |
| 色锁定（约束 2） | （无） | 0 |
| **合计 $n_1$** | **L1+L2+L3+L4** | **4** |

$$
n_{1}=\mathrm{L1}+\mathrm{L2}+\mathrm{L3}+\mathrm{L4}=4.
$$

折返不是另加一个孤立事件，而是「3 点开链要闭合，多走一次回来」。折返＝转向另一面，不是写成空白（第一卷差异纪律）。

这是「最小面边界长度 4」在一维线上的实现；后面 $k=2$ 的 B1–B4 就是把这四标记嵌进面。

$$
\boxed{n_{1}=4=\mathrm{L1}+\mathrm{L2}+\mathrm{L3}+\mathrm{L4}}
$$
**严格度：算出。** 不依赖 $S_1$、$q$、$m_0$。与第一卷 $c^{2}=-1$ 同级硬：走得通的最短闭覆盖就是 4。

**断在哪：** $n_1=3$（开链）；$n_1=\binom{3}{1}=3$（类型数）。

---

# 第四部　$k=2$：$n_2=13$ 逐步统计

纪律：下面 **13 个标记** 每一个都写明「在数什么」。抬杠时只许指着某一个标记说「这一条不该付」——不许笼统说「13 从哪来」。

---

## 第十章　支集：$3\times 3$ 九宫（先钉 9 个位置名）

$$
\lvert D_{2}\rvert=3^{2}=9.
$$

给每个位置一个固定名字，后面覆盖标记一一对应：

```text
P7=(0,2)  P8=(1,2)  P9=(2,2)
P4=(0,1)  P5=(1,1)  P6=(2,1)
P1=(0,0)  P2=(1,0)  P3=(2,0)
```

---

## 第十一章　第一笔：覆盖标记 C1–C9（共 9）

约束 0：每个位置必须被走到。**一位置 = 一标记**，不少不多。

| 标记 | 计数的是什么 | 对应位置 |
|:---:|:---|:---|
| **C1** | 走到位置 P1 | $(0,0)$ |
| **C2** | 走到位置 P2 | $(1,0)$ |
| **C3** | 走到位置 P3 | $(2,0)$ |
| **C4** | 走到位置 P4 | $(0,1)$ |
| **C5** | 走到位置 P5 | $(1,1)$ |
| **C6** | 走到位置 P6 | $(2,1)$ |
| **C7** | 走到位置 P7 | $(0,2)$ |
| **C8** | 走到位置 P8 | $(1,2)$ |
| **C9** | 走到位置 P9 | $(2,2)$ |

$$
\text{覆盖小计}=\mathrm{C1}+\cdots+\mathrm{C9}=9.
$$

**只付 C1–C9 不够。** 九个位置都可以走完，路径仍可以是开链（从一角进、从另一角出），$H_P=-1$ 漏到面外。第一卷：无外部可漏。故还必须付边界回路。

---

## 第十二章　第二笔：边界标记 B1–B4（共 4）

面上最短能把 holonomy 关住的，是**一个最小面的四边**。这四边就是第三部已锁的 $n_1=4$ 闭链嵌进面里——**不是**再发明一个 $+1$ 折返。

约定：把最小面边界的四条有向边标成 B1–B4（与线上 $0\to 1\to 2\to 1\to 0$ 同构）：

| 标记 | 计数的是什么 | 对应边界有向边（示意） |
|:---:|:---|:---|
| **B1** | 边界回路第 1 步（封住漏出口的一边） | 如 $(0,0)\to(1,0)$ 类边 |
| **B2** | 边界回路第 2 步 | 如 $(1,0)\to(2,0)$ 类边 |
| **B3** | 边界回路第 3 步（折返段） | 如 $(2,0)\to(1,0)$ 类边 |
| **B4** | 边界回路第 4 步（回到起点，回路闭合） | 如 $(1,0)\to(0,0)$ 类边 |

$$
\text{闭链小计}=\mathrm{B1}+\mathrm{B2}+\mathrm{B3}+\mathrm{B4}=4.
$$

**B 与 C 分座：**

| | C1–C9 | B1–B4 |
|:---|:---|:---|
| 在数什么 | **位置**有没有被走到 | **边界回路**有没有闭合 |
| 缺了会怎样 | 子集翻转，锁定不完整 | holonomy 漏到面外 |
| 能否互相替代 | 不能：走遍九宫 ≠ 边界闭合 | 不能：四边闭合 ≠ 九宫全走 |

---

## 第十三章　$n_2=13$ 的完整标记清单（点名）

把 13 个标记排成一张总表——**每一行就是 1**：

| # | 标记 | 这一记在数什么 |
|:---:|:---:|:---|
| 1 | C1 | 走到 $(0,0)$ |
| 2 | C2 | 走到 $(1,0)$ |
| 3 | C3 | 走到 $(2,0)$ |
| 4 | C4 | 走到 $(0,1)$ |
| 5 | C5 | 走到 $(1,1)$ |
| 6 | C6 | 走到 $(2,1)$ |
| 7 | C7 | 走到 $(0,2)$ |
| 8 | C8 | 走到 $(1,2)$ |
| 9 | C9 | 走到 $(2,2)$ |
| 10 | B1 | 边界回路第 1 步 |
| 11 | B2 | 边界回路第 2 步 |
| 12 | B3 | 边界回路第 3 步（折返） |
| 13 | B4 | 边界回路第 4 步（闭合） |

$$
n_{2}=\underbrace{9}_{\mathrm{C1\text{–}C9}}+\underbrace{4}_{\mathrm{B1\text{–}B4}}=13.
$$

### 为何必须是 C＋B，不是凑 $13$

$n_2$ 记的是**一次完整面翻转**的总事件。完整＝「九宫每个位置都翻到」**并且**「面边界 holonomy 不漏」。这两句是**合取**（都要），不是选言（二选一）。

```text
完整面翻转
    ⇔  覆盖完成（C1–C9 全付）
    ∧  边界闭合（B1–B4 全付）
```

合取在计数上就是相加：两类标记互不替代，缺一类整件事失败。

| 只付 | 数值 | 失败模式（证明不是凑数） |
|:---|:---:|:---|
| 仅 C1–C9 | 9 | 位置走遍，边界开着 → holonomy 漏出 |
| 仅 B1–B4 | 4 | 边界转圈，九宫有位置没翻 → 锁定不完整 |
| C＋B | **13** | 两类失败模式同时关掉 → 完整 |

若「$9+4$」是凑数，则改成 $9+1$ 或 $8+5$ 也应同样合法——但 $9+1$ 对不上最小面四边，$8+5$ 无标记座位。合法拆法**唯一**钉在 C1–C9 与 B1–B4 两套点名清单上，不是先看好 $13$ 再拆项。

**抬杠只许指着某一行。** 例如：

- 若说「只要 9」→ 缺 #10–#13（B1–B4），开链漏 holonomy。  
- 若说「只要 $9+1$」→ 把面降成线；最小面要四边，不是 $+1$。  
- 若说「两管各付 4」→ 把 B 双重计数；面是一张共同支集，B1–B4 **只付一套**。  
- 若说「你们在凑 13」→ 指出：缺 C 或缺 B 各有独立失败模式；合取强制相加，不是先定答案。

---

## 第十四章　排除表（$k=2$）

| 候选 | 数值 | 缺／多了哪些标记 |
|:---|:---:|:---|
| $9$ | 9 | 缺 B1–B4 |
| $9+1$ | 10 | 把 B 错收成 $+1$，不是四边 |
| $2\times 4=8$ | 8 | 缺全部 C；B 双重计数 |
| $9+8$ | 17 | C 齐了，但 B 付了两套 |
| $\binom{3}{2}=3$ | 3 | 类型数，不是事件标记 |
| $\mathrm{C1\text{–}C9}+\mathrm{B1\text{–}B4}$ | **13** | **接纳：13 行清单齐** |

$$
\boxed{n_{2}=13=\mathrm{C1}+\cdots+\mathrm{C9}+\mathrm{B1}+\mathrm{B2}+\mathrm{B3}+\mathrm{B4}}
$$

**严格度：算出。**

**断在哪：** 不点名就写「$9+4$」却说不清 C 与 B 各是什么；或把 B 双重计数。

---

# 第五部　$k=3$：$n_3=30$ 逐步统计

纪律：下面 **30 个标记** 每一个都写明「在数什么」。抬杠时只许指着某一个标记——不许笼统说「30 从哪来」。

---

## 第十五章　支集：$3\times 3\times 3$ 体（先钉 27 个位置名）

$$
\lvert D_{3}\rvert=3^{3}=27.
$$

按 $(x,y,z)$、$x,y,z\in\{0,1,2\}$ 给每个位置一个名字 $\mathrm{Q}_{xyz}$（下标三位）：

| 层 $z=0$ | 层 $z=1$ | 层 $z=2$ |
|:---|:---|:---|
| Q000 Q100 Q200 | Q001 Q101 Q201 | Q002 Q102 Q202 |
| Q010 Q110 Q210 | Q011 Q111 Q211 | Q012 Q112 Q212 |
| Q020 Q120 Q220 | Q021 Q121 Q221 | Q022 Q122 Q222 |

共 $3\times 3\times 3=27$ 个位置名。后面覆盖标记一一对应。

---

## 第十六章　第一笔：覆盖标记 V1–V27（共 27）

约束 0：每个位置必须被走到。**一位置 = 一标记。**

| 标记 | 计数的是什么 | 位置 |
|:---:|:---|:---|
| **V1** | 走到 Q000 | $(0,0,0)$ |
| **V2** | 走到 Q100 | $(1,0,0)$ |
| **V3** | 走到 Q200 | $(2,0,0)$ |
| **V4** | 走到 Q010 | $(0,1,0)$ |
| **V5** | 走到 Q110 | $(1,1,0)$ |
| **V6** | 走到 Q210 | $(2,1,0)$ |
| **V7** | 走到 Q020 | $(0,2,0)$ |
| **V8** | 走到 Q120 | $(1,2,0)$ |
| **V9** | 走到 Q220 | $(2,2,0)$ |
| **V10** | 走到 Q001 | $(0,0,1)$ |
| **V11** | 走到 Q101 | $(1,0,1)$ |
| **V12** | 走到 Q201 | $(2,0,1)$ |
| **V13** | 走到 Q011 | $(0,1,1)$ |
| **V14** | 走到 Q111 | $(1,1,1)$ |
| **V15** | 走到 Q211 | $(2,1,1)$ |
| **V16** | 走到 Q021 | $(0,2,1)$ |
| **V17** | 走到 Q121 | $(1,2,1)$ |
| **V18** | 走到 Q221 | $(2,2,1)$ |
| **V19** | 走到 Q002 | $(0,0,2)$ |
| **V20** | 走到 Q102 | $(1,0,2)$ |
| **V21** | 走到 Q202 | $(2,0,2)$ |
| **V22** | 走到 Q012 | $(0,1,2)$ |
| **V23** | 走到 Q112 | $(1,1,2)$ |
| **V24** | 走到 Q212 | $(2,1,2)$ |
| **V25** | 走到 Q022 | $(0,2,2)$ |
| **V26** | 走到 Q122 | $(1,2,2)$ |
| **V27** | 走到 Q222 | $(2,2,2)$ |

$$
\text{覆盖小计}=\mathrm{V1}+\cdots+\mathrm{V27}=27.
$$

---

## 第十七章　第二笔：闭链标记＝空集（0）

体的边界已经是**闭合的面**。走遍体时，边界 holonomy 被面本身关住——约束 1 在满维**自动满足**。

| 标记 | 计数的是什么 | 本卷是否付 |
|:---:|:---|:---|
| （无） | 「外面的线边界」四步 | **不付**（满维无外面） |

故闭链小计 $=0$。再加一套 B1–B4，等于假装体还有一条要嵌的线边界——断。

$$
\Delta_{\text{闭链}}(3)=0.
$$

---

## 第十八章　第三笔：色锁定标记 K1–K3（共 3）

仅 $k=3$（三管全参与）启动。管间循环 $e_1\to e_2\to e_3\to e_1$ 不能留下净排列。

| 标记 | 计数的是什么 | 对应动作 |
|:---:|:---|:---|
| **K1** | 沿色循环第 1 步：消掉 $e_1$ 方向净余 | $e_1$ 侧补一步 |
| **K2** | 沿色循环第 2 步：消掉 $e_2$ 方向净余 | $e_2$ 侧补一步 |
| **K3** | 沿色循环第 3 步：消掉 $e_3$ 方向净余，循环归零 | $e_3$ 侧补一步 |

$$
\text{色锁定小计}=\mathrm{K1}+\mathrm{K2}+\mathrm{K3}=3.
$$

**K 与 V 分座：**

| | V1–V27 | K1–K3 |
|:---|:---|:---|
| 在数什么 | **格点位置**走到没有 | **管与管之间**色循环是否归零 |
| 缺了会怎样 | 子集翻转 | 锁定拆成「某根管多转一圈」 |
| 能否互相替代 | 不能 | 不能 |

$k\lt 3$ 时至少一根管冻在 $0$，色循环不启动——**没有 K1–K3**。不要把这三标记抄到 $k=1,2$。

---

## 第十九章　$n_3=30$ 的完整标记清单（点名）

| # | 标记 | 这一记在数什么 |
|:---:|:---:|:---|
| 1–27 | V1–V27 | 走到 Q000…Q222 共 27 个位置（上表逐行） |
| 28 | K1 | 色循环第 1 步（$e_1$ 净余归零） |
| 29 | K2 | 色循环第 2 步（$e_2$ 净余归零） |
| 30 | K3 | 色循环第 3 步（$e_3$ 净余归零） |

为防抬杠「1–27 是糊的」，把覆盖 27 行也展开成总编号（与第十六章一一对应）：

| # | 标记 | 位置 | # | 标记 | 位置 | # | 标记 | 位置 |
|:---:|:---:|:---|:---:|:---:|:---|:---:|:---:|:---|
| 1 | V1 | $(0,0,0)$ | 10 | V10 | $(0,0,1)$ | 19 | V19 | $(0,0,2)$ |
| 2 | V2 | $(1,0,0)$ | 11 | V11 | $(1,0,1)$ | 20 | V20 | $(1,0,2)$ |
| 3 | V3 | $(2,0,0)$ | 12 | V12 | $(2,0,1)$ | 21 | V21 | $(2,0,2)$ |
| 4 | V4 | $(0,1,0)$ | 13 | V13 | $(0,1,1)$ | 22 | V22 | $(0,1,2)$ |
| 5 | V5 | $(1,1,0)$ | 14 | V14 | $(1,1,1)$ | 23 | V23 | $(1,1,2)$ |
| 6 | V6 | $(2,1,0)$ | 15 | V15 | $(2,1,1)$ | 24 | V24 | $(2,1,2)$ |
| 7 | V7 | $(0,2,0)$ | 16 | V16 | $(0,2,1)$ | 25 | V25 | $(0,2,2)$ |
| 8 | V8 | $(1,2,0)$ | 17 | V17 | $(1,2,1)$ | 26 | V26 | $(1,2,2)$ |
| 9 | V9 | $(2,2,0)$ | 18 | V18 | $(2,2,1)$ | 27 | V27 | $(2,2,2)$ |
| **28** | **K1** | 色循环 $e_1$ | **29** | **K2** | 色循环 $e_2$ | **30** | **K3** | 色循环 $e_3$ |

$$
n_{3}=\underbrace{27}_{\mathrm{V1\text{–}V27}}+\underbrace{0}_{\text{无边界 B}}+\underbrace{3}_{\mathrm{K1\text{–}K3}}=30.
$$

### 为何必须是 V＋K，不是凑 $30$

$n_3$ 记的是**一次完整体翻转**的总事件。$k=3$ 时三管全在，完整＝「$27$ 个位置都翻到」**并且**「三色循环净排列归零」。同样是**合取**：

```text
完整体翻转
    ⇔  覆盖完成（V1–V27 全付）
    ∧  色循环归零（K1–K3 全付）
    ∧  （闭链）满维边界已闭 → B 账单为空，记 0
```

| 只付 | 数值 | 失败模式（证明不是凑数） |
|:---|:---:|:---|
| 仅 V1–V27 | 27 | 格子走遍，色循环有净余 → 锁定拆成「某管多转一圈」 |
| 仅 K1–K3 | 3 | 色归零，但体上大片位置未翻 → 子集翻转 |
| V＋B＋K | 34 | 多付不存在的外面线边界（满维无 B） |
| V＋K | **30** | 覆盖失败与色锁失败同时关掉；B$=0$ 不是漏项 |

**B＝0 恰恰反证「凑数说」。**  
若目标是硬凑 $30$，满维仍可塞进 $+4$ 做成 $31$ 或 $34$。本卷**拒绝**塞 B：该约束已自动满足，账单必须空。能把一项记成 $0$，说明相加规则跟的是「约束是否启动」，不是「往某个整数靠」。

**抬杠只许指着某一行。** 例如：

- 若说「只要 27」→ 缺 #28–#30（K1–K3）。  
- 若说「$27+4$」→ 多付了一套不存在的 B1–B4。  
- 若说「$27+4+3$」→ 满维又并进线边界，再加色锁。  
- 若说「你们在凑 30」→ 指出：V 与 K 各有独立失败模式；B$=0$ 是约束已闭，不是漏凑。

---

## 第二十章　排除表（$k=3$）

| 候选 | 数值 | 缺／多了哪些标记 |
|:---|:---:|:---|
| $27$ | 27 | 缺 K1–K3 |
| $27+4$ | 31 | 多付不存在的 B1–B4 |
| $27+4+3$ | 34 | 多 B 又加 K |
| $\binom{3}{3}=1$ | 1 | 类型数，不是事件标记 |
| $\mathrm{V1\text{–}V27}+\mathrm{K1\text{–}K3}$ | **30** | **接纳：30 行清单齐** |

$$
\boxed{n_{3}=30=\mathrm{V1}+\cdots+\mathrm{V27}+\mathrm{K1}+\mathrm{K2}+\mathrm{K3}}
$$

**严格度：算出。**

**断在哪：** 不点名就写「$27+3$」却说不清 V 与 K；或满维再嵌 B1–B4。

---

# 第六部　总账与拓扑比

---

## 第二十一章　三行对照（完整统计总表）

| $k$ | 覆盖标记 | 闭链／边界标记 | 色锁定标记 | $n_k$ | 点名合计 |
|:---:|:---|:---|:---|:---:|:---|
| 1 | 走到 $0,1,2$（由 L 覆盖） | L1–L4（4 步闭覆盖） | — | **4** | $\mathrm{L1}+\mathrm{L2}+\mathrm{L3}+\mathrm{L4}$ |
| 2 | C1–C9（9 个位置） | B1–B4（最小面四边） | — | **13** | $\mathrm{C1\text{–}C9}+\mathrm{B1\text{–}B4}$ |
| 3 | V1–V27（27 个位置） | —（满维无外面） | K1–K3 | **30** | $\mathrm{V1\text{–}V27}+\mathrm{K1\text{–}K3}$ |

$$
\boxed{n_{1}=4,\quad n_{2}=13,\quad n_{3}=30}
$$

纪律：

- 闭链／边界标记只在 $k\lt 3$ 出现；满维无 B。  
- 色锁定标记 K 只在 $k=3$ 出现。  
- 抬杠只许指着某一个标记名（L／C／B／V／K），不许笼统说「数不对」。

---

## 第二十二章　拓扑比 $R=17/9$（推论，不是质量比）

$$
R=\frac{n_{3}-n_{2}}{n_{2}-n_{1}}=\frac{30-13}{13-4}=\frac{17}{9}.
$$

纯有理数，不依赖 $q$、$m_0$、$\rho$。

| 读成 | 不要读成 |
|:---|:---|
| $n_k$ 差分的拓扑比 | $m_s/m_d$；轻子质量比（还要乘 $q$ 的幂） |
| 本卷事件计数的推论 | 第六卷夸克代隙表的提前占用 |

后文的 $20$ 是三刀砍出来的，不是本卷的 $n_k$。

**严格度：算出（有理比）；读法（不进夸克表）。**

---

## 第二十三章　假计数总排除表

| 假写法 | 缺／多了哪些标记 |
|:---|:---|
| $n_1=3$ | 缺 L3 折返（开链） |
| $n_1=\binom{3}{1}=3$ | 类型数，无 L 标记 |
| $n_2=9$ | 缺 B1–B4 |
| $n_2=9+1$ | B 错收成 $+1$ |
| $n_2=8$ 或 $9+8$ | B 双重计数／缺 C |
| $n_3=27$ | 缺 K1–K3 |
| $n_3=27+4$ | 多付不存在的 B1–B4 |
| $n_3=27+4+3$ | 多 B 又加 K |
| $\{0,1,2\}=$ 三态 | 支集读歪，整表错位 |

---

# 结

---

## 本卷钉死

| 闭合句 | 地位 |
|:---|:---|
| 事件计数 ≠ 类型计数 ≠ 状态组合 | 算出（分座） |
| $D_k=\{0,1,2\}^k$，$\lvert D_k\rvert=3^k$ | 算出 |
| 三约束：全覆盖／闭链／色锁定分刀 | 算出 |
| $n_1=4=\mathrm{L1}+\mathrm{L2}+\mathrm{L3}+\mathrm{L4}$ | 算出（点名） |
| $n_2=13=\mathrm{C1\text{–}C9}+\mathrm{B1\text{–}B4}$ | 算出（点名） |
| $n_3=30=\mathrm{V1\text{–}V27}+\mathrm{K1\text{–}K3}$ | 算出（点名） |
| 分类相加＝合取总代价，非凑数 | 算出（有独立失败模式；$\Delta=0$ 可空账） |

## 本卷不假装做了的

没有重推 $S_1$、$q$。没有切 $m_\tau$。没有把 $17/9$ 写成夸克比。没有把 $\binom{3}{k}$ 并进 $n_k$。没有在 $k=3$ 再嵌一条线闭链。没有把色锁定抄到 $k=1,2$。

## 复述

1. 数的是交汇翻转在位置格上的事件，不是选管方式，也不是格点状态组合。  
2. 全覆盖付位置标记；闭链付边界标记（仅 $k\lt 3$）；色锁定付 K1–K3（仅 $k=3$）。分类是防并句；相加是合取（都要）——缺一类有独立失败模式，不是凑 $13$、$30$。  
3. 线：$\mathrm{L1}+\mathrm{L2}+\mathrm{L3}+\mathrm{L4}=4$。  
4. 面：$\mathrm{C1\text{–}C9}+\mathrm{B1\text{–}B4}=13$（C∧B）。  
5. 体：$\mathrm{V1\text{–}V27}+\mathrm{K1\text{–}K3}=30$（V∧K；B$=0$ 是空账不是漏项）。  

$$
\boxed{n_{1}=4,\quad n_{2}=13,\quad n_{3}=30}
$$

**第二十三卷结束。**  
回到第四卷：事件数齐了，才积分一次隧穿 $S_1\to q$。轻子一口井见第五卷。

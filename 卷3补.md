# 活动序列验证：正传保双态，反传翻复结构

**本篇做什么。** 回应一句尖锐质疑：正传、反传都是左乘，都是线性映射——所以正反传态「都是线性的」，卷3「反传拆单态」站不住。本篇**不借**标准模型左右手作前提；只把传态写成**可逐步重演的活动序列**，把「线性」的座位钉死，再逐格验算。结论与卷3 第三章 boxed 一致。

**与正卷的关系：**

| 卷 | 角色 |
|:---|:---|
| [卷3](卷3.md) | 正传保双态／反传拆单态；入账后果 |
| **本篇** | 活动序列逐步展开；反线性逐格验算 |

**纪律（与卷3 同根，本篇不另立）：**

| 本篇说的 | 读成 | 不要读成 |
|:---|:---|:---|
| 座位 A | $\mathbb{R}$ 线性（左乘作为实线性映射） | 卷3 的「C 线性双态」 |
| 座位 B | 右 $`\mathbb{C}_{e_{1}}`$ 模线性 | 状态账坐标线性 |
| 座位 C | 双态坐标下的 $\mathbb{C}$ 线性／反线性 | 与 A/B 并成一句 |
| 账箭头 | 由去程固定；回程相对读出 | 正／反在 C 上天然异型 |
| 箭头记号 | 左乘 $`\overset{\times e_{i}}{\longrightarrow}`$；右乘 $`\overset{\cdot\ e_{i}}{\longrightarrow}`$（卷2） | 把回程改成右乘箭头 |

---

## 开篇：质疑句与三个座位

质疑句的代数写法（与卷3 第三章同句；箭头＝卷2 左乘）：

$$
\text{正传 }q\mapsto e_i\cdot q\quad(\overset{\times e_i}{\longrightarrow}),
\qquad
\text{反传 }q\mapsto e_i^{-1}\cdot q=-e_i\cdot q\quad(\overset{\times e_i^{-1}}{\longrightarrow}).
$$

都是 $`\overset{\times e_i}{\longrightarrow}`$ 一侧。右乘对照是 $`q\mapsto q\cdot e_i`$（ $`\overset{\cdot\ e_i}{\longrightarrow}`$），本篇验反线性不用它当回程。

**「线性」三个座位（先钉死，免得并句）：**

| 座位 | 检验什么 | 正传 | 反传 |
|:---|:---|:---:|:---:|
| **A** | 实线性：加性、实数乘 | 是 | 是 |
| **B** | 右模线性（λ 从右边乘） | 是 | 是 |
| **C** | 双态坐标对 λ 缩放 | 沿 e₁ 线性；沿 e₂/e₃ 见 §3 | 反线性 |

**座位 A：**

$$
L(q+q')=L(q)+L(q'),\qquad L(tq)=tL(q),\quad t\in\mathbb{R}.
$$

**座位 B：**

$$
L(q\cdot\lambda)=L(q)\cdot\lambda.
$$

**座位 C：**

$$
f(\lambda\cdot\mathbf{z})=\lambda f(\mathbf{z})
\quad\text{（线性）};\qquad
f(\lambda\cdot\mathbf{z})=\bar\lambda f(\mathbf{z})
\quad\text{（反线性）}.
$$

该质疑所说的「都是线性」＝座位 **A**（或把左乘当成「当然线性」）。  
卷3「正传 $\mathbb{C}$ 线性／反传 $\mathbb{C}$ 反线性」＝座位 **C**，并与座位 **B** 的「右边乘与左边传态」合读。  
**A 成立推不出 C 成立。** 并成一句，后文右手单态那一刀被抹掉——这正是卷3「断在哪」。

**继承（卷1／卷2／卷3 已锁，本篇直接用）：**

$$
e_i^2=-1,\quad e_i^{-1}=-e_i,\quad e_ie_j=-e_je_i\ (i\neq j),\qquad
\mathbb{H}\cong\mathbb{C}^2.
$$

---

## 一、双态坐标：活动里怎么读

### 1.1 固定复结构标签

与卷3 第二章同句。选定记账用的复结构 $`\mathbb{C}_{e_{1}}=\mathrm{span}_{\mathbb{R}}\{1,e_{1}\}`$（标签，不是单面真空自转）。任意 $q\in\mathbb{H}$ 唯一写成：

$$
q=z_{1}+z_{2}e_{2},\qquad z_{1},z_{2}\in\mathbb{C}_{e_{1}}.
$$

活动义：一枚完整比对 $q$，在「用 $`e_{1}`$ 切复结构」这本账下，露出**两个**复分量——状态双态。记 $`\mathbf{z}=(z_{1},z_{2})`$。

左 $\mathbb{C}$ 作用（坐标缩放，座位 C）：

$$
\lambda\cdot(z_{1},z_{2})=(\lambda z_{1},\lambda z_{2}),\qquad\lambda\in\mathbb{C}_{e_{1}}.
$$

右 $`\mathbb{C}_{e_{1}}`$ 作用（座位 B，λ 从**右边**乘）。结合律保证：

$$
L_x(q)=x\cdot q,\qquad L_x(q\cdot\lambda)=L_x(q)\cdot\lambda.
$$

对任意左乘 $`L_x`$ 成立——故 **正传、反传在座位 B 上都「线性」**。若质疑停在 B，两端同型；争议不在 B。

### 1.2 本篇要验的句子

卷3 boxed：

$$
\text{正传：}\mathbb{C}\text{-线性，保双态}\qquad\text{反传：}\mathbb{C}\text{-反线性，拆单态}.
$$

还原成活动检验：

1. 写出正传／反传的逐步乘积序列；  
2. 化到双态坐标；  
3. 看坐标映射对 $\lambda\cdot\mathbf{z}$ 是乘 λ 还是乘 λ̄；  
4. 看双态是否仍能整块当作一个 $\mathbb{C}$ 线性对象入状态账。

---

## 二、预备恒等式（一次证完，后文只引用）

以下每条都是比对序列压缩，不是外加公式。

### 2.1 $`e_{2}`$ 穿过 $`\mathbb{C}_{e_{1}}`$：翻共轭

设

$$
z=a+be_{1},\qquad a,b\in\mathbb{R},\qquad \bar z=a-be_{1}.
$$

**序列：** 先 $z$，再左乘 $`e_{2}`$。

$$
e_{2}z=e_{2}(a+be_{1})=ae_{2}+be_{2}e_{1}=ae_{2}+b(-e_{1}e_{2})=ae_{2}-be_{3}.
$$

**另一压缩：**

$$
\bar z\, e_{2}=(a-be_{1})e_{2}=ae_{2}-be_{1}e_{2}=ae_{2}-be_{3}.
$$

$$
\boxed{e_{2}z=\bar z\, e_{2}}
$$

活动义： $`e_{2}`$ 与复结构标签 $`e_{1}`$ **反对易**；把 $z$ 从 $`e_{2}`$ 左边传到右边，必须改成 $\bar z$。**共轭不是外贴符号，是反对易逼出来的。**

### 2.2 $`e_{2}ze_{2}=-\bar z`$

$$
e_{2}ze_{2}=(e_{2}z)e_{2}=(\bar z\, e_{2})e_{2}=\bar z(e_{2}^2)=\bar z(-1)=-\bar z.
$$

---

## 三、正传活动序列（沿 $`e_{2}`$）

与卷3 第三章同型，逐步写开。取

$$
q_w=e_{2}\cdot q_v,\qquad q=z_{1}+z_{2}e_{2}.
$$

**步 0–4（活动 → 记录）：**

$$
\begin{aligned}
&0:\ q=z_{1}+z_{2}e_{2}.\\
&1:\ e_{2}q=e_{2}z_{1}+e_{2}z_{2}e_{2}.\\
&2:\ e_{2}z_{1}=\bar z_{1}\, e_{2}\quad\text{（2.1）}.\\
&3:\ e_{2}z_{2}e_{2}=-\bar z_{2}\quad\text{（2.2）}.\\
&4:\ e_{2}q=-\bar z_{2}+\bar z_{1}\, e_{2}.
\end{aligned}
$$

坐标映射：

$$
(z_{1},z_{2})\ \longmapsto\ (-\bar z_{2},\ \bar z_{1}).
$$

### 3.1 座位 C：是否 $\mathbb{C}$ 线性？

$$
f(\lambda z_{1},\lambda z_{2})=(-\overline{\lambda z_{2}},\ \overline{\lambda z_{1}})=(-\bar\lambda\,\bar z_{2},\ \bar\lambda\,\bar z_{1})=\bar\lambda\cdot f(z_{1},z_{2}).
$$

沿 $`e_{2}`$ 的正传，在**左 $\mathbb{C}^{2}$ 坐标**下是 **$\mathbb{C}$ 反线性**的。

### 3.2 那卷3 为何说「正传 $\mathbb{C}$ 线性」？

两句不要并：

1. **座位 B（卷3 原句「右边乘与左边传态对易」）：**

$$
e_{2}(q\cdot\lambda)=(e_{2}q)\cdot\lambda.
$$

正传作为右 $`\mathbb{C}_{e_{1}}`$ 模映射是线性的——双态作为**右模**上的自由对象被整体送走，不在模结构上拆开。

2. **座位 C（坐标读法）：** 沿与 $`e_{1}`$ 反对易的管，坐标里出现共轭，对左 $\mathbb{C}^{2}$ 缩放呈反线性。

卷3「正传保双态」的活动核：右模线性（B）⇒ 一枚 $q$ 仍整块入状态账，两个分量仍绑在同一笔传态上，当作**一个**双态对象。**保双态 $\neq$ 「坐标里绝无共轭」。** 保的是「整块／不拆单态入账」。

### 3.3 沿 $`e_{1}`$ 的正传（对照：干净的坐标线性）

$$
e_{1}(z_{1}+z_{2}e_{2})=e_{1}z_{1}+e_{1}z_{2}e_{2}.
$$

因 $`z_{1},z_{2}\in\mathbb{C}_{e_{1}}`$ 且与 $`e_{1}`$ 对易，故 $`e_{1}z_{k}`$ 仍在 $`\mathbb{C}_{e_{1}}`$ 内，无管间翻共轭：

$$
(z_{1},z_{2})\longmapsto(e_{1}z_{1},\ e_{1}z_{2}).
$$

$$
f(\lambda\mathbf{z})=\lambda f(\mathbf{z})\qquad(\text{座位 C 下 }\mathbb{C}\text{-线性}).
$$

**断在哪（正传侧）：** 把「保双态」读成「坐标映射必须毫无共轭」，会把沿 $`e_{2}`$/$`e_{3}`$ 的合法正传误判为「已经反传」。共轭出现在反对易管上，是 2.1 的后果；是否**拆单态入账**由下一节反传开关判。

---

## 四、反传活动序列（沿 $`e_{2}`$）——整段写开

与卷3 第三章公式对齐：

$$
q\mapsto e_{2}^{-1}\cdot q=(-e_{2})\cdot q.
$$

**步 0–4：**

$$
\begin{aligned}
&0:\ q=z_{1}+z_{2}e_{2}.\\
&1:\ e_{2}^{-1}=-e_{2}\quad\text{（卷2：回程＝逆，仍左乘）}.\\
&2:\ (-e_{2})q=-e_{2}z_{1}-e_{2}z_{2}e_{2}.\\
&3:\ -e_{2}z_{1}=-\bar z_{1}e_{2},\ \ -e_{2}z_{2}e_{2}=\bar z_{2}\quad\text{（2.1、2.2）}.\\
&4:\ (-e_{2})q=\bar z_{2}-\bar z_{1}\, e_{2}.
\end{aligned}
$$

$$
\boxed{e_{2}^{-1}(z_{1}+z_{2}e_{2})=\bar z_{2}-\bar z_{1}\, e_{2}}
$$

坐标映射（与卷3 一致）：

$$
(z_{1},z_{2})\mapsto(\bar z_{2},-\bar z_{1}).
$$

### 4.1 座位 C：反线性（逐格验）

设 $`g(z_{1},z_{2})=(\bar z_{2},-\bar z_{1})`$。

$$
g(\lambda z_{1},\lambda z_{2})=(\overline{\lambda z_{2}},\ -\overline{\lambda z_{1}})=(\bar\lambda\,\bar z_{2},\ -\bar\lambda\,\bar z_{1})=\bar\lambda\cdot g(z_{1},z_{2}).
$$

$$
\boxed{g(\lambda\cdot\mathbf{z})=\bar\lambda\cdot g(\mathbf{z})\qquad(\mathbb{C}\text{-反线性})}
$$

**不是** $g(\lambda\mathbf{z})=\lambda g(\mathbf{z})$。座位 C 下反传**不是** $\mathbb{C}$ 线性。

### 4.2 质疑句的精确答复

| 质疑 | 判 | 说明 |
|:---|:---:|:---|
| 都左乘 ⇒ 都 $\mathbb{R}$ 线性 | 是 | 座位 A；本篇承认 |
| 故座位 B 也都线性 | 是 | 结合律；两端同型 |
| 故座位 C 也都线性 | 否 | 见 §4.1 boxed；反线性 |
| 正传加负号就完 | 否 | 共轭来自 $`e_{2}`$ 与 $`e_{1}`$ 反对易（2.1） |
| 都线性 ⇒ 都保双态 | 否 | 把 A/B 偷换成 C |

**「反传不是正传加一个负号就完」**（卷3）的活动义：负号只完成「取逆」；共轭来自 $z\mapsto\bar z$ 与**反对易穿越复结构**。两步都要写进序列，缺一步就读成「只差个符号的线性映射」。

### 4.3 正／反不对称：轴／横 vs 账箭头

**座位 C 单独分出的是「轴／横」，不是「正／反」。**  
沿 $`e_{1}`$： $`L_{e_{1}}`$ 与 $`L_{-e_{1}}`$ 在座位 C 下**都** $\mathbb{C}$ 线性。  
沿 $`e_{2}`$/$`e_{3}`$： $`L_{e_{2}}`$ 与 $`L_{-e_{2}}`$ 在座位 C 下**都** $\mathbb{C}$ 反线性——二者只差中心元 $-1$：

$$
(-e_{2})q=-(e_{2}q).
$$

$-1$ 与一切交换，**不改变任何线性类型**。  
故：若只盯座位 C 的线性类型，$`e_{2}`$ 与 $`-e_{2}`$ 同型；「正传线性／反传反线性」不能读成「$`\pm e_{i}`$ 在 C 上天然异型」。§3.1 已示出沿 $`e_{2}`$ 的正传在 C 下也反线性——本篇不藏。

**账的箭头由去程固定。** 去程

$$
q\mapsto e_i\cdot q
$$

先发生，定下状态账怎么读双态（右模整块、座位 B；坐标用固定的 $`\mathbb{C}_{e_{1}}`$）。回程是**同一生成元的逆**，仍从左边续接（卷2）——一切记录**相对已经定下的去程箭头**读出。

> **为何必须左式回程、不可改右乘。** 右乘实现回程时，坐标映射为 $`(z_{1},z_{2})\mapsto(z_{2},-z_{1})`$，座位 C 下是 **$\mathbb{C}$ 线性**的——链上可有翻站，但**账上看不见共轭**。无共轭 ⇒ 拆单态与 $\bar{\mathbf{3}}$ 整条塌掉。左式回程才带出反对易逼出的共轭（2.1）。卷2 定义「回程＝逆、仍左乘」是账目兼容的选法，不是疏忽。

| 侧 | 入账 |
|:---|:---|
| **正传（去程）** | 保双态（一个 $\mathbb{C}$ 线性对象入状态账） |
| **反传（回程）** | 拆成两个单态；类型走 $\bar{\mathbf{3}}$ |

$$
\boxed{\text{账箭头＝去程}\ \Longrightarrow\ \text{回程读出共轭／反线性}\ \Longrightarrow\ \text{拆单态}}
$$

**断在哪：** 坚持「A/B 线性 ⇒ 不拆单态」——后文没有两个右手单态。  
**断在哪：** 把回程改成右乘以「去掉共轭」——账上共轭抹掉，拆单态论证死。

---

## 五、数值验算（可逐行重演）

取 $`z_{1}=1`$、 $`z_{2}=e_{1}`$，故 $`q=1+e_{1}e_{2}=1+e_{3}`$。只验**反传**与反线性。

**路径甲（定义）：**

$$
\begin{aligned}
(-e_{2})(1+e_{3})
&=-e_{2}-e_{2}e_{3}
=-e_{2}-e_{2}(e_{1}e_{2})
=-e_{2}-(-e_{1}e_{2})e_{2}
=-e_{1}-e_{2}.
\end{aligned}
$$

**路径乙（一般式）：** $`\bar z_{2}-\bar z_{1}e_{2}=-e_{1}-e_{2}`$。同甲。

$$
(z_{1},z_{2})=(1,e_{1})\longmapsto(-e_{1},-1)=(\bar z_{2},-\bar z_{1}).
$$

**缩放检验：** 取 $`\lambda=e_{1}`$，$`g(z_{1},z_{2})=(\bar z_{2},-\bar z_{1})`$。

$$
g(\lambda\mathbf{z})=(-1,e_{1}),\qquad
\bar\lambda\cdot g(1,e_{1})=(-1,e_{1}),\qquad
g(\lambda\mathbf{z})=\bar\lambda\cdot g(\mathbf{z})\ \text{成立}.
$$

若误按「也线性」期待 $`\lambda\cdot g(1,e_{1})=(1,-e_{1})`$，与上式 **对不上**。座位 C 下「反传也线性」被活动序列否决。

---

## 六、与卷3 入账的接口（不重推）

| 本篇验到 | 卷3 读成 |
|:---|:---|
| 正传：右模线性；双态整块 | 左手双态 |
| 反传：坐标反线性；共轭 | 双态拆单态；类型 $\bar{\mathbf{3}}$ |
| 共轭来自反对易 | 负号不是外贴；共轭是结构 |

本篇**不**重写超荷、不重开 15 与 12。只封住：「正反都 $\mathbb{R}$ 线性 ⇒ 不拆单态」这条质疑。

---

## 七、结论

**质疑句哪里对、哪里错：**

- **对：** 正传、反传作为左乘，都是座位 A 的 $\mathbb{R}$ 线性映射；在座位 B 下也都对右 $\mathbb{C}$ 线性。  
- **错：** 把 A/B 说成卷3 的「$\mathbb{C}$ 线性」。状态双态的读法是座位 C；反传满足 §4.1 boxed，是 **$\mathbb{C}$ 反线性**。  
- **错：** 「反传＝正传加负号、复结构不变」。负号只是 $`e_i^{-1}=-e_i`$；**翻共轭**来自 $`e_{2}`$ 与 $`e_{1}`$ 反对易，2.1–4 逐步逼出。  
- **错：** 「改右乘去掉共轭更干净」。右乘回程在座位 C 下线性，抹掉账上共轭，拆单态与 $\bar{\mathbf{3}}$ 无来源。

**活动序列钉死：**

$$
e_{2}^{-1}(z_{1}+z_{2}e_{2})=\bar z_{2}-\bar z_{1}\, e_{2},
\qquad
(z_{1},z_{2})\mapsto(\bar z_{2},-\bar z_{1})\ \text{反线性}.
$$

$$
\boxed{\text{账箭头＝去程}\quad\text{正传保双态}\quad\text{反传反线性⇒拆单态}}
$$

**断在哪：** 正反传都保 $\mathbb{C}$ 线性双态——手性开关失效，后文无两个右手单态。

---

## 本篇不假装做了的

- 不重证三管锁定、 $`H_{P}=-1`$、三层账、规范群、超荷。  
- 不把「反线性」升级成时空反演或「粒子跑反了」。  
- 不否认座位 A/B 上正反传同型线性——只否认由此推出「不拆单态」。  
- 不采用右乘回程「修复」——那会抹掉账上共轭。  
- 不引入标准模型粒子名作为前提；名字若出现只是读法。

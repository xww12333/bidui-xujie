# 活动序列验证：正传保双态，反传翻复结构

**本篇做什么。** 有人提出一句尖锐质疑：

> 正传 $q\mapsto e_i\cdot q$、反传 $q\mapsto e_i^{-1}\cdot q=-e_i\cdot q$，都是左乘，都是线性映射——所以正反传态「都是线性的」，卷3「反传拆单态」站不住。

本篇**不借**「标准模型本来就有左右手」作前提。只做一件事：把传态写成**可逐步重演的活动序列**，把「线性」的座位钉死，再逐格验算。结论与卷3 第三章 boxed 一致；质疑的那一半（作为 $\mathbb{R}$-线性／左乘）予以承认，并标明**那不是本卷说的线性**。

与卷3 的关系：卷3 给出结论与入账后果；本篇把第三章的代数一步还原成比对续接序列。不重证三层账、不重开规范群。

**继承（卷2／卷3 已锁，本篇直接用）：**

| 已锁 | 本篇怎么用 |
|:---|:---|
| 传态 $q_w=e_i\cdot q_v$（卷2） | 正传 = 沿管左乘一步 |
| 回程 $q_v=e_i^{-1}\cdot q_w=-e_i\cdot q_w$（卷2） | 反传 = 同一操作的逆，不是第二套生成元 |
| $e_i^2=-1$，$e_i^{-1}=-e_i$；$e_ie_j=-e_je_i$（$i\neq j$） | 展开每一步积 |
| 固定 $e_1$ 切复结构：$\mathbb{H}\cong\mathbb{C}^2$（卷3 第二章） | 双态坐标 $(z_1,z_2)$；状态账在这里读 |
| 结合律 | 同一序列左右压缩一致 |
| 活动优先 | 每一步问：比对还接不接得住；式子是记录 |

**「线性」三个座位（开篇钉死，免得并句）：**

| 座位 | 意思 | 正传 $L_{e_i}$ | 反传 $L_{-e_i}$ |
|:---|:---|:---|:---|
| **A. $\mathbb{R}$-线性** | $L(q+q')=L(q)+L(q')$，$L(tq)=tL(q)$（$t\in\mathbb{R}$） | ✓ | ✓ |
| **B. 右 $\mathbb{C}_{e_1}$-模线性** | $L(q\cdot\lambda)=L(q)\cdot\lambda$（$\lambda$ 从**右边**乘） | ✓（结合律） | ✓（结合律） |
| **C. 左 $\mathbb{C}^{2}$ 坐标线性** | 把 $q=z_1+z_2e_2$ 读成双态后，映射是否满足 $f(\lambda\cdot\mathbf{z})=\lambda f(\mathbf{z})$ | 沿 $e_1$：✓；沿 $e_2/e_3$：见正文 | 坐标出现共轭： **反线性** |

该质疑所说的「都是线性」＝座位 **A**（或把左乘当成「当然线性」）。  
卷3「正传 $\mathbb{C}$-线性／反传 $\mathbb{C}$-反线性」＝座位 **C**（状态账双态的读法），并与座位 **B** 的「右边乘与左边传态」合读。  
**A 成立推不出 C 成立。** 并成一句，后文右手单态那一刀被抹掉——这正是卷3「断在哪」。

---

## 一、双态坐标：活动里怎么读

### 1.1 固定复结构标签

选定记账用的复结构 $`\mathbb{C}_{e_1}=\mathrm{span}_{\mathbb{R}}\{1,e_1\}`$（卷3：标签，不是单面真空自转）。任意 $q\in\mathbb{H}$ 唯一写成：

$$
q=z_1+z_2 e_2,\qquad z_1,z_2\in\mathbb{C}_{e_1}.
$$

活动义：一枚完整比对 $q$，在「用 $e_1$ 切复结构」这本账下，露出**两个**复分量——状态双态。记 $\mathbf{z}=(z_1,z_2)$。

左 $\mathbb{C}$ 作用（坐标缩放，座位 C）：

$$
\lambda\cdot(z_1,z_2)=(\lambda z_1,\lambda z_2),\qquad\lambda\in\mathbb{C}_{e_1}.
$$

右 $\mathbb{C}$ 作用（座位 B）：$q\cdot\lambda$（$\lambda$ 从右边乘）。结合律保证任意左乘 $L_x(q)=x\cdot q$ 都满足 $L_x(q\cdot\lambda)=L_x(q)\cdot\lambda$——故 **正传、反传在座位 B 上都「线性」**。若质疑停在 B，两端同型；争议不在 B。

### 1.2 本篇要验的句子

卷3 boxed：

$$
\text{正传：}\mathbb{C}\text{-线性，保双态}\qquad\text{反传：}\mathbb{C}\text{-反线性，拆单态}.
$$

还原成活动检验：

1. 写出正传／反传的逐步乘积序列；  
2. 化到双态坐标；  
3. 看坐标映射对 $\lambda\cdot\mathbf{z}$ 是 $\lambda$ 还是 $\bar\lambda$；  
4. 看双态是否仍能整块当作一个 $\mathbb{C}$-线性对象入状态账。

---

## 二、预备恒等式（一次证完，后文只引用）

以下每条都是比对序列压缩，不是外加公式。

### 2.1 $e_2$ 穿过 $\mathbb{C}_{e_1}$：翻共轭

设 $z=a+be_1$（$a,b\in\mathbb{R}$），$\bar z=a-be_1$。

**序列：** 先 $z$，再左乘 $e_2$。

$$
e_2 z=e_2(a+be_1)=ae_2+be_2e_1=ae_2+b(-e_1e_2)=ae_2-be_3.
$$

**另一压缩：** $\bar z\, e_2=(a-be_1)e_2=ae_2-be_1e_2=ae_2-be_3$。

$$
\boxed{e_2 z=\bar z\, e_2}
$$

活动义：$e_2$ 与复结构标签 $e_1$ **反对易**；把 $z$ 从 $e_2$ 左边传到右边，必须改成 $\bar z$。**共轭不是外贴符号，是反对易逼出来的。**

### 2.2 $e_2z e_2=-\bar z$

$$
e_2 z e_2=(e_2 z)e_2=(\bar z\, e_2)e_2=\bar z(e_2^2)=\bar z(-1)=-\bar z.
$$

---

## 三、正传活动序列（沿 $e_2$）

正传：$q_w=e_2\cdot q_v$。取 $q=z_1+z_2e_2$。

### 3.1 逐步展开

| 步 | 活动 | 记录 |
|:---:|:---|:---|
| 0 | 给定双态读法下的 $q$ | $q=z_1+z_2e_2$ |
| 1 | 沿 $e_2$ 左乘（正传一步） | $e_2 q=e_2z_1+e_2z_2e_2$ |
| 2 | 第一项用 2.1 | $e_2z_1=\bar z_1\, e_2$ |
| 3 | 第二项用 2.2 | $e_2z_2e_2=-\bar z_2$ |
| 4 | 合并为双态形 | $e_2 q=-\bar z_2+\bar z_1\, e_2$ |

坐标映射：

$$
(z_1,z_2)\;\longmapsto\;(-\bar z_2,\;\bar z_1).
$$

### 3.2 座位 C：是否 $\mathbb{C}$-线性？

$$
f(\lambda z_1,\lambda z_2)=(-\overline{\lambda z_2},\;\overline{\lambda z_1})=(-\bar\lambda\,\bar z_2,\;\bar\lambda\,\bar z_1)=\bar\lambda\cdot f(z_1,z_2).
$$

沿 $e_2$ 的正传，在**左 $\mathbb{C}^{2}$ 坐标**下是 **$\mathbb{C}$-反线性**的。

### 3.3 那卷3 为何说「正传 $\mathbb{C}$-线性」？

两句不要并：

1. **座位 B（卷3 原句「右边乘与左边传态对易」）：**  
   $e_2(q\cdot\lambda)=(e_2 q)\cdot\lambda$。正传作为右 $\mathbb{C}_{e_1}$-模映射是线性的——双态作为**右模**上的自由对象被整体送走，不在模结构上拆开。  
2. **座位 C（坐标读法）：** 沿与 $e_1$ 反对易的管，坐标里出现共轭，对左 $\mathbb{C}^{2}$ 缩放呈反线性。

卷3「正传保双态」的活动核：右模线性（B）⇒ 一枚 $q$ 仍整块入状态账，两个分量仍绑在同一笔传态上，当作**一个**双态对象（SU(2) 下共轭双态与原双态等价，不在这里拆成两个单态入账）。  
**保双态 ≠ 「坐标里绝无共轭」。** 保的是「整块／不拆单态入账」。

### 3.4 沿 $e_1$ 的正传（对照：干净的坐标线性）

$$
e_1(z_1+z_2e_2)=e_1z_1+e_1z_2e_2.
$$

$z_1,z_2\in\mathbb{C}_{e_1}$，与 $e_1$ 对易，故 $e_1z_k$ 仍在 $\mathbb{C}_{e_1}$ 内，无管间翻共轭：

$$
(z_1,z_2)\longmapsto(e_1z_1,\;e_1z_2).
$$

$$
f(\lambda\mathbf{z})=\lambda f(\mathbf{z})\qquad(\text{座位 C 下 }\mathbb{C}\text{-线性}).
$$

**断在哪（正传侧）：** 把「保双态」读成「坐标映射必须毫无共轭」，会把沿 $e_2/e_3$ 的合法正传误判为「已经反传」。共轭出现在反对易管上，是 2.1 的后果；是否**拆单态入账**由下一节反传开关判。

---

## 四、反传活动序列（沿 $e_2$）——整段写开

反传：$q\mapsto e_2^{-1}\cdot q=(-e_2)\cdot q$。

### 4.1 逐步展开（与卷3 公式对齐）

| 步 | 活动 | 记录 |
|:---:|:---|:---|
| 0 | 同上 $q=z_1+z_2e_2$ | |
| 1 | 取逆：同一生成元的回程，不是新操作（卷2） | $e_2^{-1}=-e_2$ |
| 2 | 左乘 $-e_2$ | $(-e_2)q=-e_2z_1-e_2z_2e_2$ |
| 3 | 用 2.1、2.2 | $-e_2z_1=-\bar z_1 e_2$，$\;-e_2z_2e_2=-(-\bar z_2)=\bar z_2$ |
| 4 | 合并 | $(-e_2)q=\bar z_2-\bar z_1\, e_2$ |

$$
\boxed{e_2^{-1}(z_1+z_2e_2)=\bar z_2-\bar z_1\, e_2}
$$

坐标映射：

$$
(z_1,z_2)\;\longmapsto\;(\bar z_2,\;-\bar z_1).
$$

与卷3 第三章一致。

### 4.2 座位 C：反线性（逐格验）

设 $g(z_1,z_2)=(\bar z_2,-\bar z_1)$。

$$
g(\lambda z_1,\lambda z_2)=(\overline{\lambda z_2},\;-\overline{\lambda z_1})=(\bar\lambda\,\bar z_2,\;-\bar\lambda\,\bar z_1)=\bar\lambda\cdot g(z_1,z_2).
$$

$$
\boxed{g(\lambda\cdot\mathbf{z})=\bar\lambda\cdot g(\mathbf{z})\qquad(\mathbb{C}\text{-反线性})}
$$

**不是** $g(\lambda\mathbf{z})=\lambda g(\mathbf{z})$。座位 C 下反传**不是** $\mathbb{C}$-线性。

### 4.3 质疑句的精确答复

| 质疑 | 判 | 说明 |
|:---|:---:|:---|
| 「$-e_2$ 与 $e_2$ 都是左乘，故都 $\mathbb{R}$-线性」 | ✓ | 座位 A；本篇承认 |
| 「故座位 B 也都线性」 | ✓ | 结合律；两端同型 |
| 「故座位 C 也都线性」 | ✗ | 上式 $g(\lambda\mathbf{z})=\bar\lambda g(\mathbf{z})$，反线性 |
| 「正传加负号就完，复结构不变」 | ✗ | 负号只是 $e_2^{-1}=-e_2$；**共轭来自 $e_2$ 与 $e_1$ 反对易（2.1）**，展开回程时照样翻号 |
| 「都线性 ⇒ 都保双态 ⇒ 无右手单态」 | ✗ | 把 A/B 偷换成 C；卷3 断在哪正是这一句 |

**「反传不是正传加一个负号就完」**（卷3）的活动义：  
负号只完成「取逆」；$z\mapsto\bar z$ 来自**反对易穿越复结构**。两步都要写进序列，缺一步就读成「只差个符号的线性映射」。

### 4.4 正／反不对称：先钉「轴／横」，再钉「账的箭头」

**座位 C 单独分出的是「轴／横」，不是「正／反」。**  
沿 $e_1$（与复结构标签对易）：$L_{e_1}$ 与 $L_{-e_1}$ 在座位 C 下**都** $\mathbb{C}$-线性。  
沿 $e_2/e_3$（反对易）：$L_{e_2}$ 与 $L_{-e_2}$ 在座位 C 下**都** $\mathbb{C}$-反线性——二者只差中心元 $-1$（$(-e_2)q=-(e_2q)$），$-1$ 与一切交换，**不改变任何线性类型**。  
故：若只盯座位 C 的线性类型，$e_2$ 与 $-e_2$ 同型；「正传线性／反传反线性」不能读成「$\pm e_i$ 在 C 上天然异型」。3.2 已示出沿 $e_2$ 的正传在 C 下也反线性——本篇不藏。

**账的箭头由去程固定（结构前提，显式写出）。**  
传态是比对沿管的**续接**：去程 $q\mapsto e_i\cdot q$ 先发生，定下状态账怎么读双态（右模整块、座位 B；坐标用固定的 $\mathbb{C}_{e_1}$）。  
回程是**同一生成元的逆**，仍从左边续接（卷2；不是另换右乘）——一切记录**相对已经定下的去程箭头**读出，不是另立一支与去程对称的「反箭头账」。  

> **为何必须左式回程、不可改右乘。** 右乘实现回程时，坐标映射为 $(z_1,z_2)\mapsto(z_2,-z_1)$，座位 C 下是 **$\mathbb{C}$-线性**的——链上可有翻站，但**账上看不见共轭**。无共轭 ⇒ 4.4 后半的拆单态与 $\bar{\mathbf{3}}$ 整条塌掉。左式回程才带出反对易逼出的共轭（2.1）；两种实现各带一个互斥标记（账上共轭 ↔ 链上翻站），状态账只能读共轭那个。卷2 定义「回程＝逆、仍左乘」是账目兼容的选法，不是疏忽。

在此前提下，分派升格为结构推论（不再只是「若也整块则手性白做」的一致性话）：

| 侧 | 依据 | 入账 |
|:---|:---|:---|
| **正传（去程）** | 箭头由它定；座位 B 右模线性 ⇒ 双态**整块**搬运 | 保双态（一个 $\mathbb{C}$-线性对象入状态账） |
| **反传（回程）** | 相对已定箭头读出；展开必经反对易 ⇒ 座位 C **反线性**＋共轭 | 不能再整块当双态 ⇒ **拆成两个单态**；类型走 $\bar{\mathbf{3}}$ |

状态账要求：双态是**一个** $\mathbb{C}$-线性对象——$\lambda$ 同时缩放两个分量，且映射与 $\lambda$ 对易。  
反传映射 $g$ 只与 $\bar\lambda$ 对易。相对去程箭头，回程产物**不得**再整块当作同一支 $\mathbb{C}$-线性双态入账；两个分量分成两个 $\mathbb{C}^{1}$ 单态，$T_3=\pm 1/2$ 只作分开标记（卷3）。

$$
\boxed{\text{账箭头＝去程}\ \Longrightarrow\ \text{回程相对箭头读出共轭／反线性}\ \Longrightarrow\ \text{拆单态}}
$$

**断在哪：** 坚持「A/B 线性 ⇒ 不拆单态」——后文没有两个右手单态；$\bar{\mathbf{3}}$ 无来源。  
**断在哪：** 把回程改成右乘以「去掉共轭」——链上翻转保住，账上共轭抹掉，拆单态论证死。  
**断在哪：** 否认「箭头由去程定」——正／反在座位 C 上同型，手性入账开关失去结构来源（只剩约定）。

---

## 五、整段传态对照表（可逐行重演）

取 $z_1=1$、$z_2=e_1$，故 $q=1+e_1e_2=1+e_3$。只验**反传**与反线性（正传公式见第三节；此处不搅符号约定）。

### 5.1 反传：两条路径必须同数

**路径甲（定义）：** $(-e_2)(1+e_3)$。

$$
\begin{aligned}
(-e_2)(1+e_3)
&=-e_2-e_2e_3
=-e_2-e_2(e_1e_2)
=-e_2-(-e_1e_2)e_2
=-e_2-(e_1)
=-e_1-e_2.
\end{aligned}
$$

写成 $z_1'+z_2'e_2$：$-e_1-e_2=(-e_1)+(-1)e_2$，故 $(z_1',z_2')=(-e_1,\;-1)$。

**路径乙（一般式）：** $\bar z_2-\bar z_1 e_2=\overline{e_1}-\bar 1\, e_2=-e_1-e_2$。同甲。

$$
(z_1,z_2)=(1,e_1)\;\longmapsto\;(-e_1,\;-1)=(\bar z_2,\;-\bar z_1).
$$

两路径一致。共轭在序列里，不是事后贴的。

### 5.2 缩放检验（反传反线性）

$g(z_1,z_2)=(\bar z_2,-\bar z_1)$。取 $\lambda=e_1$：

| 计算 | 结果 |
|:---|:---|
| $\lambda\cdot(1,e_1)$ | $(e_1,\;e_1^{2})=(e_1,\;-1)$ |
| $g(\lambda\cdot\mathbf{z})=g(e_1,-1)$ | $(\overline{-1},\;-\overline{e_1})=(-1,\;e_1)$ |
| $g(1,e_1)$ | $(\overline{e_1},\;-1)=(-e_1,\;-1)$ |
| $\bar\lambda\cdot g(1,e_1)=(-e_1)\cdot(-e_1,-1)$ | $(e_1^{2},\;e_1)=(-1,\;e_1)$ |

$g(\lambda\mathbf{z})=\bar\lambda\, g(\mathbf{z})$ ✓。  

若误按「也线性」期待 $\lambda\cdot g(1,e_1)=e_1\cdot(-e_1,-1)=(1,\;-e_1)$，与上表 **对不上**。座位 C 下「反传也线性」被活动序列否决。

---

## 六、与卷3 入账的接口（不重推）

| 本篇验到 | 卷3 读成 |
|:---|:---|
| 正传：右模线性（B）；双态整块 | 左手双态；$Q_L=(\mathbf{3},\mathbf{2})$ 等 |
| 反传：坐标反线性（C）；共轭 | 双态拆单态；类型 $\bar{\mathbf{3}}$；两格 $T_3=\pm 1/2$ |
| 共轭来自反对易，非外加 | 「负号不是外贴；另一面／共轭是结构」 |

本篇**不**重写超荷、不重开 $15$ 与 $12$。只封住：「正反都（$\mathbb{R}$-）线性 ⇒ 不拆单态」这条质疑。

---

## 七、结论

**质疑句哪里对、哪里错：**

- **对：** 正传、反传作为左乘，都是座位 A 的 $\mathbb{R}$-线性映射；在座位 B 下也都对右 $\mathbb{C}$ 线性。  
- **错：** 把 A/B 说成卷3 的「$\mathbb{C}$-线性」。状态双态的读法是座位 C；反传坐标映射满足 $g(\lambda\mathbf{z})=\bar\lambda g(\mathbf{z})$，是 **$\mathbb{C}$-反线性**。  
- **错：** 「反传＝正传加负号、复结构不变」。负号只是 $e_i^{-1}=-e_i$；**翻共轭**来自 $e_2$ 与 $e_1$ 反对易，活动序列 2.1–4.1 逐步逼出。  
- **错：** 「改右乘去掉共轭更干净」。右乘回程在座位 C 下线性，抹掉账上共轭，拆单态与 $\bar{\mathbf{3}}$ 无来源。

**正／反不对称坐在哪里：**  
座位 C 单独只分「轴／横」（沿 $e_1$ 正反皆线性；沿 $e_2/e_3$ 正反皆反线性）。  
**账的箭头由去程固定；回程相对该箭头读出**——于是去程整块保双态（B），回程读出共轭／反线性并拆单态（C）。此为结构推论，不是外加约定。

**活动序列钉死：**

$$
e_2^{-1}(z_1+z_2e_2)=\bar z_2-\bar z_1 e_2,
\qquad
(z_1,z_2)\mapsto(\bar z_2,-\bar z_1)\ \text{反线性}.
$$

$$
\boxed{\text{账箭头＝去程}\quad\text{正传保双态}\quad\text{反传反线性⇒拆单态}\qquad\text{（非 }\mathbb{R}\text{-线性质疑）}}
$$

**断在哪：** 正反传都保 $\mathbb{C}$-线性双态——手性开关失效，后文无两个右手单态。

---

## 本篇不假装做了的

- 不重证三管锁定、$H_P=-1$、三层账、规范群、超荷。  
- 不把「反线性」升级成时空反演或「粒子跑反了」。  
- 不否认座位 A/B 上正反传同型线性——只否认由此推出「不拆单态」。  
- 不采用右乘回程「修复」——那会抹掉账上共轭。  
- 不引入标准模型粒子名作为前提；名字若出现只是读法。

原则：
- 保留原体系所有硬约束与结论，不改变任何已锁数字与逻辑。
- 语言更口语、更有过渡，减少「电报式」跳跃。
- 每卷开头先用白话说明「这一卷要解决什么问题」。
- 关键推导后补充「为什么必须这样，否则会断在哪里」。
- 严格保持原有纪律：不假装做了的、两口不并、座位分明。
- **活动优先，记录忠实**： $k$、 $n_{k}$、 $S_{1}$、 $q$ 是交汇区翻转与隧穿的记录；须还原到三管同时在场下的局域事件，不许先认实验室代编号再往回填格子。
- 本卷一条路走完： $`k\in\{1,2,3\}`$ → $n_{k}=4,13,30$ → $q=e^{-S_{1}}$。

---

# 第四卷　交汇与翻转

**本卷要解决的问题：**  
前三卷站住：三个面向、三管锁死、传态、 $H_{P}=-1$、三层账。本卷只收录，不重证。第一卷 22.6／第二、三卷已钉：管是面向标签， $e_{i}^{2}=-1$ 是记法，局域比对三管同时在场——本卷谈「单管无事／交汇翻转」时仍守这一句。

质量还没有。本卷只问两句：翻转在哪发生、怎么数；一次隧穿多重是多少。

走到尽头： $k=1,2,3$ 恰好三代； $n_{k}=4,13,30$； $S_{1}=2\sqrt{2}\,\rho^{3}/9$， $q=e^{-S_{1}}$。不写轻子 MeV、夸克比、CKM。

**事件计数的完整逐步统计**（支集、三约束分刀、排除表、 $n_1=4$／ $n_2=13$／ $n_3=30$ 逐步列出）见**第二十三卷**；本卷第三部给闭式与总表，不重写逐步清单。  
若 $9+4$、 $27+3$ 读来像人为拆项，可先读 **[卷4补](卷4补.md)**（螺旋几何 + 闭截面 + $`\mathbb{Z}_{2}`$ 往返），或 **[卷4序](卷4序.md)**（传态闭序列展开：同一个 9 从活动里数出 $`12-3`$），再回卷23 对 L／C／B／V／K 标记名。

框架指标先钉死，避免和实验室编号并混：

| 框架 $k$ | 参与管数 | $n_{k}$ | 谁最重 | 实验室习惯叫 |
|:---:|:---:|:---:|:---|:---|
| $1$ | 1 | $4$ | 最重 | 第 3 代 |
| $2$ | 2 | $13$ | 中间 | 第 2 代 |
| $3$ | 3 | $30$ | 最轻 | 第 1 代 |

$k$ 是几何维数，不是「第几轻」。后文一律用 $k$。禁止写「第一代 $\tau$」。

27 点格的坐标是**位置** $`\{0,1,2\}`$，不是「不在 / 在 / 翻了」三态。

---

```text
单管内部无事
        │
        ▼
交汇：三管同时在场，非交换冲突
        │
        ▼
一个框架场 g（不是 Higgs）→ 双阱 ±1
        │
        ▼
翻转只在交汇；k = 参与管数 = 代
        │
        ▼
位置格 {0,1,2}^k → n_k = 4, 13, 30
        │
        ▼
一次隧穿 S_1 → q = e^{-S_1}
```

$$
\boxed{k\in\lbrace1,2,3\rbrace\ \Longrightarrow\ n_{k}=4,13,30\ \Longrightarrow\ q=e^{-S_{1}}}
$$

## 读法与指引

**记录与活动：** $k$、 $n_{k}$、 $S_{1}$、 $q$ 仍是记录，不是前提；每一步都要能还原到交汇区翻转、位置格计数、一次隧穿鞍点这类**局域事件**本身，检验记录是否忠实。不是先认实验室代编号或 MeV，再往回填格子。

左边 $k$ 是参与管数，不是「第几轻」。中间 $n_{k}$ 是事件数，不是类型数 $\binom{3}{k}$。右边 $q$ 是一次隧穿多重。三步分开走：先定位翻转只在交汇，再数 $n_{k}$，最后才积分 $S_{1}$。

**本卷怎么走。** 先读卷首那张 $k$ 与实验室代的对照表，对着写，不许并成「第 $k$ 代」。第八章：格子坐标是位置 $`\{0,1,2\}`$，不是三态。 $90^{\circ}$ 是奇通道破缺， $60^{\circ}$ 是偶通道折返，不能同时成立——不是两个自由参数。 $\rho$ 的指数与四步同根，数值前因子仍诚实，不要升成与 $n_{k}$ 同级。 $20$ 不在本卷。

本卷的「证明」仍是规划路径上的比对序列重演。各章「断在哪」就是这种检验。

| 这里的词 | 读成 | 不要读成 |
|:---|:---|:---|
| 管 / 单管内部 | 沿一种面向**标签**看：无反对易冲突 | $\mathbb{R}^{3}$ 圆柱；擦掉另两管后的真空自转 |
| 交汇 | 三管标签同时在场；非交换冲突所在 | 外加的对称性破缺点 |
| $k=1$ | 最重（ $\tau,t,b$） | PDG 第 1 代 |
| $`\{0,1,2\}`$ | 位置 | 不在 / 在 / 翻了 |
| $n_{k}$ | 全覆盖 $+$ 闭链 $+$ 色锁定的事件数 | $\binom{3}{k}$；与 $20$ 一类 |
| $e_{i}^{2}=-1$（双阱处） | 记法； $\pm e_{i}$ 同表故双阱合法 | 单面真空自转（对齐第一卷 22.6） |
| $90^{\circ}$ / $60^{\circ}$ | 奇通道破缺 / 偶通道折返 | 两个可以同时取的圆心角 |
| $\rho$ | 指数锁定；数值诚实 | 与 $n_{1}=4$ 一样硬的格子计数 |
| 框架场 $g$ | 交汇取向 | Higgs 双态 $H$ |

读完请能说出 boxed。轻子 MeV 是下一卷。

---

# 第一部　交汇：非交换冲突区

质量若要出现，必须有翻转。翻转不能发生在任意地方。先看哪里根本没有事。

---

## 第一章　单管内部没有事

「单管」＝只沿一种面向**标签**看续接（管不是事先嵌好的圆柱）。方向由该 $e_{i}$ 标签唯一确定。框架平凡， $g\equiv\mathbf{1}$。没有「哪个在先」的歧义，没有双阱，不能隧穿。

为什么必须这样：只看一种面向时，施加者就是这一种标签；没有第二种标签在场来反对易。没有冲突，就没有两个合法真空可穿。这不是「擦掉另两管后独自自转」——锁定仍要求三管同时在场；这里只说：单标签轨道上**没有冲突可调和**。

**断在哪：** 单管内部也能隧穿 = 承认没有冲突也能翻转。后文「质量只在交汇」崩掉。把「单管无事」读成单面真空自转，与第一卷 22.6／第二卷锁定打架。

---

## 第二章　三管同时在场

交汇处三个面向标签 $e_{1},e_{2},e_{3}$ 同时在场。反对易：

$$
e_{i}e_{j}=-e_{j}e_{i}\qquad (i\neq j).
$$

先 $e_{i}$ 后 $e_{j}$ 与反过来，结果差一个负号。**无法同时确定三个方向的优先顺序。**

冲突不能消除——它就是非交换性本身。只能靠涨落在两个合法真空之间调和。不是另加的「对称性破缺点」。

上一卷已经写过：局域比对是三管同时协变的整体事件。本卷读成：整体事件发生的地方，就是交汇——「同时在场」从记账落到几何座位。负号是另一面，不是空白。

**断在哪：** 把交汇写成外加的破缺点，非交换被换成另选的势。冲突若能消去，翻转没有对象。

---

## 第三章　框架场 g

三根管锁死：拧一根，另外两根一起动。交汇区的取向自由度不是三个独立的 $\mathrm{SO}(2)$，而是**一个** $\mathrm{SU}(2)$ 值场

$$
g:\Omega\to\mathrm{SU}(2).
$$

$g=\mathbf{1}$：标准取向。 $`g\neq\mathbf{1}`$：偏离。

与上一卷「状态账只有一个 $\mathrm{SU}(2)$」同一句话：锁定压缩。本卷不重开规范群。

这个 $g$ **不是**后文的 Higgs 场。两口井分家。本卷只把 $g$ 当交汇取向。手征双阱是 $g\to -g$；电弱势、牛顿势都还没有。

**断在哪：** 三个独立转角 = 单管改写，第二卷锁定崩溃。把 $g$ 写成 $H$，后文 Higgs 从账长出的那一口被提前占用。

---

# 第二部　双阱与翻转

冲突站住了。下面问：两个合法真空是谁，翻转怎样算一次完整事件。

---

## 第四章　为何是双阱

$g\to -g$ 对应 $e_{i}\to -e_{i}$。负元平方仍记为 $-1$（ $e_{i}^{2}=-1$ 是记法，对齐第一卷 22.6；这里要的不是单面自转，而是 $\pm e_{i}$ 同表）：

$$
(-e_{i})^{2}=e_{i}^{2}=-1,
$$

反对易不变。 $+e_{i}$ 与 $-e_{i}$ 满足同一套代数。故有效势两个简并极小：

$$
g=+\mathbf{1}\quad\text{（左手真空）},\qquad g=-\mathbf{1}\quad\text{（右手真空）}.
$$

截面坐标 $\xi\in[-1,+1]$：

$$
V(\xi)=\frac{a}{4}(1-\xi^{2})^{2}.
$$

$a$ 的几何来源在第四部，此处先当势垒高度符号。

左手 / 右手的传态读法收录上一卷（正传保双态、反传拆单态）。双阱是那一手性在交汇几何上的投影。

第一卷：差异换成另一面，不是写成 $0$。这里的 $-g$ 就是另一面。两个极小都合法，不是一个真、一个空。

**断在哪：** 只有一个极小，没有隧穿，没有质量。把 $-g$ 写成 $0$，续接没有对象。

---

## 第五章　手征翻转

$$
g:+\mathbf{1}\ \xrightarrow{\text{隧穿}}\ -\mathbf{1}.
$$

格点上：某次把传态方向取反 $e_{i}\to -e_{i}$。

**完整事件。** 锁定：不能只翻一根的一部分。隧穿必须覆盖整个交汇支集。部分翻转破坏 $e_{1}e_{2}=e_{3}$，系统不再是 $\mathbb{H}$。

为什么必须整份翻：第二卷没有「只拧一根、其余不动」。翻一部分，乘法表裂开，和单管改写是同一类打结。

**断在哪：** 部分翻转 = 锁定崩溃。

---

## 第六章　翻转只发生在交汇处

单标签轨道（「单管」）：无双阱，无隧穿。交汇处三标签同时在场：势垒两侧都有真空，才能穿。

因此：**网络里唯一允许质量产生的位置是交汇。** 质量 = 施加面接到对象面的连接。没有翻转， $L$ 与 $R$ 不连，没有静止能量项。

本卷不把「连接强度」写成 MeV。下一卷才切 $m_{\tau}$，才写一口井。

**断在哪：** 管中段也能出质量 = 第一章白站。把连接强度本卷就写成 MeV，实验切口被提前用掉。

---

## 第七章　代 = 参与管数

从三根管选出 $k$ 根参与翻转：

- $k=1$：一根管的交汇支集（线）
- $k=2$：两根（面）
- $k=3$：三根（体）

没有 $k=4$（第一卷无第四面向）。故恰好三代。

选出哪几根，类型数是 $\binom{3}{k}$。那是类型计数，**不是**事件数 $n_{k}$。两件事不要并。 $\binom{3}{1}=3$ 不是 $n_{1}=4$。

翻转越多，连接越弱，质量越轻。所以 $k=1$ 最重， $k=3$ 最轻。实验室把最轻叫第 1 代，框架把最重叫 $k=1$。对着卷首表写。

**断在哪：** 写成「第 $k$ 代」，和 PDG 并混。把 $\binom{3}{k}$ 当成 $n_{k}$，第三部的闭链对不上。

---

# 第三部　nₖ

代站住了。下面数：一次完整翻转，在支集上要走多少事件。逐步统计过程（排除表、最短闭覆盖四步列出、面／体分笔）见**第二十三卷**；本卷钉闭式与三行总表。

---

## 第八章　支集是位置，不是三态

每根参与翻转的管，交汇支集沿该管是 **3 个相邻格点** $`\{0,1,2\}`$。一根管的「厚度」是 3（面向种数的一维切片）。这是横向的 $3$，不是纵向四步的 $4$。第二卷已经分家。

$$
D_{k}=\lbrace0,1,2\rbrace^{k}\times\lbrace0\rbrace^{3-k}.
$$

| $k$ | 几何 | $\lvert D_{k}\rvert$ | 图像 |
|:---:|:---|:---:|:---|
| 1 | 线 | $3$ | $x$ 轴三点 |
| 2 | 面 | $9$ | $xy$ 九宫 |
| 3 | 体 | $27$ | $3\times 3\times 3$ |

未参与的管坐标冻在 $0$。原点 $p=0$ 属于所有支集，是 $k=0$，后文六步链不进。本卷不用。

**禁止。** 不要把每点读成「不在 / 在不翻 / 在且翻」。坐标是位置档位。

> **指引。** 后文 $n_{k}$ 数的是这些位置上的覆盖事件，不是三种状态的组合。把格子读成三态， $n_{1}=4$ 会对不上闭链。

**断在哪：** $`\{0,1,2\}`$ 读成三态，事件数变成状态组合，闭链那一刀没有座位。

---

## 第九章　三条约束

翻转路径必须同时满足：

**约束 0：全覆盖。** 锁定 ⇒ 完整事件。必须走遍 $D_{k}$，不能只翻子集。代价至少 $\lvert D_{k}\rvert=3^{k}$。

**约束 1：闭链。** $H_{P}=-1$ 是拓扑不变量（第二卷）。翻转不能把 holonomy 漏到外部（第一卷：无外部）。路径边界为零。开链不够。

**约束 2：色锁定。** 仅当 $k=3$（三管全参与）。管间循环 $e_{1}\to e_{2}\to e_{3}\to e_{1}$ 不能产生净排列。额外 $3$ 步。 $k\lt 3$ 时未参与的管保持自由，这条不启动。

三条不要并成一个数。全覆盖是 $3^{k}$。闭链、色锁定是另外两刀，而且不同 $k$ 启动不同的刀。

**断在哪：** 只数 $3^{k}$，开链 holonomy 漏出去。三条揉成 $\binom{3}{k}$，事件与类型并句。

---

## 第十章　n₁=4：线上的最短闭覆盖

$k=1$ 支集是路径图 $P_{3}$：

```text
0 —— 1 —— 2
```

只有两条边。要覆盖 3 点且**闭链**：

- 不能 $0\to 1\to 2\to 0$：2 与 0 不相邻。
- 最短闭覆盖： $0\to 1\to 2\to 1\to 0$，**4 步**。这是「最小面边界长度 4」在一维线上的实现——必须折返。
- 5 步是更长的闭走，不是最小。

所以 $n_{1}=3+1$ 里的 $+1$，是「3 点开链要闭合，多走一次折返，总长 4」。不是另加一个孤立事件。折返就是另一面：走到头，转回来，不是写成空白。

$$
n_{1}=4.
$$

**算出。** 不依赖后文的比，不依赖 $S_{1}$。这是本卷最硬的一格，和第一卷 $c^{2}=-1$ 同级：走得通的最短闭覆盖就是 4。

**断在哪：** $n_{1}=3$ 是开链。 $n_{1}=\binom{3}{1}=3$ 是类型数。两者都不是事件数。

---

## 第十一章　n₂=13、 n₃=30

第十章线已经钉死： $n_{1}=4$。下面升到面、再到体。两句容易读成硬贴——「 $9+4$」「 $27+3$」。其实每加一刀都有座位：全覆盖付格子，闭链付边界，色锁定付三管轮替；哪一维已经自己闭上，哪一刀就停。

### 11.1 k=2：为何是 9+4，不是 9，也不是 9+1

$k=2$ 支集是 $3\times 3$ 九宫，位置数

$$
\lvert D_{2}\rvert=3^{2}=9.
$$

**第一笔：全覆盖 $=9$。** 约束 0：九个位置都要走到。只付 $9$，路径可以是开链——从一角进、从另一角出， $H_{P}=-1$ 会漏到面外。第一卷：无外部可漏。所以 $9$ 不够。

**第二笔：闭链还要付什么？** 面上最短能把 holonomy 关住的，是**一个最小面的四边**。第十章那条线 $0\to 1\to 2\to 1\to 0$ 恰好是「最小面边界长度 4」嵌在一维上的实现。升到 $k=2$，不是再发明一个 $+1$ 折返，而是把**整条已锁的 $n_{1}=4$ 闭链**嵌进面里付一次：

- 九宫内部的点点覆盖，由全覆盖那 $9$ 步付；
- 边界不漏，由嵌进来的 $4$ 边闭链付；
- 两笔不要并：覆盖的是**位置**，闭合的是**边界回路**。并成「再折返一次 $+1$」会把面降回线的算法，长度对不上最小面。

故

$$
n_{2}=\lvert D_{2}\rvert+n_{1}=9+4=13.
$$

不是 $9+1$（那是把面当成又一根线）；不是 $\binom{3}{2}=3$（类型数）；不是 $2\times 4=8$（两根管各付一条线闭链——面是一张共同支集，只付一次面边界）。

**断在哪：** $n_{2}=9$ 开链漏 holonomy； $n_{2}=9+1$ 把面降成线； $n_{2}=8$ 两管各付一次，双重计数边界。

### 11.2 k=3：为何是 27+3，不是 27+4，也不是 27

$k=3$ 支集是 $3\times 3\times 3$ 体，位置数

$$
\lvert D_{3}\rvert=3^{3}=27.
$$

**第一笔：全覆盖 $=27$。** 同上，必须走遍。

**第二笔：闭链还要 $+4$ 吗？** 不要。体的边界已经是**闭合的面**：走遍体时，边界上的 holonomy 被面本身关住，约束 1 在满维自动满足。再 $+4$，等于假装体还有一条「外面的线边界」要嵌——满维没有外面。所以闭链修正只在 $k\lt 3$ 启动； $k=3$ 这一刀为 $0$。

**第三笔：色锁定 $+3$。** 仅当 $k=3$（三管全参与）才启动。三根管同时在场，类型方向有轮替

$$
e_{1}\to e_{2}\to e_{3}\to e_{1}.
$$

完整翻转不能在管间留下净排列：否则锁定被拆成「某根管多转了一圈」。消掉净排列，最小是沿三色循环各补一步，共 **$+3$**。这不是又一条线闭链（那是 $4$），也不是全覆盖里已经数过的 $27$ 个位置—— $27$ 数的是格点走到没有，色锁定数的是**管与管之间的循环是否归零**。

$k\lt 3$ 时至少有一根管未参与、坐标冻在 $0$，管间循环不闭合启动，色锁定这一刀为 $0$。不要把 $+3$ 抄到 $k=1,2$。

故

$$
n_{3}=\lvert D_{3}\rvert+3=27+3=30.
$$

**断在哪：** $n_{3}=27$ 色锁定没付； $n_{3}=27+4$ 满维还装一条线边界； $n_{3}=27+4+3$ 两刀并到满维上。

### 11.3 三行对照

| $k$ | $\lvert D_{k}\rvert$ | 闭链 | 色锁定 | $n_{k}$ | 一句话 |
|:---:|:---:|:---:|:---:|:---:|:---|
| 1 | 3 | $+1$（折返成 4） | 0 | 4 | 线开链不够，最短闭覆盖必须折返 |
| 2 | 9 | $+4$（整条 $n_{1}$ 嵌进面） | 0 | 13 | 面覆盖与面边界分付；边界就是最小面四边 |
| 3 | 27 | 0（满维边界已闭） | $+3$ | 30 | 体不再 $+4$；三管全在，付色循环归零 |

闭链修正在 $k\lt 3$ 起作用，满维消失。色锁定只在 $k=3$ 起作用。不要把 $+4$ 抄到 $k=3$，也不要把 $+3$ 抄到 $k=1$。

$$
\boxed{n_{1}=4,\quad n_{2}=13,\quad n_{3}=30}
$$

**拓扑比，还不等于质量比。**

$$
R=\frac{n_{3}-n_{2}}{n_{2}-n_{1}}=\frac{17}{9}.
$$

纯有理数，不依赖 $q$、 $m_{0}$、 $\rho$。轻子质量比还要乘 $q$ 的幂。夸克代隙比另有一张表。**不要把 $17/9$ 抄成 $m_{s}/m_{d}$。** 后文的 $20$ 是三刀砍出来的，不是本卷的 $n_{k}$。

**断在哪：** $n_{2}=9$ 或 $n_{3}=27$，闭链 / 色锁定没付。把 $17/9$ 写成夸克比，第六卷的表被提前占用。

---

# 第四部　一次隧穿多重： S₁ 与 q

事件数齐了。下面问：一次隧穿的权重是多少。 $m_{0}$ 还不在这里。

---

## 第十二章　截面参数

翻转穿过鞍点。三个参数，来路分开写；**算出**与**诚实**分标，不许升格。

### 12.1 f_b=8/3（算出）

因子各认各的活动（家谱到此关上）：

| 因子 | 座位 | 本卷活动 |
|:---:|:---|:---|
| **2** | 翻转 | 双阱两向 $`+\mathbf{1}\leftrightarrow-\mathbf{1}`$（每步 2 去向） |
| **3** | 交汇 | 三管同时在场、三面向；对称化时作轮换均摊的份数 |
| **4** | 闭链 | 一次完整事件的四步闭合（与 $`n_{1}=4`$、 $`e_{i}^{4}=1`$ 同根） |

**结构。** 一次四步闭合事件，每步 2 去向，共 $`2\times 4=8`$ 次去向——8 来自翻转×闭链的会合，不是节点上另数的组合。

**对称化。** 交汇要求三面向平权：把这 8 次去向按三面向**轮换均摊**（÷3）。均摊是本卷三管／三面向对称的直接推论，不是暗箱假设，也**不是**后文色荷 $`N_{c}`$ 的比。

$$
f_{b}=\frac{2\times 4}{3}=\frac{8}{3}.
$$

**单次实现。** $`8/3`$ 是每面向的**期望份额**（有理数）。一次具体事件不能分出分数次去向，整数分配**必为** $`(3,3,2)`$ 型（三面向各得 3、3、2，总和 8）——故然：闭链四步，每步走三管两两配对中的**一条边** $`e_{i}e_{j}`$（恰三条： $`e_{1}e_{2}`$、 $`e_{2}e_{3}`$、 $`e_{3}e_{1}`$，见 [卷4序](卷4序.md)）；**三条边各至少出现一次**；每管恰属两条边，故四步在三边上的重数必为 $`(2,1,1)`$，管次即为 $`(3,3,2)`$（至多三个轮换）。这就是「一次事件到底发生了什么」；期望仍是每面向 $`8/3`$。此「边」是交汇三角上的两两配对边，不是截面 $\Sigma$。

**对称为何不被双阱拆掉（直接推论站住）。** 三面向轮换是虚单位 $`e_{1}\to e_{2}\to e_{3}`$ 的转动（ $\mathbb{H}$ 内自同构，动的是面指标）；双阱翻转是 $`e_{i}\to -e_{i}`$（反自同构，动的是方向号）。两者作用在不同标签上，互不干扰——每个阱内部三面向仍各自等权，÷3 均摊不被 $`g=\pm\mathbf{1}`$ 威胁。

**$`f_{b}`$ 本身的座位。** 它是截面权重因子，正面坐在后文势垒 $`a`$、鞍点作用量 $`S_{1}`$、隧穿权重 $`q=e^{-S_{1}}`$ 里：没有本卷这个 $`8/3`$，质量链的截面根就断了。第十三章代入 $`f_{b}=8/3`$ 写出 $`S_{1}`$ 时，座位句才算兑现。

**算出。** 因子 2、3、4 皆本卷已钉；组合规则「先 $`2\times 4`$ 再会 ÷3 轮换均摊」由三面向对称点名——故是算出，不是另立定义。无新旋钮：未引入表外参数。

**断在哪：**

- 分母 3 读成色荷 $`N_{c}`$ → 交汇对称与色荷并座。  
- 写成 $`2^{3}/3`$ → 「8」被读成节点组合数，仿佛住在格点里，不住在「每步 2 去向 × 四步」的活动里。  
- 公式写成 $`(2/3)\times 4`$ 却不点名 ÷3 → 均摊暗箱，算出依据缺一刀。  
- 三条边缺一（如管次 $`(4,3,1)`$、 $`(4,2,2)`$、 $`(4,4,0)`$）→ 有两两配对边整步缺席，交汇的轮换结构断，不再是本卷钉的闭链。  
- 另选一个截面分数 → $`S_{1}`$、 $`q`$ 失去本卷可收录的根。  
- 后文 $`S_{1}`$、 $`q`$ 不显含 $`8/3`$（或不经 $`f_{b}=8/3`$ 代入）→ 座位句成空诺，本节连根拔。

### 12.2 ρ=1-(2π)⁻⁴（指数算出；数值诚实）

截面不是完美圆。格点离散造成畸变。指数 $-4$ 与 $`e_{i}^{4}=1`$ 同一周期：截面形状对涨落的最低非平凡阶（一阶、二阶被圆对称吃掉），不是另选的幂。 $`2\pi`$ 是截面一周（后文反解给出横向一周 $=$ 纵向一周，本卷先用圆周）。

$$
\rho=1-(2\pi)^{-4}\approx 0.99936,\qquad \mathcal{I}=\rho^{2}/4.
$$

**地位分开写：**

| 部分 | 地位 | 含义 |
|:---|:---|:---|
| 指数 $-4$ | **算出** | 与四步周期同根 |
| $`2\pi`$ | 有座位 | 截面一周；本卷用圆周读法 |
| 「 $1$ 减这一项」的数值形 | **诚实** | 几何读法；紧度低于 $`n_{k}`$ |

即使 $\rho=1$， $S_{1}=2\sqrt{2}/9\approx 0.31427$，后文精细结构会偏约 $0.3$。照录公式，**不把 $\rho$ 升成与 $n_{1}=4$ 同级的格子计数**。

$90^{\circ}$ 与 $60^{\circ}$ 在这里不要写成两个可以同时取的圆心角。奇通道破缺是 $90^{\circ}$，偶通道折返是 $60^{\circ}$。本卷积分用的是相消之后还能用的截面参数，不是把两个角平均成 $75^{\circ}$。

### 12.3 势垒高度 a（由已锁参数合成）

$$
a=\frac{f_{b}\,\rho^{4}}{4\times 3}.
$$

分母仍是同一套座位：**4** = 四步闭合（亦与 $`\mathcal{I}=\rho^{2}/4`$ 里的 4 同数对照），**3** = 交汇；分子是已锁的 $`f_{b}`$ 与 $`\rho^{4}`$。不新开旋钮。第四章双阱里的 $`a`$ 在这里落地。

**算出（合成）。** 输入全是 12.1–12.2 已标地位的量；本式只做组装。  
**断在哪：** 分母的 3 再读成色荷；或把 $a$ 当成可调势垒旋钮。

---

## 第十三章　BPS 鞍点作用量

截面双阱 $V(\xi)=\frac{a}{4}(1-\xi^{2})^{2}$。活动是单向翻转 $+\mathbf{1}\to-\mathbf{1}$（第五章），不是往返。

截面动能按 $\mathcal{I}\dot{\xi}^{2}$（无额外 $\tfrac12$）写入。欧氏鞍点上 $\mathcal{I}\dot{\xi}^{2}=V$，故作用量

$$
S_{1}=\int\bigl(\mathcal{I}\dot{\xi}^{2}+V\bigr)\,d\tau=2\int_{-1}^{1}\sqrt{\mathcal{I}\,V(\xi)}\,d\xi.
$$

外因子 $2$ 来自这条 virial，不是往返。 $\mathcal{I}=\rho^{2}/4$ 已在第十二章钉死。

代入：

$$
\sqrt{\frac{\rho^{2}}{4}\cdot\frac{a}{4}(1-\xi^{2})^{2}}=\frac{\rho}{2}\cdot\frac{\sqrt{a}}{2}(1-\xi^{2}).
$$

$$
S_{1}=\frac{\rho\sqrt{a}}{2}\int_{-1}^{1}(1-\xi^{2})\,d\xi=\frac{\rho\sqrt{a}}{2}\cdot\frac{4}{3}=\frac{2\rho\sqrt{a}}{3}.
$$

再代入 $a=f_{b}\rho^{4}/12$：

$$
S_{1}=\frac{\sqrt{f_{b}}\,\rho^{3}}{3\sqrt{3}}.
$$

$f_{b}=8/3$（兑现第十二章座位句：截面因子显入 $S_{1}$）：

$$
\sqrt{f_{b}}/\sqrt{3}=\sqrt{8/3}/\sqrt{3}=\sqrt{8}/3=2\sqrt{2}/3,
$$

故

$$
\boxed{S_{1}=\frac{2\sqrt{2}\,\rho^{3}}{9}\approx 0.31367.}
$$

积分加已锁的 $f_{b}=8/3$、分母里的交汇因子 3：算出。 $\rho$ 的指数 $-4$ 与四步同根；数值前因子仍诚实。第十二章的座位期票在此兑付：若本式不经 $8/3$ 代入，12.1 连根拔。

逐步写，是为了检验打不打结：每一刀都有座位。跳到「大约 $0.31$」而不写积分，后文 $1/\alpha$ 的第 0 刀没有可收录的正文。

**断在哪：** 另选一个作用量旋钮。把单向翻成往返再塞一个根号 $2$。把动能改成 $\tfrac12\mathcal{I}\dot{\xi}^{2}$ 却不改积分约定。把 $\rho$ 升成与 $n_{1}=4$ 同级，诚实标被拿掉。

---

## 第十四章　量子权重

每次翻转是隧穿。鞍点振幅

$$
\boxed{q=e^{-S_{1}}\approx e^{-0.31367}\approx 0.7308.}
$$

$n$ 次独立翻转相乘 $q^{n}$。独立性来自乘法局域：一个交汇的框架涨落不直接改远处交汇。后文指数定律用这个 $q$。本卷只钉 $q$，不写 $m_{0}$。

下一卷才说：类型单态的带电列走 $m=m_{0}q^{n_{k}}$；切口只切 $m_{\tau}$。本卷没有 MeV。

**断在哪：** 本卷就切 $m_{\tau}$，实验切口提前。把 $q^{n}$ 套到夸克，一口井被抄错对象。

---

# 结

---

## 本卷钉死

| 符号 | 地位 | 含义 |
|:---|:---|:---|
| 翻转只在交汇 | 算出 | 唯一允许质量产生的位置 |
| 恰好三代 | 算出（无第四管） | $k=1,2,3$ |
| $`D_{k}=\{0,1,2\}^{k}`$ | 算出 | 位置格，不是三态 |
| $n_{k}=4,13,30$ | **算出** | 全覆盖 + 闭链 + 色锁定（逐步统计见第二十三卷） |
| $R=17/9$ | 算出（拓扑） | 不要抄成夸克比 |
| $f_{b}=8/3$ | **算出** | $(2\times 4)/3$ 轮换均摊；单次必 $(3,3,2)$；座位在 $a,S_{1},q$（13 章兑付） |
| $\rho=1-(2\pi)^{-4}$ | 指数**算出**；数值**诚实** | 紧度低于 $n_{k}$；不升格 |
| $a=f_{b}\rho^{4}/(4\times 3)$ | **算出**（合成） | 分母同为四步×交汇 |
| $S_{1}\approx 0.31367$ | 算出 | BPS 积分 |
| $q\approx 0.7308$ | 算出 | $e^{-S_{1}}$ |

## 本卷不假装做了的

- 没有 $m_{0}$，没有实验切口。  
- 没有夸克四条比。  
- 没有把 $m=m_{0}q^{n}$ 套到夸克。  
- 没有 $\alpha_{s}$、 $v_{H}$ 的用法。  
- $\rho$ 的指数与四步同根；数值前因子不是格子计数。  
- 框架场 $g$ 不是 Higgs。  
- 没有把 $90^{\circ}$ 与 $60^{\circ}$ 平均成第三个角。  
- 没有把「单管无事」读成单面真空自转；管是标签，交汇才是三标签同时在场（对齐第一卷 22.6／第二卷）。

## 复述

1. 单标签轨道无阱；交汇三管同时在场才有非交换冲突；框架一个 $\mathrm{SU}(2)$；双阱 $\pm\mathbf{1}$（ $\pm e_{i}$ 同表；平方是记法）。另一面，不是 $0$。  
2. 翻转是完整事件，只在交汇。 $k=$ 参与管数 = 代。无第四代。 $k=1$ 最重，不是 PDG 第 1 代。  
3. 支集是 $`\{0,1,2\}^{k}`$ 的位置格。 $n_{1}=4$ 是线的最短闭覆盖； $n_{2}=9+4$（覆盖九宫 $+$ 嵌进面的整条 $n_{1}$ 边界）； $n_{3}=27+3$（满维不再 $+4$，三管全在付色循环归零）。  
4. $S_{1}=2\sqrt{2}\rho^{3}/9$， $q=e^{-S_{1}}$。本卷不切 MeV。

**第四卷结束。**  
下一卷问：类型单态的带电列，三代质量是不是同一口井上只改事件数？——进入轻子一口井。

原则：
- 保留前卷硬约束与结论，不改变任何已锁数字与逻辑。
- **活动优先，记录忠实**：Bell／CHSH 的式子是比对续接活动的记录，不是希尔伯特先验；每一步先还原活动，再认符号。
- 本卷一条路走完：可分活动 $\Rightarrow$ Bell／CHSH $\lvert S\rvert\le 2$；交汇无优先 $\Rightarrow$ 单态 $\Rightarrow E=-\hat{a}\cdot\hat{b}$ $\Rightarrow\lvert S\rvert=2\sqrt{2}$。违规＝纠缠。与面积律 $\ln 2$ 分座。
- 不写超距传态；不写意识坍缩公理；不把 CHSH 并进 $G$／$\hbar$ 分母。

---

# 第二十一卷　量子纠缠

**本卷要解决的问题：**  
第二十卷只给读法：纠缠＝共享历史的路径序关联。本卷把它**闭成定理**：在传态网络已有表示下，可分联合比对的四设置组合不能超过 $2$；交汇无优先写入的单态给出 $E=-\hat{a}\cdot\hat{b}$，最优设置达到 $2\sqrt{2}$。破界本身就是不可因子化——这就是纠缠。

本卷只问一句：**纠缠是哪一段比对活动；Bell／CHSH 在记哪条硬约束。**

走到尽头：联合比对不可因子化；可分 $\Rightarrow\lvert S\rvert\le 2$；单态 $\Rightarrow\lvert S\rvert=2\sqrt{2}$；无超距传态；与割边 $\ln 2$ 两口不并。

$$
\boxed{\text{纠缠}=\text{不可因子化联合比对};\quad\text{可分}\Rightarrow\lvert S\rvert\le 2;\quad\text{单态}\Rightarrow\lvert S\rvert=2\sqrt{2}}
$$

不写新常数。不回头改 $G$、$\hbar$、谱。不把面积律 $\ln 2$ 写成 EPR。不把测量写成意识。

四件不要并：

| 本卷有 | 本卷没有 |
|:---|:---|
| 活动链：局域读出／交汇写入／对照兑现 | 先认 $\lvert\psi\rangle$ 再倒套活动 |
| 可分 $\Rightarrow\lvert S\rvert\le 2$；单态 $\Rightarrow\lvert S\rvert=2\sqrt{2}$（**算出**） | 超距传态边 |
| 符号还原回比对轴向、相对配对、对照读出 | 把面积律 $\ln 2$ 写成 CHSH |
| 无信号＝单侧边缘不含联合页 | 测量＝意识；改 $G,\hbar$ |

---

```text
活动（先）
    两侧各做局域比对读出（选轴向 → 落到 ±1 面）
        │
        ├─ 可分：共享历史只给「各边已钉死」的参数
        │         └─ 四个设置对上的对照组合 |S| ≤ 2
        │
        └─ 交汇无优先：只写入「两边相对如何」
                  └─ 对照读出：合面率 = −â·b̂
                        └─ 四对轴向最优 ⇒ |S| = 2√2
                              └─ |S|>2 ⇔ 联合比对拆不开 ⇔ 纠缠

记录（后）：ℍ≅ℂ²；e_i↔iσ_i；⊗；E；S
```

## 读法与指引

本卷是第二十卷纠缠条目的**算出正文**：主链闭成定理；面积律座位只对照、不吞并。不许外贴标准 QM 结论当前提——符号须还原到卷二、三已锁的比对活动。

**先活动、后记录。** 读每一式之前三问（与卷一同纲）：

1. **活动里发生了什么？** 谁与谁近邻比对？选了哪条轴向？对照时合面还是反面？
2. **这式子在记什么？** 可分、无优先、关联、CHSH 界——各对应哪条续接约束？
3. **能否还原？** 把 $\sigma$、$E$、$S$ 读回「沿轴向比对／两边对照」，重演打不打结。

| 检验 | 忠实（活动） | 不忠实（像代数必然） |
|:---|:---|:---|
| $A=\hat{n}\cdot\boldsymbol{\sigma}$ | 沿 $\hat{n}$ 做局域比对，落到 $\pm 1$ 面 | 「Pauli 矩阵本征值就是 $\pm 1$」 |
| $\lvert S\rvert\le 2$ | 各边读出已由本侧设置＋可分历史钉死，四对对照抵消到 $2$ | 「CHSH 经典界是不等式定理」 |
| $\Psi_{-}$ | 交汇只写相对配对、不写绝对朝向 | 「单态是希尔伯特里的那个矢量」 |
| $E=-\hat{a}\cdot\hat{b}$ | 两边对照时合面期望＝轴向夹角的负余弦 | 「自旋关联公式本来如此」 |
| $\lvert S\rvert=2\sqrt{2}$ | 四对读出轴向摆到最优，对照失衡破 $2$ | 「Tsirelson 界抄过来」 |

**本卷怎么走。** 第一部：局域读出与双侧对照（活动 $\to$ 记录）。第二部：可分 $\Rightarrow$ Bell／CHSH $\le 2$。第三部：交汇单态与 $E=-\hat{a}\cdot\hat{b}$。第四部：CHSH $=2\sqrt{2}$、Tsirelson、无信号。第五部：与 $\ln 2$ 分座。结。

| 符号 | 活动里 | 不要读成 |
|:---|:---|:---|
| $\hat{a},\hat{b}$ | $A$/$B$ 侧本次比对选的轴向 | 超距指令；先验空间矢量本体 |
| $a,b=\pm 1$ | 该次比对落到的一面（另一面） | 粒子内部隐藏开关 |
| $\lambda$（可分） | 过去写入、且已把各边读出钉死的共享参数 | 跨距实时电缆 |
| $C_{AB}$／$\Psi_{-}$ | 交汇写入的联合比对约束（相对配对） | 跨距实体弦 |
| $E(\hat{a},\hat{b})$ | 多次对照：$ab$ 的平均（合面 $+1$／反面 $-1$） | 力或能量 |
| $S$ | 四对设置下对照平均的固定线性组合 | 面积律熵；$\ln 2$ |

读完请能说出 boxed，并分清：Bell／CHSH 座位 vs 面积律座位。不回头改任何已闭常数。

---

# 第一部　活动：局域读出与双侧对照

---

## 第一章　收录（不重证）

| 句 | 站住在 | 活动用法 |
|:---|:---|:---|
| 近邻；局域比对＝三管整体事件 | 卷一、二 | 禁超距；交汇才能写入联合约束 |
| $e_{i}\leftrightarrow i\sigma_{i}$；$U\in\mathrm{SU}(2)$ | 卷二 | 轴向读出与面向代数同一套 |
| $\mathbb{H}\cong\mathbb{C}^{2}$ | 卷三 | 一枚 $q$ 的双态＝两侧之一的状态账 |
| 复合＝张量积 | 卷三 | 同一事件两边同时入账，不是两次分开 |
| $H_{P}=-1$ $\Rightarrow$ 自旋 $1/2$ | 卷二、十六 | $\pm 1$ 面读出合法 |
| 面积律每边 $\ln 2$ | 卷二、十六 | **另一座位** |
| 保范；混合丢可辨 | 卷二、十八 | 分离后联合约束可在、逐步序可对不齐 |

**断在哪：** 本卷另造 Pauli／$\otimes$，却还原不到卷二、三的比对活动。

---

## 第二章　单侧活动：选轴向，比对落到一面

**活动。** 一侧带着持续续接的比较状态（记录：$q\in\mathbb{H}$）。实验者（或下一跳近邻）选定一个读出轴向 $\hat{n}$：意思是——**沿哪一个面向组合做这一次局域比对**。比对结果只有两面：记 $+1$ 与 $-1$（卷一：另一面，不是空白）。

**记录。** 第三卷 $\mathbb{H}\cong\mathbb{C}^{2}$；第二卷 $e_{i}\leftrightarrow i\sigma_{i}$。同一次活动记成

$$
A(\hat{n})=\hat{n}\cdot\boldsymbol{\sigma},\qquad\text{本征值 }\pm 1.
$$

（$\sigma_{i}^{2}=1$、反对易来自 $e_{i}^{2}=-1$ 与面向反对易经同一代数转入——检验：还原回乘法表，打不打结。）

选 $\hat{n}$＝选仪器设置。读出 $a=\pm 1$＝该次比对落到哪一面。**不是**意识；**不是**改写远端。

单侧期望 $\langle A(\hat{n})\rangle$＝同一活动多次重演时 $+1/-1$ 的平均。本卷主线要的是**两侧对照**的平均。

**断在哪：** 把 $\sigma_{i}$ 读成与 $e_{i}$ 无关的外加矩阵；或把选 $\hat{n}$ 读成拧远端旋钮。

---

## 第三章　双侧活动：各读各的，再对照

**活动。** 两侧 $A$、$B$ 空间上已分离：各自只与自己的近邻比对（卷一近邻）。  
- $A$ 选轴向 $\hat{a}$，读出 $a=\pm 1$；  
- $B$ 选轴向 $\hat{b}$，读出 $b=\pm 1$；  
- **对照**：看这一对结果是合面（$ab=+1$）还是反面（$ab=-1$）。多次重演，合面减反面的平均就是关联。

没有第三条跨距边在传 $a$ 或 $b$。对照发生在**记录／事后比对表**上，或发生在未来某次两边世界线再交汇时——都不是测量瞬间的超距传态。

**记录。** 第三卷：同一事件两本账同时在 $\Rightarrow\otimes$（不是直和）：

$$
\Psi_{AB}\in\mathcal{H}_{A}\otimes\mathcal{H}_{B}\cong\mathbb{C}^{2}\otimes\mathbb{C}^{2}.
$$

$$
E(\hat{a},\hat{b}):=\big\langle\Psi\big\rvert(\hat{a}\cdot\boldsymbol{\sigma})\otimes(\hat{b}\cdot\boldsymbol{\sigma})\big\lvert\Psi\big\rangle=\sum_{a,b}ab\,P(a,b\mid\hat{a},\hat{b}).
$$

**活动还原一句。** $E$＝「两边按各自轴向读完之后，合面（$+1$）与反面（$-1$）之差的期望」。

**断在哪：** 写成直和；或先画超距边再定义 $E$。

---

## 第四章　可分 vs 纠缠：联合活动能不能拆开

**可分（活动）。** 过去的共享历史只留下参数 $\lambda$，使得：  
一旦 $\lambda$ 与 $A$ 的轴向 $\hat{a}$ 给定，$A$ 侧读出已钉死；  
一旦 $\lambda$ 与 $B$ 的轴向 $\hat{b}$ 给定，$B$ 侧读出已钉死。  
两侧**不必再互相参照**才能各自落地。联合只是两边落地结果的事后对照。

**记录。**

$$
P(a,b\mid\hat{a},\hat{b})=\int\mathbf{1}_{A(\hat{a},\lambda)=a}\,\mathbf{1}_{B(\hat{b},\lambda)=b}\,d\mu(\lambda).
$$

**纠缠（活动）。** 不存在这样的 $\lambda$：联合约束要求**对照时才能兑现完整结果**——单独看一边，读出未按「可分历史」钉死；两边合在一起，相对关系却钉死。联合比对**拆不成**「只约束 $A$」×「只约束 $B$」。

$$
\boxed{\text{纠缠}\;\Leftrightarrow\;\text{联合比对活动不可因子化}}
$$

**断在哪：** 「已经分开」＝可分。分开只说明当下无超距边；可分要看联合活动能不能拆开。

---

# 第二部　可分活动 $\Rightarrow$ Bell／CHSH 经典界

---

## 第五章　可分下，关联在记什么

可分时，每一次重演先抽到 $\lambda$，再两边各自读出。关联

$$
E(\hat{a},\hat{b})=\int A(\hat{a},\lambda)\,B(\hat{b},\lambda)\,d\mu(\lambda)
$$

＝「在可分历史混合下，合面与反面之差的平均」。$\lvert A\rvert=\lvert B\rvert=1$＝每次比对必落到一面（另一面，不是 $0$）。

---

## 第六章　Bell 界：活动版

**活动假设（可分＋同向必反面）。** 对一切 $\lambda$、一切轴向 $\hat{n}$：若两边选**同一**轴向，读出必反面——$A(\hat{n},\lambda)=-B(\hat{n},\lambda)$。  
（第三部单态会**算出**这件事；此处先问：若联合活动可分且坚持同向必反面，对照能自由到什么程度。）

取三轴向 $\hat{a},\hat{b},\hat{c}$。对固定的一次历史 $\lambda$，四个 $\pm 1$ 面之间的代数恒等式给出（记录推导同标准 Bell）：

$$
\bigl\lvert A(\hat{a})A(\hat{b})-A(\hat{a})A(\hat{c})\bigr\rvert=1-A(\hat{b})A(\hat{c}).
$$

**活动读法。** 左边：换 $B$ 侧轴向（$\hat{b}\to\hat{c}$）时，与 $A$ 对照的合面差能变多大；右边：受「$\hat{b}$ 与 $\hat{c}$ 在 $A$ 的可分账里已钉死」限制。对历史混合积分：

$$
\boxed{\bigl\lvert E(\hat{a},\hat{b})-E(\hat{a},\hat{c})\bigr\rvert\;\le\;1+E(\hat{b},\hat{c})}
$$

**还原。** 可分＋同向必反面时，你不能任意调节三对对照；换轴向带来的关联差，被另一对关联顶住。

**断在哪：** 丢掉「同向必反面」却仍写这一式；或把界读成单态预言。

---

## 第七章　CHSH 界：活动版

**活动。** 准备四对设置：$(\hat{a},\hat{b})$、$(\hat{a},\hat{b}')$、$(\hat{a}',\hat{b})$、$(\hat{a}',\hat{b}')$。每对做多次对照，得四个 $E$。组合成

$$
S:=E(\hat{a},\hat{b})+E(\hat{a},\hat{b}')+E(\hat{a}',\hat{b})-E(\hat{a}',\hat{b}').
$$

可分时：每一次历史 $\lambda$ 上，$A,A',B,B'\in\{\pm 1\}$ 都已钉死。对这一次 $\lambda$，

$$
AB+AB'+A'B-A'B'=A(B+B')+A'(B-B'),
$$

$B=\pm B'$ 两种情形下绝对值恰为 $2$。混合不能放大：

$$
\boxed{\text{可分活动}\quad\Longrightarrow\quad\lvert S\rvert\le 2}
$$

**活动还原。** $\lvert S\rvert\le 2$＝「四对对照的加减，在『各边读出已由本侧设置＋可分历史钉死』时，净失衡到不了超过 $2$」。  
若 $A$ 的读出竟依赖 $B$ 的轴向 $\hat{b}$，活动已变成超距——卷一近邻断，经典 CHSH 前提被拆。

**断在哪：** 允许设置依赖对方仪器还谈 $\lvert S\rvert\le 2$。

---

## 第八章　经典界＝可分活动的定量封印

$$
\boxed{\lvert S\rvert\le 2\;=\;\text{联合比对可因子化在四对对照上的硬界}}
$$

若活动给出 $\lvert S\rvert>2$，则**不存在**可分拆法——联合比对不可因子化——即纠缠。下一部给出网络里谁打破它。

---

# 第三部　交汇活动写入单态；对照给出 $E=-\hat{a}\cdot\hat{b}$

---

## 第九章　生成：只能在局域交汇写入

**活动。** 两边曾在某一交汇处同时入账（卷二：三管整体事件）。那一次近邻作用写下联合约束 $C_{AB}$。之后各走各的近邻续接——**没有**跨距边继续传。  
$C_{AB}$ 是过去写入的账，不是当下电缆。

$$
\boxed{\text{纠缠生成}=\text{过去局域交汇上的联合写入}}
$$

**断在哪：** 两地遥控写入；或测量瞬间拉一条跨距弦。

---

## 第十章　无优先写入：只写相对，不写绝对朝向

**活动。** 交汇三管同时在场。写入时**不点名**「整体朝哪一个绝对轴向」（无优先——与卷十同族：没被点名的方向不得潜入）。  
于是联合约束只能规定**两边相对如何配对**，不能规定整体指向。

**记录。** 纯态且在 $U\otimes U$（$U\in\mathrm{SU}(2)$）下不变（相位不论）的射线唯一：单态

$$
\Psi_{-}=\frac{1}{\sqrt{2}}\big(\lvert\uparrow\downarrow\rangle-\lvert\downarrow\uparrow\rangle\big).
$$

**活动还原。** $\Psi_{-}$＝「相对反面配对、整体无优先」这笔联合账的记录名。  
单侧单独读：哪一面都可能（绝对朝向没写）。两边对照：相对关系钉死。

**断在哪：** 把 $\Psi_{-}$ 当先验矢量，却还原不到「交汇只写相对」；或把三重态（有整体指向）当成无优先。

---

## 第十一章　同轴对照：活动上必反面

**活动。** 两边选**同一**轴向做局域比对，再对照。无优先相对配对 $\Rightarrow$ 合面期望应是完美反面。

**记录验算。** $\sigma_{3}\lvert\uparrow\rangle=+\lvert\uparrow\rangle$，$\sigma_{3}\lvert\downarrow\rangle=-\lvert\downarrow\rangle$，故

$$
(\sigma_{3}\otimes\sigma_{3})\lvert\uparrow\downarrow\rangle=-\lvert\uparrow\downarrow\rangle,\quad
(\sigma_{3}\otimes\sigma_{3})\lvert\downarrow\uparrow\rangle=-\lvert\downarrow\uparrow\rangle,
$$

$$
\langle\Psi_{-}\rvert\sigma_{3}\otimes\sigma_{3}\lvert\Psi_{-}\rangle=-1.
$$

$\mathrm{SU}(2)$ 无优先 $\Rightarrow$ 各轴同等：$\langle\sigma_{i}\otimes\sigma_{i}\rangle=-1$；异轴 $\langle\sigma_{i}\otimes\sigma_{j}\rangle=0$（$i\neq j$）。

$$
\boxed{\langle\sigma_{i}\otimes\sigma_{j}\rangle=-\delta_{ij}}
$$

**还原。** $-\delta_{ij}$＝「同轴对照必反面；异轴对照无净合面偏好」的记录。

**断在哪：** 漏两边本征值异号，误得同轴 $+1$。

---

## 第十二章　任意轴向：$E=-\hat{a}\cdot\hat{b}$

**活动。** $A$ 沿 $\hat{a}$ 读，$B$ 沿 $\hat{b}$ 读，对照合面期望应随两轴向夹角平滑变化：同向（夹角 $0$）全反面；正交无净关联；反向则全合面——与 $-\hat{a}\cdot\hat{b}$ 同形。

**记录。**

$$
E(\hat{a},\hat{b})=\sum_{i,j}a_{i}b_{j}(-\delta_{ij})=-\hat{a}\cdot\hat{b}.
$$

$$
\boxed{E(\hat{a},\hat{b})=-\hat{a}\cdot\hat{b}\qquad\text{（单态对照活动的记录）}}
$$

**特例。** $\hat{a}=\hat{b}\Rightarrow E=-1$：第六章「同向必反面」由单态活动**算出**。  
**单侧。** 只看 $A$，哪一面各半——绝对朝向从未写入（无信号座位）。

**断在哪：** 外贴 $E=-\hat{a}\cdot\hat{b}$，却不经同轴／异轴对照活动。

---

# 第四部　CHSH 破界；无信号

---

## 第十三章　四对对照摆到最优：$\lvert S\rvert=2\sqrt{2}$

**活动。** 可分时四对对照的加减到不了超过 $2$。单态下关联是 $-\hat{a}\cdot\hat{b}$，选四对轴向让合面／反面的加减尽可能失衡：

$$
S=-\hat{a}\cdot(\hat{b}+\hat{b}')-\hat{a}'\cdot(\hat{b}-\hat{b}').
$$

取

$$
\hat{b}=\frac{\hat{x}+\hat{z}}{\sqrt{2}},\;
\hat{b}'=\frac{-\hat{x}+\hat{z}}{\sqrt{2}},\;
\hat{a}=\hat{z},\;
\hat{a}'=\hat{x}
$$

（活动：$\hat{a}$ 与 $\hat{a}'$ 正交；$\hat{b},\hat{b}'$ 夹在其间 $45^{\circ}$ 族）。得 $S=-2\sqrt{2}$，

$$
\boxed{\lvert S\rvert_{\Psi_{-}}=2\sqrt{2}}
$$

| 活动情形 | $\lvert S\rvert$ |
|:---|:---|
| 联合比对可分 | $\le 2$ |
| 交汇无优先单态 | $=2\sqrt{2}$ |

$$
\boxed{2\sqrt{2}>2\;\Longrightarrow\;\text{联合比对不可因子化}\;\Longrightarrow\;\text{纠缠}}
$$

**主定理（活动句）。** CHSH 破 $2$，就是在说：这两侧的对照活动，**没法**拆成「各边读出已由可分历史钉死」。破界本身就是纠缠的操作判据；单态给出网络里破到 $2\sqrt{2}$ 的显式活动。

**断在哪：** 只抄 $2\sqrt{2}$ 不写四对轴向如何摆；或把 $2\sqrt{2}$ 并进面积律／$G$。

---

## 第十四章　Tsirelson 上界（双体 dichotomic）

单态已给出 $\lvert S\rvert=2\sqrt{2}$。再问：在 $\mathbb{C}^{2}\otimes\mathbb{C}^{2}$、两侧 dichotomic $\pm 1$ 局域读出下，还能不能更大？

**命题。** 任意此类态与设置，$\lvert S\rvert\le 2\sqrt{2}$。

**证明。** 令跨侧对易的 $\pm 1$ 观测量 $A,A'$（$A$ 侧）与 $B,B'$（$B$ 侧），$A^{2}=A'^{2}=B^{2}=B'^{2}=I$。CHSH 算子

$$
\mathcal{B}=A\otimes B+A\otimes B'+A'\otimes B-A'\otimes B'.
$$

直接展开（跨侧对易）：

$$
\mathcal{B}^{2}=4I-[A,A']\otimes[B,B'].
$$

$\pm 1$ 观测量的对易子满足 $\lVert[A,A']\rVert\le 2$、$\lVert[B,B']\rVert\le 2$，故

$$
\lVert[A,A']\otimes[B,B']\rVert\le 4,\qquad\lVert\mathcal{B}^{2}\rVert\le 8,\qquad\lVert\mathcal{B}\rVert\le 2\sqrt{2}.
$$

因此对任意态 $\lvert\langle\mathcal{B}\rangle\rvert\le 2\sqrt{2}$。单态在第十三章最优角上饱和此界。

$$
\boxed{\text{可分 }\le 2\;\lt\;\text{单态 }2\sqrt{2}=\text{双体 dichotomic 上界}}
$$

**算出。** 上界与单态饱和。本卷不讨论更高维或通信复杂度放宽后的更大违规。

**断在哪：** 把 $2\sqrt{2}$ 写成可任意突破的实验拟合旋钮；或把 Tsirelson 并进面积律分母。

---

## 第十五章　无信号：单侧活动不含联合页

**活动。** 只看 $A$ 侧多次读出：哪一面各半，**不**随 $B$ 选哪个轴向而变。$A$ 没法靠选 $\hat{a}$ 给 $B$ 发可控比特。  
关联只在**两边对照**时出现——兑现的是交汇写入的相对页，不是当下传态。

**记录。** $\rho_{A}=I/2$ $\Rightarrow P(a\mid\hat{a})=1/2$，与 $\hat{b}$ 无关。

$$
\boxed{\text{测一边钉死相对另一边}=\text{兑现联合页；单侧边缘无信号}}
$$

**「立刻」活动还原。** 不是有东西跑过去；是联合页本来成对——一边落地，对照栏一并可读。类空下谁先读，只是谁先揭开同一页。

**断在哪：** 「立刻」＝ $>c$ 传态。

---

## 第十六章　回收并钉死第二十卷

分离后各自近邻续接。共享的是交汇写入的相对配对（记录名 $\Psi_{-}$）。测量＝局域读出＋对照兑现（测量活动本身见第二十二卷）。

$$
\boxed{\text{纠缠}=\text{共享历史的不可因子化联合比对（单态为其规范代表）}}
$$

第二十卷读法句在此获得**算出**身份；CHSH 破界＝其定量判据。

---

# 第五部　与面积律 $\ln 2$ 分座

---

## 第十七章　两个座位，两种活动

| 座位 | 活动 | 记录 |
|:---|:---|:---|
| **Bell／CHSH** | 两侧局域读出再对照；问联合能不能拆开 | $E$、$S$、$2\sqrt{2}$ |
| **面积律／事件** | 区域边界上穿越边的传态二分 | 每边 $\ln 2$；事件锁 |

$$
\boxed{\ln 2\text{（割边活动）}\;\neq\;S\text{（双体对照活动）}}
$$

禁止并分母。

---

# 结

---

## 本卷钉死

| 闭合句 | 地位 |
|:---|:---|
| 读出＝沿轴向局域比对落到 $\pm 1$ 面；$E$＝对照合面期望 | **算出**（活动＋记录） |
| 可分 $\Rightarrow$ Bell 界；$\lvert S\rvert\le 2$ | **算出** |
| 交汇无优先 $\Rightarrow$ 单态 $\Psi_{-}$（唯一性） | **算出** |
| $\langle\sigma_{i}\otimes\sigma_{j}\rangle=-\delta_{ij}$；$E=-\hat{a}\cdot\hat{b}$ | **算出** |
| 最优设置 $\Rightarrow\lvert S\rvert=2\sqrt{2}>2$ $\Rightarrow$ 纠缠 | **算出** |
| 双体 dichotomic：$\lvert S\rvert\le 2\sqrt{2}$（Tsirelson）；单态饱和 | **算出** |
| 无信号：边缘 $I/2$ | **算出** |
| 生成座位＝局域交汇；无超距传态 | **算出**（近邻纪律） |
| $\ln 2$ 割边 ≠ CHSH | 纪律 |

## 本卷不假装做了的

- 没有用面积律 $\ln 2$ 导出对照关联。  
- 没有探测器效率／loophole 工程论。  
- 没有多体 GHZ／W／拓扑序的完整分类。  
- 没有坍缩公理或意识。  
- 没有改 $G$、$\hbar$；没有新常数。  
- 没有更高维或通信辅助下的超 Tsirelson 构造。

## 复述（先活动）

1. **活动：** 两侧各选轴向做局域比对，落到 $\pm 1$ 面；对照合面／反面。  
2. **可分：** 共享历史已把各边读出钉死 $\Rightarrow$ 四对对照 $\lvert S\rvert\le 2$（Bell／CHSH）。  
3. **交汇无优先：** 只写相对配对 $\Rightarrow$ 单态；$E=-\hat{a}\cdot\hat{b}$。  
4. **破界：** 最优轴向 $\Rightarrow\lvert S\rvert=2\sqrt{2}>2$ $\Rightarrow$ 联合比对拆不开 $\Rightarrow$ **纠缠**；且 $2\sqrt{2}$ 为双体 dichotomic 上界。  
5. **测量：** 兑现联合页；单侧边缘无信号；无超距传态。  
6. **分座：** 双体对照 ≠ 割边 $\ln 2$。  
7. **记录**须还原回活动；还原不了就不认。

$$
\boxed{\text{纠缠}=\text{不可因子化联合比对};\quad\text{判据 }\lvert S\rvert>2;\quad\text{单态给出 }\lvert S\rvert=2\sqrt{2}}
$$

**第二十一卷结束。**  
下一卷问：测量本身在传态网络里是哪一段活动——系统与仪器的交汇，而非另立坍缩。

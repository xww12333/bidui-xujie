原则：
- 保留原体系所有硬约束与结论，不改变任何已锁数字与逻辑。
- 语言更口语、更有过渡，减少「电报式」跳跃。
- 每卷开头先用白话说明「这一卷要解决什么问题」。
- 关键推导后补充「为什么必须这样，否则会断在哪里」。
- 严格保持原有纪律：不假装做了的、两口不并、座位分明。
- **活动优先，记录忠实**：管、传态、 $H_{P}$ 都是比对续接的记录；须还原到三管同时在场的活动，不许先画圆柱再塞比对。
- 本卷一条路走完： $\mathbb{H}$ → 三管锁死 → 传态 → $H_{P}=-1$。

---

# 第二卷　三管与传态

**本卷要解决的问题：**  
上一卷站住：不可约比对持续近邻续接，面向恰好三个，记录名叫 $\mathbb{H}$。乘法表、反对易、 $e₁e₂=e₃$ 全部收录，不重证。 $e_{i}^{2}=-1$ 也收录——但上一卷 22.6 已钉：闭表后这是**记法**，真实转向是另两边合成再作用（如 $e₂e₃e₁$），不是单面真空自转。

现在只问一句：三个面向在持续续接里长成什么？绕最小闭环留下什么因子？

走到尽头：三根正交且锁死的管；比对沿管延续叫做传态；绕最小面一周， $H_{P}=-1$。状态是 $\mathbb{H}^{3}$。

本卷停在这里。不写三层账、规范群、粒子、时空。代数符号仍是记录。管还不是 $\mathbb{R}^{3}$ 里的圆柱。顶点还不是空间点。

---

```text
ℍ（三个面向已闭）
        │
        ▼
三根管：四步周期 · 正交可辨 · 锁定不能单改
        │
        ▼
传态 = 比对沿管延续
        │
        ▼
事件与螺旋是同一句话
        │
        ▼
闭环不可避免 → 最小闭环是面
        │
        ▼
绕面一周 H_P = −1
        │
        ▼
ℍ³；保范不保远距可辨（λ = ln 5）
```

$$
\boxed{\mathbb{H}\ \Longrightarrow\ \text{三管锁死}\ \Longrightarrow\ \text{传态}\ \Longrightarrow\ H_{P}=-1}
$$

## 读法与指引

**记录与活动：** 代数符号仍是记录，不是前提；管、传态、事件/螺旋、 $H_{P}$ 每一步都要能还原到比对沿三根管**持续续接**的活动本身，检验记录是否忠实。不是先画三根圆柱，再把比对塞进去。

四个词是四步，不是四个同义词。 $\mathbb{H}$ 上一卷已闭。本卷只问：三个面向在续接里长成什么，绕最小面留下什么因子。不要倒过来：不是先画了三根圆柱，再把比对塞进去。

**本卷怎么走。** 第一部把管站住（四步、正交、锁定）。第二部才说传态。第七章钉死：事件与螺旋是同一句话，不要拆成两套机制。第三部才绕面算出 $H_{P}=-1$——逐步写，不跳。第四部的 $D(n)$、面积律不升级成度规或视界。

本卷的「证明」仍是上一卷那一句：沿规划路径重演比对序列，检验打不打结。各章「断在哪」就是这种检验。

| 这里的词 | 读成 | 不要读成 |
|:---|:---|:---|
| 管 | 沿一种面向**标签**的续接轨道；三管同时在场（第四章） | $\mathbb{R}^{3}$ 里事先嵌好的圆柱；单管真空自转 |
| 传态 | 比对沿管延续： $q_{w}=eᵢ\cdot qᵥ$ | 已经有粒子在管子里跑 |
| 事件 / 螺旋 | 同一传态的代数读 / 几何读 | 两套并列的机制 |
| $e_{i}^{2}=-1$ | 两步到另一面（记法）；闭表活动见上一卷 22.6 | 擦掉另两管后的独自平方 |
| 另一面 | 两步送到 $-q$；差异还在 | 写成 $0$；差异被抹去 |
| $H_{P}=-1$ | 绕最小面一周的代数因子；与 $e_{i}^{2}=-1$ 同属「另一面」 | 时空曲率；物质源 $T_{\mu\nu}$ |
| $\lambda=\ln 5$ | $3\times 2-1$：三管正反，回程不当新分支 | 新耦合；已经是视界半径 |
| $3$ 与 $4$ | 横向三点 $N_{c}$；纵向 $e_{i}^{4}=1$ | 同一个数的两种写法 |

读完请能说出 boxed。账本、规范群、费米子是下一卷。本卷没有空间点。

---

# 第一部　三根管

三个面向已经站住。下面问：同一种面向若反复施加，续接留下什么轨道。还没有网络，还没有粒子。

---

## 第一章　面向已经是方向

上一卷的「方向」不是空间轴。它是：一种不可约比对可以持续施加。

把这种持续施加叫做沿一根**管**走。管是「按某一种面向标签反复续接」的轨道——**标签**是 $e_{i}$，不是说另两根管不在场。第四章锁定会钉死：局域比对是三管同时协变；没有「只拧一根、其余假装没动」。

没有事先嵌好的舞台——舞台若出现，是后文从邻接长出来的，本卷不画。

三种面向 ⇒ 三根管。上一卷第二十三章：没有第四个面向。故没有第四根管。

为什么必须这样：面向是「可以持续施加的那一种」。持续施加就是沿同一条续接标签走下去。三种就三根。不是外选的几何维数。

**断在哪：** 承认第四根管 = 承认第四个面向。上一卷 $f^{2}=-1$ 已排除。把「沿一根管」读成单面真空自转 = 擦掉锁定，退回上一卷「一个面向」。

---

## 第二章　四步周期

沿标签 $e_{i}$ 每一步左乘。收录上一卷： $e_{i}^{2}=-1$，故 $e_{i}^{4}=1$。

$$
q\;\mapsto\;e_{i} q\;\mapsto\;e_{i}^{2} q=-q\;\mapsto\;e_{i}^{3} q=-e_{i} q\;\mapsto\;e_{i}^{4} q=q.
$$

四步回到 $q$。两步得到 $-q$，不是 $q$，更不是空白。

**与上一卷 22.6 对齐。** 式子 $e_{i}^{2}=-1$ 是记法。三面向已闭、三管锁死后，「 $e_{1}$ 转向」的真实续接是 $e₂e₃e₁$（另两边合成 $e_{1}$，再 $e_{1}$ 作用），不是擦掉 $e₂,e₃$ 后独自平方。本章沿管写 $eᵢeᵢ$，是**按管标签记账**；活动侧始终三管同时在场（第四章）。 $-q$ 仍是「另一面」，与 22.6 的 $-1$ 同纪律。

**螺旋是什么意思。** 走完一个 $e_{i}^{2}$ 周期，状态被送到对面——取反。再走一圈才回到自己。走回来的不是你，是你的取反。这是螺旋，不是圆。

上一卷已经钉过：差异换成另一面，不是写成 $0$。这里的 $-q$ 就是那一句在管上的读法。两步不是「减没了」，是整圈轮替的反转。四步才回到「什么都没变」。

没有新参数。四步来自 $e_{i}^{2}=-1$，不是另设的周期。

**$3$ 与 $4$ 不要并。** $4$ 是沿一根管标签走的纵向周期。 $3$ 是面向种数，后面读成横向点数。同一套续接的两个座位，不是一个数的两种写法。

**断在哪：** 把两步当成回到自己，等于把 $-q$ 写成 $q$，另一面被抹掉。把两步当成 $0$，续接没有对象可施加。把沿管 $e_{i}^{2}$ 读成单面真空自转，抹掉三管锁定。

---

## 第三章　正交：三根管可辨

上一卷： $\lbrace 1,e₁,e₂,e₃\rbrace$ 四维线性无关。故 $e_{i}$ 不能写成其他 $e_{j}$ 的实线性组合。三根管是三个独立方向。

可辨在这里的读法：你能说出「这次走的是哪一根」。说不出，三种面向在续接里不可指认，等于上一卷白站。

为什么必须这样：差异存在要求可辨。三种面向若在管上混成「反正是一根」，种数在续接中消失。

**断在哪：** 三管不可辨 = 面向种数在续接中消失。

---

## 第四章　锁定：不能单管改写

上一卷： $e₁e₂=e₃$。沿 $e_{1}$ 作用会改 $e_{2}$ 方向上的状态：

$$
e_{1}(e_{2} q)=e_{3} q,\qquad e_{2}(e_{1} q)=-e_{3} q.
$$

两边不等。先走这根再走那根，与反过来，差一个负号——另一面，不是空白。改一根，另外两根一起动。

正交保证独立方向存在；锁定保证它们是不可分割的整体。

- 差正交：三管不可辨。  
- 差锁定：乘法表破了，系统不再是 $\mathbb{H}$。

上一卷已预告：一次近邻作用当场涌出第三者。本卷读成：没有「只拧一根管、其余不动」的操作。局域比对一旦发生，三根管同时在场。

$$
\boxed{\text{局域比对}=\text{三管同时协变的整体事件}}
$$

**断在哪：** 单管改写破坏 $e₁e₂=e₃$，闭包裂开。

---

# 第二部　传态

管站住了。下面把「比对沿管走一步」写成网络上的一笔。符号是记录。

---

## 第五章　传态就是比对沿管延续

上一卷：比对的结果仍是比对，可进入下一步。差异存在就是持续续接。

从实例 $v$ 沿 $e_{i}$ 走一步到相邻实例 $w$：

$$
q_{w}=e_{i}\cdot q_{v},\qquad q_{v}\in\mathbb{H}.
$$

这叫**传态**。不是在外部空间里搬家。上一步的结果成为下一步的对象——续接写成网络上的一步。

$v$ 是一个比较闭包实例，不是 $\mathbb{R}^{3}$ 里的点。有向边是「用哪种不可约操作连接两个实例」。空间若从邻接涌现，是后文的事。本卷禁止先画坐标再「传播」。

$$
\boxed{\text{网络不是「做」传态。网络是传态。}}
$$

没有另设一台机器在旁边搬运状态。网络就是这些近邻续接本身。

**断在哪：** 先有空间点、再在点上传播 = 把舞台当成前提。上一卷已经禁止倒过来。

---

## 第六章　三条直接后果

全部来自上一卷，本卷只改读法。

| 性质 | 上一卷 | 本卷含义 |
|:---|:---|:---|
| 保范 | $e_{i}$ 可逆， $eᵢ^{-1}=-eᵢ$；差异不在这一步消亡 | 结构上 $\Vert q_{w}\Vert$ 与 $\Vert q_{v}\Vert$ 同（模长公式仍未写，不消亡） |
| 局域 | 近邻作用 | 比对必须触及对象，不能超距 |
| 可逆 | 逆元自动； $eᵢ^{-1}=-eᵢ$ | 每一步有回程 |

反向传态：

$$
q_{v}=e_{i}^{-1}\cdot q_{w}=-e_{i}\cdot q_{w}.
$$

回程用的是同一个 $e_{i}$ 的逆，不是第二套操作。正传 / 反传作为入账开关，下一卷才用。本卷只钉回程存在。

保范这一句，读成：传态不丢信息。差异没有被抹成空白。后面第十三章会补：不丢的是结构；远距离上对不齐的是可辨。打乱的是路径序，不是内容写成 $0$。

**断在哪：** 这一步涌出空白 = 上一卷无零因子崩溃。超距 = 承认外部通道。

---

## 第七章　事件与螺旋是同一句话

近邻比对一旦发生，就是锁死三元组的整体事件（第四章）。事件不是另加的一层。

传态沿管延续， $e_{i}^{4}=1$，四步一周期。几何包络是螺旋。

$e₁e₂=e₃$ 的代数读法是乘法表；几何读法是三正交螺旋。不是旁边另有一套。

**离散与连续。** 步是离散的（近邻比对、四步一周期）。延续是连续的（沿管不停）。不是先有格点再取连续极限。两种读法同一传态。

为什么必须并成一句：拆成「先有离散事件、再有连续螺旋」，就是把续接拆成两把尺子。上一卷的差异存在只有一种活动：近邻比对在持续。代数记它，几何也记它，不是两套机制。

> **指引。** 后文凡把「事件」和「螺旋」拆成两把尺子，都是把本章拆开。翻转打断乘法表时必须折返，不是另立一套几何。

后文若出现翻转打断 $e₁e₂=e₃$，必须折返才能回到乘法表。那不是新规则，是本章锁定的恢复。本卷不证翻转。

本卷不写 $\ln 2$ 的精确纠缠、曲率振幅、格距数字。

**断在哪：** 事件一套、螺旋一套 = 同一传态被写成两个生成元。

---

## 第八章　闭环不可避免

三根管、有限种操作、 $\mathbb{H}$ 四维闭合。沿不同管交替走若干步，路径必然回访已到过的区域——状态用 $\mathbb{H}$ 值标记，乘法结构有限。

闭环不是被设计的回路条件。有限种不可约操作下，持续传态必然闭环。

最小闭环就是下一部的**面**。

为什么必须这样：上一卷基有限。有限种面向、有限维记录，走下去一定碰上已经到过的组合。不是人为画了一个圈。

**断在哪：** 若传态永不回访，等于承认无限种新状态从外部涌入。上一卷基有限，不许。

---

# 第三部　面 holonomy

闭环已经不可避免。下面取最小的那一种，算出绕一周留下什么。

---

## 第九章　面

从实例 $v$ 出发：沿 $e_{i}$ 一步 → 沿 $e_{j}$（ $j\neq i$）一步 → 沿 $e_{i}$ 反向一步 → 沿 $e_{j}$ 反向一步，回到 $v$。四条边，记为面 $P=(eᵢ,eⱼ)$。

三种独立面型： $e₁,e₂$； $e₂,e₃$； $e₃,e₁$。没有第四种——没有第四根管。

这是组合上的最小闭环，还不是时空里的小面积。面积、度规是后文的事。

**断在哪：** 第四种面 = 第四根管 = 第四个面向。上一卷已排除。

---

## 第十章　绕面一周： H_P=-1

绕面一周，传态总累积因子

$$
\boxed{H_{P}=-1}
$$

对三种面型统一成立。下面逐步算，不跳。

### 10.1 逆元

$$
e_{i}^{-1}=-e_{i},
$$

因为

$$
e_{i}(-e_{i})=-e_{i}^{2}=1.
$$

收录上一卷第十九章、第十五章。回程不是第二套操作。

### 10.2 面 e₁,e₂ 逐步

先 $e_{1}$ 正、再 $e_{2}$ 正、再 $e_{1}$ 反、再 $e_{2}$ 反（循环重标不改结论）：

$$
H_{P}=e_{2}^{-1}\cdot e_{1}^{-1}\cdot e_{2}\cdot e_{1}.
$$

代入逆元 $eᵢ^{-1}=-eᵢ$：

$$
H_{P}=(-e_{2})(-e_{1})e_{2}e_{1}=e_{2}e_{1}e_{2}e_{1}=(e_{2}e_{1})^{2}.
$$

反对易： $e₂e₁=-e₁e₂=-e₃$。故

$$
H_{P}=(-e_{3})^{2}=e_{3}^{2}=-1.
$$

为什么必须是 $-1$：绕最小面一周，用的就是乘法表自己。没有新参数。对面出现的是取反，不是空白。 $H_{P}=-1$ 是另一面在闭环上的显现。

### 10.3 另外两面

同一步骤：

$$
(e_{3}e_{2})^{2}=(-e_{1})^{2}=e_{1}^{2}=-1,
$$

$$
(e_{1}e_{3})^{2}=(-e_{2})^{2}=e_{2}^{2}=-1.
$$

三种面型同一个因子。

**唯一来源。** $e_{i}^{2}=-1$，即上一卷「差异不自湮灭／只能转向」。闭表后该式是记法（22.6）；绕面算出的 $H_{P}=-1$ 与之**同属「另一面」**——不是对某个特殊网络另算的数，是乘法表在闭环上的投影。

**禁止。** 不要把 $H_{P}=-1$ 读成时空曲率，也不要读成物质源。本卷的 $-1$ 是代数面因子：处处都在，不随「这里有没有一个质量」开关。后文的源是动力学超额，不是把这个 $-1$ 写进 $T_{\mu\nu}$。也不要把「来自 $e_{i}^{2}$」读成「单管真空自转绕了一圈」。

**断在哪：** 绕一周得到 $+1$，等于两步已经回到自己，另一面被抹掉。绕一周得到 $0$，续接涌出空白。

---

## 第十一章　ℤ₂ 冗余

每个实例可内部重标 $qᵥ\to gᵥqᵥ$，其中 $g_{v}$ 只取 $+1$ 或 $-1$。边上多一个 $gᵥg_{w}$。绕面一周，每个实例出现两次（一进一出）， $g_{v}^{2}=1$。故

$$
H_{P}'=H_{P}=-1.
$$

这不是外加的规范群。闭环分辨不出中间节点的整体 $\pm 1$。内禀冗余。下一卷的规范群从账本自同构读，座位不同，不要并。

**断在哪：** 把这一笔 $\pm 1$ 升级成 $\mathrm{SU}(2)$ 或 $\mathrm{U}(1)_{Y}$，就是把冗余写成下一卷的群。本卷不停在那里。

---

## 第十二章　2π 得 -1

绕最小面一周得 $-1$。若把这个最小闭环在连续读法里看成转一小圈——结构上对应 $2\pi$——则转 $2\pi$ 得到 $-1$，转 $4\pi$ 才回到 $+1$。这是自旋 $1/2$ 的定义性质。

逻辑只走到这里：

$$
e_{i}^{2}=-1\ \longrightarrow\ H_{P}=-1\ \longrightarrow\ 2\pi\text{ 得 }-1.
$$

| 步骤 | 地位 |
|:---|:---|
| $H_{P}=-1$ | **算出** |
| $2\pi\to-1$、 $4\pi\to+1$ | **算出**（连续读法） |
| 「因此是费米子」 | **不在本卷内证**（需自旋统计，那是场论定理，不是从比对推出来的） |

不要写成「从差异存在严格推出了自旋统计」。连续读法把最小闭环看成一小圈，得到定义性质；统计定理是另一口。

---

# 第四部　可辨、状态、连续读法

$H_{P}=-1$ 已算出。下面三件不要升级：可辨衰减不是度规，面积律是机制级，Yang-Mills 系数是读法。

---

## 第十三章　保范不保可辨

每步差异不消亡（第六章）。但 $n$ 步之后，走法数目按分支增长。

怎么数：三根管，每根正反两向，共 $6$。回程存在（第六章），刚走来的反向不当新分支， $6-1=5$。故

$$
N_{n}\sim 5^{n},\qquad D(n)\sim e^{-\lambda n},\qquad \lambda=\ln 5.
$$

$\lambda$ 是 $3\times 2-1$ 的对数，不是另选的衰减率，也不是新耦合。

上一卷的可辨是局域自证：差异自己站住。不是无限远仍可指认。差异存在维持自身结构（保范），但不在远距离上维持可辨认性。

读成上一卷那一句：传态不丢信息。远了之后对不齐的是路径序，显现像只看见一个面。结构还在，不是写成 $0$。本卷不把「序可以再对齐」写成磁盘或无线电——那是后文的读法。这里只钉：保范 $\neq$ 远距可辨。

本卷不把 $D(n)$ 写成度规，也不写成视界半径。

**断在哪：** 把 $\lambda=\ln 5$ 当成新耦合，或把 $D(n)$ 写成视界，都是升级。把可辨衰减写成「信息没了」，是把另一面记成空白。

---

## 第十四章　面积律（机制级）

穿越一块区域边界的每条传态边，连接内外各一个实例。边上有正反向两种读法（回程，第六章），粗计贡献一份二分，记一份 $\ln 2$。边界共 $\lvert\partial A\rvert$ 条边，面积项 $\lvert\partial A\rvert\ln 2$。再加 $H_{P}=-1$ 的拓扑一份 $\ln 2$。

信息带宽等于最小割——图论的 max-flow min-cut。面积律是图的必然，不是另选的全息公设。

**地位。** 机制级。精确纠缠、黑洞熵公式本卷不写。后文若写面积律接到体验或 $H_{P}$ 接到突触，那是把本卷机制级升成认知机制，禁止。

---

## 第十五章　状态是 ℍ³

每根管上，比较状态是一枚完整的 $q\in\mathbb{H}$。 $\mathbb{H}$ 闭合，状态用不了更小的不可约块。三根管：

$$
\mathcal{Q}_{v}=\mathbb{H}^{3},\qquad \dim_{\mathbb{R}}=3\times 4=12.
$$

$3$ 来自面向种数， $4$ 来自上一卷维数。不是 $\mathbb{R}^{3}$。也还不是类型 $\times$ 状态的张量积——那要先做手性投影，下一卷。

$3$ 与 $4$ 在这里相乘得 $12$，仍是两个座位：管数 $\times$ 一枚 $q$ 的实维。不要并成「截面也是四步」或「纵向也是三点」。

本卷停在 $\mathbb{H}^{3}$。

---

## 第十六章　偏离 -1 的代价（机制级）

比较操作可有微小偏离纯 $e_{i}$（上一卷：稳定给出确定结果；偏离仍是操作）。包络上把边因子读成 $U\in\mathrm{SU}(2)$。 $\mathrm{SU}(2)$ 来自 $\mathbb{H}$ 虚部与远程矩阵实现 $eᵢ\leftrightarrow i\sigmaᵢ$。同一代数，不是另加的内部空间。

小面展开（标准 Wilson 圈读法）：

$$
H_{P}=U_{1}U_{2}U_{1}^{-1}U_{2}^{-1}\approx\mathbf{1}+iga^{2}F_{\mu\nu}^{a}\tfrac{\sigma^{a}}{2}+O(a^{4}).
$$

偏离 $-1$ 有能量代价。格点上用 $\mathrm{Re}\,\mathrm{tr}(H_{P})$ 衡量，连续读法写成 Yang-Mills。

| 步骤 | 地位 |
|:---|:---|
| $H_{P}=-1$ 是拓扑极值 | **算出** |
| 偏离 $-1$ 有能量代价 | **结构算出** |
| 展开系数、 $g$、 $\beta=2/g^{2}$ | **读法**（标准 Wilson 圈，不是本卷新分数） |
| $S_{\mathrm{YM}}=-\frac{1}{4}\int F^{2}$ | **连续极限的标准匹配，不装成逐步推出的每个系数** |

本卷闭的是：作用量是 holonomy 偏离 $-1$ 的代价，不是外贴的场。不说「从比对推出了 Yang-Mills 拉氏量的每一个系数」。

---

# 结

---

## 本卷钉死

| 符号 | 站住在 | 含义 |
|:---|:---|:---|
| 三根管 | 第一、二章 | 按 $e_{i}$ 标签的四步续接轨道（三管同时在场） |
| 正交 + 锁定 | 第三、四章 | 可辨且不可单改 |
| 传态 $q_{w}=eᵢ\cdot qᵥ$ | 第五章 | 比对沿管延续 |
| $e_{i}^{2}=-1\to$ 四步／ $-q$ | 第二章；活动对齐上一卷 22.6 | 记法；非单面真空自转 |
| $eᵢ^{-1}=-eᵢ$ | 收录上一卷，第六章用于回程 | 同一套操作的逆 |
| $H_{P}=-1$ | 第十章 **算出** | 代数面因子；与 $e_{i}^{2}$ 同属另一面 |
| $\mathbb{Z}_{2}$ 冗余 | 第十一章 | 闭环看不出整体 $\pm 1$ |
| $2\pi\to-1$ | 第十二章 | 连续读法 |
| $D(n)$， $\lambda=\ln 5$ | 第十三章 **算出**（ $3\times 2-1$） | 保范不保远距可辨 |
| $\mathbb{H}^{3}$，实维 $12$ | 第十五章 | 三管各一枚 $q$ |

## 本卷不假装做了的

- 没有三层账，没有 $\mathrm{SU}(3)\times\mathrm{SU}(2)\times\mathrm{U}(1)_{Y}$。  
- 没有一代费米子表，没有超荷。  
- 没有把 $H_{P}=-1$ 当成时空曲率或物质源。  
- 没有算出 $G$、 $\hbar$。  
- Yang-Mills 系数是读法。自旋统计不在本卷内证。  
- 没有翻转、没有质量。  
- 没有把沿管 $e_{i}^{2}=-1$ 读成单面真空自转；管是标签，锁定要求三管同时在场（对齐上一卷 22.6）。  
- 没有把可辨衰减写成「信息没了」；没有写磁盘、无线电、突触。

## 复述

1. 三个面向 = 三根四步周期的管（标签 $e_{i}$；三管同时在场）。正交可辨，锁定不可单改。两步是 $-q$，另一面，不是 $0$； $e_{i}^{2}=-1$ 是记法，活动对齐上一卷 22.6。  
2. 传态是比对沿管的续接。保范、局域、可逆。网络就是传态。不丢信息；远了丢的是可辨。  
3. 事件与螺旋是同一句话。最小闭环是四面。乘法表给出 $H_{P}=-1$，与 $e_{i}^{2}=-1$ 同属「另一面」。  
4. $\pm 1$ 重标看不出； $2\pi$ 得 $-1$。  
5. 状态是 $\mathbb{H}^{3}$。 $\lambda=\ln 5$ 来自 $3\times 2-1$，不把 $D(n)$ 写成度规。

**第二卷结束。**  
下一卷问：同一笔传态怎么入账？保这套账的变换是什么群？全部合法入账方式是谁？

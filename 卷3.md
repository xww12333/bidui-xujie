原则：
- 保留原体系所有硬约束与结论，不改变任何已锁数字与逻辑。
- 语言更口语、更有过渡，减少「电报式」跳跃。
- 每卷开头先用白话说明「这一卷要解决什么问题」。
- 关键推导后补充「为什么必须这样，否则会断在哪里」。
- 严格保持原有纪律：不假装做了的、两口不并、座位分明。
- **活动优先，记录忠实**：账本与群名是同一笔传态如何入账的记录；须还原到三管锁定下的传态活动，不许先列标准模型再往回套。
- 本卷一条路走完： $3$– $2$– $1$ → 规范群 → 一代入账。超荷从相对相位读，不用 $Q=T_{3}+Y$ 倒推。

---

# 第三卷　三层账

**本卷要解决的问题：**  
上一卷站住：三管锁死、传态、 $H_{P}=-1$、状态 $\mathbb{H}^{3}$。本卷只收录，不重证。第一卷 22.6／第二卷已钉： $e_{i}^{2}=-1$ 是记法，管是标签，局域比对三管同时在场——本卷摊账时仍守这一句。

同一笔传态已经发生。现在只问三句：它怎么入账？保这套账的变换是什么群？全部合法入账方式是谁？

走到尽头：三层账 $3$– $2$– $1$；自同构 $\mathrm{SU}(3)\times\mathrm{SU}(2)\times\mathrm{U}(1)_{Y}$；一代费米子是正传 / 反传下的全部格子。超荷分数从锁定后的相对相位读出，不用电磁荷倒推。

本卷停在这里。不写质量、代、 $G$、光子、时空。

---

三件东西不要并：

| 本卷分开写 | 不要并成 |
|:---|:---|
| 手性 = 一枚 $q$ 的左右乘 | 类型账（走哪根管） |
| $\mathrm{SU}(2)$ = 锁定压成的**一个** | 三管各一个 |
| $Y$ 从相对相位逐格读 | 先列表、再用 $Q=T_{3}+Y$ 倒推 |

```text
同一笔传态
        │
        ▼
手性（左右乘）≠ 类型（哪根管）
        │
        ▼
三层账：类型 ℂ³ · 状态 ℂ² · 相对相位
        │
        ▼
自同构 → SU(3)×SU(2)×U(1)_Y
        │
        ▼
两个开关 → 一代入账
        │
        ▼
Y 从相对相位读（单位 1/6）
```

$$
\boxed{3\text{–}2\text{–}1\ \Longrightarrow\ \mathrm{SU}(3)\times\mathrm{SU}(2)\times\mathrm{U}(1)_{Y}\ \Longrightarrow\ \text{一代入账}}
$$

## 读法与指引

**记录与活动：** 账本与群名仍是记录，不是前提；手性、三层账、规范群、一代入账每一步都要能还原到**同一笔传态**如何入账的活动本身，检验记录是否忠实。不是先列标准模型粒子表，再往回套进网络。

箭头是「同一笔传态怎么入账 → 保账的变换是什么群 → 全部合法格子是谁」。不是三个时空维变出标准模型。拆账不是另造结构，是把同一事件已经携带的信息分开写清楚。

**本卷怎么走。** 第一部先分手性（左右乘）和类型（走哪根管）。第二部才摊开三层账：张量积，不是直和。第三部从账本自同构读规范群： $\mathrm{SU}(2)$ 是锁定压成的**一个**，不是三管各一个；两路 $\mathrm{U}(1)$ 被锁定叠掉一路。第四部入账。第五部超荷从相对相位读，不许用 $Q=T_{3}+Y$ 倒推。

本卷的「证明」仍是规划路径上的比对序列重演。各章「断在哪」就是这种检验。

| 这里的词 | 读成 | 不要读成 |
|:---|:---|:---|
| $3$– $2$– $1$ | 类型账、状态账、相位账 | 空间三维；已经是三代 |
| 手性 | 一枚 $q$ 的左乘 / 右乘 | 走哪根管 |
| 固定 $e_{1}$ | 记账用的复结构标签 | 单面真空自转；擦掉另两管 |
| $\mathrm{SU}(2)$ | 锁定压成的一个双态 | 三根管各一个弱群 |
| 规范群 | 保三层账的自同构 | 先验标准模型；外贴标签 |
| $Y$ | 锁定后相对相位的读数 | 先列表再用电磁荷倒推 |
| $15$ 与 $12$ | 一代 Weyl 条数；顶点实维 | 同一个计数 |
| $\nu_{R}$ 的 $0$ | 相对相位两份对消；可记可不记 | 谱上少了一个粒子 |

读完请能说出 boxed。本卷不写 MeV、代、 $G$。

---

# 第一部　手性：左右乘，不是哪根管

还没有问「走哪根管」。先站住：同一枚 $q$，左乘和右乘不是同一件事。

---

## 第一章　左乘和右乘不是同一件事

第一卷：施加者与对象角色不同， $e_{i}e_{j}=-e_{j}e_{i}$。同一枚 $q$，左乘 $e_{i}q$ 与右乘 $qe_{i}$ 给出不同结果。

这是**乘法位置**的差异。还没有问沿哪根管走。

手性 ≠ 类型。后文两套开关分开用：类型非平凡 / 单态是一刀，正传 / 反传是另一刀。并成一句，两套开关塌成一套，格子数不对。

**断在哪：** 把左右乘读成「走哪根管」，手性被类型吃掉。后文入账少一刀。

---

## 第二章　固定 $e_{1}$，一枚 $q$ 是双态

要在 $\mathbb{H}$ 上读出双态，须选定一个复结构。记法上用 $e_{1}^{2}=-1$，取 $\mathrm{span}_{\mathbb{R}}\{1,e_{1}\}$ 为 $\mathbb{C}_{e_{1}}$。这是**记账标签**：选定「用哪一根管标签切复结构」，不是宣称 $e_{1}$ 在真空里独自自转（第一卷 22.6；第二卷锁定：三管同时在场）。

任意 $q\in\mathbb{H}$：

$$
q=a+be_{1}+ce_{2}+de_{3}=(a+be_{1})+(c+de_{1})e_{2}=z_{1}+z_{2}e_{2}.
$$

用了 $e_{3}=e_{1}e_{2}$。 $z_{1},z_{2}\in\mathbb{C}_{e_{1}}$。

$$
\boxed{\mathbb{H}\;\cong\;\mathbb{C}^{2}}
$$

两个复分量就是状态账的双态。 $N_{w}=2$ 在这里涌现：不是外选的弱同位旋维数，是 $\mathbb{H}$ 作为左 $\mathbb{C}_{e_{1}}$-模的维数。

为什么必须这样：一枚完整的 $q$ 在选定一个复结构之后，恰好两个复坐标。少一个，闭包不够；多一个，上一卷没有第四个面向。换标签（固定 $e_{2}$ 或 $e_{3}$ 切复结构）只是换记账，不另开物理。

在这组坐标里，绕 $e_{1}$ 的左乘对角读成

$$
z_{1}:\ T_{3}=+\tfrac{1}{2},\qquad z_{2}:\ T_{3}=-\tfrac{1}{2}.
$$

$T_{3}$ 不是电磁荷。它是双态里「上 / 下」的标记。拆单态时还要用。本卷自始至终不用 $Q=T_{3}+Y$。

**断在哪：** 把 $N_{w}=2$ 写成外选的弱维数，双态不再从 $\mathbb{H}$ 长出。把 $T_{3}$ 写成电荷，第五部的 $Y$ 就被倒推污染。把「固定 $e_{1}$」读成单面真空自转，锁定与 22.6 被抹掉。

---

## 第三章　正传保双态，反传拆单态

正传 $q_{w}=e_{i}\cdot q_{v}$。反传 $q_{w}=e_{i}^{-1}\cdot q_{v}=-e_{i}\cdot q_{v}$。上一卷：回程是同一套操作的逆，不是第二套。

$\mathbb{C}_{e_{1}}$ 从**右边**乘。右边乘与左边传态对易，故正传是 $\mathbb{C}$-线性的：双态送到双态，不拆开。

反传取逆。对 $e_{2}$ 算一遍（其余循环相同）。 $z_{1}$ 与 $e_{2}$ 反对易， $e_{1}$ 被翻号：

$$
e_{2}^{-1}(z_{1}+z_{2}e_{2})=\bar z_{2}-\bar z_{1}\,e_{2}.
$$

坐标里：

$$
(z_{1},z_{2})\mapsto(\bar z_{2},-\bar z_{1}).
$$

这是 $\mathbb{C}$-**反**线性的。反线性映射不能保 $\mathbb{C}$-线性双态。 $\mathbb{C}^{2}$ 被拆成两个 $\mathbb{C}^{1}$；共轭把类型三重态读成共轭三重态。

为什么必须拆：反传不是「正传加一个负号就完」。取逆翻了复结构，两个分量对调并共轭。上一卷的另一面在这里读成：双态被送到对面，不是写成 $0$。

$$
\boxed{\text{正传：}\mathbb{C}\text{-线性，保双态}\qquad\text{反传：}\mathbb{C}\text{-反线性，拆单态}}
$$

左手双态 / 右手单态从此读出。不是外加的手性安排。把四个字贴到粒子名字上：读法。正传保双态、反传出共轭：算出。

**断在哪：** 正反传都保双态，反传那一刀白做，后文没有两个右手单态。把反传说成「粒子跑反了」，乘法位置被换成空间方向。

---

# 第二部　同一笔传态的三层账

手性站住了。下面摊开：同一笔传态同时记三本账。不是三种过程。

---

## 第四章　类型账：沿哪根管

三种不可约操作；三根管可辨。每根管的正向复分量是一个 $z_{1}^{(i)}$。三根并在一起：

$$
\mathcal{T}\;\cong\;\mathbb{C}^{3}.
$$

类型记的是：**用哪种不可约操作在传**。三种操作代数关系相同，类型可以连续混合，不只是换标签。

正交 ⇒ 三根管可辨 ⇒ 类型三重态合法。  
锁定 ⇒ 三根管是不可分割的整体 ⇒ 类型单态也合法。

走类型账的，后文叫做夸克。三管作为整体入账、不携带单管标签的，后文叫做轻子。不是外加分类。正交给出「能指出哪一根」；锁定给出「也可以不指出、整笔入账」。两格都合法。

**断在哪：** 只有三重态、没有单态 = 丢掉锁定。只有单态、没有三重态 = 丢掉正交。

---

## 第五章　状态账：这枚 $q$ 的两个分量

第二章： $\mathbb{H}\cong\mathbb{C}^{2}$。

$$
\mathcal{S}\;\cong\;\mathbb{C}^{2}.
$$

状态记的是：被比对的那枚 $q$ 在复结构 $e_{1}$ 下的两个分量。它不问走哪根管。

$\mathcal{T}$ 的 $3$ 来自管数。 $\mathcal{S}$ 的 $2$ 来自 $\mathbb{H}$ 的复维。两本账来源不同。不要把状态账写成「正传还是反传」——那是手性开关，不是这本账。

$$
\boxed{N_{w}=2}
$$

**断在哪：** 把状态账的 $2$ 写成管数的一部分，两本账并成一本。

---

## 第六章　顶点是张量积，不是直和

节点同时携带「沿哪根管」和「这枚 $q$ 的分量」。两本账来源不同，又在同一事件上同时发生（上一卷：局域比对是三管协变的整体事件）。组合只能是张量积：

$$
\mathcal{Q}_{v}\;\cong\;\mathcal{T}\otimes\mathcal{S}\;\cong\;\mathbb{C}^{3}\otimes\mathbb{C}^{2}.
$$

若写成直和，类型和状态互斥——一笔传态不能同时记两本账。

复维 $3\times 2=6$。实维 $3\times 2\times 2=12$。另一侧： $\mathbb{H}^{3}$ 实维也是 $12$。两侧同为 $12$，因为 $\mathbb{H}\cong\mathbb{C}^{2}$、三管 $=3$。 $\mathbb{H}^{3}$ 不是外选的状态空间。

为什么必须是张量积：上一卷的整体事件不允许「先记类型、再记状态、两次分开」。同一笔，两本账同时在。

**断在哪：** 直和 = 一笔传态只能记一本账。维数对不上 $12$，上一卷 $\mathbb{H}^{3}$ 被改写。

---

## 第七章　相位账：相对相位

类型来自第一卷三种操作。状态来自一枚 $\mathbb{H}$ 的复分解。两本账没有公共生成元，顶点又同时带着它们。

因此存在第三本账：类型与状态之间的**相对相位**。锁定之前的朴素形式：

$$
(t,s)\;\longmapsto\;\bigl(e^{i\beta}t,\;e^{i\alpha}s\bigr).
$$

$\alpha,\beta$ 平面二维。第三部证明锁定吃掉一维，剩下的才是超荷。这里先站住：有相对相位这本账。还不是 $Y$。

**断在哪：** 两本账来源不同却不许有相对相位 = 顶点带着两本账，比较结果对不齐。

---

## 第八章　三层总表

同一笔传态同时记三层。不是三种过程，是同一局域事件的三种读法：

| 账 | 记什么 | 空间 | 来源 |
|:---|:---|:---|:---|
| 类型 | 沿哪根管 | $\mathcal{T}\cong\mathbb{C}^{3}$ | 面向种数 $=3$ |
| 状态 | $q$ 的两个复分量 | $\mathcal{S}\cong\mathbb{C}^{2}$ | $\mathbb{H}\cong\mathbb{C}^{2}$ |
| 相位 | 两账相对相位 | $\mathrm{U}(1)$ | 来源不同；锁定剩一维 |

$$
\boxed{\text{同一笔传态交易有三层账：}3\text{–}2\text{–}1}
$$

对准类型层看 $\mathrm{SU}(3)$。对准状态层看 $\mathrm{SU}(2)$。对准相位层看 $\mathrm{U}(1)_{Y}$。三层同时看，就是规范群。这是读法，不是三个不同的场。

$3$– $2$– $1$ 不是空间三维，也不是三代。三代是后文交汇维 $k$，本卷不写。

---

# 第三部　规范群是账本的自同构

账摊开了。下面问：哪些变换保这套账不动。那不是外贴标签，也不是「标准模型群本来就该是这样」——是**同一笔传态的记账对称**读成的群名。

---

## 第九章　自同构问哪句话

规范群不是外贴标签。它是保持下列四件都不动的变换：类型账的 Hermitian 结构；状态账的 Hermitian 结构；相对相位；管间锁定 $e_{1}e_{2}=e_{3}$。

禁止并成「管间一个、管内三个」。

**断在哪：** 丢掉锁定再数自同构，会数出三个 $\mathrm{SU}(2)$，乘法表裂开。

---

## 第十章　类型账 $\to\mathrm{SU}(3)$

三根管的正向分量构成 $\mathcal{T}\cong\mathbb{C}^{3}$。保 Hermitian 内积的复线性自同构是 $\mathrm{U}(3)$。

必须复线性：手性投影已经把 $\mathbb{H}$ 切成复分量。类型混合若抹掉复结构，正反手性那一刀白做。不是 $\mathrm{SO}(3)$（那只转实三元组）。不是 $S_{3}$（离散置换是子群，不是全部；三种操作代数对称，连续混合合法）。

$$
\mathrm{U}(3)=\frac{\mathrm{SU}(3)\times\mathrm{U}(1)_{B}}{\mathbb{Z}_{3}}.
$$

$\mathrm{SU}(3)$：行列式 $1$ 的类型混合。后文叫做颜色。 $\mathrm{U}(1)_{B}$：三根管的共同相位，还不是超荷。 $\mathbb{Z}_{3}$：中心数两次，商掉。实维 $3^{2}-1=8$。

$$
\boxed{\text{类型账的连续自同构}=\mathrm{SU}(3)}
$$

把 $\mathrm{SU}(3)$ 叫做颜色：读法。分解本身：算出。本卷不把八个生成元升级成胶子动力学。

**断在哪：** 写成 $\mathrm{SO}(3)$ 或 $S_{3}$，类型不能连续混合，或复结构被抹掉。

---

## 第十一章　状态账 $\to$ 一个 $\mathrm{SU}(2)$

管锁死。沿任何一根管旋转，另外两根同时动。不能单管改写。

因此不是 $\mathrm{SU}(2)^{3}$。内部旋转被压成**一个** $\mathrm{SU}(2)$，作用在 $\mathcal{S}\cong\mathbb{C}^{2}$ 上：混合 $z_{1}$ 与 $z_{2}$，不是三根管的标签。

若三管各一个 $\mathrm{SU}(2)$， $e_{1}e_{2}=e_{3}$ 被拆开，系统不再是 $\mathbb{H}$。实维 $2^{2}-1=3$。

$$
\boxed{\text{状态账在锁定下的自同构}=\text{一个 }\mathrm{SU}(2)}
$$

叫做 $\mathrm{SU}(2)_{L}$：读法（正传保双态）。「一个而不是三个」：算出。

> **指引。** 后文荷电流只读这一个双态。写成三管各一个弱群， $e_{1}e_{2}=e_{3}$ 就拆了。后文若出现「三代管」四个字，就是把本章与交汇维并成一句。

**断在哪：** 三个 $\mathrm{SU}(2)$ = 单管改写，上一卷锁定崩溃。

---

## 第十二章　两路 $\mathrm{U}(1)$ 重叠

类型贡献 $\mathrm{U}(1)_{B}$。状态侧 $\mathrm{U}(2)$ 里除掉 $\mathrm{SU}(2)$ 还剩 $\mathrm{U}(1)_{W}$：双态的共同相位。朴素计数两路阿贝尔。锁定不允许两路都独立。

绕 $e_{1}$ 转 $\theta$： $e^{e_{1}\theta}=\cos\theta+e_{1}\sin\theta$。左乘到 $e_{2}$：

$$
e^{e_{1}\theta}\cdot e_{2}=(\cos\theta)\,e_{2}+(\sin\theta)\,e_{3}.
$$

右乘 $e^{-e_{1}\theta}$，用 $e_{2}e_{1}=-e_{3}$：

$$
e_{2}\cdot e^{-e_{1}\theta}=(\cos\theta)\,e_{2}+(\sin\theta)\,e_{3}.
$$

$$
\boxed{e^{e_{1}\theta}\cdot e_{2}=e_{2}\cdot e^{-e_{1}\theta}}
$$

管内绕 $e_{1}$ 转 $+\theta$，等价于在类型方向 $e_{2}$ 上贴一个 $-\theta$ 的相位。状态账上的一转，同时在类型账上记了一笔。朴素平面里对角线 $\alpha=\theta$、 $\beta=-\theta$ 是同一动作的两种读法，不能数两次。二维减一维，剩一维。

为什么必须叠掉一路：锁定就是「改一根，另外两根一起动」。两路相位若都独立，等于又允许单管改写。

**断在哪：** 两路 $\mathrm{U}(1)$ 都留下，锁定被绕开，超荷多一个自由参数。

---

## 第十三章　剩下的相对相位是 $\mathrm{U}(1)_{Y}$

独立的阿贝尔生成元是类型与状态之间真正的相对相位：

$$
(t,s)\;\longmapsto\;\bigl(e^{i\phi}\,t,\;e^{-i\phi}\,s\bigr).
$$

它对 $\mathrm{SU}(3)$、 $\mathrm{SU}(2)$ 都对易。这个 $\mathrm{U}(1)$ 记为 $\mathrm{U}(1)_{Y}$，其荷叫做超荷。

$$
\boxed{G=\mathrm{SU}(3)_{C}\times\mathrm{SU}(2)_{L}\times\mathrm{U}(1)_{Y}}
$$

不是另选的电磁群。本卷不写光子、Weinberg 混合。重叠存在、剩一维：算出。命名为 $Y$：读法。

**断在哪：** 把 $\mathrm{U}(1)_{Y}$ 叫做光子，混合角被提前写进本卷。

---

## 第十四章　维数 $8+3+1=12$

$$
\dim G=8+3+1=12=\dim_{\mathbb{R}}\mathbb{H}^{3}.
$$

规范变换穷尽顶点内部的实自由度。没有第十三种可再加，也不缺一种。 $8$ 来自 $\mathbb{C}^{3}$ 的迹零酉代数， $3$ 来自 $\mathbb{C}^{2}$， $1$ 来自锁定后的相对相位； $12$ 来自三管 $\times$ 四元数实维。两侧是同一份 $\mathbb{H}^{3}$。

维数相等：算出。读成「规范场吃尽内部自由度」：读法。

这一笔 $12$ 是顶点内部实维。后文一代 Weyl 条数是 $15$。两笔不要并。

**断在哪：** 再加一个 $\mathrm{U}(1)$ 或第四个 $\mathrm{SU}(2)$，维数超过 $12$，上一卷 $\mathbb{H}^{3}$ 被撑破。

---

# 第四部　一代入账

群站住了。下面问：正传 / 反传、类型非平凡 / 单态，全部格子是谁。不是从词典另选五种粒子。

---

## 第十五章　两个开关，不是一张表示表

费米子不是从词典里另选五种。它们是三层账在正传 / 反传下的全部入账方式。

| 开关 | 问的是 | 不是 |
|:---|:---|:---|
| 类型 | 三重态 / 单态 | 手性 |
| 手性 | 正传保双态 / 反传拆单态 | 类型 |

|  | 正传（双态） | 反传（单态） |
|:---|:---|:---|
| 类型非平凡 | $\mathbf{3},\mathbf{2}$ | $\bar{\mathbf{3}},\mathbf{1}$，两个 |
| 类型单态 | $\mathbf{1},\mathbf{2}$ | $\mathbf{1},\mathbf{1}$ |

反传是 $\bar{\mathbf{3}}$ 不是 $\mathbf{3}$：反线性引入共轭。反传占两格：双态拆开， $T_{3}=\pm 1/2$ 把两个单态分开。

**断在哪：** 两个开关并成一个，格子不够一代。反传仍写 $\mathbf{3}$ 不写 $\bar{\mathbf{3}}$，共轭那一刀白做。

---

## 第十六章　四格怎么住

**$Q_{L}=(\mathbf{3},\mathbf{2})$。** 正传，类型走三根管的正向分量。复维 $3\times 2=6$。两个分量共享同一类型、同一手性，后文共享同一超荷。

**$\bar u_{R},\bar d_{R}=(\bar{\mathbf{3}},\mathbf{1})$。** 反传，类型翻成 $\bar{\mathbf{3}}$，双态拆成两个 $\mathbb{C}^{1}$。各复维 $3$。超荷不同，不是同一表示复制两次。

**$L_{L}=(\mathbf{1},\mathbf{2})$。** 锁定允许三管作为整体入账。正传，双态完好。复维 $2$。无色，因为没走类型账。

**$\bar e_{R}=(\mathbf{1},\mathbf{1})$。** 类型单态，反传拆双态。荷非零的那一张，复维 $1$。

**$\nu_{R}=(\mathbf{1},\mathbf{1})_{0}$。** 同一格里荷为零的那一张。可以记进表，也可以不记——记账取舍，不是谱上少了一个粒子。

$$
\boxed{\nu_{R}\text{ 可记可不记，不是缺失粒子}}
$$

复维合计 $6+3+3+2+1=15$。这是一代 Weyl 分量的个数，**不是**顶点内部实维 $12$。 $12$ 是几何 / 规范代数； $15$ 是入账条数（ $\nu_{R}$ 不计入 $15$）。两笔不要并。

为什么 $15\neq 12$：一个数顶点里还能怎么转，一个数同一笔传态有几种入账格子。并成同一个维数，规范代数和费米子谱被焊死成一句，后文对不上。

---

## 第十七章　入账总表（超荷下一章填）

| 粒子 | 手性 | $\mathrm{SU}(3),\mathrm{SU}(2),Y$ | 入账 |
|:---|:---|:---|:---|
| $Q_{L}$ | 正传 | $\mathbf{3},\mathbf{2}$， $Y=1/6$ | 类型三重态 $\times$ 状态双态 |
| $\bar u_{R}$ | 反传 | $\bar{\mathbf{3}},\mathbf{1}$， $Y=-2/3$ | 类型共轭 $\times$ 单态（ $T_{3}$ 上） |
| $\bar d_{R}$ | 反传 | $\bar{\mathbf{3}},\mathbf{1}$， $Y=+1/3$ | 类型共轭 $\times$ 单态（ $T_{3}$ 下） |
| $L_{L}$ | 正传 | $\mathbf{1},\mathbf{2}$， $Y=-1/2$ | 类型单态 $\times$ 状态双态 |
| $\bar e_{R}$ | 反传 | $\mathbf{1},\mathbf{1}$， $Y=+1$ | 类型单态 $\times$ 状态单态 |
| $\nu_{R}$ | 反传 | $\mathbf{1},\mathbf{1}$， $Y=0$ | 同上，荷为零；可略 |

格子穷尽：算出。粒子名字：读法。 $Y$ 的来路在第五部，不在本表「看出来」。表上先写下分数，是为了对着后文核；推导在第二十、二十一章。

本卷没有三代。 $k=1,2,3$ 是后文交汇维，不是本卷的类型账。

---

# 第五部　超荷从相对相位读出

格子齐了。下面逐格读 $Y$。先钉单位，再正向，再反向。不许用电磁荷倒推。

---

## 第十八章　先钉单位

不用 $Q=T_{3}+Y$ 倒推。本卷还没有电磁。 $T_{3}$ 只用第二章的上 / 下标记。

**类型周期 $\mathbb{Z}_{3}$。** 三种类型循环 $e_{1}\to e_{2}\to e_{3}\to e_{1}$。沿三根管各传一次，类型账回到等效状态。相对相位必须跟着闭合，否则比较的结果不可再被比较。三次之后 $e^{i\cdot 3Y\cdot\phi}=1$，故 $3Y\in\mathbb{Z}$。

**状态周期 $\mathbb{Z}_{2}$。** 双态绕一圈得 $-1$，与上一卷 $H_{P}=-1$ 是同一件 $-1$。两周才回到 $+1$。故 $2Y\in\mathbb{Z}$。

两本账同时对齐：

$$
Y\in\tfrac{1}{3}\mathbb{Z}\cap\tfrac{1}{2}\mathbb{Z}=\tfrac{1}{6}\mathbb{Z}.
$$

$$
\boxed{Y_{0}=\tfrac{1}{6}}
$$

没有新参数。后文每一格都是 $n/6$。

为什么必须这样：类型走三步闭合，状态走两步（另一面）才闭合。相对相位要同时跟两本账，单位只能是 $1/6$。不是从电荷表倒 squint 出来的。

**断在哪：** 用 $Q=T_{3}+Y$ 先写出电荷再反解 $Y$，电磁被提前塞进本卷。真空中性那一套是后文的读法，不在这里定 $Y_{0}$。

---

## 第十九章　一格上怎么计数

锁定之后，独立动作是

$$
(t,s)\mapsto(e^{i\phi}t,\,e^{-i\phi}s).
$$

张量拾到的相位是类型绕的圈减状态绕的圈。整体符号是约定：让正向、类型三重态、状态双态那一格取正的最小单位 $+1/6$。

| 入账 | 类型贡献 | 理由 |
|:---|:---|:---|
| $\mathbf{3}$ | $1/3$ | 走一根管；三管循环的一份 |
| $\mathbf{1}$ | $1$ | 三管作为锁定整体；三份加齐 |
| $\bar{\mathbf{3}}$ | $-1/3$ | 反传共轭，类型贡献翻号 |

| 入账 | 状态贡献 | 理由 |
|:---|:---|:---|
| 双态 $\mathbf{2}$ | $1/2$ | 双态共同的 $\mathbb{Z}_{2}$ 半周期 |
| 单态 | 见下 | 双态已拆， $T_{3}$ 并进相位账 |

轻子的类型贡献是 $1$ 不是 $0$：单态不是「没走类型」，是三管整笔写入。夸克走一根，写 $1/3$。这是锁定 vs 正交。

---

## 第二十章　正向两格

**$Q_{L}$。** 类型 $1/3$，状态 $1/2$：

$$
Y(Q_{L})=\tfrac{1}{2}-\tfrac{1}{3}=\tfrac{1}{6}.
$$

两个分量共享同一个 $Y$。

**$L_{L}$。** 类型单态贡献 $1$，状态 $1/2$：

$$
Y(L_{L})=\tfrac{1}{2}-1=-\tfrac{1}{2}.
$$

轻子比夸克「更负」：轻子把三根管整笔写入类型贡献，夸克只写一根。这是锁定 vs 正交在相位账上的直接读数。

$$
\boxed{Y(Q_{L})=\tfrac{1}{6},\qquad Y(L_{L})=-\tfrac{1}{2}}
$$

**断在哪：** 轻子类型贡献写成 $0$，锁定在相位账上消失， $Y(L_{L})$ 对不上。

---

## 第二十一章　反向：共轭翻号，再并入 $T_{3}$

反传两件事同时发生：类型共轭（正向 $Y$ 翻号，得到共轭双态的基值）；双态拆开， $T_{3}=\pm 1/2$ 并入相位账（锁定已消灭独立的 $\mathrm{U}(1)_{W}$）。

$$
Y_{\pm}=Y_{\text{共轭基值}}\pm\tfrac{1}{2}.
$$

夸克： $Y(Q_{L})=+1/6$，共轭基值 $-1/6$：

$$
Y(\bar u_{R})=-\tfrac{1}{6}-\tfrac{1}{2}=-\tfrac{2}{3},\qquad Y(\bar d_{R})=-\tfrac{1}{6}+\tfrac{1}{2}=+\tfrac{1}{3}.
$$

轻子： $Y(L_{L})=-1/2$，共轭基值 $+1/2$：

$$
Y(\bar e_{R})=+\tfrac{1}{2}+\tfrac{1}{2}=+1,\qquad Y(\nu_{R})=+\tfrac{1}{2}-\tfrac{1}{2}=0.
$$

$\nu_{R}$ 的 $0$ 是相对相位两份对消，所以可记可不记。不是缺失，是这格荷为零。

$$
\boxed{Y(\bar u_{R})=-\tfrac{2}{3},\quad Y(\bar d_{R})=+\tfrac{1}{3},\quad Y(\bar e_{R})=+1,\quad Y(\nu_{R})=0}
$$

仍不用 $Q$。整体符号是约定；格子之间的比不依赖它。

**断在哪：** 两个右手夸克写成同一个 $Y$，双态拆开那一刀白做。用 $Q$ 倒推这四个分数，第五部的纪律崩掉。

---

## 第二十二章　账目完备（一次型）

五种入账是同一笔传态的全部格子。荷的一次型必须闭合，否则同一事件的两本读法对不上。验的是自洽，不是另选表示凑零。本卷不引入时空。

$$
\sum Y=6\cdot\tfrac{1}{6}+3\cdot\bigl(-\tfrac{2}{3}\bigr)+3\cdot\tfrac{1}{3}+2\cdot\bigl(-\tfrac{1}{2}\bigr)+1=1-2+1-1+1=0.
$$

$Q_{L}$ 复维 $6$ 是色 $3$ 乘双态 $2$。四则算术：算出。把相消读成「 $\mathbb{H}^{3}$ 自洽」：读法。分数与一圈标准模型相同——原因是框架的入账，不是另调的荷。本卷不把这一行升级成引力定理。

---

# 结

---

## 本卷钉死

| 符号 | 站住在 | 含义 |
|:---|:---|:---|
| $N_{w}=2$ | 第二章 | $\mathbb{H}\cong\mathbb{C}^{2}$（复结构标签，非单面自转） |
| $3$– $2$– $1$ | 第八章 | 类型、状态、相对相位 |
| $G=\mathrm{SU}(3)\times\mathrm{SU}(2)\times\mathrm{U}(1)_{Y}$ | 第十至十三章 | 账本自同构（非先验 SM） |
| $8+3+1=12$ | 第十四章 | 与 $\mathbb{H}^{3}$ 实维相同 |
| $Y_{0}=1/6$ | 第十八章 | $\mathbb{Z}_{3}$ 与 $\mathbb{Z}_{2}$ 对齐 |
| 一代五格 + $\nu_{R}$ 可略 | 第十五至十七章 | 两个开关的全部格子 |
| $Y$ 五条分数 | 第二十、二十一章 | 相对相位逐格读出 |

## 本卷不假装做了的

没有质量、代、CKM、时空、 $G$。没有把 $Y$ 加成电磁荷，没有把 $\mathrm{U}(1)_{Y}$ 叫做光子。没有用 $Q=T_{3}+Y$ 倒推超荷。没有把手性与类型并句，没有写三个 $\mathrm{SU}(2)$，没有把 $\nu_{R}$ 写成缺失，没有把 $15$ 与 $12$ 并成同一个维数。没有把「固定 $e_{1}$」读成单面真空自转；没有把规范群读成先验标准模型。

## 复述

1. 左乘 ≠ 右乘。固定 $e_{1}$ 是复结构记账标签（非单面自转）；一枚 $q$ 是双态。正传保双态，反传拆单态。手性不是类型。  
2. 同一笔传态记三层：类型 $\mathbb{C}^{3}$、状态 $\mathbb{C}^{2}$、相对相位。张量积，实维 $12$。  
3. 自同构： $\mathrm{SU}(3)$ 转类型；**一个** $\mathrm{SU}(2)$ 转状态；两路 $\mathrm{U}(1)$ 被锁定叠掉一路，剩下 $\mathrm{U}(1)_{Y}$。 $8+3+1=12$。群名是记账对称，不是先验 SM。  
4. 两个开关的全部格子就是一代费米子。 $\nu_{R}$ 可记可不记。  
5. $Y$ 对齐 $\mathbb{Z}_{3}$ 与 $\mathbb{Z}_{2}$，单位 $1/6$。正向读出 $1/6$ 与 $-1/2$；反向共轭再并入 $T_{3}$，读出 $-2/3$、 $+1/3$、 $+1$、 $0$。

**第三卷结束。**  
下一卷问：翻转在哪发生、怎么数；一次隧穿多重是多少。——进入交汇与翻转。

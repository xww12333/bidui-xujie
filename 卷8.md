原则：
- 保留原体系所有硬约束与结论，不改变任何已锁数字与逻辑。
- 语言更口语、更有过渡，减少「电报式」跳跃。
- 每卷开头先用白话说明「这一卷要解决什么问题」。
- 关键推导后补充「为什么必须这样，否则会断在哪里」。
- 严格保持原有纪律：不假装做了的、两口不并、座位分明。
- **活动优先，记录忠实**： $V$、三个模、 $60^{\circ}$ 是代空间混路与奇通道锁定的记录；须还原到三管同时在场下的交汇活动，不许先认实验 CKM 再往回填角。
- 本卷一条路走完：混路 → 三块 $2\times 2$ → $V$； $K_{12}=e^{-i\pi/3}$ → $\lvert\gamma\rvert=60^{\circ}$。不写选序 $65^{\circ}$。

---

# 第八卷　CKM 与弱 CP

**本卷要解决的问题：**  
上一卷站住：六味对角 MeV。本卷只吃四条代隙比，不改尺子，不重写对角谱。前几卷已钉：管是面向标签， $e_{i}^{2}=-1$ 是记法，局域比对三管同时在场——本卷谈「单管顺序／升降」时仍守这一句。

本卷只问一句：代空间里站不直的那一截，怎么读进荷电流。

走到尽头：两套「三」不可互换；混路让质量基与弱基站不直，故 CKM 不是单位阵；偶通道杀掉代混合、奇通道就是锁定；三个独立模来自三块单扇区 $2\times 2$；单路径 $\lvert\gamma\rvert=60^{\circ}$。

不写选序后的 $65^{\circ}$，不写位置带，不写中微子混合，不把两套 $3\times 3$ 各自对角化。 $65^{\circ}$ 只预告，后文才闭。

四件东西不要并：

| 本卷有 | 本卷没有 |
|:---|:---|
| 代空间混路、三个独立模、单路径 $60^{\circ}$ | 对角 MeV（只吃比） |
| CKM 混的是代 | 把色和代并成「三代管」 |
| 三块单扇区 $2\times 2$ | 两套 $3\times 3$ 各自对角化 |
| $\lvert\gamma\rvert=60^{\circ}$ | 把位置带并进来凑 $65^{\circ}$ |

---

```text
两套「三」分家：色 ≠ 代 ≠ 弱荷
        │
        ▼
混路：质量基与弱基站不直 → V ≠ 1
        │
        ▼
偶交叉 = 0；奇交叉 = 锁定
        │
        ▼
三块单扇区 2×2 → 三个独立模
        │
        ▼
K₁₂ = e^{-iπ/3} → |γ| = 60°
```

$$
\boxed{\text{混路}\ \Longrightarrow\ \text{三块 }2\times 2\ \Longrightarrow\ V;\quad
K_{12}=e^{-i\pi/3}\ \Longrightarrow\ \lvert\gamma\rvert=60^{\circ}}
$$

## 读法与指引

**记录与活动：** $V$、三个独立模、 $K_{12}$、 $60^{\circ}$ 仍是记录，不是前提；混路、奇偶通道、单扇区每一步都要能还原到交汇处三管同时在场、升降／锁定这类**局域活动**本身，检验记录是否忠实。不是先认实验 CKM 角，再往回填 $G$。

CKM 混的是**代**，不是色，不是三根管的标签。三个独立模来自三块单扇区 $2\times 2$，不是两张质量矩阵各自对角化再相乘。本卷闭的 $60^{\circ}$ 是单路径读 $G$；实验窗 $65^{\circ}$– $68^{\circ}$ 是后文选出一条管序之后的事，这里只预告，不要把位置带并进来凑角。

**本卷怎么走。** 第一部先拆两套「三」。第五部才写三个模。第十九章： $f_{12}=2/n_{1}$ 是奇通道插入；偶通道 $1/\sqrt{2}$ 会把 $\lvert V_{cb}\rvert$ 抬到 $0.061$，去处是中微子段，不进交汇处的 $G$。第六部才读相位。矩阵元用实验室下标，几何用框架 $k$，对着写。

本卷的「证明」仍是规划路径上的比对序列重演。各章「断在哪」就是这种检验。

| 这里的词 | 读成 | 不要读成 |
|:---|:---|:---|
| 管 / 单管顺序 | 面向**标签**；禁「先走完一根再假装锁定」 | $\mathbb{R}^{3}$ 圆柱；单面真空自转 |
| $e_{i}^{2}=-1$ | 记法（对齐第一卷 22.6） | 擦掉另两管后的独自平方 |
| 两套「三」 | 类型账 vs 交汇维 $k$ vs 状态账 | 「三代管」四个字 |
| 三个模 | 三块 $2\times 2$ | 两套 $3\times 3$ 对角化相乘 |
| $60^{\circ}$ | 单路径 $Ge_{1}$ | 已经是实验 $65^{\circ}$；螺旋均值；选序后的硬值 |
| $1/\sqrt{2}$ | 偶通道振幅 | $f_{12}$；塞进 $V_{cb}$；KM $\gamma$ |
| $\omega=1$ | 输入约定 | 投影定理 |

读完请能说出 boxed。三个模不回头改。中微子、选序是后两卷。

---

# 第一部　两套「三」不可互换

先站住：色、代、弱荷不是同一套指标。问不清，后面的角全坐错。

---

## 第一章　手里有什么

| 符号 | 是什么 | 站住在 | 本卷用法 |
|:---|:---|:---|:---|
| $e_{1},e_{2},e_{3}$ | 三种不可约操作（ $e_{i}^{2}=-1$ 是记法；活动对齐第一卷 22.6） | 第一卷 | 锁定；奇通道就是它 |
| $N_{c}=3$ | 管数 = 色数（标签种数） | 第一卷 | 类型账维数；不是代数 |
| 同时协变 | 局域比对是三管整体事件 | 第二卷 | 禁单标签顺序（非圆柱自转） |
| 正传 / 反传 | 保双态 / 拆单态 | 第三卷 | 上型 / 下型；奇通道相对负号 |
| 类型账 / 状态账 | $\mathrm{SU}(3)_{C}$ / 一个 $\mathrm{SU}(2)_{L}$ | 第三卷 | 色 vs 荷电流 |
| $k=1,2,3$ | 交汇维 = 参与管数 | 第四卷 | 代；不是三管标签 |
| $n_{1}=4$ | $k=1$ 最短闭覆盖 | 第四卷 | $f_{12}=2/n_{1}$ |
| 折返均值 $\langle\alpha\rangle=60^{\circ}$ | 二次折返之后的切向 | 第四、五卷 | 堆积生成元 $G$ |
| 四条代隙比 | $20$， $45.2$， $579$， $135.6$ | 第六卷 | 三个模的输入 |

以上全是收录。不重推 $n_{k}$，不重开四条比，不套 $m_{0}q^{n_{k}}$。上一卷的六味 MeV 本卷只吃比，尺约掉。

框架 $k=1$ 最重（ $t,b$），对应实验室第 3 代。本卷写矩阵元用实验室下标，写几何用框架 $k$。两套编号对着写，不许并成「第 $k$ 代」。

---

## 第二章　三管是类型账，不是代数

三管 $e_{1},e_{2},e_{3}$ 来自第一卷：恰好三种不可约操作（ $e_{i}^{2}=-1$ 是记法，不是单面真空自转）。第二卷把它们读成三根锁死的管（面向**标签**的续接轨道，三管同时在场）。第三卷把类型账的自同构读成 $\mathrm{SU}(3)_{C}$。

混的是色。禁闭之后测不到。荷电流不读这一层。「三代管」四个字把本节和下节并成一句：指标对错，相位坐错空间。禁止。

**断在哪：** 把管标签当成代指标，相位坐进类型账。把管读成事先嵌好的圆柱，活动与记录对调。

---

## 第三章　三代是交汇维，不是三管

三代来自第四卷：翻转覆盖 $1/2/3$ 根管，是交汇维层级 $k$。 $\binom{3}{k}$ 计类型子集， $n_{k}$ 计事件数，都不是三管本身。 $k$ 回答「这次完整事件覆盖了几根管」，不是「沿哪一根管走」。

**断在哪：** $k$ 写成「沿第 $k$ 根管走」，交汇维退化成管标签。

---

## 第四章　荷电流只读状态账

第三卷： $\mathrm{SU}(2)_{L}$ 是锁定压成的**一个**双态，混合的是 $z_{1}$ 与 $z_{2}$，不是三根管的标签。荷电流只读这一层：弱基里上下型成对，电流是代空间单位阵。它不读类型账，也不读交汇维 $k$。质量基里 $k$ 才露面。混在荷电流里可读的，只能是代。

**断在哪：** 荷电流去读色或去读管序，弱基不再是单位阵。

---

## 第五章　两套「三」不可互换

**命题。** 三管 $e_{1},e_{2},e_{3}$ 与三代 $k=1,2,3$ 不是同一套指标。CKM 混的是代，不是色。

**第一步。** 第二章：三管是类型账。混色。禁闭后测不到。

**第二步。** 第三章：三代是交汇维。 $\binom{3}{k}$ 与 $n_{k}$ 都不是管标签。

**第三步。** 第四章：荷电流只读状态账。弱基里上下型成对；质量基里 $k$ 是覆盖了几根管。可读的混合只能是代。

**第四步。** 把「三代」写成「三代管」，把前两步并成一句。指标对错，相位坐错空间。

色是类型账。代是交汇维。弱荷是状态账。三层望远镜，不能并。

$$
\boxed{\text{CKM 混的是代，不是色}}
$$

> **指引。** 读任何一个矩阵元之前先问：指标坐在类型账、交汇维，还是状态账。问不清，后面的 $60^{\circ}$、 $1/\sqrt{2}$、 $f_{12}$ 都会坐错。

---

# 第二部　混路 ⇒ CKM 不是单位阵

两套「三」拆开了。下面问：为什么 $V$ 不是 $1$。

---

## 第六章　同时协变禁止选出单管顺序

第二卷：局域比对是三管同时协变的整体事件。单标签改写（「先走完一根再假装锁定还在」）破坏 $e_{1}e_{2}=e_{3}$。这一刀必须整块落下——不是圆柱里独自自转，而是标签活动不许拆成先后。

**断在哪：** 先走完一根再锁定，锁定变成事后补丁。把「单管顺序」读成圆柱自转，与第一卷 22.6／第二卷打架。

---

## 第七章　先后不定禁止事件有锐顺序

第四卷：交汇处无法同时确定三个方向的优先顺序。若事件有一条合法的锐顺序， $k$ 就是锐标签：质量矩阵在 $k$ 基对角，荷电流变到质量基仍是单位阵， $V=1$。

**断在哪：** 事件有锐顺序， $k$ 锐， $V=1$，本卷无事可做。

---

## 第八章　混路 ⇒ V≠ 1

**命题。** 同时协变禁止选出单管顺序；先后不定又禁止事件有锐顺序。故 $k$ 不是事件算子的锐本征值。质量基与弱基不对齐。

**第一步。** 这一刀整块落下。

**第二步。** 禁止选出一条合法顺序。

**第三步。** 若第二步不成立， $k$ 锐， $V=1$。

**第四步。** 前两步同时成立。真实事件是多条顺序的叠加（第二卷：不同路径积累不同相位）。 $k$ 不是锐本征值。质量基与弱基站不直。

CKM $\neq 1$ 不是外加的味混合。它是锁与不定同时成立时，事件在代空间里站不直。

**机制级。** 本卷不另写格点逐边的路径叠加。

$$
\boxed{\text{锁与不定同时成立}\ \Longrightarrow\ \text{代空间站不直}\ \Longrightarrow\ V\neq 1}
$$

---

# 第三部　偶通道杀掉代混合，奇通道就是锁定

$V\neq 1$ 站住了。下面问：哪条通道真在干活，哪条通道杀掉跨管交叉。

---

## 第九章　升降，不是三管一次乘完

一次交汇事件在代空间里做的是**一次多一根管**的 Clifford 乘法（升降 $k$），不是三管乘积的交替和。

记 $U_{\sigma}$ 为按管序 $\sigma$ 的近邻作用。轨道是第一卷乘法表的六步闭合链：

$$
e_{1}\xrightarrow{\times e_{2}}e_{3}\xrightarrow{\times e_{1}}-e_{2}\xrightarrow{\times e_{3}}-e_{1}\xrightarrow{\times e_{2}}-e_{3}\xrightarrow{\times e_{1}}e_{2}\xrightarrow{\times e_{3}}e_{1}.
$$

$\mathrm{sgn}(\sigma)$ 来自 $e_{i}e_{j}=-e_{j}e_{i}$。六步链走遍 $\wedge^{1}$ 与 $\wedge^{2}$，不踏进 $k=3$ 与 $k=0$。

六个排列的三元乘积交替和为零：三元积把标量送到体元，不是质量核。故 $\mathcal{E}=\sum_{\sigma}\mathrm{sgn}(\sigma)\,U_{\sigma}$ **不能**直接当 $M$。真在干活的是升降算子 $c(e_{i})$。

代空间矩阵元

$$
K_{kk'}=\langle D_{k}\vert c(e)\vert D_{k'}\rangle.
$$

对角 $K_{kk}$：完整覆盖 $D_{k}$ 的振幅，对应第四卷对角主项。非对角：混路。 $D_{k}\cap D_{k'}$ 非空——低维交汇嵌在高维里。

**算出。** 交替和不能当 $M$（三元积送体元）。升降图像：机制级。

**断在哪：** 把交替和当 $M$，核坐进体元，不是质量。

---

## 第十章　反交换子杀掉跨管偶交叉，对易子就是锁定

第一卷： $e_{i}^{2}=-1$， $e_{i}e_{j}=-e_{j}e_{i}$（ $i\neq j$）。写成

$$
\{e_{i},e_{j}\}=-2\delta_{ij},\qquad [e_{i},e_{j}]=2\epsilon_{ijk}e_{k}.
$$

**第一步。** $i\neq j$ 时 $`\{e_{i},e_{j}\}=0`$。两次对消留下的偶通道里，不同管的交叉项为零。偶通道不能跨管升降。

**第二步。** $i\neq j$ 时 $[e_{i},e_{j}]=2\epsilon_{ijk}e_{k}\neq 0$。这就是锁定 $e_{1}e_{2}=e_{3}$，不是噪声。奇通道交叉核非零。

偶通道交叉 $=0$。奇通道交叉 $=$ 锁定。

另一面不是空白：偶通道杀掉的是跨管交叉，不是把整条偶通道写成 $0$。管作用在自己的 Hodge 对偶上，偶残差可以留下。

**断在哪：** 把锁定读成噪声，奇通道交叉被抹掉， $V$ 回到 $1$。

---

## 第十一章　轻子先迹掉奇通道

轻子是类型单态（第三卷）：三管作为锁定整体入账，不携带可辨的单根面向标签。对类型求迹，只见偶通道。

Hodge 对齐下， $k=1$ 基 $`\{e_{i}\}`$， $k=2$ 基 $`\{*e_{i}\}=\{e_{2}e_{3},\,e_{3}e_{1},\,e_{1}e_{2}\}`$。奇是 $L-R$ 除以 $2$，偶是 $L+R$ 除以 $2$。

**第一步。** $k=1\to 2$ 奇通道三张迹皆 $0$。单态 $\mathbf{1}^{\top}M\mathbf{1}/3$ 分别是 $2/3$、 $0$、 $-2/3$。三管等权： $K^{(\ell)}_{12}=0$。偶通道三张全是零矩阵。此即 $`\{e_{i},e_{j}\}=0`$（ $i\neq j$）在升降块上的内容。代表元

$$
c_{\mathrm{odd}}(e_{1})=\begin{pmatrix}0&0&0\\0&0&1\\0&1&0\end{pmatrix},
$$

其余两张由轮替给出。

**第二步。** $k=2\to 3$：左乘 $=$ 右乘，整块偶。 $e_{i}(*e_{i})=$ 体元。单态
$$
\frac{1-1+1}{\sqrt{3}}=\frac{1}{\sqrt{3}}.
$$
第十章第一步不禁止管作用在自己的 Hodge 对偶。

**第三步。** $k=1\to 3$ 须两步。六条有序路单态 $\pm 1/\sqrt{3}$ 各三，等权求和为零。轻子无优先管序。

故

$$
K^{(\ell)}_{12}=0,\qquad K^{(\ell)}_{13}=0,\qquad K^{(\ell)}_{23}=\frac{1}{\sqrt{3}}\ \text{（偶残差）}.
$$

带电轻子只有一张核。场重定义把它转成对角，残差进中微子段。不是第二套 $K^{(d)}=-K^{(u)}$。本卷不写 PMNS。

把同一 $k$ 的类型加总再算内积，得到 Gram $G_{12}\approx 0.808$，那是「类型还没收成色」的中间量；轻子若用它，带电混合 $O(1)$。正确手续是迹，不是 Gram。

**算出。** $`K^{(\ell)}_{12}=K^{(\ell)}_{13}=0`$。偶 $23$ 残差可吸收：读法。

**断在哪：** 轻子用 Gram 不当迹，带电混合变成 $O(1)$。

---

## 第十二章　夸克走类型账，奇通道留在代空间

夸克场是色三重态，不能投成单态。要投的是质量算符：

$$
\bar q^{a}_{k} M_{kk'} q^{b}_{k'}\propto\delta_{ab}.
$$

希尔伯特空间是张量积，不是类型求和：

$$
\mathcal{H}=\mathcal{H}_{\mathrm{gen}}\otimes\mathbb{C}^{3}_{\mathrm{color}}.
$$

类型指标锁进 $\mathrm{SU}(3)_{C}$。质量算符若在色上非 $\delta_{ab}$，破坏色规范。静态内积必须是 $\langle k,a\vert k',b\rangle_{0}=\delta_{kk'}\delta_{ab}$。非对角只来自事件奇通道 $A_{kk'}\delta_{ab}$。

夸克不走第十一章那一步平均：类型锁进 $\delta_{ab}$ 之后，代空间核仍是事件奇通道。 $K$ 非对角。六步链走遍 $k=1$ 与 $k=2$ 的类型、**不进** $k=3$；若误当代混合，会预言最重两代混得最凶，与实验相反。

**断在哪：** 六步链误当代混合，最重两代混得最凶。

---

## 第十三章　偶杀掉代混合；奇就是锁定

**命题。** $i\neq j$ 时偶通道交叉核为零。奇通道交叉核就是 $e_{1}e_{2}=e_{3}$，非零。轻子无带电 CKM；夸克 $V\neq 1$。

轻子对类型求迹，只见偶通道， $`K^{(\ell)}_{12}=K^{(\ell)}_{13}=0`$。夸克类型指标就是身份，奇通道留在代空间。

轻子无带电 CKM，因为奇通道迹掉、偶交叉不能跨管升降。夸克 $V\neq 1$，因为奇通道就是锁定。

$$
\boxed{\text{偶交叉}=0\qquad\text{奇交叉}=\text{锁定}\qquad\text{轻子无带电 CKM}}
$$

---

## 第十四章　正传 / 反传：奇通道相对负号

对角走偶通道：完整覆盖、自逆闭链，正反相同、实。

奇通道：正传左乘，交叉项 $\propto[e_{i},e_{j}]$；反传右乘（或逆序），
$$
(e_{i}e_{j})^{\mathrm{rev}}=e_{j}e_{i}=-[e_{i},e_{j}]/2.
$$
奇通道翻号：

$$
K^{(d)}_{\mathrm{odd}}=-K^{(u)}_{\mathrm{odd}}.
$$

正传 $\mathbb{C}$-线性，反传 $\mathbb{C}$-反线性。复相位可以坐在正反这一差上。但反线性**不是**「同一张复核再打一个星号」——那条路给不出 KM 角（第二十二章）。奇通道的相对负号保留；几何系数在 $\theta=0$ 为偶，不再额外共轭。

**推论。** $K^{(u)}\neq K^{(d)}\Rightarrow$ 上型转角与下型转角不是同一套 $\Rightarrow V\neq 1$。轻子没有「正传满色体积 / 反传单通道」这套不对称，没有带电 CKM。

**断在哪：** 反传打星号，差变实， $J=0$。

---

# 第四部　几何平均，以及禁止两套 3×3

通道分清了。下面钉死：非对角怎么读，哪条路不许走。

---

## 第十五章　ω=1（约定，不是投影定理）

代空间质量矩阵的几何重叠系数取 $\omega=1$。CKM 只吃质量比，不吃类型 Gram。

**第一步。** 第十二章：类型已锁进色，代空间静态内积是 $\delta_{kk'}$。再乘 $G_{kk'}$ 把同一几何数算两次。

**第二步。** 双重计数： $n_{1}=4=3+1$， $n_{2}=13=9+4$， $n_{3}=30=27+3$。 $\lvert D_{k}\rvert=3^{k}$ 已在对角质量里。多出来的 $1,4,3$ 才是事件增量。

**第三步。** 排除： $\omega=G$ 把 Cabibbo 压到 $0.17$； $\omega=1/\sqrt{3}$ 压到 $0.13$。不采用。

**地位。** 规范要求加排除，不是闭式投影定理。三个模在 $\omega=1$ 下贴实验，是本卷输入约定。

**断在哪：** $\omega=G$ 或 $\omega=1/\sqrt{3}$，Cabibbo 掉到 $0.17$ 或 $0.13$。

---

## 第十六章　非对角走质量几何平均

纯组合比不等于 $\sqrt{m_{i}/m_{j}}$。非对角必须带对角质量的几何平均：

$$
M_{kk'}=m_{0}\,\omega_{kk'}\sqrt{\Delta_{k}\Delta_{k'}},\qquad \omega=1.
$$

层次极限 $m_{\mathrm{light}}\ll m_{\mathrm{heavy}}$ 下， $\lvert M_{kk'}/M_{k'k'}\rvert=\sqrt{m_{\mathrm{light}}/m_{\mathrm{heavy}}}$。

**第一步。** Gram 邻元 $\sim 0.8$，实验 Cabibbo $0.225$。差一个层次。轻子公式的 $q^{\Delta n/2}$ 也凑不出 Cabibbo： $\omega_{23}q^{17/2}\approx 0.040$，落在 $V_{cb}$ 档。原因：夸克对角不是 $m_{0}q^{n_{k}}$（下型第一隙被色压到 $20$ 而不是 $207$，第六卷）。

**第二步。** 混路振幅相对对角隧穿振幅，最小假设就是几何平均。不是新公设，是「非对角 / 对角」在层次极限下的读法。

**断在哪：** 用轻子 $q^{\Delta n/2}$ 凑 Cabibbo，落到 $V_{cb}$ 档。

---

## 第十七章　禁止两套 3×3 各自对角化

$K^{(u)}$ 与 $K^{(d)}$ 共享偶通道 $D$，差在奇通道。独立对角化两张形状相同的 Fritzsch，会把两套 $O(1)$ 转角相减：

$$
\lvert V_{us}\rvert\sim\bigl\lvert\sqrt{m_{d}/m_{s}}-\sqrt{m_{u}/m_{c}}\bigr\rvert=\lvert 0.224-0.042\rvert=0.182.
$$

对角填满再加几何平均，则 $1$– $2$ 块近秩一，混合 $\sim 45^{\circ}$。 $M_{13}=0$ 时 $K^{(d)}=-K^{(u)}$ 让 $V$ 可整体变实， $J=0$。打开 $M_{13}$ 则 $J$ 大两个量级，且 $\lvert V_{cb}\rvert$ 被加成 $\sqrt{m_{c}/m_{t}}+\sqrt{m_{s}/m_{b}}\approx 0.24$。

下型 $k=1\leftrightarrow 2$ 的 $\sqrt{m_{s}/m_{b}}=0.149$ **不得**进 $V_{cb}$。 $V$ 来自扇区归属，不是 $U_{u}^{\dagger}U_{d}$ 的两套大角相乘。

禁止两套 $3\times 3$ 各自对角化。本卷不走那条路，也不把它当「近似再修正」。

**算出。** $0.182$、 $0.24$、 $J=0$（反事实）。排除：本卷钉死。

**断在哪：** 两套大角相乘，Cabibbo 掉到 $0.182$，或 $V_{cb}$ 抬到 $0.24$，或 $J=0$。

---

# 第五部　三个独立模

禁止写完。下面只读三块单扇区。

---

## 第十八章　扇区归属

荷电流读弱双态。上型走正传、满色体积；下型走反传、单色通道。 $M_{u}\neq M_{d}$。每一块最近邻混合由第十六章的几何平均给出。

CKM 的三个独立模来自三块单扇区 $2\times 2$，不是两张完整 $3\times 3$。

| 元 | 主导扇区 | 公式 | 本卷 | 实验 |
|:---|:---|:---|:---:|:---:|
| $\lvert V_{us}\rvert$ | 下型 $k=3\leftrightarrow 2$ | $\sqrt{m_{d}/m_{s}}$ | $0.224$ | $0.225$ |
| $\lvert V_{cb}\rvert$ | 上型 $k=2\leftrightarrow 1$ | $\sqrt{m_{c}/m_{t}}/2$ | $0.043$ | $0.041$ |
| $\lvert V_{ub}\rvert$ | 上型 $k=3\leftrightarrow 1$ | $\sqrt{m_{u}/m_{t}}$ | $0.00357$ | $0.0037$ |

**第一步。** Cabibbo 两端都在折返之后（ $k=2$ 与 $k=3$），升降是单管 Clifford， $f=1$。跨代 $k=1\leftrightarrow 3$ 走体元重叠，跳过四步闭链， $f=1$。

**第二步。** 上型 $k=1\leftrightarrow 2$ 只坐在第二次折返上（第十九章）， $f_{12}=1/2$。下型同一块的 $0.149$ 若放进 $U_{d}$，会把 $V_{cb}$ 抬到 $0.12$– $0.23$。故 $V_{cb}$ 跟上型走。

**第三步。** 数字，输入第六卷： $\sqrt{1/20}=0.2236$； $\sqrt{1/135.6}/2=0.0429$； $\sqrt{1/(579\times 135.6)}=0.00357$。单扇区 $2\times 2$ 的 $\lvert\sin\theta\rvert=\lvert C\rvert/m_{\mathrm{heavy}}$ 在层次极限下就是上表。

三个模是单扇区 $2\times 2$ 的数值贴，输入是第六卷夸克比。核矩阵元尚未从 $D_{k}\cap D_{k'}$ 的闭式投影推出。 $\lvert V_{td}\rvert$、 $\lvert V_{ts}\rvert$ 不另造平方根。

$$
\boxed{\lvert V_{us}\rvert=\sqrt{m_{d}/m_{s}},\quad
\lvert V_{cb}\rvert=\sqrt{m_{c}/m_{t}}/2,\quad
\lvert V_{ub}\rvert=\sqrt{m_{u}/m_{t}}}
$$

**断在哪：** 下型 $0.149$ 进 $V_{cb}$，模抬到 $0.12$– $0.23$。

---

## 第十九章　f₁₂=2/n₁

上型 $k=1\leftrightarrow 2$ 的额外因子

$$
f_{12}=\frac{2}{n_{1}}=\frac{2}{4}=\frac{1}{2}.
$$

只戴在这一块 $2\times 2$ 上。Cabibbo 与 $V_{ub}$ 的 $f=1$。

**第一步。** $\alpha=90^{\circ}$ 时切向 $G=e_{2}$， $Ge_{1}=-e_{3}$，像仍是一根管。第一次破缺只在 $k=1$ 里转轴，不进 $k=2$。

**第二步。** $\alpha=60^{\circ}$ 时 $G=\sin\alpha\, e_{2}+\cos\alpha\, e_{3}$，切向出现 $e_{3}$ 分量。 $Ge_{1}$ 才把 $k=1$ 送到 $k=2$ 的 Hodge 对偶。故 $k=1\leftrightarrow 2$ 的混合只坐在第二次折返上。

**第三步。** 第四卷： $k=1$ 的最小闭链是折返

$$
0\to 1\to 2\to 1\to 0,\qquad n_{1}=4.
$$

前两步出去（破缺），后两步回来（折返）。 $e_{1}G=-Ge_{1}$ 就是这两向。对易子 $[e_{1},G]=2e_{1}G$ 里的 $2$ 是两个方向。闭链长度是 $4$，不是 $2$。奇通道插入占折返那一半： $2/n_{1}=1/2$。

**第四步。** $L-R$ 除以 $2$ 等于 $L$，会把 $2$ 消掉：那是把闭链当成 $\mathbb{Z}_{2}$ 而不是四步面。偶通道均方 $1/\sqrt{2}$ 是曲率振幅，不是这块奇通道插入；用它会把 $\lvert V_{cb}\rvert$ 抬到 $0.061$。它的去处是中微子段（下一卷），不是交汇处的 $G$。

写入 $2\times 2$：

$$
C_{cb}=\frac{1}{2}\sqrt{m_{c}m_{t}}\,e^{-i\pi/3}.
$$

$\lvert\sin\theta\rvert=\sqrt{m_{c}/m_{t}}/2=0.043$（实验 $0.041$）。相位下一章读。

**算出。** $f_{12}=2/n_{1}$。只在上型 $k=1\leftrightarrow 2$：钉死。

**断在哪：** 偶通道 $1/\sqrt{2}$ 塞进 $G$， $\lvert V_{cb}\rvert$ 抬到 $0.061$。 $f_{12}$ 抄到 Cabibbo 或 $V_{ub}$，那两块被腰斩。

---

# 第六部　相位：单路径 |γ|=60°

三个模站住了。下面只读一个不可吸收相位。

---

## 第二十章　恰好三代 ⇒ 一个弱 CP 相位

$n$ 代酉阵，场重定义吸收 $2n-1$ 个相位，剩下
$$
\frac{(n-1)(n-2)}{2}
$$
个物理相位。 $n=2$ 零个（Cabibbo 实）； $n=3$ 恰好一个； $n=4$ 三个。框架无第四代（第四卷）。「恰好三代」在这里是弱 $CP$ 的存在条件，不只是质谱分层。

符号层 $\mathrm{sgn}(\sigma)$ 只给 $0$ 或 $\pi$，不够产生 $\gamma\sim 60^{\circ}$。几何层必须再读 $90^{\circ}$ 与 $60^{\circ}$ 之差。

**算出。** 相位计数。无第四代：收录第四卷。

**断在哪：** 第四代进来，物理相位变成三个，本卷一个角不够。

---

## 第二十一章　额外相位只在 A_k=1,k=2

路径 $\mathrm{SU}(2)$ 相位的支集是 $A_{k=1,k=2}$。Cabibbo 块与 $V_{ub}$ 块不从六步链领取额外路径相位。

**第一步。** 六步链从 $e_{1}$ 出发，逐步左乘，走遍 $\wedge^{1}$ 与 $\wedge^{2}$，不踏进 $k=3$ 与 $k=0$。路径相位的支集因此是 $k=1\leftrightarrow 2$。

**第二步。** 框架 $k=1\leftrightarrow 2$ $=$ 实验室第 3 代 $\leftrightarrow$ 第 2 代 $=$ $V_{cb}$ 所在块。

**第三步。** 场重定义可消掉任意一代的公共相位。若 $A_{12}$ 与 $A_{13}$ 带同一个额外相位，等于只转了最重一代，KM 角回到 $90^{\circ}$。不可吸收的那一个相位，恰好是「只在 $1\leftrightarrow 2$、不在 $1\leftrightarrow 3$」——与六步链支集重合。

单管内部挠率 $\pi/4$、偶通道振幅 $1/\sqrt{2}$（ $\arctan(1/\sqrt{2})\approx 35^{\circ}$）与奇通道插入 $f_{12}=1/2$ 是三句不同的话。前两句禁止塞进交汇处的 $G$；第三句才是本章 $V_{cb}$。 $\sin(\pi/4)=1/\sqrt{2}$ 是同一偶通道的三角读，座位仍不在这块 $G$。

**断在哪：** $A_{12}$ 与 $A_{13}$ 共一个额外相位，KM 角回到 $90^{\circ}$。

---

## 第二十二章　共轭配对给不出 KM 角

若 $K^{(d)}=(K^{(u)})^{*}$，且额外角 $\psi$ 写在 $A_{12}=Ae^{i\psi}$ 里，则一阶

$$
V_{jk}\propto B^{*}-B=-2i\,\mathrm{Im}(B).
$$

相位仍是 $\pm\pi/2$，只改模。全局把 $i$ 换成 $e^{i\pi/3}$ 同样：所有非对角仍共相， $\gamma$ 仍是 $90^{\circ}$。

故反传不能是「同一张复 $A$ 再打星号」。第十四章只保留奇通道相对负号。

**算出。** 共轭 $\Rightarrow\gamma=90^{\circ}$。排除。

---

## 第二十三章　堆积生成元 G=e^{e₁π/6}e₂

交汇处 $\theta=0$：破缺生成元 $e_{1}$，堆积切向

$$
G=\sin\alpha\, e_{2}+\cos\alpha\, e_{3}=\frac{\sqrt{3}}{2}e_{2}+\frac{1}{2}e_{3},\qquad \alpha=60^{\circ}.
$$

$G^{2}=-I$， $`\{e_{1},G\}=0`$。在第三卷的分解下（ $q=z_{1}+z_{2}e_{2}$， $e_{1}$ 当 $i$）

$$
G=e^{e_{1}\pi/6}\,e_{2},\qquad \mathrm{arg}_{\mathbb{C}}(z)=\pi/6.
$$

**第一步。** $e_{3}=e_{1}e_{2}$，故 $G=(\sin\alpha+\cos\alpha\, e_{1})e_{2}$。 $\alpha=60^{\circ}$ 时 $z=\sqrt{3}/2+(1/2)e_{1}=e^{e_{1}\pi/6}$。 $\lvert z\rvert=1$， $\mathrm{arg}(z)=\pi/2-\alpha=\pi/6$。

**第二步。** $\alpha=60^{\circ}$ 时直接展开 $G^{2}=-I$。 $`\{e_{1},G\}=0`$，因 $e_{1}$ 与 $e_{2}$、 $e_{3}$ 都反对易。

$\alpha=90^{\circ}$ 时 $G=e_{2}$，退回一根管，与第十九章第一步一致。对易子相对破缺 $i$ 的辐角仍是 $\pi/6$。

**算出。** $G=e^{e_{1}\pi/6}e_{2}$。切向图像：机制级。本卷不重推 $S_{1}$。

**断在哪：** $\alpha=90^{\circ}$ 停在 $G=e_{2}$， $k=1$ 进不了 $k=2$。

---

## 第二十四章　直接读 K₁₂ 与 V_cb 辐角

$$
K^{(u)}_{k=1,k=2}=e^{-i\pi/3},\qquad
K^{(d)}_{k=1,k=2}=-e^{-i\pi/3}.
$$

上型主导的 $V_{cb}$ 辐角是 $-60^{\circ}$。其余块走单管升降：正传 $+i$，反传 $-i$。

**第一步。** $G=z_{G}e_{2}$， $z_{G}=e^{e_{1}\pi/6}$。正传左乘：破缺轴 $z_{1}=e_{1}$ 上

$$
z_{2}'=z_{G}\cdot(-e_{1})=e^{e_{1}\pi/6}e^{-e_{1}\pi/2}=e^{-e_{1}\pi/3}.
$$

即 $Ge_{1}=e^{-e_{1}\alpha}e_{2}$。故 $K^{(u)}_{12}=e^{-i\pi/3}$。

**第二步。** 反传右乘：在 $z_{1}=e_{1}$ 上 $z_{2}'=z_{G}e_{1}=-e^{-i\pi/3}$，即 $e_{1}G=-Ge_{1}$。几何系数在 $\theta=0$ 为偶，**不再打星号**。故 $`K^{(d)}_{12}=-K^{(u)}_{12}`$。

**第三步。** 若再令 $K^{(d)}=-(K^{(u)})^{*}$，则差变实， $J=0$。反传只剩奇通道相对负号。

**第四步。** $k=2\leftrightarrow 3$、 $k=1\leftrightarrow 3$ 不走 $G$，只走单管升降，本征值 $\pm i$。

| 矩阵元 | 主导 | 核 | 辐角 |
|:---|:---|:---|:---:|
| $V_{us}$ | 下型 $\sqrt{m_{d}/m_{s}}$ | $K^{(d)}_{23}=-i$ | $-90^{\circ}$ |
| $V_{cb}$ | 上型 $\sqrt{m_{c}/m_{t}}/2$ | $K^{(u)}_{12}=e^{-i\pi/3}$ | $-60^{\circ}$ |
| $V_{ub}$ | 上型 $\sqrt{m_{u}/m_{t}}$ | $K^{(u)}_{13}=i$ | $+90^{\circ}$ |

$J>0$ 选定这一套整体号。对调正反传只翻符号。 $z_{G}=e^{i\pi/6}$ 是 $G/e_{2}$； $K_{12}=e^{-i\pi/3}$ 是 $Ge_{1}/e_{2}$。进 $V_{cb}$ 的是后者。不要把级联灌进 $V_{ub}$：那会把模从 $0.00357$ 胀到 $0.007$。一阶三个混合是独立输入。

**算出。** $K_{12}=e^{-i\pi/3}$。其余块 $\pm i$。三个模不改。

**断在哪：** 级联灌进 $V_{ub}$，模从 $0.00357$ 胀到 $0.007$。

---

## 第二十五章　Jarlskog 与 |γ|=60°

一阶、第十八章的三个模、第二十四章的辐角：

$$
J=\mathrm{Im}\bigl(V_{ud}V_{cb}V_{ub}^{*}V_{cd}^{*}\bigr)=2.97\times 10^{-5},
$$

$$
\gamma=\arg\Bigl(-\frac{V_{ud}V_{ub}^{*}}{V_{cd}V_{cb}^{*}}\Bigr),\qquad \lvert\gamma\rvert=60^{\circ}.
$$

实验 $J\approx 3.08\times 10^{-5}$， $\gamma\approx 65^{\circ}$– $68^{\circ}$。

**第一步。** $V_{ud}\approx 1$， $V_{cd}\approx -V_{us}$。代入辐角：

$$
J=\lvert V_{us}V_{cb}V_{ub}\rvert\cos\varphi,\qquad \varphi=\pi/6,
$$

$$
0.2236\times 0.0429\times 0.00357\times \frac{\sqrt{3}}{2}=2.97\times 10^{-5}.
$$

**第二步。** 重相位不变量 $\gamma=\varphi-\pi/2=-60^{\circ}$。正传 / 反传对调翻成 $+60^{\circ}$。几何预言的是模 $60^{\circ}$，正负由 $J>0$ 选定。

**第三步。** 若整张 $A$ 共一个 $i$，则 $\gamma=90^{\circ}$、 $J=3.43\times 10^{-5}$。角不对。 $\varphi=45^{\circ}$（误用单管挠率 $\pi/4$）给出 $J=2.42\times 10^{-5}$，更差，排除。

**第四步。** Wolfenstein 接到 $O(\lambda^{3})$： $\lambda=0.2236$， $A=0.859$， $r=0.372$。 $\lvert\gamma\rvert=60^{\circ}$ 时 $\bar\rho=0.186$， $\bar\eta=0.322$（实验约 $0.159$、 $0.348$）。 $\bar\rho$ 偏大，因为 $60^{\circ}$ 比 $65^{\circ}$ 更靠实轴。

**地位。** $J$、 $\gamma$ 是一阶扇区相位加硬角差，不是完整 $3\times 3$ 对角化。三个模不回头改。

$$
\boxed{K^{(u)}_{12}=e^{-i\pi/3},\qquad \lvert\gamma\rvert=60^{\circ},\qquad J=2.97\times 10^{-5}}
$$

**断在哪：** 整张 $A$ 共一个 $i$， $\gamma=90^{\circ}$。误用 $\pi/4$， $J$ 更差。

---

## 第二十六章　预告： 65° 后文才闭

本卷的 $60^{\circ}$ 是单路径读 $G$。选出一条管序入账之后，硬值 $\lvert\gamma\rvert=65^{\circ}$， $J=3.10\times 10^{-5}$。实验窗口 $65^{\circ}$– $68^{\circ}$ 是三个相位序 $e_{1},e_{2},e_{3}$ 钉不死谁当 $i$。三个模不改。本卷数字不回改。

**本卷停在这里。** 不写位置带怎么读，不写 $30^{\circ}$ 按 $3!$ 均分。本卷若把位置带并进来凑 $65^{\circ}$，就和三个模、单路径 $K_{12}$ 并句。禁止。

$60^{\circ}$ 本卷闭。 $65^{\circ}$ 后文闭。位置带不进本卷。

**断在哪：** 本卷凑 $65^{\circ}$，单路径与选序并句。

---

# 第七部　嵌入荷电流

角读完了。下面只嵌入，不另造平方根。

---

## 第二十七章　三块 2×2 嵌入，其余元由幺正性补齐

$W$ 只读状态账。弱基里电流是代空间单位阵；质量基里剩下 $V$。 $V$ 由第十八章三块单扇区按弱双态对号嵌入，其余元由幺正性补齐，不再另造平方根：

$$
\mathcal{J}^{\mu}=\frac{g}{\sqrt{2}}\,\bar u_{Li}\gamma^{\mu}V_{ij}d_{Lj}\,W_{\mu}^{+}+\mathrm{h.c.}
$$

| 元 | 来路 | 模 |
|:---|:---|:---:|
| $V_{us},V_{cb},V_{ub}$ | 第十八章三块，相位第二十四章 | $0.224,\ 0.043,\ 0.00357$ |
| $V_{ud}$ | $1-\lambda^{2}/2$ | $0.975$ |
| $V_{cd}$ | 一阶反对称 $-V_{us}$ | $0.224$ |
| $V_{ts},V_{tb}$ | 幺正性 | $0.043,\ 1$ |
| $V_{td}$ | $A\lambda^{3}(1-\bar\rho-i\bar\eta)$ | $0.0084$ |

$\lvert V_{td}\rvert$ 实验 $\sim 0.0086$， $\lvert V_{ts}\rvert$ 实验 $\sim 0.041$。行模偏离 $1$ 的最大量是 $2.5\times 10^{-3}$，来自未写的 $O(\lambda^{4})$，不是新参数。重相位后不可吸收相位落在 $V_{ub}$。第十七章：不能用两套 $3\times 3$ 代替这条纪律。

---

# 第八部　三件禁止

---

## 第二十八章　本卷停在哪

**把两套「三」并成「三代管」。** 色混进代，相位坐错空间。第五章已钉死。

**两套 $3\times 3$ 各自对角化。** Cabibbo 掉到 $0.182$，或 $V_{cb}$ 被抬到 $0.24$，或 $J=0$。第十七章已排除。

**把位置带并进本卷凑 $65^{\circ}$。** 单路径是 $60^{\circ}$。选序后文才写。三个模不改。

不给：选序 $65^{\circ}$、位置带；中微子混合；强 $\theta$；对角 MeV（只吃比）；两套 $3\times 3$ 的「修正版」。三个独立模不拟合。 $\lvert V_{cb}\rvert$ 的 $1/2$ 不是自由参数。

诚实边界：三个模是单扇区数值贴，不是闭式投影。 $\omega=1$ 不是投影定理。 $\lvert\gamma\rvert=60^{\circ}$ 是单路径； $65^{\circ}$ 本卷不装成已闭。

---

# 结

---

## 本卷钉死

| 符号 | 地位 | 含义 |
|:---|:---|:---|
| 两套「三」分家 | 钉死 | 全部混合角 |
| 混路 $\Rightarrow V\neq 1$ | 机制级 | 荷电流 |
| $`K^{(\ell)}_{12}=K^{(\ell)}_{13}=0`$ | 算出 | 无带电 CKM |
| $`K^{(d)}_{\mathrm{odd}}=-K^{(u)}_{\mathrm{odd}}`$ | 算出 | 扇区归属 |
| $\omega=1$ | 约定 | 三个模 |
| 三个 $\lvert V\rvert$ | 算出： $0.224,\ 0.043,\ 0.00357$ | 后文只引用 |
| $f_{12}=2/n_{1}$ | 算出 | 不抄到 Cabibbo、 $V_{ub}$；偶通道 $1/\sqrt{2}$ 不进 $G$ |
| $K^{(u)}_{12}=e^{-i\pi/3}$， $\lvert\gamma\rvert=60^{\circ}$， $J=2.97\times 10^{-5}$ | 算出 | 后文不改矩阵元 |

## 本卷不假装做了的

- 没有选序 $65^{\circ}$、位置带。  
- 没有 PMNS、强 $\theta$。  
- 没有两套 $3\times 3$ 对角化。  
- 没有核的闭式投影。  
- 没有把偶通道 $1/\sqrt{2}$ 塞进交汇处的 $G$。  
- 没有回头改三个模。  
- 没有把「单管顺序／三管」读成圆柱自转； $e_{i}^{2}=-1$ 是记法，管是面向标签（对齐前几卷）。

## 复述

1. 三管是类型账（面向标签同时在场），三代是交汇维 $k$，弱荷是状态账。CKM 混的是代，不是色。不许写「三代管」。  
2. 同时协变禁止单标签顺序，先后不定禁止锐顺序。 $k$ 不是锐本征值。质量基与弱基站不直， $V\neq 1$。  
3. 升降是一次多一根管，不是三管交替和。偶通道交叉为零；奇通道就是锁定 $e_{1}e_{2}=e_{3}$。  
4. 轻子类型单态，迹掉奇通道， $`K^{(\ell)}_{12}=K^{(\ell)}_{13}=0`$，无带电 CKM。夸克类型锁进色，奇通道留在代空间。正反传给出 $`K^{(d)}_{\mathrm{odd}}=-K^{(u)}_{\mathrm{odd}}`$。  
5. 禁止两套 $3\times 3$ 各自对角化。三个独立模来自三块单扇区： $V_{us}$ 走下型第一隙， $V_{cb}$ 走上型第二隙并戴 $f_{12}=2/n_{1}$， $V_{ub}$ 走上型满根。偶通道 $1/\sqrt{2}$ 不进交汇处的 $G$。  
6. 额外相位只在 $A_{12}$。堆积生成元 $G=e^{e_{1}\pi/6}e_{2}$，正传读出 $K^{(u)}_{12}=e^{-i\pi/3}$。一阶 $J=2.97\times 10^{-5}$， $\lvert\gamma\rvert=60^{\circ}$。  
7. $65^{\circ}$ 是选出一条管序之后的硬值，后文才闭。位置带不进本卷。三个模不回头改。

**第八卷结束。**  
下一卷问：中微子另开哪把尺，为什么不走 $m_{0}q^{n}$。

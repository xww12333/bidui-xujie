原则：
- 保留原体系所有硬约束与结论，不改变任何已锁数字与逻辑。
- 语言更口语、更有过渡，减少「电报式」跳跃。
- 每卷开头先用白话说明「这一卷要解决什么问题」。
- 关键推导后补充「为什么必须这样，否则会断在哪里」。
- 严格保持原有纪律：不假装做了的、两口不并、座位分明。
- **活动优先，记录忠实**： $\ell$、Gram、 $t$、 $ds^{2}$ 是传态网络因果几何的记录；须还原到比对续接活动本身，不许先嵌 $\mathbb{R}^{n}$ 再往回读舞台。
- 本卷一条路走完：网络 → $\ell$、Gram、 $t$ → 离散 $3+1$ → $ds^{2}$。不写 $T_{\mu\nu}$、 $G$。

---

# 第十四卷　时空涌现

**本卷要解决的问题：**  
上一卷站住： $m_{W}$、 $m_{Z}$、 $m_{H}$。粒子物理侧已结束。本卷不重开场，不许把手征井抄成牛顿势。前几卷已钉：管是面向标签， $e_{i}^{2}=-1$ 是记法，网络是传态——本卷谈「舞台涌现」时仍守这一句；不许先画圆柱／闵氏再塞比对。

本卷只问一句：舞台从哪长出来。不问源。

走到尽头：时空 = 传态网络的因果几何。距离来自 $\ell=\|U-1\|$，度规来自 Gram，时间是更新序（不是第四面向 $e_{4}$），签名 $3+1$，连续极限 $ds^{2}=-c^{2}\,dt^{2}+g_{ij}\,dx^{i}dx^{j}$。

不写 $T_{\mu\nu}$、 $8\pi G$、 $G$ 的数字、 $r_{s}=2GM$、压强、绑定能。

四件不要并：

| 本卷有 | 本卷没有 |
|:---|:---|
| 顶点是比对节点；瞬时回路 $W=1$ | 先验坐标、 $ds^{2}$ 当舞台 |
| $\ell=\Vert U-1\Vert$，Gram 给出 $g_{x}$ | 把代数 $H_{P}=-1$ 当 $T_{\mu\nu}$ |
| 时间 = 更新计数 | 第四个虚基元 $e_{4}$ |
| 因果锥斜率 $c_{\mathrm{eff}}$；光锥标度连续极限 | $G$ 的数字； $r_{s}=2GM$ |

---

```text
传态网络 𝔑 是全部（尚非时空）
        │
        ├─ 顶点：比对节点，不是空间点
        ├─ 瞬时回路 W = 1：快照偷运不了曲率
        └─ H_P = -1：代数极值，不是宏观源
                │
                ▼
        ℓ = ‖U−1‖ → 最短路 d 是度量
                │
                ▼
        传态虚部的 Gram → g_x（涌现场）
                │
                ▼
        时间 = 更新计数（不是 e₄）
                │
                ▼
        因果锥 + c_eff → 离散 3+1
                │
                ▼
        光锥标度 a→0 → ds²
```

$$
\boxed{\mathfrak{N}\ \xrightarrow{\ell,\,\mathrm{Gram},\,t}\ \text{离散 }3{+}1\ \xrightarrow{a\to 0}\ ds^{2}}
$$

## 读法与指引

**记录与活动：** $\ell$、 $g_{x}$、 $t$、 $ds^{2}$ 仍是记录，不是前提；代价、Gram、更新序每一步都要能还原到传态网络上的**局域活动**本身，检验记录是否忠实。不是先画闵氏舞台，再把比对塞进去。

舞台从传态网络长出，不是先验坐标。时间是更新序，**不是**第四个面向 $e_{4}$——第一卷已经没有第四面向。 $H_{P}=-1$ 仍是代数极值，本卷不把它写成物质源。 $G$ 的数字还不在这里。

**本卷怎么走。** 第二部先钉「网络尚非时空」。第三部 $\ell=\|U-1\|$ 才有距离。第四部 Gram 给度规。第五部才定义时间。第七部连续极限。第八部曲率形状，停在源之前。跳数距离与 $\ell$ 最短路是两套，不要并。

本卷的「证明」仍是规划路径上的比对序列重演。各章「断在哪」就是这种检验。

| 这里的词 | 读成 | 不要读成 |
|:---|:---|:---|
| 管 / 边标签 | 面向**标签**的续接轨道 | $\mathbb{R}^{3}$ 事先嵌好的圆柱；单面真空自转 |
| $e_{i}^{2}=-1$ | 记法；与 $H_{P}=-1$ 同属另一面 | 擦掉另两管后的独自平方（对齐第一卷 22.6） |
| 顶点 | 比对节点 | 已经是空间点 |
| 时间 | 更新计数 | $e_{4}$；先验 $t$ 轴；第四根管 |
| $H_{P}=-1$ | 代数极值的形状 | 宏观 $T_{\mu\nu}$ |
| $d_{\mathrm{hop}}$ 与 $d$ | 跳数； $\ell$ 最短路 | 同一套距离 |
| $ds^{2}$ | 连续极限 | 本卷开头就有的舞台； $G$、 $r_{s}$ |

读完请能说出 boxed。源是下一卷。

---

# 第一部　手里有什么

先站住：网络是全部。本卷只问舞台，不问源。

---

## 第一章　收录表

| 符号 | 是什么 | 站住在 | 本卷用法 |
|:---|:---|:---|:---|
| $e_{1},e_{2},e_{3}$ | 三种不可约操作（面向标签） | 第一卷 | 空间面向；没有 $e_{4}$ |
| $e_{i}^{2}=-1$ | 记法；与 $H_{P}$ 同属另一面 | 第一卷 | $H_{P}=-1$；纯虚 $a^{2}=-|a|^{2}$；对齐 22.6 |
| $\mathrm{Im}(\mathbb{H})$ | $\mathrm{span}\{e_{1},e_{2},e_{3}\}$ | 第一卷 | Gram 的取值空间 |
| 传态 $q_{w}=e_{i}\cdot q_{v}$ | 比对沿面向标签续接 | 第二卷 | 网络的边 |
| 保范 $\Vert q_{w}\Vert=\Vert q_{v}\Vert$ | 传态不消模长 | 第二卷 | 单位四元数 |
| $H_{P}=-1$ | 边标签绕最小面 | 第二卷 | 只当代数极值；不当 $T$ |
| 近邻、只依赖序列 | 续接局域、有序 | 第一卷 | 因果锥；时间方向 |

范数 $|q|^{2}=q\bar q$：第一卷已标诚实。本卷用它算 $\ell$，不重开构造。不把 $\rho$ 谱当 $T$，不写 $8\pi G$、 $r_{s}$。上一卷的 $m_{W}$、 $m_{Z}$、 $m_{H}$ 本卷不重开。

---

## 第二章　统一对象

$$
\mathfrak{N}=\mathcal{V},\,\mathcal{E},\,\{e_{1},e_{2},e_{3}\},\,\{q_{v}\}_{v\in\mathcal{V}}.
$$

$\mathcal{V}$ 顶点， $\mathcal{E}$ 边， $q_{v}\in\mathbb{H}$ 是状态。边带着面向**标签** $e_{i}$（管不是事先嵌好的圆柱）。

第二卷：网络不是「做」传态，网络是传态。本卷钉：这个网络就是全部——没有预先存在的空间、时间、度规、联络、坐标。后文只能从 $\mathfrak{N}$ 长出，不能先嵌进 $\mathbb{R}^{n}$ 再往回读。

**断在哪：** 先嵌进 $\mathbb{R}^{n}$ 再往回读，舞台被当成前提。把边标签读成圆柱自转，活动与记录对调。

---

# 第二部　网络尚非时空

统一对象站住了。先钉：顶点不是点，快照不是曲率。

---

## 第三章　顶点不是空间点

顶点 $v\in\mathcal{V}$ 是一次比对闭包发生的位置，不是先验空间里的点。没有坐标，没有 $ds^{2}$，没有「前后左右」。有的只是传态关系 $q_{w}=e_{i}\cdot q_{v}$。

第二卷已禁「把 $v$ 画成 $\mathbb{R}^{3}$ 里的点再传播」。本卷把禁令升级成出发点：空间若出现，必须从代价里长出。

**断在哪：** 把顶点画成 $\mathbb{R}^{3}$ 里的点，空间被当前提。

---

## 第四章　瞬时快照读不出曲率

相邻顶点的传态链接

$$
U_{ij}:=q_{i}^{-1}q_{j}\in\mathbb{H}.
$$

这不是位移向量。它是「把 $i$ 的状态接到 $j$」的操作。单位四元数时 $U_{ji}=U_{ij}^{-1}=\bar U_{ij}$。

固定一场 $\{q_{v}\}$，沿闭合路径 $\gamma$：

$$
W(\gamma)=U_{i_{1}i_{2}}U_{i_{2}i_{3}}\cdots U_{i_{n}i_{1}}
=q_{i_{1}}^{-1}q_{i_{2}}\cdot q_{i_{2}}^{-1}q_{i_{3}}\cdots q_{i_{n}}^{-1}q_{i_{1}}
=\mathbf{1}.
$$

中间的 $q$ 全部消掉。这叫望远镜。

**结论。** 瞬时快照里，状态场链接的闭合回路 holonomy 都是 $1$。这一份里读不出时空曲率。

误读：把 $W=1$ 说成「网络没有曲率」。它只说：曲率不能从瞬时状态场里偷运。若时空要从网络涌现，必须来自代价分布，或更新之后的动力学连接。

后文再引望远镜，用来拆两套 holonomy。本卷先站住「快照不够」。

另一面不是空白： $W=1$ 不是把曲率写成 $0$。差异还在边标签和更新里，只是这一份快照读不出来。

**断在哪：** $W=1$ 说成网络没有曲率，快照与代价并句。

---

## 第五章　$H_{P}=-1$ 是代数极值，不是宏观源

第二卷：绕最小面一周，边标签乘法给出 $H_{P}=-1$。唯一来源 $e_{i}^{2}=-1$（记法；闭表活动对齐第一卷 22.6）。与当时 $\{q_{v}\}$ 无关。真空有，有粒子也有。处处都在。第二卷已禁止把它读成时空曲率或物质源。本卷再钉一次身份。

| 是 | 不是 |
|:---|:---|
| 边标签的代数面因子 | 瞬时状态场回路 $W$ |
| 格点尺度的极值 $H_{P}=-1$ | 宏观 Riemann 曲率 |
| 自旋 / 可定向扇区的底座 | 随质量 $m$ 开关的 $T_{\mu\nu}$ |

后文会把 $H_{P}\approx\mathbf{1}+a^{2}\Omega$ 代入 $-1$，得到 $\Omega=-2/a^{2}$。算术本卷第二十二章会写。**物理身份**是代数极值，不是宏观物质源。两套 holonomy 谁能写进 Einstein 右边，是下一卷。本卷禁止并句。

$$
\boxed{H_{P}=-1\text{ 是代数极值，不是宏观物质源}}
$$

**断在哪：** $H_{P}=-1$ 写进 Einstein 右边，代数极值当宏观源。

---

# 第三部　传态代价 $\Rightarrow$ 距离

快照不够。距离从代价长出。

---

## 第六章　边的传态代价

对边 $e$ 从 $i$ 到 $j$：

$$
\ell(e):=\|U_{ij}-\mathbf{1}\|_{\mathbb{H}}=\sqrt{|U_{ij}-\mathbf{1}|^{2}}.
$$

$\ell(e)=0$ 当且仅当 $U_{ij}=\mathbf{1}$：两节点状态相同，无差异，代价为零。

传态代价度量的是相邻节点之间的差异幅度。它不是事先画在纸上的边长。没有新参数。

**断在哪：** 先把边长画在纸上再叫 $\ell$，代价被当前提。

---

## 第七章　非负性与对称性

**第一步。** $\ell(e)\ge 0$，等号当且仅当 $U_{ij}=\mathbf{1}$。模长非负。 $\|U-\mathbf{1}\|=0\Leftrightarrow U=\mathbf{1}$。

**第二步。** 若 $|q_{i}|=|q_{j}|=1$，则 $\ell(i\to j)=\ell(j\to i)$。单位四元数 $U_{ji}=\bar U_{ij}$。展开

$$
|U_{ij}-\mathbf{1}|^{2}=(U_{ij}-\mathbf{1})(\bar U_{ij}-\mathbf{1})=|U_{ij}|^{2}-U_{ij}-\bar U_{ij}+1.
$$

$|U_{ij}|=1$， $U+\bar U=2\mathrm{Re}\,U$，故 $|U_{ij}-\mathbf{1}|^{2}=2-2\mathrm{Re}\,U_{ij}$。同理 $|U_{ji}-\mathbf{1}|^{2}=2-2\mathrm{Re}\,\bar U_{ij}$。两者相等。

单位四元数从哪来：第二卷保范。初值模长 $1$，传态保持模长 $1$。后文凡写对称性，都在这一假设下。

---

## 第八章　$d$ 是度量，且只由传态定义

路径 $\gamma$ 的长度 $L(\gamma):=\sum_{k}\ell(e_{k})$。涌现距离

$$
d(x,y):=\inf\bigl\{L(\gamma):\gamma\text{ 从 }x\text{ 到 }y\bigr\}.
$$

无路径连通时 $d=\infty$。空路径 $L=0$，故 $d(x,x)=0$。

**第一步。** $d(x,x)=0$。

**第二步。** $d(x,y)=d(y,x)$。第七章第二步：逆向边代价相等，逆向路径长度相等，下确界相等。

**第三步。** $d(x,z)\le d(x,y)+d(y,z)$。近优路径拼接后总代价任意接近 $d(x,y)+d(y,z)$； $d(x,z)$ 是全体路径的下确界，故 $\le$。

**第四步。** 网络连通且非平凡边 $\ell>0$（$U\neq\mathbf{1}$）时， $d(x,y)=0\Rightarrow x=y$。

$d$ 的定义只使用 $\{U_{ij}\}$（即 $\{q_{v}\}$）与图的道路。构造未引用任何嵌入 $\mathcal{V}\hookrightarrow\mathbb{R}^{n}$。

空间距离不是外加几何。它是传态代价的最短路。

禁止的读法：先把顶点嵌进 $\mathbb{R}^{3}$，再把欧氏边长叫做 $d$。那是把要涌现的东西当前提。

**断在哪：** 欧氏边长叫做 $d$，嵌入当前提。

---

# 第四部　局域度规：Gram

距离有了。局域内积从传态虚部长出。

---

## 第九章　传态虚部

对 $x$ 的每个邻点 $y\in N(x)$：

$$
v_{y}:=\mathrm{Im}\,\mathrm{Log}\,U_{xy}\in\mathrm{Im}(\mathbb{H})\cong\mathbb{R}^{3}.
$$

$U_{xy}$ 接近 $\mathbf{1}$ 时 $\mathrm{Log}$ 取主支，唯一。 $U=\exp(v)$、 $v\in\mathrm{Im}(\mathbb{H})$ 落在 $0$ 的邻域内，则 $v_{y}=v$。

$v_{y}$ **不是**「从 $x$ 到 $y$ 的位移向量」——还没有预先存在的空间。它是这次传态在三根管上的投影。

小偏离下 $\exp(v)=1+v+O(\|v\|^{2})$，故 $\ell$ 与 $\|v\|$ 同阶。Gram 用的 $v$ 和代价用的 $\ell$ 在通有区一致，不是两套尺子。

$\mathrm{Im}(\mathbb{H})$ 收录第一卷。

**断在哪：** $v_{y}$ 读成事先存在的位移向量，空间又被当前提。

---

## 第十章　Killing 型

$\mathrm{Im}(\mathbb{H})$ 上取

$$
\langle a,b\rangle_{\mathbb{H}}:=-\mathrm{Re}(ab).
$$

$a,b$ 纯虚时 $\bar a=-a$，故 $\langle a,b\rangle=\mathrm{Re}(\bar a b)$，与模长 $|a|^{2}=a\bar a$ 同根。也可写成 $\langle a,b\rangle=-\frac{1}{2}\mathrm{tr}_{\mathbb{H}}(ab)$，其中 $\mathrm{tr}_{\mathbb{H}}(q)=2\mathrm{Re}\,q$。两种写法同一双线性型。

**正定。** $a\in\mathrm{Im}(\mathbb{H})$、 $a\neq 0$ 时，写 $a=a_{1}e_{1}+a_{2}e_{2}+a_{3}e_{3}$。交叉项反对易成对抵消，对角三项各贡献 $-a_{i}^{2}$，得 $a^{2}=-\lvert a\rvert^{2}\lt 0$。于是

$$
\langle a,a\rangle=-\mathrm{Re}(a^{2})=|a|^{2}>0.
$$

正定性从第一卷 $e_{i}^{2}=-1$ 加线性逐步写出，不甩「Killing 型」当黑盒。

**断在哪：** Killing 型当黑盒，正定性不从 $e_{i}^{2}=-1$ 写出。

---

## 第十一章　Gram 存在，且是涌现场

定义

$$
g_{x}\Bigl(\sum_{y}\alpha_{y}v_{y},\,\sum_{z}\beta_{z}v_{z}\Bigr):=\sum_{y,z}\alpha_{y}\beta_{z}\langle v_{y},v_{z}\rangle_{\mathbb{H}}.
$$

当 $\{v_{y}\}_{y\in N(x)}$ 张成 $\mathrm{Im}(\mathbb{H})$ 全部三维时（通有：三根管都在传）， $g_{x}$ 是 $T_{x}^{\mathrm{eff}}\cong\mathbb{R}^{3}$ 上的正定内积。

$\langle\cdot,\cdot\rangle$ 在 $\mathrm{Im}(\mathbb{H})$ 上正定（第十章）。满秩时 Gram 矩阵正定。

冻结相：某些管不传， $\{v_{y}\}$ 落进真子空间， $g_{x}$ 退化。通有相排除冻结，见第六部。

$g_{x}$ 由 $\{U_{xy}\}_{y\in N(x)}$ 唯一决定。 $\{U_{xy}\}$ 由 $\{q_{v}\}$ 决定。 $\{q_{v}\}$ 随更新而变。故 $g_{x}=g_{x}[\{q_{v}\}]$。

度规不是先验场。它是每个节点处传态虚部的 Gram。状态变，度规变。是涌现场，不是舞台。

下一卷会用同一句：kink 改的就是 $U$，额外代价不能不进 $g$。本卷先钉：只有这一套 $U$，只有这一张 $g$。

**断在哪：** 另造第二套度规，舞台与场并句。

---

# 第五部　时间 = 更新序，不是 $e_{4}$

空间从代价长出了。时间是过程，不是第四根管。

---

## 第十二章　时间的操作定义

$$
t:=\text{传态更新的次数}.
$$

一次更新：网络按局域规则调整 $\{q_{v}\}$（或等价地 $\{U_{ij}\}$），使传态关系接续。

**为何不能是别的东西。** 假设存在独立于网络的「外部时钟」。它的嘀嗒本身是一个差异序列。第一卷：差异不可否定，比对必须由差异操作编码。故嘀嗒必须由传态网络实现，计数必须归结为更新计数。

任何「外部时间」都必须通过传态网络实现。更新计数是时间的唯一内禀定义。

用的是第一卷的「没有外部」，不是另设的时间公设。

**断在哪：** 另设外部时钟，嘀嗒不经传态网络。

---

## 第十三章　时间有方向

更新是序列（第一卷）。反传是沿同一条边用 $e_{i}^{-1}$ 走回去（第二卷），不是把整网的更新序倒放。正传 / 反传作为入账开关是第三卷；本卷只用：正向更新与反向更新不是同一个过程。

本目录未单独立卷重写结构熵公式。本卷只收：

时间有方向。方向不是外加的轴，而是传态过程不可逆的显现。

不在本卷内从比对逐步推出熵公式。

---

## 第十四章　时间不是第四面向 $e_{4}$

| | 空间方向 | 时间方向 |
|:---|:---|:---|
| 来自 | $\mathrm{Im}(\mathbb{H})$ 的三个不可约面向 | 传态更新的离散序 |
| 属于 | 差异的**内容** | 差异的**过程** |
| 代数座位 | $e_{1},e_{2},e_{3}$ | 不在 $\mathrm{Im}(\mathbb{H})$ 里 |

第一卷已排除第四个面向：闭合之后再设 $e_{4}$，系数条件变成 $f^{2}=-1$，实数域无解。更新序若被写成 $e_{4}$，就是把过程范畴的对象塞进代数的第四个基矢。第一卷禁止这件事。本卷禁止同一件事的几何版：把 $t$ 画成第四根管。

$$
\boxed{\text{空间是差异的内容（三面向）。时间是差异的过程（更新序）。}}
$$

> **指引。** 想给时间再找一根管，就是卷一已经排除的 $e_{4}$。签名 $3+1$ 从内容和过程分家长出，不是先画闵氏空间再拆。

签名 $3+1$ 从这一句长出，不是先假定洛伦兹四维再拆成 $3+1$。

**断在哪：** $t$ 画成第四根管，过程塞进代数第四个基矢。

---

# 第六部　因果锥、 $c$、空间三维

内容和过程分家了。下面写锥和维数。

---

## 第十五章　因果结构

传态是局域的（第一卷近邻、第二卷局域）：一次更新中， $x$ 的态至多影响跳数 $\le 1$ 的邻域 $\mathcal{N}_{1}(x)=\{x\}\cup N(x)$。 $r$ 步后可达 $\mathcal{N}_{r}(x)=\{y:d_{\mathrm{hop}}(x,y)\le r\}$。

**两套距离不要并。** $d_{\mathrm{hop}}$ 是跳数。 $d$ 是 $\ell$ 的最短路。因果锥先用跳数写； $c_{\mathrm{eff}}$ 再把 $\ell$ 接进去。

在更新时刻 $t$，事件 $x,t$ 的因果过去与未来：

$$
\mathrm{J}^{-}(x,t)=\bigl\{(y,s):s\le t,\ d_{\mathrm{hop}}(y,x)\le t-s\bigr\},
$$

$$
\mathrm{J}^{+}(x,t)=\bigl\{(y,s):s\ge t,\ d_{\mathrm{hop}}(x,y)\le s-t\bigr\}.
$$

传态影响不能逃出 $\mathrm{J}^{\pm}$。

归纳。 $1$ 步只达 $\mathcal{N}_{1}$。设 $r$ 步达 $\mathcal{N}_{r}$。第 $r+1$ 步中， $\mathcal{N}_{r}$ 里每个节点只影响自己的邻域，故可达集 $\subseteq\mathcal{N}_{r+1}$。超出跳数预算的格点，在给定步数内没有收到从 $x$ 出发的传态。

**断在哪：** 跳数与 $\ell$ 最短路并成一套距离，锥的斜率坐错尺。

---

## 第十六章　$c_{\mathrm{eff}}$ 与普适性

$$
c_{\mathrm{eff}}:=\sup_{e}\frac{\ell(e)}{\Delta t}\Big|_{\Delta t=1\text{ 次更新}}.
$$

规则均匀的网络上， $c_{\mathrm{eff}}$ 是常数。 $r$ 步能走的最大涌现距离 $\le r\cdot\sup_{e}\ell(e)$。令 $r=|t_{x}-t_{y}|$，得

$$
d(x,y)\le c_{\mathrm{eff}}\cdot|t_{x}-t_{y}|.
$$

电磁波、引力扰动、所有物理信号，都是同一张网络上关系的重组。它们共享同一个传态网络，因此受同一个上界 $c_{\mathrm{eff}}$ 约束。

$c$ 不是某一种相互作用自己的传播速度。它是网络的结构上界——因果锥的斜率。电磁波和引力波同速，不是巧合，是同一张锥。

$c_{\mathrm{eff}}$ 本卷只钉成斜率。后文不把 $c$ 当第二切口，也不给这条斜率另领一把实验尺。与 $G$ 的交叉是几何锁； $\hbar$ 的形式用这条斜率，SI 数值不是合法靶。引力波的波动方程要等 Einstein 左边站住再写。本卷只闭上界。

**断在哪：** $c$ 写成某一种相互作用自己的速度，锥裂成多张。

---

## 第十七章　空间维数恰为 $3$

**上界。** 每个 $v_{y}$ 取值在 $\mathrm{Im}(\mathbb{H})\cong\mathbb{R}^{3}$，故 $T_{x}^{\mathrm{eff}}\subseteq\mathrm{Im}(\mathbb{H})$， $\dim T_{x}^{\mathrm{eff}}\le 3$。

上界来自第一卷：恰好三个虚基元。不是观测到「好像是三维」再回填。

**排除稳定二维。** 若所有 $v_{y}$ 落在某二维子空间 $W=\mathrm{span}\{e_{1},e_{2}\}$，则 $e_{1}\cdot e_{2}=e_{3}\notin W$。两种操作的复合激活第三方向。

$\mathrm{Im}(\mathbb{H})$ 不可约：不含对乘法封闭的真子空间（第一卷：任意两个虚基元的乘积生成第三个）。稳定二维要求 $W$ 对乘法封闭，矛盾。

一维同理：单标签轨道上没有交汇（第四卷：只沿一种面向标签看，无冲突；不是圆柱自转），通有相传态不是单标签冻结。

二维空间不稳定。只要两种差异操作在运行，复合就激活第三个方向。

通有（非冻结）相：上界 $3$，排除稳定 $\lt 3$，故 $\dim_{\mathrm{space}}=3$。

空间为什么是三维？因为 $\mathbb{H}$ 恰好有三个虚基元。为何恰好三个，第一卷逐步算过：一种交换被排除，两种关不上，四种 $f^{2}=-1$ 无解。本卷收录。

**断在哪：** 先假定洛伦兹四维再拆成 $3+1$，维数被当前提。把「单管」读成圆柱自转。

---

# 第七部　离散 $3+1$ 与连续极限

锥和维数齐了。下面收成离散因果集，再取极限。

---

## 第十八章　离散洛伦兹型因果集

对象 $\mathcal{M}_{\mathrm{disc}}=\mathcal{V}\times\mathbb{Z}_{t}$ 配备：空间度量 $d$（第八章）、时间步 $t\in\mathbb{Z}$（第十二章）、因果锥 $\mathrm{J}^{\pm}$（第十五章）、局域正定内积 $g_{x}$（第十一章）。

签名：三空间方向来自 $g_{x}$ 正定（Killing 型）；一时间方向来自更新序，不属于 $\mathrm{Im}(\mathbb{H})$。

$\mathcal{M}_{\mathrm{disc}}$ 连同 $d$、 $t$、 $\mathrm{J}^{\pm}$、 $g_{x}$ 是**离散洛伦兹型因果集**。

签名 $3+1$ 不是假设。它来自差异存在的两个层面：内容（三维空间）和过程（一维时间）。

还没有连续流形。下一章才取极限。

---

## 第十九章　连续极限

设粗粒化尺度 $a\to 0$，并设：

**条件一。** $g_{x}$ 在通有区非退化（$\{v_{y}\}$ 满秩）。

**条件二。** $\Delta t=a/c_{\mathrm{eff}}$（光锥标度）。

**条件三。** 涌现场在尺度 $a$ 上 Hölder 正则。

**则**存在连续流形极限 $\mathcal{M}$，局域度规

$$
ds^{2}=-c_{\mathrm{eff}}^{2}\,dt^{2}+g_{ij}(x,t)\,dx^{i}dx^{j}.
$$

**第一步，必须光锥标度。** 一次更新的最大传播距离为 $c_{\mathrm{eff}}\Delta t$（第十六章）。胞腔线度取 $a$，令 $\Delta t=a/c_{\mathrm{eff}}$，则 $a/\Delta t=c_{\mathrm{eff}}$ 有限且与 $a$ 无关——双曲型标度。

若改取扩散标度 $\Delta t\sim a^{2}$，则 $a/\Delta t\to\infty$，极限是抛物型：因果锥无限宽，与第十五章矛盾。故必须光锥标度。

**第二步，空间部分。** 条件一加条件三保证 $g_{x}$ 正定，且在胞腔上变化受控。图距离 $d$ 限制在尺度 $a$ 的路径上时，与 $g_{x}$ 诱导的 Riemann 长度至多差高阶小量。 $a\to 0$ 时收敛到 $g_{ij}\,dx^{i}dx^{j}$。

**第三步，时间部分。** 更新计数粗粒化成连续坐标 $t$，与第一步合成 $-c_{\mathrm{eff}}^{2}\,dt^{2}$。

**第四步。** 合起来即得所述 $ds^{2}$。

条件三是条件。本卷不从比对逐步推出 Hölder 指数。极限的存在标 **条件性**。光锥标度本身是算出的：与有限因果锥相容的唯一标度。

后文把 $c_{\mathrm{eff}}$ 写成 $c$。不是新常数，是同一个斜率换记号。

**断在哪：** 扩散标度 $\Delta t\sim a^{2}$，因果锥无限宽。把 $ds^{2}$ 当本卷开头就有的舞台。

---

## 第二十章　主定理

$$
\boxed{\text{时空}=\text{传态网络的因果几何}}
$$

| 时空要素 | 涌现自 | 本卷 |
|:---|:---|:---:|
| 空间点 | 网络节点的粗粒化 | 第三、十九章 |
| 空间距离 | 传态代价的最短路 | 第八章 |
| 空间度规 | 传态虚部的 Gram | 第十一章 |
| 空间维数 $3$ | $\mathrm{Im}(\mathbb{H})$ 不可约三维 | 第十七章 |
| 时间 | 传态更新计数 | 第十二章 |
| 时间箭头 | 过程不可逆 | 第十三章 |
| 因果锥 / $c$ | 局域更新的传播上界 | 第十五、十六章 |
| 签名 $3+1$ | 内容 vs 过程 | 第十四、十八章 |

---

# 第八部　曲率形状（停在源之前）

舞台站住了。曲率只写形状，不写源。

---

## 第二十一章　联络自动兼容

单位四元数链接 $U_{ij}=q_{i}^{-1}q_{j}\in\mathrm{SU}(2)$。 $\mathrm{SU}(2)$ 来自 ℍ 虚部与 Pauli 的同一代数，不是另加的内部空间（第二卷）。

连续极限中，链接推广为

$$
\omega_{\mu}=\frac{1}{2}\,\omega_{\mu}^{ab}\,e_{a}e_{b}\in\mathrm{Im}(\mathbb{H}).
$$

每条边上的 $U_{ij}$ 就是离散联络。联络不是外加的场。

$\omega_{\mu}\in\mathfrak{su}(2)$ 反对称，保持第十章的 Killing 型，故 $\nabla_{\mu}g_{\nu\rho}=0$。

联络取值在 $\mathfrak{su}(2)$ $\Leftrightarrow$ 无穷小转动保持 $\langle\cdot,\cdot\rangle$。Killing 型就是内部度规。保持内部度规 = 度规兼容。

Levi-Civita 不是额外假设。它是联络为纯虚四元数的自动后果。

下一卷变分用的 $g$，就是这张已经兼容的 $g$。没有第二套联络。

**断在哪：** 另加一套联络，兼容变成外贴。

---

## 第二十二章　$\Omega\sim 1/a^{2}$ 是代数极值的形状

小面展开（与第二卷连续读法同一套）：

$$
H_{P}=\exp\Bigl(\int_{P}\Omega_{\mu\nu}\,dx^{\mu}\wedge dx^{\nu}\Bigr)\approx\mathbf{1}+a^{2}\,\Omega_{12}+O(a^{4}),
$$

$\Omega_{\mu\nu}=\partial_{\mu}\omega_{\nu}-\partial_{\nu}\omega_{\mu}+[\omega_{\mu},\omega_{\nu}]$。若把代数极值 $H_{P}=-\mathbf{1}$ 代入：

$$
-\mathbf{1}=\mathbf{1}+a^{2}\,\Omega_{12}+O(a^{4})\qquad\Longrightarrow\qquad\Omega_{12}=-\frac{2}{a^{2}}.
$$

三种面型同理。Riemann 与 $\Omega$ 线性相关，故格点尺度 $|R_{\mu\nu}^{ab}|\sim 1/a^{2}$。均匀格点、 $\partial\omega=0$ 时曲率只剩二次项：

$$
R_{\mu\nu}=\frac{C_{R}}{a^{2}}\,g_{\mu\nu},\qquad R=\frac{4C_{R}}{a^{2}},
$$

$C_{R}$ 由 $\epsilon_{ijk}$ 决定，是纯数。本卷不把它换成 $G$。

| 算术 | 身份 |
|:---|:---|
| $\Omega=-2/a^{2}$ 从 $H_{P}=-1$ 展开 | **算出**（格点尺度） |
| $R\sim 1/a^{2}$ | **算出**（同一代入） |
| 宏观 $R$ 被粗粒化压下来 | **收录**第十一卷：软化 $H_{P}^{\mathrm{eff}}=-1+\delta$ |
| $\Omega=-2/a^{2}$ 当作 $T$ | **禁止**。分家在下一卷 |

格点尺度极度弯曲，是代数极值的几何读法。宏观近似平坦，是粗粒化平均，不是另设「真空 $R=0$」。平均之后还剩的**超额**才能当源——那一句属于下一卷。本卷不写 $J_{v}$，不写 $8\pi G$。

**断在哪：** $\Omega=-2/a^{2}$ 当作 $T$，形状与源并句。

---

## 第二十三章　测地线（给后文收录，不写源）

格点上，自由传播走传态累积代价最小的路。连续极限，代价变成 Riemann 长度，变分 $\delta\int_{\gamma}\sqrt{g_{\mu\nu}\dot x^{\mu}\dot x^{\nu}}\,d\tau=0$ 给出

$$
\frac{d^{2}x^{\mu}}{d\tau^{2}}+\Gamma^{\mu}_{\nu\rho}\frac{dx^{\nu}}{d\tau}\frac{dx^{\rho}}{d\tau}=0.
$$

$\Gamma$ 由已经兼容的 $g$ 决定（第二十一章）。**算出**（Euler–Lagrange）。

自由粒子是沿网络代价最小路径走的粒子。

下一卷把对角 $m$ 写成权重，收成 $S_{m}=-\int m\,ds$。本卷不写 $m$，不写 $T^{00}$。等效原理的完整陈述也留到那里：本卷只准备了「同一套 $U$，同一张 $g$」。

---

# 结

---

## 本卷钉死

| 符号 | 地位 | 含义 |
|:---|:---|:---|
| $\mathfrak{N}$ 尚非时空 | 算出 | 全部几何 |
| $U_{ij}=q_{i}^{-1}q_{j}$ | 望远镜 $W=1$ | 后文两套 holonomy |
| $\ell=\Vert U-1\Vert$ | 算出 | 后文额外代价 |
| $d$ 是度量 | 算出 | 连续 $ds$ |
| $g_{x}$ | 算出 | 后文变分的同一张 $g$ |
| $t=$ 更新计数 | 算出 | $ds^{2}$ 的 $dt$ |
| $t\neq e_{4}$ | 算出 | 签名不再另设 |
| $\mathrm{J}^{\pm}$， $c_{\mathrm{eff}}$ | 算出 | 后文不改定义；不另领实验尺 |
| $\dim=3$ | 算出 | 不再另选维数 |
| $ds^{2}=-c^{2}dt^{2}+g_{ij}dx^{i}dx^{j}$ | 条件性 | 后文 $S_{m}$ |
| $\omega\in\mathfrak{su}(2)$， $\nabla g=0$ | 算出 | 后文 |
| $\Omega=-2/a^{2}$ 的形状 | 算出（身份：代数极值） | 后文分家 |
| 测地线 | 算出 | 后文 $S_{m}$ |

$$
\boxed{\mathfrak{N}\ \Longrightarrow\ \ell,\ g,\ t,\ \mathrm{J}^{\pm}\ \Longrightarrow\ 3{+}1\ \Longrightarrow\ ds^{2}}
$$

## 本卷不假装做了的

没有写 $T_{\mu\nu}$、 $T^{00}$、Bianchi $\to$ Einstein、 $8\pi G$、 $G$ 的数字、 $r_{s}=2GM$。没有把 $H_{P}=-1$ 当宏观源（分家在下一卷），没有把更新序写成 $e_{4}$。Hölder 条件标条件性。结构熵公式本卷不从比对逐步推出。Friedmann、红移、暗能量 / 暗物质、Wheeler–DeWitt 不写。没有把边标签／「单管」读成圆柱自转； $e_{i}^{2}=-1$ 是记法（对齐前几卷）。

诚实边界：宏观平坦来自粗粒化，不是真空每面 $R=0$； $H_{P}=-1$ 处处都在，不当 $T$； $c_{\mathrm{eff}}$ 本卷只是斜率，数字在后文。

## 复述

1. 网络是全部（边带着面向标签）。顶点不是空间点。瞬时快照 $W=1$，偷运不了曲率。  
2. 边代价 $\ell=\|U-1\|$。最短路 $d$ 是度量。只由传态定义。  
3. 传态虚部的 Gram 给出 $g_{x}$。度规随 $\{q_{v}\}$ 变，是场，不是舞台。  
4. 时间是更新计数。不是 $e_{4}$，不是第四根管。空间是内容，时间是过程。  
5. 局域更新给出因果锥。 $c$ 是锥的斜率，一切信号共用。不是实验输入，后文不另领一把尺。  
6. 上界 $3$，稳定二维被乘法表排除。通有相空间维恰为 $3$。  
7. 光锥标度连续极限给出 $ds^{2}=-c^{2}dt^{2}+g_{ij}dx^{i}dx^{j}$。联络自动兼容。 $\Omega=-2/a^{2}$ 是代数极值的形状，不是源。

**第十四卷结束。**  
下一卷问：两套 holonomy 怎么分家，对角 $m$ 怎样写成额外代价。

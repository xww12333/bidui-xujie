原则：
- 保留原体系所有硬约束与结论，不改变任何已锁数字与逻辑。
- 语言更口语、更有过渡，减少「电报式」跳跃。
- 每卷开头先用白话说明「这一卷要解决什么问题」。
- 关键推导后补充「为什么必须这样，否则会断在哪里」。
- 严格保持原有纪律：不假装做了的、两口不并、座位分明。
- 本卷一条路走完：路径混合（微扰 $+$ 分支） $\Rightarrow$ $D(n)$；质量带 $\lambda_{\mathrm{eff}}$（机制级）；局域最大带宽 $\Rightarrow$ 可辨饱和半径 $\Rightarrow$ 与 $40^{12}$ 堆积同根。不写 Friedmann 数字、不写 $H_{0}$、不写暗能量观测值、不把 $D_{\ast}$ 调成 $2GM$、不把 $\lambda_{\mathrm{eff}}$ 当粒子寿命。

---

# 第十八卷　局域带宽与路径混合

**本卷要解决的问题：**  
第二卷给出 $D(n)\sim e^{-\lambda n}$、 $\lambda=\ln 5$，标机制级；第十卷钉死「混合丢可辨、不丢存在」，触发只有步数与质量带；第十六卷钉死 $40^{12}$ 与 $G$。卷十四写明 Friedmann、红移、暗能量数字不写。本卷不回头改 $G$、 $\hbar$、谱、CKM。本卷把两件机制级的事**闭成定理**：路径混合为何不是信息丢失；局域传态最大带宽为何存在、如何接到宇宙学尺度。

本卷只问两句：路径混合的定量机制是什么；局域传态的最大带宽是什么。

走到尽头：每步传态带 $\mathrm{SU}(2)$ 微扰，分支 $\times 5$ 使逐步路径序不可读出；可辨度 $D(n)=e^{-\lambda n}$ 是干涉结果，不是模长湮灭。每节点每更新最多 $f_{\mathrm{loop}}=40$ 条可分辨传态方向，边界 min-cut 给面积带宽；可辨饱和半径 $r_{\ast}=(a/\lambda)\ln(1/D_{\ast})$。IR 堆积 $40^{12}$ 与 $N_{\mathrm{oct}}=12$ 同根：每一层只有 $O(1)$ 步可辨深度，必须粗粒化 $40$ 倍才能从 UV 传到 IR 而不丢全局序。

不写 $H_{0}$、 $\Lambda_{\mathrm{obs}}$、Friedmann 方程数字。不把 $D_{\ast}$ 与 $r_{s}=2GM$ 并句。不把事件 $\ln 2$ 与几何 min-cut 叠进同一分母。不把带宽修正 $\hbar$ 或 $G$。不把 $\lambda_{\mathrm{eff}}$ 写成粒子寿命。

不要并：

| 本卷有 | 本卷没有 |
|:---|:---|
| 微扰 $+$ 分支 $\Rightarrow$ $D(n)$ | 混合 $=$ 信息丢失 |
| $B_{\max}=40c/a$（每节点）；min-cut 面积带宽 | 另引比特率常数 |
| $n_{\ast}=\lambda^{-1}\ln(1/D_{\ast})$ 与 $40^{12}$ 同根 | $D_{\ast}=2GM$ |
| 质量带 $\lambda_{\mathrm{eff}}=\chi\ln 5$（机制级） | $\Gamma\propto e^{-\lambda_{\mathrm{eff}}n}$（粒子寿命） |
| 宇宙学读法：可辨饱和 $\Leftrightarrow$ 堆积层数 | $H_{0}$、暗能量数字 |

---

```text
路径混合（第一部）
    每步 U∈SU(2) 微扰 + 保范
        └─ 有效分支 5 = 3×2−1
              └─ N_n ~ 5^n；相位不对齐
                    └─ D(n) = e^{−λn}，λ=ln5
                          ├─ 丢对比，不丢 |q|
                          └─ 质量带：λ_eff = (ln5)/√(1−ξ)（机制级）

局域带宽（第二部）
    局域事件 = 三管整体比对（一次更新一跳）
        ├─ 每节点方向上界 f_loop = 40
        ├─ 每边每更新 ≤ 1 可辨事件
        └─ min-cut：|∂A| 条边 × ln2（事件读法，机制级）
              └─ B_node = 40c/a；B_∂A = |∂A|·c/a
                    └─ 饱和：n* 步后 D→D*
                          └─ r* = (a/λ)ln(1/D*)
                                └─ 40^12：每层 O(1) 可辨深度 × 12 层
```

$$
\boxed{\text{混合}≠\text{丢失};\quad D(n)=e^{-\lambda n};\quad B_{\max}=\frac{40c}{a};\quad n_{\ast}=\frac{1}{\lambda}\ln\frac{1}{D_{\ast}}}
$$

## 读法与指引

本卷是第十六卷物理侧的**延伸**，不是第十七卷认知的重复。认知卷问路径序怎么被维持重演；本卷问**传态网络本身**在多远、多快之后逐步序不可读。 $G$、 $\hbar$ 只引用，不新造。

**本卷怎么走。** 第一部从微扰边因子闭 $D(n)$，并给质量带 $\lambda_{\mathrm{eff}}$ 的机制级闭式。第二部从 $f_{\mathrm{loop}}$ 与 min-cut 闭带宽。第三部把 $n_{\ast}$ 接到 $N_{\mathrm{oct}}=12$ 与 $40^{12}$，给宇宙学**结构读法**，不给 $H_{0}$ 数字。

---

# 第一部　路径混合：微扰、分支、可辨衰减

第二卷第十三章数了分支，第十六章写了边因子 $U\in\mathrm{SU}(2)$ 微扰。第十卷钉死混合丢可辨不丢存在。本卷把两句闭成一条链。

---

## 第一章　每步传态：保范与微扰

沿边 $e=(v\to w)$ 的传态（第二卷）：

$$
q_{w}=U_{e}\,q_{v},\qquad U_{e}\in\mathrm{SU}(2).
$$

纯极限 $U_{e}=e_{i}$（四元数基）。实际比较可有偏离（第二卷第十六章）：

$$
U_{e}=e_{i}\,e^{\mathrm{i}\varepsilon_{e}\,\hat n_{e}\cdot\boldsymbol\sigma/2},\qquad \varepsilon_{e}\ll 1.
$$

$\varepsilon_{e}$ 是**微扰**，不是新耦合。 $\mathrm{SU}(2)$ 来自 $\mathbb{H}$ 虚部，不是另加空间。

**命题（保范）。** $\lvert q_{w}\rvert=\lvert q_{v}\rvert$。

**证明。** $U_{e}\in\mathrm{SU}(2)$ 保范数。第一卷无零因子：非零态不能映到零。

**断在哪：** 把微扰写成模长衰减，或把混合当成 $q\to 0$。

---

## 第二章　每步分支： 5=3×2-1

节点 $v$ 有三根管，每管正反两向。一步后有效新分支（第二卷第六章回程不当新分支）：

$$
N_{\mathrm{br}}=3\times 2-1=5.
$$

$n$ 步独立分支数：

$$
N_{n}\sim 5^{n}.
$$

$\lambda=\ln 5$ 来自这个计数，不是另选的衰减率（第二卷第十三章）。

**断在哪：** 把 $\lambda$ 当成可调耦合；或把 $6$ 当分支（忘记扣回程）。

---

## 第三章　微扰使路径相位不可对齐

仅有分支还不够。若所有路径累积相位相同，干涉仍可相干加强， $D$ 不下降。

微扰使每条路径 $\gamma$ 积累不同相位：

$$
\Phi_{\gamma}=\sum_{e\in\gamma}\varepsilon_{e}\,\chi_{e},\qquad \chi_{e}\in\lbrace-1,+1\rbrace.
$$

不同路径 $\gamma≠\gamma'$ 的 $\Phi_{\gamma}-\Phi_{\gamma'}$ 一般非零。 $N_{n}$ 条路径的等权叠加对比度

$$
D(n)\sim\left|\frac{1}{N_{n}}\sum_{\gamma}e^{\mathrm{i}\Phi_{\gamma}}\right|^{2}\sim\frac{1}{N_{n}}=5^{-n}=e^{-\lambda n}.
$$

**算出。** 分支数 $5^{n}$ 与微扰致相位去相干共同给出 $D(n)\sim e^{-\lambda n}$。缺微扰：路径可相干，不会逐步读不出。缺分支：没有多路径可混。

**地位。** 这是对第二卷 $D(n)$ 的机制闭合，不是新指数。

**断在哪：** 只写分支不写微扰，或只写微扰不写回程扣减。

---

## 第四章　混合丢可辨，不丢存在

**命题。** 路径混合是**逐步路径序不可读出**，不是信息丢失，不是能量漏掉。

| 量 | 混合后 | 说明 |
|:---|:---|:---|
| $\lvert q\rvert$ | 不变 | 保范 |
| holonomy 结构 | 仍在 | 卷十第三章 |
| 逐步管序 | 不可指认 | $D\to D_{\ast}$ |
| $T^{00}$、曲率源 | 不因混合而消失 | 超额 holonomy 另口 |

第一卷：不可辨认 $=$ 没有被指认，不是操作产出零。

**读法。** 远了之后对不齐的是**路径序**；显现像只看见一个面。结构还在，不是写成 $0$（第二卷第十三章）。这不是磁盘擦除，不是无线电噪声——是太多路径、相位微扰叠加，**无法再逐步读出每一步传态**。

**断在哪：** 混合 $=$ 信息丢失；混合 $=$ 能量漏掉；混合 $=$ 正交破缺（卷十第九章）。

---

## 第五章　有效可辨度与阈值 D∗

定义有效可辨度（路径 ensemble 对比度）：

$$
\boxed{D(n)=e^{-\lambda n},\qquad \lambda=\ln 5.}
$$

$D_{\ast}$ 是**可辨阈值**： $D\lt D_{\ast}$ 时逐步路径序不可读。 $D_{\ast}$ 本框架**不钉数值**（第十卷中间带不写）；只作结构常数进入 $n_{\ast}$。

**两种触发**（第十卷第四章，本卷收录不重证）：

| 触发 | 机制 |
|:---|:---|
| 步数累积 | $n$ 增大， $D(n)$ 下降 |
| 质量集中 | 同一步上相位扩散更大，更快到 $D_{\ast}$ |

$\alpha_{s}$ 不是第三触发。质量带如何改每步扩散：见第六章（机制级）。

**断在哪：** $D_{\ast}=2GM$ 或 $D_{\ast}$ 与 $r_{s}$ 并句。

---

## 第六章　质量带有效衰减率（机制级）

第十卷钉死：质量集中加强的是该处**相位扩散**，不是改分支数，不是第三触发。真空 $\lambda=\ln 5$ 已锁（第二章、第五章）。本章给质量带上的有效率 $\lambda_{\mathrm{eff}}$。

### 6.1 分支不变；质量只进微扰

分支仍 $5=3\times 2-1$。质量**不**把回程改成新分支，**不**改 5-向概率权重。

把每步微扰幅度记为 $\varepsilon$。真空标定

$$
\varepsilon_{0}=\sqrt{\ln 5},
$$

使相位扩散记账 $D\sim e^{-n\varepsilon^{2}}$ 在真空与 $\lambda=\ln 5$ 对拍。

质量带抬高局域过程密度：

$$
\varepsilon(x)=\varepsilon_{0}\sqrt{\chi(x)},\qquad \chi\ge 1,\quad \xi\to 0\Rightarrow\chi\to 1.
$$

于是

$$
\lambda_{\mathrm{eff}}(x)=\chi(x)\,\ln 5.
$$

### 6.2 标量 χ：固有空间密度，不是跟钟

卷十五：外部球对称真空

$$
g_{00}=-(1-\xi),\qquad g_{rr}=\frac{1}{1-\xi},\qquad \xi=\frac{2GM}{r},\qquad g_{rr}=-\frac{1}{g_{00}}.
$$

混合是**空间传态**（局域 hop），按固有空间密度相对坐标格计。定义标量

$$
\chi(x)\ :=\ \frac{1}{\sqrt{-g_{00}(x)}}.
$$

外部上三写法等价：

$$
\chi=\frac{1}{\sqrt{-g_{00}}}=\frac{1}{\sqrt{1-\xi}}=\sqrt{g_{rr}}.
$$

弱场 $\xi\ll 1$： $\chi=1+\tfrac12\xi+O(\xi^{2})$，与各向同性弱场空间因子 $\sqrt{1+\xi}$ 同阶。  
$\xi\to 1^-$： $\chi\to\infty$，高质量带在有限区域内到 $D_{\ast}$（第十卷方向）。

**禁止**用 $\sqrt{-g_{00}}$（跟静态钟）作混合率：那会使 $\lambda$ 下降，与质量带叙事反号。  
**禁止**只放大径向边、切向仍真空：有效率被切向托底，端点不发散。  
**禁止**把分支概率按质量重权： $\sum p^{2}$ 上升会使碰撞型 $\lambda$ 下降，同样反号。

$$
\boxed{\chi=\dfrac{1}{\sqrt{1-\xi}},\qquad
\lambda_{\mathrm{eff}}=\dfrac{\ln 5}{\sqrt{1-\xi}}
\quad（\mathrm{机制级}）}
$$

### 6.3 带内饱和半径

均匀质量带内 $D(n;M)=e^{-\lambda_{\mathrm{eff}}n}$：

$$
r_{\ast}(M)=\frac{a}{\lambda_{\mathrm{eff}}}\ln\frac{1}{D_{\ast}}=\frac{a\sqrt{1-\xi}}{\ln 5}\ln\frac{1}{D_{\ast}}.
$$

$M\to 0$（ $\xi\to 0$）回到第七章步数触发式。 $M$ 增大 $\Rightarrow r_{\ast}$ 缩小。

**地位。** 机制级闭式。 $\chi:=1/\sqrt{-g_{00}}$ 接卷十五外部解与「混合跟固有空间密度走」；不是另开自由本构参数。 $D_{\ast}$ 数值仍不钉。

**断在哪：** $r_{\ast}(M)=2GM$；把 $\lambda_{\mathrm{eff}}$ 升成已从比对逐步钉死的 I 档唯一式；用 $\lambda_{\mathrm{eff}}$ 改粒子寿命／ $\Gamma$（第二十四卷：衰变走 $G_F$、 $m^5$，与可辨衰减分家）。

---

## 第七章　可辨饱和半径（步数触发）

均匀网络上 $n=r/a$（第十卷第六章）。 $D(r_{\ast})=D_{\ast}$：

$$
\boxed{r_{\ast}=\frac{a}{\lambda}\ln\frac{1}{D_{\ast}}=\frac{a}{\ln 5}\ln\frac{1}{D_{\ast}}.}
$$

右边无质量 $M$：纯步数触发不给 $M$ 有关的半径。质量带另刀（第六章）：改变**每步相位扩散**，不是改 $n=r/a$ 的定义。

**地位。** 机制闭。不升级成 Schwarzschild 视界公式。

**断在哪：** 把 $r_{\ast}$ 写成 $2GM$。

---

# 第二部　局域传态最大带宽

第二卷：信息带宽 $=$ min-cut。第十六卷： $f_{\mathrm{loop}}=40$，无零因子 $\Leftrightarrow$ 完全自维持。第十四卷：一次更新一跳， $c$ 是因果锥斜率。本卷闭「最大带宽」定义。

---

## 第八章　局域传态事件与一次更新

**定义。** 一次**局域传态事件** $=$ 三管同时协变的整体比对（第二卷第七章）。不是单管先走。

一次网络更新（第十四卷第十二章）：

- 时间步 $\Delta t$ 计一次；
- 空间上 $x$ 的态至多影响 $`\mathcal{N}_{1}(x)`$（跳数 $\le 1$）。

故**每更新每节点**最多向邻接边发送**一轮**局域传态；不能超距、不能一更新跳多格。

---

## 第九章　每节点方向带宽： f_loop=40

第十六卷第十章：节点 $v$ 的完备传态方向

$$
f_{\mathrm{loop}}=1+N_{c}+N_{c}^{2}+N_{c}^{3}=40.
$$

不足 $\Rightarrow$ 零因子；过度 $\Rightarrow$ 悬挂。故**每节点每更新**可分辨传态方向数上界为 $40$，不是可调参数。

在标度 $a$ 上，一步时间（第十六卷第十九章） $\Delta t=a/c$。定义**节点最大带宽**（可分辨传态方向数/时间）：

$$
\boxed{B_{\mathrm{node}}=\frac{f_{\mathrm{loop}}}{\Delta t}=\frac{40c}{a}.}
$$

$c$ 是因果锥斜率（第十四卷），不是第二实验切口。

**断在哪：** 另引「比特/秒」常数；把 $40$ 再乘进 $T^{00}$。

---

## 第十章　边界面积带宽：min-cut

穿越区域 $A$ 边界 $\partial A$ 的每条传态边，每更新至多承载**一个**可辨局域事件（近邻纪律）。边数 $\lvert\partial A\rvert$。

第二卷第十四章（机制级）：信息带宽 $=$ min-cut。事件读法：每边贡献 $\ln 2$ 纠缠刻度——**事件前**座位，不乘进几何 $G$ 分母（第十六卷纪律）。

几何读法（本卷带宽）：**每边每更新 $1$ 个可辨事件**。面积标度 $\lvert\partial A\rvert\sim (L/a)^{d-1}$（ $d=3$ 时 $\sim L^{2}/a^{2}$）。

边界最大事件率：

$$
\boxed{B_{\partial A}=\frac{\lvert\partial A\rvert}{\Delta t}=\frac{\lvert\partial A\rvert\,c}{a}.}
$$

球半径 $L$： $\lvert\partial A\rvert\sim 4\pi L^{2}/a^{2}$，故 $B_{\partial A}\sim 4\pi L^{2}c/a^{3}$。

**两口分家。** $\ln 2$ 管纠缠熵（事件锁）； $B_{\partial A}$ 管可辨传态事件计数（几何/堆积读法）。禁止 $\ln 2$ 乘进 $16\pi c_{\kappa}$。

**断在哪：** min-cut 的 $\ln 2$ 再叠进 $G$ 分母；把带宽当 $\hbar$ 修正。

---

## 第十一章　带宽与可辨深度的对偶

$n_{\ast}$ 步后 $D(n_{\ast})=D_{\ast}$。可读链最大步数：

$$
\boxed{n_{\ast}=\frac{1}{\lambda}\ln\frac{1}{D_{\ast}}.}
$$

**对偶命题。** 带宽饱和 $\Leftrightarrow$ 路径混合到阈值：若累积步数 $n\gt n_{\ast}$，逐步序不可读；不是传态停止，是**读出带宽耗尽**。

每节点每步最多 $40$ 个方向； $n_{\ast}$ 步后干涉使 $D\lt D_{\ast}$——方向再多也无法逐步对齐路径序。

**断在哪：** 把饱和说成传态停断或信息擦除。

---

# 第三部　与宇宙学尺度的结构接合

卷十四不写 Friedmann。本卷只给**结构读法**：为何 $N_{\mathrm{oct}}=12$ 与 $40^{12}$ 同根于带宽 $+$ 混合。

---

## 第十二章　每一层只有 O(1) 可辨深度

在格距 $a$ 上，可读步数 $n_{\ast}$ 由 $D_{\ast}$ 定，与 $a$ 无关（ $n_{\ast}$ 是步数）。

例： $D_{\ast}=10^{-2}$ $\Rightarrow$ $n_{\ast}=\ln(100)/\ln 5\approx 2.86$。

**命题。** 在**同一分辨率** $a$ 上，只能维持 $O(1)$ 步逐步可辨传态序。更远必须粗粒化，不能靠「多走几步」无限延续逐步读出。

**为什么必须这样。** 否则 $D(n)$ 指数衰减与「无零因子续接」矛盾：路径序在指数步数后不可读，但模长仍在——需要下一层粗粒化重锚。

**断在哪：** 假设同一 $a$ 上可辨深度 $\propto L/a$ 无上限。

---

## 第十三章　40 粗粒化与 N_oct=12

第十六卷： $L_{n+1}=40L_{n}$， $N_{\mathrm{oct}}=12$，

$$
\frac{L_{\mathrm{IR}}}{a_{\mathrm{UV}}}=40^{12}=f_{\mathrm{loop}}^{\,N_{\mathrm{oct}}}.
$$

**结构读法。** 从 UV 到 IR 要跨 $12$ 个八度层。每一层：局域带宽 $B\propto c/a$，可辨深度只有 $n_{\ast}=O(1)$ 步；传到下一层必须粗粒化因子 $40$（无零因子夹逼的等号，第十六卷第十章）。

堆积 $40^{12}$ **不是**微观常数凑指数，是**每层 $O(1)$ 可辨深度 $\times$ $12$ 次粗粒化**的几何必然。

$$
\boxed{\log_{40}\frac{L_{\mathrm{IR}}}{a_{\mathrm{UV}}}=N_{\mathrm{oct}}=12.}
$$

**断在哪：** 把 $12$ 当人为选择；把 $40^{12}$ 当与带宽无关的纯计数。

---

## 第十四章　宇宙可观测上界（结构，不给 H₀）

第十卷：步数累积触发 $\Rightarrow$ 宇宙可观测上界。本卷接法：

1. 低质量带： $\lambda=\ln 5$， $r_{\ast}=(a/\lambda)\ln(1/D_{\ast})$（第七章）。
2. 质量带： $\lambda_{\mathrm{eff}}=(\ln 5)/\sqrt{1-\xi}$， $r_{\ast}$ 更短（第六章）；仍不并 $r_{s}$。
3. IR 端格距 $\sim L_{\mathrm{IR}}=2\pi\hbar c/\Omega_{0}$（字典 $\hbar c$，第十六卷）。
4. 从 UV 到 IR 已走 $40^{12}$ 粗粒化；宏观信号在 IR 层读带宽与混合。

**命题（结构）。** 全宇宙可逐层读出的路径序，受两条饱和约束：

- **混合饱和**：累积 hop $n\gt n_{\ast}$ $\Rightarrow$ $D\lt D_{\ast}$；
- **带宽饱和**：边界事件率 $\le B_{\partial A}$。

二者同根：局域传态事件机制。红移、视界、膨胀率**本卷不写成 Friedmann 数字**；留给后文，当 $D_{\ast}$ 与 IR 边界几何闭合后再接。

**诚实边界。** 不给 $H_{0}$、 $\Lambda_{\mathrm{obs}}$、暗能量。不把 $r_{\ast}$ 与 $r_{s}$ 并句。不把绝对真空能写进 $\Lambda_{\mathrm{eff}}$（第十六卷第十七章）。

**断在哪：** 本卷直接写 $H_{0}$；把带宽当暗能量拟合。

---

## 第十五章　与前后卷接口

| 卷 | 接口 |
|:---|:---|
| 第二卷 | $D(n)$、 $\lambda=\ln 5$、min-cut、 $\ln 2$ 机制级 |
| 第十卷 | 混合丢可辨；两触发； $r_{\ast}$ 无 $M$（步数刀）； $D_{\ast}≠ r_{s}$ |
| 第十五卷 | 外部 $g_{00}$、 $g_{rr}$； $\chi=1/\sqrt{-g_{00}}$ |
| 第十四卷 | 因果锥、 $c$、一次更新一跳 |
| 第十六卷 | $f_{\mathrm{loop}}=40$、 $40^{12}$、 $L_{\mathrm{IR}}$、两口不并 |
| 第十七卷 | 认知 $=$ 路径序重演；本卷不碰突触 |
| 第二十四卷 | $\lambda_{\mathrm{eff}}$ $≠$ 粒子 $\Gamma$；衰变另口 |

---

# 结

---

## 本卷钉死

| 符号 | 地位 | 含义 |
|:---|:---|:---|
| $U_{e}\in\mathrm{SU}(2)$， $\varepsilon_{e}\ll 1$ | 收录 $+$ 闭链 | 每步微扰 |
| $N_{\mathrm{br}}=5$ | 算出 | $3\times 2-1$ |
| $D(n)=e^{-\lambda n}$ | 算出 | 微扰 $+$ 分支干涉 |
| 混合 $≠$ 丢失 | 钉死 | 保范；holonomy 在 |
| $\lambda_{\mathrm{eff}}=(\ln 5)/\sqrt{1-\xi}$ | **机制级** | 质量带相位扩散； $\chi=1/\sqrt{-g_{00}}$ |
| $B_{\mathrm{node}}=40c/a$ | 算出 | 节点最大带宽 |
| $B_{\partial A}=\lvert\partial A\rvert c/a$ | 算出 | 边界 min-cut 事件率 |
| $n_{\ast}=\lambda^{-1}\ln(1/D_{\ast})$ | 算出 | 可辨饱和步数（真空／低质量带） |
| $r_{\ast}=(a/\lambda)\ln(1/D_{\ast})$ | 算出 | 步数触发饱和半径 |
| $40^{12}$ 与 $n_{\ast}$ 同根 | 结构读法 | $12$ 层粗粒化 |

## 本卷不假装做了的

- 没有 $H_{0}$、Friedmann、红移数字、暗能量观测值。  
- 没有 $D_{\ast}=2GM$ 或 $r_{\ast}=r_{s}$。  
- 没有把 $\lambda_{\mathrm{eff}}$ 升成 I 档「比对逐步唯一钉死」。  
- 没有用 $\lambda_{\mathrm{eff}}$ 写粒子寿命或 $\Gamma$。  
- 没有把 $\ln 2$ 并进 $16\pi c_{\kappa}$ 或 $\hbar_{\mathrm{eff}}$。  
- 没有把带宽写成认知突触机制。

## 复述

1. 每步传态： $U_{e}\in\mathrm{SU}(2)$ 微扰，保范。有效分支 $5$。 $N_{n}\sim 5^{n}$，相位不对齐 $\Rightarrow$ $D(n)=e^{-\lambda n}$， $\lambda=\ln 5$。  
2. 混合丢逐步可辨对比，不丢模长，不丢 holonomy，不是信息擦除，不是正交破缺。  
3. 触发：步数累积、质量带相位扩散。 $\alpha_{s}$ 不是第三触发。质量带 $\lambda_{\mathrm{eff}}=(\ln 5)/\sqrt{1-\xi}$（机制级）；分支均权，禁概率重权，禁跟钟。  
4. 节点带宽 $B_{\mathrm{node}}=40c/a$。边界带宽 $B_{\partial A}=\lvert\partial A\rvert c/a$。min-cut； $\ln 2$ 与几何分母两口不并。  
5. $n_{\ast}=\ln(1/D_{\ast})/\ln 5$。同一 $a$ 上可辨深度 $O(1)$ 步。 $40^{12}=40^{N_{\mathrm{oct}}}$：十二层粗粒化。  
6. 宇宙学：混合饱和与带宽饱和同根于局域传态事件。Friedmann 数字不写。

**第十八卷结束。**

$$
\boxed{\text{微扰}+\text{分支}\Rightarrow D(n);\quad \lambda_{\mathrm{eff}}=\chi\ln 5;\quad f_{\mathrm{loop}}=40\Rightarrow B_{\max};\quad n_{\ast}\ \text{步}\Leftrightarrow\ \text{读出饱和};\quad 40^{12}\Leftrightarrow 12\ \text{层可辨传递}}
$$

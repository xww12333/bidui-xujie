# 传态闭序列展开： 同一个 9， 从活动里数出 $`9+4`$

**本篇做什么。** 把第四卷 $`n_{2}=9+4`$、 $`n_{3}=27+3`$ **还原到局域传态活动**： 三管两两、 四步闭合、 三条互锁序列逐步摊开。 终点数字与卷4／卷23 **不变**； 本篇补的是「计数公式与活动之间」 那一层， 不是另起一套 $`n_{k}`$。

**问题从哪来。** 读者常只看见

$$
n_{2}=3^{2}+n_{1}=9+4,\qquad n_{3}=3^{3}+N_{c}=27+3,
$$

却对不上「每一步传态做了什么」。 间隙一出现， $`9+4`$ 就像硬贴。 本篇专补这个间隙。

**与正卷／补卷的关系：**

| 卷 | 角色 |
|:---|:---|
| [卷4](卷4.md) | 闭式与三条约束； **算出** $`n_{k}`$ |
| [卷4补](卷4补.md) | 螺旋几何 + 闭截面图像 |
| [卷23](卷23.md) | L／C／B／V／K **点名审计**； 争议只认此卷 |
| [卷1](卷1.md)／[卷2](卷2.md) | 三面向、 四步、 锁定； 本篇整体性的根 |
| **本篇** | **活动序列展开**： 同一 9 如何从闭序列里数出来 |

**纪律：**

| 本篇说的 | 读成 | 不要读成 |
|:---|:---|:---|
| $`3^{2}=9`$ 与 $`12-3=9`$ | **同一个 9** 的两口读法（ 计数压缩／活动展开） | 两个碰巧相等的无关整数 |
| $`12`$ | 三条互锁 × 各 4 步的**毛活动预算** | $`n_{2}`$ 或可替代 13 的事件数 |
| 每条第 4 步 | 闭合重复第一对；**无新格** | 又一次新的位点覆盖 |
| 管对 × 三种情况 | 活动侧与九宫／C1–C9 **同数对照** | 改写支集定义； 替换卷23 标记 |
| $`e_{1}e_{2}e_{3}=-1`$ | 三管乘积已闭（ 卷1） | 与色锁定 $`+3`$ 并成一句 |

---

## 一、 间隙： 只看公式为何对不上

计数层（ 卷4） 写的是**完成条件的账单**：

- 全覆盖： $`\lvert D_{k}\rvert=3^{k}`$
- 闭链： $`k<3`$ 时付最少闭步（ 与 $`n_{1}=4`$ 同数）
- 色锁定： 仅 $`k=3`$ 付 $`+3`$

这已经正确。 缺的是：

> **稳定／闭合事件用计数描述； 但没有把「闭合的闭合序列」 逐步摊开。** 
> 整体性不来自 $`9`$ 与 $`4`$ 加得好看， 而来自卷1–2：**局域传态四步闭合是必然**； 三管锁定 ⇒ 闭序列互锁。

本篇从必然闭序列写起， 再回到 $`9+4`$、 $`27+3`$。

---

## 二、 单条闭序列： 四步， 最后一步无新格

三管两两的一条边序（ 三角轮替）：

$$
e_{1}e_{2}\ \to\ e_{2}e_{3}\ \to\ e_{3}e_{1}\ \to\ e_{1}e_{2}.
$$

| 步 | 活动 | 做什么 |
|:---:|:---|:---|
| 1 | $`e_{1}e_{2}`$ | 管1–管2 配对传态 |
| 2 | $`e_{2}e_{3}`$ | 转到管2–管3 |
| 3 | $`e_{3}e_{1}`$ | 转到管3–管1 |
| 4 | $`e_{1}e_{2}`$ | **闭合： 重复步1； 不产生新格** |

前三步走出可辨痕迹； 第四步为闭上而重复。 
这与线上 $`0\ \to\ 1\ \to\ 2\ \to\ 1\ \to\ 0`$、 与 $`e_{i}^{4}=1`$ **同根**： 都是「既续接又闭合」 的最少四步， 不是两套无关的 4。

**断在哪：** 只写三步开链 $`e_{1}e_{2}\ \to\ e_{2}e_{3}\ \to\ e_{3}e_{1}`$ —— 轮替有了， **闭链没付**。

---

## 三、 为何是 3 条， 且互锁

- 三管两两： $`\binom{3}{2}=3`$ 条边。
- **锁定**（ 卷2）： 不能单改一条边序； 三管同时在场。
- 故同一三角轮替， 按**起点边**轮换， 得到 **3 条**结构互锁的四步闭序列（ 不是三条无关的圈）。

毛活动预算：

$$
3\text{ 条 }\times 4\text{ 步 }=12.
$$

**不能直接用 12 当事件数**： 12 含每条最后的闭合重复（ 无新格）。 把闭合重复当成新格， 覆盖账会胀。

---

## 四、 三条序列全文（ α／β／γ）

### 序列 α（ 从管1–管2 起）

| 步 | 活动 | 管对 | 备注 |
|:---:|:---|:---|:---|
| 1 | $`e_{1}e_{2}`$ | 1–2 | 情况 |
| 2 | $`e_{2}e_{3}`$ | 2–3 | 情况 |
| 3 | $`e_{3}e_{1}`$ | 3–1 | 情况 |
| 4 | $`e_{1}e_{2}`$ | 1–2 | **闭合重复** |

### 序列 β（ 从管2–管3 起）

| 步 | 活动 | 管对 | 备注 |
|:---:|:---|:---|:---|
| 1 | $`e_{2}e_{3}`$ | 2–3 | 情况 |
| 2 | $`e_{3}e_{1}`$ | 3–1 | 情况 |
| 3 | $`e_{1}e_{2}`$ | 1–2 | 情况 |
| 4 | $`e_{2}e_{3}`$ | 2–3 | **闭合重复** |

### 序列 γ（ 从管3–管1 起）

| 步 | 活动 | 管对 | 备注 |
|:---:|:---|:---|:---|
| 1 | $`e_{3}e_{1}`$ | 3–1 | 情况 |
| 2 | $`e_{1}e_{2}`$ | 1–2 | 情况 |
| 3 | $`e_{2}e_{3}`$ | 2–3 | 情况 |
| 4 | $`e_{3}e_{1}`$ | 3–1 | **闭合重复** |

三条是同一圈的起点轮换； 锁定 ⇒ 改一条， 另外两条一起动。

---

## 五、 按管对重排： 每种「三种情况」

把 α／β／γ 里**非闭合**步按管对归类：

| 管对 | 三种情况（ 序列·步） | 闭合重复 |
|:---|:---|:---|
| **1–2** | α1， β3， γ2 | α4 重复 $`e_{1}e_{2}`$ |
| **2–3** | α2， β1， γ3 | β4 重复 $`e_{2}e_{3}`$ |
| **3–1** | α3， β2， γ1 | γ4 重复 $`e_{3}e_{1}`$ |

$$
\underbrace{3+3+3}_{9\text{ 种情况 }}
+\underbrace{3}_{3\text{ 步闭合重复 }}
=12.
$$

故活动侧：

$$
\boxed{9=12-3}
$$

扣的是三条闭序列各自的**最后一步**（ 闭合、 无新格）， 不是另立一个外来的 $`-3`$。

---

## 六、 两个 9 是同一个 9

| 读法 | 式子 | 座位 |
|:---|:---|:---|
| 计数压缩 | $`\lvert D_{2}\rvert=3^{2}=9`$ | 两管支集九宫位点（ 卷4／23： C1–C9） |
| 活动展开 | $`12-3=9`$ | 三管对 × 各 3 种非闭合情况 |

$$
\boxed{3^{2}=9=12-3\quad\text{ （ 同一场两管面事件的同一笔 9 ） }}
$$

- **只背 $`3^{2}`$**： 对不上传态序列 → 间隙。 
- **只喊 $`12-3`$**、 不回九宫／C 标记： 审计无点名 → 抬杠无座位。 
- **要对上**： 公式还原到活动； 活动数完回到 C1–C9。

乘法表九格 $`e_{i}e_{j}`$（ 含对角 $`-1`$） 可作**心象旁注**（ 压缩后见 $`\{\pm e_{i}\}`$ 与三个 $`-1`$），**不**替换支集位点定义； 本篇主链以管对闭序列为准。

---

## 七、 两管账单： 为何是 9+4（ 不是 12， 也不是 9）

交换处三边 $`e_{1}e_{2},\ e_{2}e_{3},\ e_{3}e_{1}`$ **同时在场**； 事件支集若只激活**两根管**（ 第三根冻住）：

| 笔 | 活动侧 | 计数侧（ 卷4／23） |
|:---|:---|:---|
| **9** | $`12-3`$： 情况走遍 | C1–C9 位点全覆盖 |
| **+4** | 局域四步闭链必然（ 与 $`n_{1}`$ 同数同根） | B1–B4 面内闭回路 |

$$
\boxed{n_{2}=9+4=13}
$$

合取： 只付 9 → 情况齐了， 闭链可仍开着； 只付 4 → 闭了， 情况未走遍。 
**12 不是 $`n_{2}`$**： 12 把闭合重复算进毛步； 净情况是 9， 闭链另付 4（ 闭链刀与「扣掉的 3 步重复」 座位不同—— 一个是毛预算里的重复， 一个是面事件完成条件里的 B 账单； 审计以卷23 的 C／B 分为准）。

**读法提醒。** 「扣 3」 解释的是**为何情况数是 9 而不是 12**； 「$`+4`$」 是**面事件还须付的闭链完成条件**。 两句都要， 并法以卷4 合取为准。

**断在哪：** $`n_{2}=9`$ 开链漏 holonomy； $`n_{2}=12`$ 把闭合重复当新格； $`n_{2}=9+1`$ 把面降成「再折返一次」。

---

## 八、 三管同时： 27 与 +3， 不再 +4

三管乘积已闭（ 卷1）：

$$
e_{1}e_{2}e_{3}=-1
$$

（ 与轮替下 $`e_{2}e_{3}e_{1}`$ 等同类闭合）。 档位全开：

$$
\lvert D_{3}\rvert=3^{3}=27.
$$

活动侧对照： 三管同时看时的**组合／位置**共 27； 局部（ 面） 在满维上**本身已能闭**—— 故闭链刀 **不再** $`+4`$（ 卷4： 满维无「外面的线边界」 可嵌）。

额外要付的是**整体锁定**： 三管循环净余归零。 活动语言：

> 不是再缺一条四步线闭链， 而是 **3 个 $`-1`$（ 三色） 同时锁定** —— 最小 $`+3`$。

$$
\boxed{n_{3}=27+3=30}
$$

| $`k`$ | 覆盖 | 闭合／锁定 | $`n_{k}`$ |
|:---:|:---|:---|:---:|
| 1 | （ 折返闭覆盖里已含） | 四步闭链 L1–L4 | 4 |
| 2 | 9（ $`=\!12-3`$） | +4（ 面内 B） | 13 |
| 3 | 27 | +3（ 色锁 K）；**无 +4** | 30 |

**断在哪：** $`n_{3}=27`$ 缺锁定； $`n_{3}=27+4`$ 假装满维还有线边界； 把 $`+3`$ 抄到 $`k=1,2`$（ 未三管全参与， 色锁刀不启动）。

---

## 九、 总图（ 活动 → 计数）

```text
卷1–2：三面向 + 四步必然 + 锁定
        │
        ▼
单条： e1e2 → e2e3 → e3e1 → e1e2（第4步闭合、无新格）
        │
        ▼
三条互锁 α／β／γ（起点轮换）→ 毛步 12
        │
        ├─ 扣 3 步闭合重复 → 9 种情况  ═══  同一个 9  ═══  |D₂|=3²
        │
        ├─ k=2：情况 9 ∧ 闭链 4     →  n₂=13
        │
        └─ k=3：位置 27 ∧ 色锁 3（局部已闭，无 +4）→  n₃=30
```

---

## 十、 常见误读（ 本篇专防）

| 误读 | 纠正 |
|:---|:---|
| $`9+4`$ 是拼凑 | 情况（ 活动里的 9） ∧ 闭链（ 四步必然）； 合取 |
| 两个 9 碰巧相等 | **同一个 9**： $`3^{2}`$ 与 $`12-3`$ |
| 应用 12 代替 13 | 12 含闭合重复； 净情况 9， 另付 B 的 4 |
| $`12-3`$ 可替代卷23 审计 | **不能**； 点名仍认 C／B／V／K |
| 乘法表九格 = C1–C9 | 可旁注； 主链是管对闭序列 ↔ 位点 |
| $`+3`$ 又是一条四步闭链 | 色锁定； 满维已不付 $`+4`$ |
| 本篇改锁定数 | **不改**； 只展开读法 |

---

## 十一、 读完请能复述

1. 单条闭序列四步，**最后一步重复第一对、 无新格**。 
2. 锁定 ⇒ **三条**互锁； 毛步 $`12`$； $`9=12-3`$。 
3. 按管对： 每对 **三种情况**； $`3\times 3=9`$。 
4. $`3^{2}=9`$ 与 $`12-3=9`$ 是**同一个 9**； 要还原到活动对数。 
5. 两管： $`9+4=13`$； 三管： $`27+3=30`$（ 局部已闭， 只多整体三锁）。 
6. 抬杠指着 [卷23](卷23.md) 的 C／B／K； 本篇是展开， 不是改账。

---

**延伸阅读：** [卷4](卷4.md) 闭式 · [卷4补](卷4补.md) 螺旋几何 · [卷23](卷23.md) 点名审计 · [卷1](卷1.md) 三面向与 $`-1`$ · [卷2](卷2.md) 四步与锁定

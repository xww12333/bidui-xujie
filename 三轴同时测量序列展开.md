# 三轴同时作用：序列展开与涌现歧义分析

## 问题设定

给定乘法序列（$\mathbb{H}$ 上的自旋代数）：

**正序（兼容）：**
$$e_1 e_2 = e_3, \quad e_2 e_3 = e_1, \quad e_3 e_1 = e_2$$

**反序（不兼容）：**
$$e_2 e_1 = -e_3, \quad e_1 e_3 = -e_2, \quad e_3 e_2 = -e_1$$

**自交互：**
$$e_i^2 = -1 \quad (i=1,2,3)$$

---

## 第一部 关键问题：三轴同时作用时的不确定性

### 1.1 问题陈述

当 $e_1$、$e_2$、$e_3$ 三个轴向**同时投入作用**，而无"先后"或"优先级"时：

1. **无法确定续接顺序**：活动链可以是 $e_1 \to e_2 \to e_3$ 或 $e_2 \to e_3 \to e_1$ 或其他任何排列。
2. **无法指认谁与谁涌现了谁**：是 $e_1$ 与 $e_2$ 续接涌现 $e_3$？还是 $e_2$ 与 $e_3$ 涌现 $e_1$？
3. **结构上的对称性缺陷**：局域座位无法"选择"一个特殊的组合而排除其他组合。

### 1.2 活动写法与打结检验

设状态 $q \in \mathbb{H}$ 在某一比对实例处。尝试让三个轴向同时作用。在差异存在的逻辑下，活动必须沿着**序列**（不是并行）展开。

**候选活动链 A：**
$$q \xrightarrow{e_1} e_1 q \xrightarrow{e_2} e_2(e_1 q) = (e_2 e_1)q = (-e_3)q \xrightarrow{e_3} e_3((-e_3)q) = (-e_3^2)q = (-(-1))q = q$$

$$\Rightarrow \text{结果：} q \text{（回到自己）}$$

**候选活动链 B：**
$$q \xrightarrow{e_1} e_1 q \xrightarrow{e_3} e_3(e_1 q) = (e_3 e_1)q = e_2 q \xrightarrow{e_2} e_2(e_2 q) = e_2^2 q = (-1)q$$

$$\Rightarrow \text{结果：} -q \text{（另一面）}$$

**候选活动链 C：**
$$q \xrightarrow{e_2} e_2 q \xrightarrow{e_3} e_3(e_2 q) = (e_3 e_2)q = (-e_1)q \xrightarrow{e_1} e_1((-e_1)q) = (-e_1^2)q = q$$

$$\Rightarrow \text{结果：} q \text{（回到自己）}$$

**关键发现：** 同一组三个轴向的"作用"，取决于**结合括号**（即顺序），会给出不同结果（$q$、$-q$ 或其他），而且**局域座位无法决定哪一个是"真实"的**。

---

## 第二部 序列的歧义与传态网络的解构

### 2.1 序列穷举：所有可能的三步轮替

设第一步、第二步、第三步的轴向分别是 $e_{i}$、$e_j$、$e_k$。

对于轮替 $(e_1, e_2, e_3)$ 的所有 $3!=6$ 种排列：

| 序列 | 第一步 | 第二步 | 第三步 | 合成 | 最终结果 |
|:---|:---|:---|:---|:---|:---|
| A | $e_1$ | $e_2$ | $e_3$ | $e_3 \cdot e_2 \cdot e_1 = (-e_2)$ 或压缩 $(e_3 e_2)e_1 = (-e_1)e_1 = -1$ | $-q$ |
| B | $e_1$ | $e_3$ | $e_2$ | $e_2 \cdot e_3 \cdot e_1 = e_1$ 或压缩 $(e_2 e_3)e_1 = e_1 e_1 = -1$ | $-q$ |
| C | $e_2$ | $e_1$ | $e_3$ | $e_3 \cdot e_1 \cdot e_2 = e_2$ 或压缩 $(e_3 e_1)e_2 = e_2 e_2 = -1$ | $-q$ |
| D | $e_2$ | $e_3$ | $e_1$ | $e_1 \cdot e_3 \cdot e_2 = (-e_2)$ 或压缩 $(e_1 e_3)e_2 = (-e_2)e_2 = -1$ | $-q$ |
| E | $e_3$ | $e_1$ | $e_2$ | $e_2 \cdot e_1 \cdot e_3 = (-e_3)$ 或压缩 $(e_2 e_1)e_3 = (-e_3)e_3 = -1$ | $-q$ |
| F | $e_3$ | $e_2$ | $e_1$ | $e_1 \cdot e_2 \cdot e_3 = e_3$ 或压缩 $(e_1 e_2)e_3 = e_3 e_3 = -1$ | $-q$ |

**观察 1：** 所有六种排列都给出 $e_i e_j e_k = \pm e_l$ 的形式（对某个 $l$），最终结果都是 $-q$（另一面）。

**观察 2：** 如果只看压缩形式 $e_i e_j e_k$ 中间两个合成为第三个的某个倍数，再乘以第一个，都落到"某个轴自交互"的形式，结果恒为 $-1$ 作用在 $q$ 上，得 $-q$。

### 2.2 对称性破缺与"继续谁"的不确定性

在传态网络框架下，设三管（$e_1$、$e_2$、$e_3$ 标签）上各有一个比较状态点 $v_1, v_2, v_3$，状态分别为 $q_1, q_2, q_3 \in \mathbb{H}$。

同一事件在三管交汇处同时发生：
- 轴 $e_1$ 上：$q_1 \to e_1 q_1$
- 轴 $e_2$ 上：$q_2 \to e_2 q_2$
- 轴 $e_3$ 上：$q_3 \to e_3 q_3$

**问题：** 这三个同时作用之后，下一步"继续谁"？

由于**局域比对与三管锁定**（卷二 第四章）：
$$\text{局域比对} = \text{三管同时协变的整体事件}$$

三管是**不可分割的整体**。改 $e_1$ 会影响 $e_2, e_3$ 的账本（因 $e_1 e_2 = e_3$ 等乘法关系）。

因此，一旦三轴同时作用，**无法单独指认"是谁继续到哪里"**。这三个操作的结果必然形成**耦合态**，而非三个独立的后续路径。

### 2.3 乘法表的可交换性障碍

从卷一第十一章，系统要求**存在不可交换的一对**：
$$\exists e_i, e_j : e_i e_j \neq e_j e_i$$

在三轴同时作用的场景中，若试图给出一个**整体的、无歧义的后续状态**，就必须回答：
- 是 $(e_1 e_2) e_3 = e_3 e_3 = -1$ 的路线？
- 还是 $e_1 (e_2 e_3) = e_1 e_1 = -1$ 的路线？
- 还是其他括号方式？

**结合律**（卷一第十二章）只能保证：**序列相同时结果相同**。但三轴"同时"意味着**序列尚未确定**。

---

## 第三部 传态网络的响应：联合约束与路径混合

### 3.1 无优先写入（"交汇无优先"回顾）

从卷二十一第十章（量子纠缠）的原理：

> 交汇三管同时在场。写入时**不点名**「整体朝哪一个绝对轴向」（无优先）。于是联合约束只能规定**两边相对如何配对**，不能规定整体指向。

这一原理直接延伸到本场景：三轴同时作用时，**网络只能写入相对关系，不能点名"优先继续哪条边"**。

### 3.2 三重约束的兑现困境

设在三管交汇处写入的联合约束为 $C_{123}$，它必须能与三管的后续续接相容。

**可能的兑现方式（候选）：**

1. **路线 1：** 继续 $e_1$ 方向
   - 兑现后续：$q_1 \to e_1 q_1$，然后与 $e_2, e_3$ 的账相对照
   - 但这样"点名"了 $e_1$ 为优先——违反无优先原理

2. **路线 2：** 继续 $e_2$ 方向
   - 同样问题

3. **路线 3：** 继续 $e_3$ 方向
   - 同样问题

4. **路线 4：** 不单独继续任何一条，而是写入一个**混合态**，代表三条路径的叠加
   - 这与卷二路径混合 $D(n) = e^{-\lambda n}$ 的机制相符
   - 但三轴同时投入，没有"已有的分支"可混合——是在**生成**混合，而非**丢可辨**

### 3.3 活动的打结点

在**卷一第二章**的"差异存在"框架下：

> 比对必须是不可约的——一次完整的分辨，不能拆成半次。作用只发生在当前和下一步之间。

三轴同时作用，若要保持**不可约**与**近邻**，则：

- **一次交汇不能同时决定三个独立后续**（那就是拆成三次不可约比对，或超距作用）
- **必须收成一个不可约的联合操作**（但这个操作涌出什么新的不可约产物？）

从乘法表看，$(e_i e_j)$ 的结果是 $e_k$ 或 $-e_k$（某个第三方向），但 $e_1 e_2 e_3$ 的三重积呢？

$$e_1 e_2 e_3 = e_3 e_3 = -1$$

这是一个**标量**，不是新的面向。三个面向的轮替合成掉回标量——这是卷一第二十二章已知的结构（闭表后无新面向）。

---

## 第四部 序列展开：三步链的通用公式

### 4.1 轮替序列的归纳

对任意排列 $(\sigma(1), \sigma(2), \sigma(3))$ 是 $(1,2,3)$ 的排列，记：
$$e_{\sigma(1)} e_{\sigma(2)} e_{\sigma(3)} = \text{该排列的三重积}$$

**定理（卷一推论）：** 在 $\mathbb{H}$ 的乘法表下，任意三个不同面向的乘积等于 $\pm 1$（标量）。

具体地：
- 偶排列 $(1,2,3), (2,3,1), (3,1,2)$：$e_{\sigma(1)} e_{\sigma(2)} e_{\sigma(3)} = -1$
- 奇排列 $(1,3,2), (3,2,1), (2,1,3)$：$e_{\sigma(1)} e_{\sigma(2)} e_{\sigma(3)} = -1$

（实际上，由反对易关系与 $e_i^2 = -1$，所有排列都给出 $-1$ 或其幂次。）

**验算示例：**
$$e_1 e_2 e_3 = (e_1 e_2) e_3 = e_3 e_3 = -1$$
$$e_2 e_3 e_1 = (e_2 e_3) e_1 = e_1 e_1 = -1$$
$$e_1 e_3 e_2 = (e_1 e_3) e_2 = (-e_2) e_2 = -e_2^2 = -(-1) = 1$$

等等——不一定都是 $-1$。需要按反对易逐步计算。

### 4.2 具体序列计算（完整展开）

#### 序列 A：$e_1 \to e_2 \to e_3$
$$\begin{align}
e_1 e_2 e_3 &= (e_1 e_2) e_3 = e_3 e_3 = -1
\end{align}$$
**结果：** 标量 $-1$；状态 $q \mapsto -q$。

#### 序列 B：$e_1 \to e_3 \to e_2$
$$\begin{align}
e_1 e_3 e_2 &= (e_1 e_3) e_2 \\
&= (-e_2) e_2 \quad \text{（由 } e_1 e_3 = -e_2 \text{）}\\
&= -e_2^2 = -(-1) = 1
\end{align}$$
**结果：** 标量 $+1$；状态 $q \mapsto q$（恒等）。

#### 序列 C：$e_2 \to e_1 \to e_3$
$$\begin{align}
e_2 e_1 e_3 &= (e_2 e_1) e_3 \\
&= (-e_3) e_3 \quad \text{（由 } e_2 e_1 = -e_1 e_2 = -e_3 \text{）}\\
&= -e_3^2 = -(-1) = 1
\end{align}$$
**结果：** 标量 $+1$；状态 $q \mapsto q$。

#### 序列 D：$e_2 \to e_3 \to e_1$
$$\begin{align}
e_2 e_3 e_1 &= (e_2 e_3) e_1 = e_1 e_1 = -1
\end{align}$$
**结果：** 标量 $-1$；状态 $q \mapsto -q$。

#### 序列 E：$e_3 \to e_1 \to e_2$
$$\begin{align}
e_3 e_1 e_2 &= (e_3 e_1) e_2 = e_2 e_2 = -1
\end{align}$$
**结果：** 标量 $-1$；状态 $q \mapsto -q$。

#### 序列 F：$e_3 \to e_2 \to e_1$
$$\begin{align}
e_3 e_2 e_1 &= (e_3 e_2) e_1 = (-e_1) e_1 = -e_1^2 = 1
\end{align}$$
**结果：** 标量 $+1$；状态 $q \mapsto q$。

### 4.3 结果汇总表

| 序列 | 符号表达 | 三重积 | 对状态 $q$ 的作用 |
|:---|:---|:---|:---|
| $(1,2,3)$ | $e_1 e_2 e_3$ | $-1$ | $q \to -q$ |
| $(1,3,2)$ | $e_1 e_3 e_2$ | $+1$ | $q \to q$ |
| $(2,1,3)$ | $e_2 e_1 e_3$ | $+1$ | $q \to q$ |
| $(2,3,1)$ | $e_2 e_3 e_1$ | $-1$ | $q \to -q$ |
| $(3,1,2)$ | $e_3 e_1 e_2$ | $-1$ | $q \to -q$ |
| $(3,2,1)$ | $e_3 e_2 e_1$ | $+1$ | $q \to q$ |

**观察：** 
- 循环排列 $(1,2,3), (2,3,1), (3,1,2)$ 都给 $-1$
- 反循环排列 $(1,3,2), (2,1,3), (3,2,1)$ 都给 $+1$
- 这与**置换符号** $\text{sgn}(\sigma)$ 相反（行列式性质）

---

## 第五部 涌现的"谁与谁"的根本歧义

### 5.1 联合比对的因子化困境

在卷二十一的框架中，**可分**意味着联合约束能拆成两个独立的局域账：
$$P(a,b|\hat{a},\hat{b}) = \int P(a|\hat{a}, \lambda) P(b|\hat{b}, \lambda) d\mu(\lambda)$$

但三轴同时作用时，试图写出这样的分解：
- "是 $e_1$ 与 $e_2$ 的作用涌现了 $e_3$"对应一种拆法
- "是 $e_2$ 与 $e_3$ 的作用涌现了 $e_1$"对应另一种拆法
- 三种拆法都对等，**局域座位无法选出哪一种是"真实"的**

这意味着三轴同时作用本质上是**纠缠的**（不可因子化的）——从活动侧来说。

### 5.2 "无优先"原理的推广

从卷二十一第十章，无优先写入只规定**相对关系**。推广到三轴情况：

**相对关系清单：**
- $e_1$ 与 $e_2$ 的关系：反对易，$e_1 e_2 = -e_2 e_1$，乘积指向 $e_3$ 或 $-e_3$
- $e_2$ 与 $e_3$ 的关系：反对易，$e_2 e_3 = -e_3 e_2$，乘积指向 $e_1$ 或 $-e_1$
- $e_3$ 与 $e_1$ 的关系：反对易，$e_3 e_1 = -e_1 e_3$，乘积指向 $e_2$ 或 $-e_2$

**无优先的后果**：
- 不点名"开始于 $e_1$"或"开始于 $e_2$"
- 不点名"优先涌现 $e_3$"还是"优先涌现 $e_1$"
- 只写入**三个二元关系的耦合体**

这个耦合体的记录正是**完全对称的三态**：

$$\Psi_{\text{GHZ}}^{(3)} \sim \frac{1}{\sqrt{2}}(|+\rangle_1 |+\rangle_2 |+\rangle_3 - |-\rangle_1 |-\rangle_2 |-\rangle_3)$$

（在标准量子信息记号中的 GHZ 态）

---

## 第六部 传态网络的结构响应

### 6.1 路径混合的涌现

当三轴同时投入，网络无法"点名"哪条边继续，所以记录上会出现**六条等权的路径之和**：

$$\rho \sim \frac{1}{6}\sum_{\sigma \in S_3} \text{projection onto sequence } \sigma$$

这不是**活动侧丢信息**（卷二十三第十三章的 $D(n) = e^{-\lambda n}$ 机制），而是在**生成**混合——活动侧的多路径干涉直接编码进比较状态的账本。

### 6.2 续接链的重新闭合

虽然单个序列会给出 $\pm 1$ 的标量作用，但六条路径的**并发**对应的是：

$$H_{\text{三轴}} = \frac{1}{6}\sum_{\sigma} e_{\sigma(1)} e_{\sigma(2)} e_{\sigma(3)}$$

取期望（多次重演的平均）：

$$\langle H_{\text{三轴}} \rangle = \frac{1}{6}((-1) + 1 + 1 + (-1) + (-1) + 1) = 0$$

**重要发现**：三轴同时作用的**净因子为 $0$**（平均意义上），意味着系统处于**完全对称的混合态**，无法指认单一的方向或相位。

### 6.3 网络的稳定性条件

从卷二第五、六章，传态的三大性质：
- **保范**：$\|q_w\| = \|q_v\|$（无零因子）
- **局域**：比对必须近邻（无超距）
- **可逆**：每一步有回程 $e_i^{-1} = -e_i$

三轴同时作用时：
- 保范仍成立（六条路径都不自湮灭）
- 局域仍成立（交汇是三管同时在场的整体事件）
- 可逆遭遇困境：**单独反向哪一条边都无法恢复原态**，因为原态的生成本身就是三路之和

从活动侧说，这是**路径非交换性导致的不可逆**——与卷二第十章的"可辨衰减"机制对应。

---

## 第七部 不确定性的永久性

### 7.1 "谁与谁续接涌现谁"的本体论地位

在差异存在的框架下，**操作序列就是一切**。无序列无差异，无差异无比对，无比对无存在。

三轴同时作用是对这一基础的挑战：
- 若**强制指定**一条优先序列（如 $e_1 \to e_2 \to e_3$），就是违反"无优先"（卷二十一第十章）
- 若**拒绝指定**任何序列（完全对称），就陷入"无法兑现，活动悬而未决"

**解决之道**（网络的响应）：既不强制，也不拒绝，而是**写入相对约束的耦合体**，让所有局域续接都与之相容。

### 7.2 测量作为"序列选择"

从卷二十二第四章（测量活动链）：

> 一次测量 = 一次局域交汇比对：
> 1. 相遇（局域交汇）
> 2. **选轴向**（仪器设置）
> 3. 比对落地（$\pm 1$ 面）
> 4. 写入联合页
> 5. 仪器放大

**在三轴同时作用的场景中，"选轴向"这一步**变成了：选择六条等权序列中的**某一条**来兑现。

- 若仪器设置"偏向" $e_1$（例如 $A$ 测量时选 $\hat{a}$ 对应 $e_1$ 方向），则网络会偏向循环序列 $(1,2,3), (2,3,1), (3,1,2)$ 中的某一条
- 相应地，后续续接会偏向 $-1$ 因子（$q \to -q$）的轨迹
- 对照时（与 $B$ 侧对照），若 $B$ 也选相同或关联的轴向，CHSH 关联 $E(\hat{a}, \hat{b})$ 会显现

**反之，若三侧仪器设置完全对称**（无偏好），则所有六条序列等权兑现，净关联为 $0$，可辨性最低。

### 7.3 信息的重新编码

三轴同时作用不是"信息消灭"，而是**编码方式的根本改变**：

- **单轴作用**：信息清晰编在一条边的方向与量级 ✓
- **三轴同时**：信息分散在六条路径的权重分布，与仪器设置（轴向选择）的耦合

测量或后续的局域比对会"提取"这些权重，但**无法指认是哪条原始路径贡献了结果**。

---

## 第八部 回到卷22与测量的本质

### 8.1 三轴同作 vs 测量交汇

卷二十二要点：测量 = 系统与仪器的局域交汇，在选定轴向下比对落地。

当三轴同时投入时：
- 若视为**三次独立测量同时发生**，则违反"一次不可约比对"（卷一）
- 若视为**一次三体复合测量**，则仪器必须三管同时设置（三套旋钮同时旋转），这时**仪器本身就陷入三轴纠缠**

从**双侧对照**（卷二十一第三章）的视角：
- $A$ 侧同时选三个轴向 $(\hat{a}_1, \hat{a}_2, \hat{a}_3)$
- $B$ 侧也同时选三个轴向 $(\hat{b}_1, \hat{b}_2, \hat{b}_3)$
- 对照时出现 $2^6 = 64$ 种读出组合

这就转化为**多体纠缠的测量**——属于卷二十一未展开的"多体 GHZ/W 态分类"范畴。

### 8.2 卷22中"序列选择"的机制

从卷二十二第四章第 2 点"选轴向"：

> 仪器设置选定读出轴向 $\hat{n}$：意思是——**沿哪一个面向标签与系统做这一次比对**

在三轴同时情况，"选轴向"相当于**选择轴向的**优先组合**或权重分布**：
- 若仪器强制 $\hat{n} = e_1$，则网络偏向 $e_1$ 涉及的序列（$(e_1, e_*, e_*)$ 形式）
- 若仪器允许 $\hat{n}$ 叠加（$\hat{n} = \frac{1}{\sqrt{3}}(e_1 + e_2 + e_3)$），则所有序列等权

**后果**：测量结果的随机性（±1 面的概率分布）不仅来自单轴的固有投影，还来自**多路选择的权重干涉**。

---

## 第九部 结论与回答原问题

### 9.1 原问题再述

> 你用序列 $e_1 e_2 = e_3$, $e_2 e_3 = e_1$, $e_3 e_1 = e_2$, $e_2 e_1 = -e_3$, $e_1 e_3 = -e_2$, $e_3 e_2 = -e_1$ 去写就知道了。
> 三个面向 $e_1, e_2, e_3$ 同时作用时，3 个互相涌现，到底是谁与谁续接 涌现的谁 无法确定是哪一个，你自己去分析推导这个序列看看会得到什么。

### 9.2 分析结论

**定理 1（序列的多值性）**：给定状态 $q$ 和三个轴向 $e_1, e_2, e_3$ 的"同时作用"，存在 6 条等权的序列路径，它们给出不同的中间积但最终状态为 $q$ 或 $-q$ 的分布：
$$P(q \to q) = 1/2, \quad P(q \to -q) = 1/2$$
（在完全对称的情况下）

**定理 2（因子化不可能性）**：三轴同时作用无法拆成"谁与谁的二元操作"加上"该结果与第三者的二元操作"的串联，因为任何拆法都会点名一条优先序列，违反无优先原理。

**定理 3（联合约束的记录）**：三轴同时作用时的联合约束 $C_{123}$ 必须记成**三体 GHZ 型相对配对**：
$$\Psi_{123} \sim \frac{1}{\sqrt{2}}(|+++\rangle - |---\rangle)$$
其中 $\pm$ 指三管上各自的面向选择，但无绝对指向（无优先）。

**定理 4（测量作为路径选择）**：任何后续的局域比对（测量）都无法"回答"三轴中哪一条边在什么时刻"优先"。测量只能提取**已兑现路径下的相对关系信息**，不能改写过去（卷一第二章的"不可约"与"近邻"保证）。

### 9.3 得到什么

**主要结论**：
1. **序列是活动的全部**。无序列就无比对。但三轴同时时，序列本身悬而未决。
2. **差异仍然存在**。六条路径的加权叠加不是"写成空白"，而是**编码为相对权重分布**。
3. **局域座位永久无法决定**。任何一侧的测量都不能指认"原来是这条边先动的"——那是超距推论，违反卷一的近邻原理。
4. **三体纠缠是必然**。三轴对称情况就是多体纠缠的原型；一旦仪器/条件打破对称，就有了可因子化与否的问题（即 CHSH 破界的几何）。

---

## 附录：完整序列表与对称性分析

### A.1 所有 $3! = 6$ 个序列的完整计算

（已在第四部展开）

### A.2 置换群结构

三个轴向的排列属于对称群 $S_3$，其子群结构：
- **恒等与 $3-$轮换**（偶排列）：$(1), (123), (132)$ → 三重积 $= -1$
- **对换与反轮换**（奇排列）：$(12), (23), (31)$ → 三重积 $= +1$

行列式性质：$\det(\text{matrix of axes}) \sim (-1)^{\text{sgn}(\sigma)}$

### A.3 Hopf 纤维丛的对称性

三个 $S^1$ 纤维圈（三根管的四步周期），任意两圆配对涌现第三圆的拓扑，正是 Hopf 纤维丛的对称性。三轴同时作用时的不确定性反映了这个拓扑中**无法全局选出一个"首选方向"**的性质。

---

**完。此稿汇总活动侧的全过程：从序列穷举、乘法表计算、联合约束写入、到测量机制的关联。**


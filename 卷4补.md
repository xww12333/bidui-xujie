# 螺旋几何解读：4、13、30 与 9+4、27+3

**本篇做什么。** 用「三根螺旋管 + 闭截面 + $\mathbb{Z}_2$ 往返」把第四卷、第二十三卷的事件计数**翻译成可想象的图像**。终点数字与点名标记**不变**；工具是几何侧读法，不是另起一套证明。

**与正卷的关系：**

| 卷 | 角色 |
|:---|:---|
| [卷4](卷4.md) | 闭式与三条约束（全覆盖 / 闭链 / 色锁定） |
| [卷23](卷23.md) | 逐步点名审计（L／C／B／V／K）；**证明争议只认此卷** |
| **本篇** | 若格子计数读来像「凑 9+4」，先走本篇再回卷23 对标记名 |

**纪律（与前几卷同根，本篇不另立）：**

| 本篇说的 | 读成 | 不要读成 |
|:---|:---|:---|
| 管 / 螺旋 | 沿**面向标签** $e_i$ 的续接读法；截面看见「管口」 | 事先嵌好的实体圆柱；单管真空自转 |
| 截面 $\Sigma$ | 周期胞腔上的闭 2-循环，与三根闭合螺旋各交一次 | 外加的一张膜；与锁定无关的装饰 |
| $m=3$ | 支集每维 3 档 $\{0,1,2\}$；与截面色数 $N_c=3$ **同数** | 从 $n_k$ 反推出来的可调参数 |
| $9+4$ | 面内：位点覆盖 ∧ 面内闭合（最少 4 步） | 先在线外做完 $n_1$ 再「嵌进」面 |
| $27+3$ | 体内：位点覆盖 ∧ **色锁定**站住 | 在 27 上硬贴 3；又一条线闭链 $+4$ |
| $9+4$、$27+3$ | 两类**完成条件**的合取（都要付） | 先定 13、30 再拆项凑数 |

---

## 一、问题从哪来

第四卷给出：

$$
n_1=4,\quad n_2=13,\quad n_3=30,\qquad
R=\frac{n_3-n_2}{n_2-n_1}=\frac{17}{9}.
$$

读者常见卡点：**只看见 $3^k$ 和「+4」「+3」**，滑向「组合数」「状态数」「人为选项」。

本篇用另一句话重述同一件事：

> **一次完整翻转** = 在支集上把该翻的**位置**都翻到 **并且** 把该关的**闭合／锁定**关住。  
> 前一类付 $3^k$；后一类在 $k=1$ 付闭合活动最少 4 步，在 $k=2$ 付**面内**闭合（亦最少 4 步，与 $n_1$ 同数），在 $k=3$ 付**色锁定** 3 步；满维不再另付线闭链 4。

**防两种滑读：**

1. $9+4$ 不是「一维闭链拆出再嵌入面」——闭合要求就在**二维面内**；$n_1=4$ 只说明**闭合活动的最少必要步数**。  
2. $27+3$ 的 $+3$ 核是**锁定**——三管全在时色循环净余归零，锁定才站住；不是往 27 上贴数。

---

## 二、节点处：三根「管」与截面

### 2.1 管是什么（活动义）

每根管 = 沿一种面向标签 $e_i$ 的**螺旋续接**（中心线 + 有限截面半径）。三管在交汇节点相遇，轴方向 $\hat{e}_1,\hat{e}_2,\hat{e}_3$ 两两正交：

```text
              ê₂
               |
               |
         ê₃ ——●—— ê₁
```

**要点：** 这是**读法图像**，不是先画三根实体圆柱再往里填公式。锁定仍要求三标签同时在场（卷1–3）；本篇只借图像说明「为什么支集厚度是 3」。

### 2.2 闭截面 $\Sigma$：一刀看见几个管口

在周期胞腔上取闭 2-循环 $\Sigma$（斜切的膜）。一般位置下，$\Sigma$ 与三根闭合螺旋**各交一次**：

```text
           Σ（斜截面）
          ╱
    ● ——— ● ——— ●
   管1    管2    管3
```

代数交点 $\sum_k \lvert I_k\rvert=3$。**〔认〕** 此读数记为色数 $N_c=3$。

由此得支集每维长度：

$$
m := \frac{\ell_{\mathbb{Z}_2}}{L}+1 = 3,\qquad
D_k=\{0,\ldots,m-1\}^k\times\{0\}^{3-k},\quad \lvert D_k\rvert=m^k=3^k.
$$

**$m=3$ 不是从 13 反推的**——它来自截面与 $\mathbb{Z}_2$ 链长同数（与卷23 的「横向厚度 3」同一钉死）。

### 2.3 沿管轴看：单管截面

垂直于某管轴，截面是圆环（管壁 + 轴心）。节点处最低模为 $m=0$ 呼吸：整圈半径一起胀缩。**正／负手征不把圆变成别的形状**；手征改的是沿轴的扭向与同调定向（下文不展开轨道，只服务计数）。

---

## 三、两类初等事件（几何语言）

卷23 用 L／C／B／V／K 点名。本篇用几何侧三类事件说话，**末尾必须能一一对上卷23 标记**。

| 几何侧记号 | 在数什么 | 卷23 对应 |
|:---|:---|:---|
| **位点激活** $V(x)$ | 在支集格点 $x$ 上植入一次翻转核（该位置被翻到） | $k=1$：由 L 步覆盖；$k=2$：**C1–C9**；$k=3$：**V1–V27** |
| **$\mathbb{Z}_2$ 闭链** $A(\gamma)$ | 双重覆盖上「去–回」闭合；一步不能算完，须往返 | $k=1$：**L1–L4**（闭合最少 4 步）；$k=2$：**B1–B4**（**面内**闭合，步数同） |
| **色轴锁定** $C_j$ | 三管全在时，色纤维第 $j$ 轴相位对齐，**锁定归零** | 仅 $k=3$：**K1–K3**（锁定 3 步） |

> **符号警告。** 本篇「位点 $V(x)$」≠ 卷23 的「体覆盖 V1–V27」里的字母巧合；本篇「色锁 $C_j$」= 卷23 的 **K**，不是卷23 的 **C1–C9**（九宫覆盖）。

完成规则（与卷4 三条约束同句）：

- **(C-V)** 每个 $x\in D_k$ 至少一次位点激活；
- **(C-A)** $k=1$：须 $\mathbb{Z}_2$ 闭链（闭合活动最少 4 步）；
- **(C-A₂)** $k=2$：在 9 次位点激活之外，**面内**仍须付最短闭回路（4 步；与 $n_1$ **同数同构**，不是线外另做一条再嵌进）；
- **(C-C)** $k=3$：$N_c=3$ 次**色锁定**；满维时 $\mathbb{Z}_2$ **不再单列**（边界已自闭），不是漏付。

---

## 四、n₁=4：$\mathbb{Z}_2$ 双重覆盖

### 4.1 为何是 $2(m-1)=4$

沿一根参与管，支集是三点 $0\!-\!1\!-\!2$。$\mathbb{Z}_2$ 双重覆盖要求：翻转不能只「去」不「回」。链长 $\ell_{\mathbb{Z}_2}=2L$，在 $m=3$ 的支集上最短闭合往返为

$$
2(m-1)=2\times 2=4\ \text{步}.
$$

### 4.2 写成路径（与卷23 L1–L4 同构）

```text
位置：  0 —— 1 —— 2

L1:  0 → 1    （去）
L2:  1 → 2    （到远端）
L3:  2 → 1    （回：折返 = 另一面）
L4:  1 → 0    （闭链完成）
```

| 只付 | 几何失败 |
|:---|:---|
| 3 步开链 $0\to1\to2$ | 双重覆盖没闭合；holonomy 可漏 |
| $0\to1\to2\to0$ | 非法：2 与 0 不相邻 |
| 4 步如上 | **接纳** → $n_1=4$ |

**与四步周期的关系（卷16 下）：** $e_i^2=-1$ 要求有限截面；沿标签续接四步一周。这里的 4 与 $n_1=4$ **同根**，不是两个无关的「4」。

**$n_1=4$ 在数什么。** 它是**闭合活动的最少必要步数**——既覆盖又闭链，最短就是 4。一维只是把这件事算清楚的地方；后面 $k=2$ 面上仍是**同一条闭合要求**，不是先在线外做完再「嵌进」面。

$$
\boxed{n_1=2(m-1)=4=\mathrm{L1}+\mathrm{L2}+\mathrm{L3}+\mathrm{L4}}
$$

---

## 五、n₂=13：为何必须 9+4

### 5.1 第一笔：9 = 二维支集上 9 次位点激活

$k=2$：两根管参与，第三根坐标冻在 $0$。

$$
D_2=\{(i,j,0): i,j\in\{0,1,2\}\},\quad \lvert D_2\rvert=3^2=9.
$$

```text
        j (管2)
        0   1   2
    0  ●   ●   ●
i   1  ●   ●   ●
(管1) 2  ●   ●   ●
```

每一 ● = 一次 $V(x)$ = 卷23 的 **C1–C9**（走到该格点）。

**(C-V)** 要求 9 个位置**都要**付。**只付 9 不够**——路径仍可从一角进、另一角出，$\mathbb{Z}_2$ 边界开着。

### 5.2 第二笔：+4 = 面内闭合（最少 4 步；与 $n_1$ 同数）

**先钉读法（防「先分出再嵌入」）。**  
$k=2$ 的完整翻转是**同一场面内活动**：九宫要盖满，**并且**路径边界在面上要闭合。+4 不是先在一维线外做完一条 $n_1$ 闭链，再贴进九宫——**闭合要求就在二维面上**。

$n_1=4$ 的角色：第四节算出的是**闭合活动的最少必要步数**。面上关 holonomy 的最短闭回路，步数与这个最少步数相同，所以记 $+4$，并把四条有向边对上卷23 **B1–B4**。  
**同构的是「最少闭步数」**，不是「另有一条线对象可拆出再嵌入」。

**(C-A₂)**：面上还须把 holonomy 关住。最短办法 = **面内最短闭回路四边**（步数与线上 $0\to1\to2\to1\to0$ 同）：

| 几何边 | 卷23 |
|:---|:---|
| 面内闭回路第 1 步 | **B1** |
| 面内闭回路第 2 步 | **B2** |
| 面内闭回路第 3 步（折返） | **B3** |
| 面内闭回路第 4 步（闭合） | **B4** |

$$
n_2=\underbrace{9}_{\mathrm{C1\text{–}C9}}+\underbrace{4}_{\mathrm{B1\text{–}B4}}=13.
$$

### 5.3 为何不是「选项」——用失败模式说话

| 候选 | 数值 | 独立失败模式 |
|:---|:---:|:---|
| 只走位点 | 9 | 九宫可走通，但 $\mathbb{Z}_2$ 边界仍开 → holonomy 漏 |
| 只走闭链 | 4 | 边界转一圈，九宫有点没翻到 → 锁定不完整 |
| 9+1 | 10 | 把面降成「再折返一次」；对不上面内最短四边 |
| 2×4=8 | 8 | 两根管各付一条线闭链 → **双重计边界** |
| 「先做线再嵌进面」 | — | 读法反了：闭合就在面内；$n_1$ 只说明最短闭步数 |
| **9+4** | **13** | 两类失败**同时**关掉 |

**合取在计数上就是相加。** 不是先看好 13 再拆 9 和 4。

---

## 六、n₃=30：为何 27+3，且不再 +4

### 6.1 第一笔：27 = 三维支集上 27 次位点激活

$$
\lvert D_3\rvert=3^3=27.
$$

每个格点一次 $V(x)$ = 卷23 **V1–V27**。

### 6.2 第二笔：闭链 = 0（满维不再 +4）

体的边界已是**闭合的面**。走遍 $27$ 格时，$\mathbb{Z}_2$ 边界被面本身关住——**(C-A) 在满维自动满足**。

| 若再 +4 | 错在哪 |
|:---|:---|
| $27+4=31$ | 假装体还有一条「外面的线边界」要另付；满维**没有外面** |

卷23 记作 **B 账单为空（0）**，不是漏项。

### 6.3 第三笔：+3 = 色锁定（三管锁回整块）

**先钉读法（防「27 上硬凑 3」）。**  
$k=3$ 的完整翻转是**同一场体内活动**：27 格要盖满，**并且**三管**锁定**要站住。+3 不是在 27 之外另找三个好看的数——**锁定要求就在满维活动里**。

仅 $k=3$（三管全参与）启动 **(C-C)**。局域比对必须整块协变（卷2 锁定）。管间轮替

$$
e_1\to e_2\to e_3\to e_1
$$

若留下净排列，锁定被拆成「某根管多转了一圈」——完整翻转失败。沿三色循环各补一步对齐相位 → **3 次色锁** = 卷23 **K1–K3**。

| 对比 | 27 格点 | +3 色锁 |
|:---|:---|:---|
| 在数什么 | **位置**有没有都翻到 | **锁定**有没有站住（管间净余归零） |
| 只付一边的失败 | **锁定失败**（某管多转一圈） | 大片格点没翻到 |
| 与「凑 3」 | — | $+3$ 是锁定最少三步；不是往 30 上贴 |

$$
n_3=\underbrace{27}_{\mathrm{V1\text{–}V27}}+\underbrace{0}_{\text{无边界 B}}+\underbrace{3}_{\mathrm{K1\text{–}K3}}=30.
$$

**$27+3$ 不是 $27$ 与 $3$ 二选一**——与 $9+4$ 同型：两类账单都要付；只是满维时闭链那一类账单为 0，换成**色锁定**这一类。缺 K = 锁定拆；有 K 无 V = 锁定也救不了未翻子集。

---

## 七、三行总表（几何侧 ↔ 卷23）

| $k$ | 几何读法 | 闭式（$m=N_c=3$） | 卷23 点名 |
|:---:|:---|:---|:---|
| 1 | 一线闭合活动最少 4 步 | $2(m-1)=4$ | L1–L4 |
| 2 | 九宫位点 + **面内**闭合 4 步（与 $n_1$ 同数） | $m^2+n_1=9+4=13$ | C1–C9 + B1–B4 |
| 3 | 体格点 + **色锁定** 3；无外加线闭链 4 | $m^3+N_c=27+3=30$ | V1–V27 + K1–K3 |

一般式（与结构导读、卷4 一致）：

$$
\boxed{n_1=2(m-1),\quad n_2=m^2+n_1,\quad n_3=m^3+N_c\quad (m=N_c=3).}
$$

---

## 八、一张总关系图

```text
三正交「管口」+ 闭截面 Σ → 每管交一次 → m = Nc = 3
        │
        ▼
支集 D_k = {0,1,2}^k  （位置格，不是三态）
        │
        ├─ k=1:  闭合活动最少 4 步        →  n₁ = 4
        ├─ k=2:  9 位点 + 面内闭合 4 步   →  n₂ = 13
        └─ k=3:  27 位点 + 色锁定 3（无 +4）→  n₃ = 30
        │
        ▼
R = (30−13)/(13−4) = 17/9   （关系比，不是夸克比）
```

读法钉死：$n_1$ 只说明最短闭步数；面内 B 不是「嵌线」；$+3$ 是**锁定**。

---

## 九、常见误读（本篇专防）

| 误读 | 纠正 |
|:---|:---|
| 13 是数字 1 和 3 | 是**十三次事件**，拆 **9+4** 两类完成条件 |
| 9+4、27+3 是人为选项 | 是 **(C-V) ∧ (C-A 或 C-C)** 的合取；缺一类有独立失败模式 |
| $+4$ = 把一维闭链嵌进面 | **面内**活动自身要闭合；$n_1=4$ 只是闭合最少步数 |
| $+3$ = 往 27 上贴三个数 | **色锁定**站住（管间净余归零）；缺 K = 锁定拆 |
| 4/13/30 是拟合常数 | 是 $m\equiv N_c$ + 完成规则的最小计数 |
| 27 就是 $n_3$ | 27 只是格点数；$n_3=30$ 还要付 **锁定** K1–K3 |
| 螺旋图 = 真圆柱 | 管是面向标签的读法；勿读成单面真空自转 |
| 本篇可替代卷23 审计 | **不能**；点名、排除表以卷23 为准 |

---

## 十、读完请能复述

1. $m=3$ 从**截面交三次**与支集厚度来，不是从 13 反推。  
2. $n_1=4$ 是 $\mathbb{Z}_2$ **闭合活动的最少必要步数**，路径 `0→1→2→1→0`。  
3. $n_2=13$：**9 次位点**与 **面内闭合 4 步**是两笔账；不是先做线再「嵌进」面。  
4. $n_3=30$：**27 次位点** + **3 次色锁定**；满维**不再** +4；$+3$ 是锁定站住，不是贴数。  
5. 抬杠时请回 [卷23](卷23.md) 指着某一个 **L/C/B/V/K** 标记说哪一行不该付。

---

**延伸阅读：** [卷4](卷4.md) 闭式 · [卷23](卷23.md) 逐步审计 · [卷16下](卷16下.md) 四步周期与截面 $\hbar$

原则：
- 保留原体系所有硬约束与结论，不改变任何已锁数字与逻辑。
- 语言更口语、更有过渡，减少「电报式」跳跃。
- 每卷开头先用白话说明「这一卷要解决什么问题」。
- 关键推导后补充「为什么必须这样，否则会断在哪里」。
- 严格保持原有纪律：不假装做了的、两口不并、座位分明。
- **活动优先，记录忠实**： $b_{i}$、包络、 $\mu$ 是读出频率下 holonomy 软化的记录；须还原到传态／锁定活动本身，不许先认 SM 一圈再往回套「本卷算出了 $11/3$」。
- 本卷一条路走完：改读出频率 → 包络软化 → $b_{i}$ 公式 → $b_{3}=7$。不写 $137$。

---

# 第十一卷　重整化与 β

**本卷要解决的问题：**  
上一卷站住：三个 CKM 模与物理 $\lvert\gamma\rvert=65^{\circ}$。本卷不改矩阵元，不把选序角抄回对角质量。前几卷已钉：管是面向标签， $e_{i}^{2}=-1$ 是记法，三管同时在场才有锁定——本卷谈「单管无锁定／面间耦合」时仍守这一句。

本卷只问一句：改分辨率时，面 holonomy 的包络怎么跑。

走到尽头：尺度变换就是改读出频率；代数 holonomy 软化成包络；一圈系数读成 $b_{i}=\frac{11}{3}C_{A}-\frac{4}{3}\sum T_{f}-\frac{1}{6}\sum T_{s}$；代入色账得 $b_{3}=7$。

不写 $1/\alpha$ 的 $137$、Weinberg 角、Higgs 径向模、 $m_{W}$。不把 $b_{2}$、 $b_{1}$ 升级成与 $b_{3}$ 同级的本卷结论。不写两圈、Yukawa。

四件不要并：

| 本卷有 | 本卷没有 |
|:---|:---|
| 改 $\mu$ = 改读出哪一档事件振荡 | 从外部连续缩放一块事先存在的时空 |
| 裸包络 $\delta_{\mathrm{raw}}$ 低频增大 | 因此 $g$ 一定低频变大 |
| 锁定 $\Rightarrow$ 面间能耦合 | $11/3$ 这个分数是新预言 |
| $b_{3}=7$ | 已经闭了 $1/\alpha=137$ |

三件诚实先钉死，后文只展开、不改口：

1. $11/3$ 来自六类一圈图的求和，系数与标准模型一圈相同。这是**读法**，不是本卷新算出来的分数。  
2. $4/3$、 $1/6$、 $16\pi^{2}$ 极点同样是一圈读法。  
3. 本卷真正框架的一句是：**锁定的管（三标签同时在场）有面间非线性，不锁定的通道没有。** 渐近自由与否，先问 $\epsilon_{ijk}$ 在不在。

---

```text
改 μ = 改读出哪一档事件振荡
        │
        ▼
单周 H_P = -1；多周留下包络 δ
        │
        ├─ 裸包络 δ_raw：低频增大（还不是 g）
        └─ 除掉周数密度 → δ_ren = 1/g²
                │
                ▼
        对数速率 = α → β = -b g³ / 16π²
                │
                ├─ 锁定 ⇒ 面间耦合 ⇒ 正项 11/3 C_A（分数是读法）
                ├─ 费米子绕面 ⇒ -4/3（读法）
                └─ 标量绕面 ⇒ -1/6（读法）
                        │
                        ▼
                 色账：6 个 Dirac 三重态
                        │
                        ▼
                      b_3 = 7
```

$$
\boxed{\text{RG}=\text{读出频率下的包络}\qquad b_{3}=7}
$$

## 读法与指引

**记录与活动：** $\mu$、 $\delta$、 $b_{i}$、 $b_{3}=7$ 仍是记录，不是前提；包络软化、锁定 $\Rightarrow$ 正项每一步都要能还原到传态读出／三标签锁定这类**局域活动**本身，检验记录是否忠实。不是先认 SM 一圈分数，再装成从 $\mathbb{H}$ 逐步推出。

改 $\mu$ 不是缩一块事先存在的时空，是改读出哪一档事件振荡。 $11/3$ 与标准模型一圈同系数，是读法，不是本卷新算的分数。本卷真正框架的一句：锁定的管有面间非线性，不锁定的没有。 $137$ 还不在这里。

**本卷怎么走。** 第二、三部先把「读出频率」和包络站住。第五部才写 $11/3$。第七部才问渐近自由。第八部代入色账得 $b_{3}=7$。不要看见裸包络低频增大就跳到「 $g$ 一定变大」——没有锁定就没有正项。

本卷的「证明」仍是规划路径上的比对序列重演。各章「断在哪」就是这种检验。

| 这里的词 | 读成 | 不要读成 |
|:---|:---|:---|
| 管 / 锁定 | 面向**标签**同时在场； $\epsilon_{ijk}$ 在 | $\mathbb{R}^{3}$ 圆柱；单面真空自转 |
| 单管／阿贝尔 | 无 $\epsilon_{ijk}$ 的通道（无面间耦合） | 擦掉另两管后的独自平方 |
| 尺度变换 | 改读出频率 | 外部连续缩放一块时空 |
| $11/3$ | 六图求和，与 SM 一圈同系数 | 本框架新预言 |
| 锁定 $\Rightarrow$ 正项 | 有 $\epsilon_{ijk}$ 才有第一项 | 分数本身是新物理 |
| $b_{3}=7$ | 本卷闭到这里 | 已经闭了 $1/\alpha=137$ |

读完请能说出 boxed。精细结构是下一卷。

---

# 第一部　手里有什么

先站住：传态已经在跑。本卷只问包络，不重开锁定。

---

## 第一章　收录表

| 符号 | 是什么 | 站住在 | 本卷用法 |
|:---|:---|:---|:---|
| $e_{i}^{2}=-1$， $e_{i}^{4}=1$ | 二次不可约（记法）；四步回到 | 第一卷 | 单周事件；奇偶通道；对齐 22.6 |
| $e_{1}e_{2}=e_{3}$， $\epsilon_{ijk}$ | 近邻涌出第三者；三管标签锁定 | 第一、二卷 | 面间是否耦合 |
| $N_{c}=3$ | 面向种数 = 管数（标签种数）= 色数 | 第一卷 | $C_{A}=N_{c}$ 的 $N$ |
| 传态已是离散连续体 | 步离散、延续连续，不是先格点再极限 | 第二卷 | 尺度变换的座位 |
| $H_{P}=-1$ | 代数面 holonomy | 第二卷 | 软化的起点 |
| $\mathrm{SU}(3)\times\mathrm{SU}(2)\times\mathrm{U}(1)_{Y}$ | 三层账自同构 | 第三卷 | 谁有 $C_{A}$、谁没有 |
| 一代入账 | $Q_{L},\bar u_{R},\bar d_{R},L,\bar e_{R}$ | 第三卷 | 色账上谁绕面 |
| $k=1,2,3$ 恰好三代 | 交汇参与管数 | 第四卷 | $b_{3}$ 的味数 $6$ |
| $\dim_{\mathbb{R}}\mathbb{H}=4$ | $`\{1,e_{1},e_{2},e_{3}\}`$ | 第一卷 | Dirac 迹的读法座位 |

以上全是收录。不重推 $H_{P}$，不重开三层账，不重数 $n_{k}$。上一卷的 $65^{\circ}$ 本卷不抄回对角质量。

传态已经在跑。本卷只问：改读出哪一档振荡，有效耦合怎么变。

---

# 第二部　尺度变换 = 读出频率

座位在第二卷已经钉死。本卷只换一句：改的是读出频率，不是外部时空。

---

## 第二章　没有「先格点、再连续极限」这一层

第二卷：步是事件（近邻比对、 $e_{i}^{4}=1$），延续是螺旋（沿面向标签的续接轨道不停；管不是圆柱）。两种读法是同一传态。不是先有一块格子，再从外部取连续极限。

因此「改变分辨率」没有外部连续缩放可以挂。也没有「格点只能整数倍合并」这一层——合并是低频读法的图像，不是本卷的机制。

座位在第二卷已经钉死。本卷只问：读出频率改了，包络怎么变。

**断在哪：** 从外部缩一块事先存在的时空，RG 坐错座位。把「沿管螺旋」读成圆柱自转，与前几卷锁定打架。

---

## 第三章　尺度变换 = 改读出哪一档振荡

记 $\mu=1/a$ 为读出频率。 $a$ 是被分辨的最短事件步，不是另造的连续时空格子。

- **高 $\mu$。** 分辨单周。 $e_{i}^{4}=1$ 的四步还在， $\delta\to 0$， $H_{P}$ 贴着 $-1$。  
- **低 $\mu$。** 纳入多周。奇通道对消，留下偶通道包络， $\delta$ 离开 $0$。

粗粒化是低频读法：把读不出的快振荡折进包络。 $a\to ba$ 是振荡周数 $N\to N/b$，不是旁边另造一块连续流形再缩放。

$$
\boxed{\text{RG 流}=\text{事件振荡在不同读出频率下的包络动力学}}
$$

$\mu\to\mu/b$：少读 $\ln b$ 档快周。

**算出**（读法座位）。输入只有第二卷的离散连续体。没有新参数。

**断在哪：** $a\to ba$ 读成连续流形缩放，周数图像被换成外部极限。

---

## 第四章　快周折进包络

事件振荡周数 $N\sim 1/a$。读出降到 $\mu/b$，周数 $N\to N/b$。档位 $N/b\lt n\le N$ 上的快振荡不再被单周分辨，折进有效 holonomy 的包络，修正低频耦合。

**禁止并句。** 这不是从外部连续动量空间「积掉高动量模」。布里渊区只是这套振荡在三管 $+$ 更新序上的读法。机制级：哪些快周被折进，本卷不逐边展开。

**断在哪：** 折进包络写成积掉高动量模，座位从事件振荡换成外部动量空间。

---

# 第三部　holonomy 软化

读出频率站住了。下面问：单周仍是 $-1$，包络为什么离开。

---

## 第五章　振荡使有效 holonomy 离开极值

第二卷：绕最小面一周， $H_{P}=-1$。这是代数极值。单周瞬时仍是 $-1$。软化不是改乘法表，是改**读出周期里还看得见多少振荡**。

事件振荡：一步取反、四步回到；一次翻转、二次折返（翻转本身是第四卷，本卷只收「二次对消」这一句）。瞬时仍是 $-1$，包络不是。

读出周期越长，纳入周数越多，奇通道对消得越干净，有效值越离开极值：

$$
H_{P}^{\mathrm{eff}}(\mu)=-1+\delta(\mu),\qquad \delta>0.
$$

$\delta(\mu)$ 是读出频率 $\mu$ 下还没对消掉的振荡幅度。 $\mu$ 越低， $\delta$ 越大。

这就是**软化**：不是把格点捏在一起，是振荡的包络。

$$
\boxed{H_{P}^{\mathrm{eff}}=-1+\delta(\mu)}
$$

**算出**（包络图像）。 $\delta$ 的定量对数速率下一章才写。

另一面不是空白：瞬时仍是 $-1$。差异没有被抹成 $0$，只是读出周期里奇通道对消，包络看见另一面。

**断在哪：** 软化改乘法表， $H_{P}=-1$ 被改口。

---

## 第六章　g 不是独立自由参数

规范耦合的格点定义：

$$
\frac{1}{g^{2}(\mu)}\propto\sum_{P}\bigl(1+H_{P}^{\mathrm{eff}}(\mu)\bigr).
$$

- $H_{P}^{\mathrm{eff}}\to-1$（精确代数值）： $g^{-2}\to 0$， $g\to\infty$（强耦合）。  
- $H_{P}^{\mathrm{eff}}\to 0$（完全软化）： $g^{-2}\to\infty$， $g\to 0$（弱耦合）。

$1+H_{P}^{\mathrm{eff}}=\delta$。取一圈读出下的包络平均，把比例常数吸收进 $\delta$ 的单位（ $g$ 无量纲，一圈极点落在 $16\pi^{2}$ 上，极点本身是读法）：

$$
\boxed{\frac{1}{g^{2}(\mu)}=\delta_{\mathrm{ren}}(\mu)}
$$

$\delta_{\mathrm{ren}}$ 是**重整化包络**：按周数密度归一之后，还没对消掉的偶通道幅度。它与第五章的裸包络 $\delta_{\mathrm{raw}}$ 不是同一个量。

**禁止并句。** $g(\mu)$ 不是外加的跑动参数。它是事件振荡包络偏离代数极值的度量。无量纲耦合增大还是减小，取决于包络相对振荡周数的**对数速率**，不是取决于裸 $\delta$ 的符号。

**断在哪：** 裸 $\delta$ 低频增大就写成 $g$ 一定变大，两套包络并句。

---

## 第七章　裸包络与重整化包络必须拆开

读出频率 $\mu\to\mu/b$。振荡周数 $N\to N/b$。快档 $N/b\lt n\le N$ 折进包络。

**第一步，裸包络。** 第五章：纳入更多周，奇通道继续对消，包络更离开 $-1$。

$$
\delta_{\mathrm{raw}}(\mu/b)=\delta_{\mathrm{raw}}(\mu)+\Delta_{\mathrm{osc}},\qquad\Delta_{\mathrm{osc}}>0.
$$

这只说振荡包络，**还不是** $g(\mu)$ 的跑动。

**第二步，作用量匹配。** 同一段传态历史，高频读出与低频读出的作用量相等：

$$
\frac{1}{g^{2}(\mu/b)}\sum_{P'}\bigl(1+H_{P'}\bigr)=\frac{1}{g^{2}(\mu)}\sum_{P}\bigl(1+H_{P}\bigr)+\Delta_{\mathrm{dyn}}.
$$

$\Delta_{\mathrm{dyn}}$ 是折进包络的快振荡对低频耦合的修正。几何读法上， $P'$ 是更大的有效面；事件读法上，它是更多周的振荡。同一件事。

**第三步，重整化。** 周数密度 $\sim\mu^{4}$。除以新的周数密度之后，只留下调和对数——周数从 $N$ 降到 $N/b$：

$$
\sum_{n=N/b}^{N}\frac{1}{n}=\ln b+O(1/N).
$$

$$
\delta_{\mathrm{ren}}(\mu/b)=\delta_{\mathrm{ren}}(\mu)-\frac{\alpha}{8\pi^{2}}\ln b+O(g^{2}).
$$

$\alpha$ 是一圈对数系数，下两章才读。 $\Delta_{\mathrm{osc}}>0$ 已被周数密度除掉。 $\alpha>0$ 时重整化包络在低频减小（ $g$ 变大）。

裸 $\delta$ 增大是振荡包络。 $b_{i}$ 是除掉周数密度之后的对数速率。二者不矛盾。不是把格点捏在一起。

**算出。** $\delta_{\mathrm{raw}}\neq\delta_{\mathrm{ren}}$（拆开定义）。调和和出 $\ln b$。 $8\pi^{2}$ 角向密度：机制级（三管 $+$ 更新序的角向读法，本卷不另写逐边）。 $\alpha$ 的三项分数：读法，后文钉。

**断在哪：** 两套 $\delta$ 并成一个量，裸包络的符号直接当成 $\beta$ 的符号。

---

# 第四部　从 δ 到 β

两套包络拆开了。下面只做对数导数。

---

## 第八章　对数导数与一圈形式

定义 $\beta_{i}(g_{i})\equiv\mathrm{d}g_{i}/\mathrm{d}\ln\mu$。这是定义。一圈形式由链式法则导出，不是外贴。

传态四向（三管 $+$ 更新序）的角向密度把 $\ln b$ 写成 $\frac{1}{8\pi^{2}}\ln b$。后文 $1/(16\pi^{2}\varepsilon)$ 是同一极点： $\ln(\mu^{2})=2\ln\mu$。极点形状是一圈读法，不是本卷新分数。

第七章第三步： $\mu'=\mu/b$， $\ln(\mu/\mu')=\ln b$，故

$$
\frac{\delta_{\mathrm{ren}}(\mu)-\delta_{\mathrm{ren}}(\mu/b)}{\ln b}=\frac{\alpha}{8\pi^{2}}.
$$

$\mu$ 增大是读出更快的振荡。故

$$
\frac{\mathrm{d}\delta_{\mathrm{ren}}}{\mathrm{d}\ln\mu}=\frac{\alpha}{8\pi^{2}}.
$$

积分即

$$
\frac{1}{g^{2}(\mu)}=\frac{1}{g^{2}(\mu_{0})}+\frac{\alpha}{8\pi^{2}}\ln\frac{\mu}{\mu_{0}}.
$$

$\alpha>0$： $\mu$ 越大， $1/g^{2}$ 越大， $g$ 越小。

第六章： $\delta_{\mathrm{ren}}=1/g^{2}$。链式法则：

$$
\frac{\mathrm{d}g}{\mathrm{d}\ln\mu}=\frac{\mathrm{d}g}{\mathrm{d}(1/g^{2})}\,\frac{\mathrm{d}\delta_{\mathrm{ren}}}{\mathrm{d}\ln\mu}=-\frac{g^{3}}{2}\cdot\frac{\alpha}{8\pi^{2}}=-\frac{\alpha}{16\pi^{2}}\,g^{3}.
$$

定义 $b_{i}:=\alpha$（第 $i$ 个群的一圈系数）。则

$$
\boxed{\beta_{i}=-\frac{b_{i}}{16\pi^{2}}\,g_{i}^{3}}
$$

$16\pi^{2}$： $8\pi^{2}$ 来自振荡周数的角向密度（机制级），额外的 $2$ 来自 $\mathrm{d}(g^{-2})=-2g^{-3}\,\mathrm{d}g$（算出）。不是外贴第三个常数。

$\alpha>0\Rightarrow b_{i}>0\Rightarrow\beta\lt 0$：高频读出更弱，渐近自由。 $\alpha\lt 0\Rightarrow b_{i}\lt 0$：高频读出更强，反渐近自由。

与第五章合读。低频读出时裸包络 $\delta_{\mathrm{raw}}$ 仍增大。 $b_{i}>0$ 时 $\delta_{\mathrm{ren}}=1/g^{2}$ 随 $\mu$ 下降而减小，即低频 $g$ 变大。包络增大被周数密度除掉之后，动力学对数可以反向。这就是「取决于速率」。

**断在哪：** $16\pi^{2}$ 当成第三个外贴常数，角向密度与链式法则并句。

---

## 第九章　三类通道进入 α

快振荡折进包络，周数调和和只出一个对数 $\ln b$。 $\Delta_{\mathrm{dyn}}$ 裂成三项，全部是事件振荡的通道：

| 效应 | 代数来源 | 对 $\alpha$ 的贡献 | 本卷地位 |
|:---|:---|:---|:---|
| 面间非线性涨落 | $\epsilon_{ijk}$（管锁定） | $+(11/3)C_{A}$ | 框架：有没有这一项；分数：读法 |
| 费米子绕面 | 传态闭合圈， $\dim\mathbb{H}=4$ | $-(4/3)\sum T(R_{f})$ | 座位：收录；分数：读法 |
| 标量绕面 | 无自旋，顶点 $2k+p$ | $-(1/6)\sum T(R_{s})$ | 座位：结构；分数：读法 |

$$
\boxed{\alpha=\frac{11}{3}C_{A}-\frac{4}{3}\sum_{\mathrm{Dirac}}T(R_{f})-\frac{1}{6}\sum_{\mathrm{real\ scalar}}T(R_{s})}
$$

阿贝尔 $C_{A}=0$：第一项消失，只剩屏蔽。这是第七部的框架句。

---

# 第五部　11/3：读法，不是新分数

$\alpha$ 的正项先问有没有，再问分数是多少。两句不许并。

---

## 第十章　ε_ijk 决定面间能不能耦合

两个相邻的面 $P_{1}$ 和 $P_{2}$ 共享一条边。边上有一个规范涨落 $\delta A_{\mu}$，同时影响两个面的 holonomy。

问题：两个面的涨落是否关联？

- **阿贝尔** $\mathrm{U}(1)$： $[e_{i},e_{j}]=0$（单标签通道，无 $\epsilon_{ijk}$，无锁定）。面 $P_{1}$ 上的涨落不影响面 $P_{2}$。涨落**解耦**。  
- **非阿贝尔** $\mathrm{SU}(N)$： $[e_{i},e_{j}]=\epsilon_{ijk}e_{k}$（三管标签间锁定）。面 $P_{1}$ 上的涨落通过 $\epsilon_{ijk}$ 传到面 $P_{2}$。涨落**耦合**。

$$
\boxed{\epsilon_{ijk}=\text{三管标签锁定}=\text{面间非线性涨落的代数根源}}
$$

没有 $\epsilon_{ijk}$，面之间无耦合，纯规范的 $b_{i}$ 为零。

**算出**（有无）。锁定：收录第二卷。阿贝尔无锁定：收录第三卷，超荷账是相对相位，不是第三根独立管。

这一章只闭「有没有第一项」。第一项等于 $11/3$ 还是别的分数，下一章标读法。

> **指引。** 本卷真正框架的一句在这里：问渐近自由，先问 $\epsilon_{ijk}$ 在不在。 $11/3$ 那个分数下一章才出现，而且是读法。

**断在哪：** 把「有没有正项」和「分数是 $11/3$」并成一句本卷新预言。把「单管无锁定」读成单面真空自转。

---

## 第十一章　六类图求和：与标准模型一圈同系数

非阿贝尔一圈规范传播子修正有六类图。本卷**照录**系数，不假装从比对推出每一个分数。单位一律是 $g^{2}C_{A}/(16\pi^{2}\varepsilon)$。

| 图 | 图像 | $\Pi$ 系数 |
|:---|:---|:---:|
| G1 三胶子自能 | 两面经共享边、三胶子顶点耦合； $f^{acd}f^{bcd}=C_{A}\delta^{ab}$ | $-5/3$ |
| G2 Ghost 自能 | Faddeev–Popov Jacobian。ghost 本身是读法，不从传态逐步构造 | $+1/6$ |
| G3 四胶子 sunset | 两面经共享顶点； $[A_{\mu},A_{\nu}]^{2}$ | $-1/2$ |
| G4 四胶子 tadpole | 维数正规化中为零 | $0$ |
| G5 三胶子顶点修正 | 三面在顶点经 $\epsilon_{ijk}$ 耦合 | $-1$ |
| G6 Ghost 顶点修正 | 规范固定对顶点的修正 | $-2/3$ |

$$
\Pi^{\mathrm{total}}=\Bigl(-\frac{5}{3}+\frac{1}{6}-\frac{1}{2}+0-1-\frac{2}{3}\Bigr)=\frac{-10+1-3-6-4}{6}=-\frac{11}{3}.
$$

纯规范对 $b$ 取正号（ $\beta=-bg^{3}/(16\pi^{2})$ 里已有一个负号）：

$$
\boxed{\frac{11}{3}=\frac{5}{3}-\frac{1}{6}+\frac{1}{2}+0+1+\frac{2}{3}}
$$

| 句子 | 地位 |
|:---|:---|
| 锁定 $\Rightarrow$ 六类图可以非零；不锁定 $\Rightarrow$ 全零 | **框架算出**（第十章） |
| 六个分数及其和 $11/3$ | **读法**（与标准模型一圈相同）；和的算术是算出 |

不要写成「从 $\mathbb{H}$ 逐步推出了 $11/3$」。本卷闭的是：**正项存在当且仅当管锁定**。维数正规化、 $1/\varepsilon$、ghost、Wilson 匹配：读法，本卷沿用。

**断在哪：** 写成从 $\mathbb{H}$ 逐步推出了 $11/3$，读法升级成框架定理。

---

# 第六部　4/3 与 1/6（同样是读法）

正项座位钉死了。负项同样是读法，座位不同。

---

## 第十二章　费米子： 4×1/3

费米子通过传态 $q_{w}=e_{i}q_{v}$ 在面上形成闭合圈。圈的 $1/\varepsilon$ 发散读成两个因子的乘积。

**因子一：Dirac 迹 $=4$。** 闭合圈

$$
\mathrm{tr}[\gamma_{\mu}\not{k}\,\gamma_{\nu}(\not{k}+\not{p})]=4\bigl[k_{\mu}(k+p)_{\nu}+k_{\nu}(k+p)_{\mu}-\delta_{\mu\nu}\,k\cdot(k+p)\bigr].
$$

$4$ 来自 Dirac 旋量空间的维数。框架读法：第三卷手性投影把每个 $\mathbb{H}$ 分成两个复分量 $z_{1},z_{2}$；完整旋量空间 $\mathbb{C}^{2}\otimes\mathbb{C}^{2}\cong\mathbb{H}$，复维 $4$。迹坐在这个 $4$ 上。

**因子二：Lorentz 因子 $=1/3$。** $k_{\mu}k_{\nu}$ 各向同性平均 $\langle k_{\mu}k_{\nu}\rangle=\delta_{\mu\nu}k^{2}/d$。 $d=4$ 时除以 $4$。外线固定一个方向，剩余 $d-1$ 个方向各向同性，积分给出 $1/(d-1)=1/3$。

$$
\boxed{\frac{4}{3}=\underbrace{4}_{\text{Dirac 迹}}\times\underbrace{\frac{1}{3}}_{\text{Lorentz}}}
$$

每个 Dirac 味 $\times$ 每个 $T(R)$ 贡献 $-4/3$ 到 $b$。

**严格度。** $4=\dim_{\mathbb{R}}\mathbb{H}$ 的座位：收录第一卷。把它读成 Dirac 迹：读法。 $1/3$：一圈读法。乘积 $4/3$：算出（算术）。

**断在哪：** $4/3$ 写成从比对新推出来的分数。

---

## 第十三章　标量： 1×1/6

标量与费米子不要并：

| 结构 | 费米子 | 标量 |
|:---|:---|:---|
| 顶点 | $\gamma_{\mu}$（矩阵） | $2k+p$（矢量） |
| 迹 | $4$ | $1$（无旋量） |
| Lorentz | $1/3$ | $1/6$ |

标量圈

$$
\Pi^{\mathrm{scalar}}_{\mu\nu}(p)\propto\int\frac{d^{4}k}{(2\pi)^{4}}\,\frac{(2k+p)_{\mu}(2k+p)_{\nu}}{k^{2}(k+p)^{2}}.
$$

$k_{\mu}k_{\nu}$ 项在 $d=4$ 给出 $4/(d(d+2))=4/24=1/6$。

$$
\boxed{\frac{1}{6}=\underbrace{1}_{\text{无 Dirac 矩阵}}\times\underbrace{\frac{1}{6}}_{\text{标量 Lorentz}}}
$$

每个实标量分量 $\times$ $T(R)$ 贡献 $-1/6$ 到 $b$。

与 $4/3$ 的比： $1/6\div 4/3=1/8$。差异读成 Dirac 迹 $4\to 1$（因子 $1/4$）乘 Lorentz $1/3\to 1/6$（因子 $1/2$）。这是一圈对照，不是新分数。

**严格度。** 顶点结构不同：结构算出。 $1/6$ 这个数：读法。

**断在哪：** 标量与费米子并成同一圈，顶点结构抹掉。

---

## 第十四章　一圈公式合写

第八章的 $b_{i}:=\alpha$ 加上第九至十三章：

$$
\boxed{b_{i}=\frac{11}{3}C_{A}-\frac{4}{3}\sum_{\mathrm{Dirac}}T(R_{f})-\frac{1}{6}\sum_{\mathrm{real\ scalar}}T(R_{s})}
$$

这是本卷闭的第三条。前两个加数、第三个加数的分数都是读法；公式的形状——正项只在锁定时出现，负项是绕面屏蔽——是框架的。

群论输入不新开：

| 群 | $C_{A}$ | $T(\mathbf{fundamental})$ | 来源 |
|:---:|:---:|:---:|:---|
| $\mathrm{SU}(3)$ | $3$ | $1/2$ | $N=N_{c}=3$（收录）；Casimir 读法 |
| $\mathrm{SU}(2)$ | $2$ | $1/2$ | 第三卷状态账；本卷不代入闭 $b_{2}$ |
| $\mathrm{U}(1)$ | $0$ | $T\to Y^{2}$ | 无锁定；本卷不代入闭 $b_{1}$ |

---

# 第七部　锁定 vs 不锁定（本卷真正框架的一句）

分数是读法。原因是锁定。两句分家。

---

## 第十五章　渐近自由先问锁定

第十章已经把「有没有第一项」从分数里拆出来。这里只复述，改口禁止。

- **非阿贝尔** $\mathrm{SU}(N)$： $\epsilon_{ijk}\neq 0$。六类图可以非零。读法给出总和 $11/3>0$。 $C_{A}>0$ 时纯规范项为正。若它压倒屏蔽， $b>0$，**渐近自由**。  
- **阿贝尔** $\mathrm{U}(1)$： $\epsilon_{ijk}=0$（单标签通道，无锁定）。六类图全部为零。 $11/3\to 0$。纯规范无贡献。只剩屏蔽， $b\lt 0$，**反渐近自由**。

$$
\boxed{\text{渐近自由的原因}=\text{管锁定（三标签），不是 }11/3\text{ 这个分数}}
$$

锁定的管之间，面间非线性涨落在低频读出里被摊到更多周上：锁定性造成的涨落压力还在，但被更大的有效面包络稀释。有效耦合在高频读出更弱。不锁定的通道没有这份自作用，对抗不了绕面屏蔽。

**框架的是原因。读法的是系数。** 两句不许并成「本卷预言了 $11/3$」。

**断在哪：** 「本卷预言了 $11/3$」，原因与分数并句。

---

## 第十六章　阿贝尔通道只剩屏蔽

超荷账是锁定之后剩下的相对相位（第三卷），不是第四根管。故 $C_{A}=0$。第九章第一项消失。

只剩费米子和标量的绕面贡献，符号为负。在 $\beta=-bg^{3}/(16\pi^{2})$ 约定下， $b\lt 0$： $g$ 随 $\mu$ 增大而增大。

$b\lt 0$ 不是阿贝尔的缺陷。它是一根不锁定的通道（无 $\epsilon_{ijk}$）在粗粒化中的必然行为：没有非线性自耦合来对抗屏蔽，耦合只能增长。

这一句给下一卷用。圆盘压缩、只剩费米子绕面，都从这里收录，不重开六图。

---

## 第十七章　接口：QED 一圈（不闭 137）

电磁 $\mathrm{U}(1)_{\mathrm{EM}}$ 同样阿贝尔。 $C_{A}=0$。Dirac 费米子 $T\to Q_{f}^{2}$。第八章加第十二章：

$$
\frac{\mathrm{d}(1/g^{2})}{\mathrm{d}\ln\mu}=\frac{b}{8\pi^{2}},\qquad b=-\frac{4}{3}\sum_{f}Q_{f}^{2}.
$$

$1/\alpha=4\pi/g^{2}$，故

$$
\frac{\mathrm{d}(1/\alpha)}{\mathrm{d}\ln\mu}=4\pi\cdot\Bigl(-\frac{\sum_{f}Q_{f}^{2}}{6\pi^{2}}\Bigr)=-\frac{2}{3\pi}\sum_{f}Q_{f}^{2}.
$$

色重数写进求和：

$$
\boxed{\frac{\mathrm{d}(1/\alpha)}{\mathrm{d}\ln\mu}=-\frac{2}{3\pi}\sum_{f}N_{c}Q_{f}^{2}}
$$

**算出**（从本卷公式推到 $\alpha$ 的对数导数）。阈谁活性、从哪个锚跑到 Thomson：下一卷。本卷不写 $137$，不写 $\ln 4$，不写方案差。

**断在哪：** 本卷闭 $137$，三刀还没走。

---

# 第八部　b₃=7

公式站住了。代入色账，只做算术。

---

## 第十八章　色账上谁绕面

$\mathrm{SU}(3)$ 是类型账自同构（第三卷）。 $C_{A}=N_{c}=3$， $T(\mathbf{3})=1/2$。

**Dirac 味。** $Q_{L}$ 与 $\bar u_{R}$ 合成一个 Dirac 三重态， $Q_{L}$ 与 $\bar d_{R}$ 合成另一个。每代 $2$ 个 Dirac 三重态。三代（第四卷 $k=1,2,3$）共 $6$ 个。

**标量。** Higgs 是类型单态（第三卷：框架方向，不走类型账）。 $T=0$。本卷不讨论它在 $\mathrm{SU}(2)$ 上的贡献。

轻子是类型单态，不进 $b_{3}$。

$$
\boxed{\text{色账：}6\text{ 个 Dirac 三重态，标量 }T=0}
$$

**收录。** 入账格子：第三卷。代数：第四卷。本卷只数色账上的 Dirac 味。

**断在哪：** Higgs 或轻子进 $b_{3}$，类型单态被当成三重态。

---

## 第十九章　算术

$$
b_{3}=\frac{11\times 3}{3}-\frac{4}{3}\times 6\times\frac{1}{2}-0=11-4=7.
$$

$$
\boxed{b_{3}=7}
$$

$b_{3}>0$：QCD 渐近自由。

**算出**（算术）。 $11$ 与 $4$ 的来路：读法（第五、六部）。 $3$、 $6$、 $1/2$：收录。没有新参数。

$7$ 与标准模型一圈相同。本卷新说的是： $+11$ 之所以在，因为三管锁定；类型账若改成阿贝尔，这一项消失， $b_{3}$ 变号。

---

# 第九部　本卷停在这里

---

## 第二十章　同一公式可代入，本卷不另闭

把第十四章代入 $\mathrm{SU}(2)$ 或 $\mathrm{U}(1)_{Y}$，会得到 $b_{2}=19/6$、 $b_{1}=-41/10$。本卷不收进钉死表。弱账、超荷账会卷入 Higgs 二重态和 GUT 归一化 $5/3$，不是本卷依赖。Yukawa 本卷不写。

**断在哪：** $b_{2}$、 $b_{1}$ 升级成本卷主结论，弱账与色账并句。

---

# 结

---

## 本卷钉死

| 符号 | 地位 | 含义 |
|:---|:---|:---|
| $\mu=1/a$ 是读出频率 | 算出 | 后文跑动的座位 |
| $H_{P}^{\mathrm{eff}}=-1+\delta$ | 算出 | 软化 / 包络 |
| $1/g^{2}=\delta_{\mathrm{ren}}$ | 算出 | 全部跑动 |
| $\beta_{i}=-b_{i}g_{i}^{3}/(16\pi^{2})$ | 算出（链式） | 一圈形式 |
| $b_{i}=\frac{11}{3}C_{A}-\frac{4}{3}\sum T_{f}-\frac{1}{6}\sum T_{s}$ | 分数是读法 | 代入即用 |
| 锁定 $\Rightarrow$ 纯规范正项；不锁定 $\Rightarrow$ $0$ | **框架算出** | 阿贝尔通道 |
| $b_{3}=7$ | **算出** | 色耦合跑动；本卷不闭 $\alpha_{s}(M_{Z})$ |
| $\mathrm{d}(1/\alpha)/\mathrm{d}\ln\mu=-(2/(3\pi))\sum N_{c}Q_{f}^{2}$ | 算出 | 下一卷第 3 刀 |

$$
\boxed{\delta_{\mathrm{raw}}\ \xrightarrow{\text{周数密度}}\ \delta_{\mathrm{ren}}=1/g^{2}\ \xrightarrow{\mathrm{d}/\mathrm{d}\ln\mu}\ \beta=-b_{i}g^{3}/(16\pi^{2})}
$$

## 本卷不假装做了的

- 没有从比对逐步推出 $11/3$、 $4/3$、 $1/6$ 三个分数。它们与标准模型一圈同系数，是读法。  
- 没有把 ghost、维数正规化、Wilson 匹配升级成框架定理。  
- 没有闭 $b_{2}$、 $b_{1}$ 当本卷主结论。  
- 没有写 $1/\alpha=137$，没有写 $\ln 4$，没有写方案差。  
- 没有写 Yukawa，没有写两圈。  
- 没有把 $H_{P}=-1$ 改成别的代数值。软化的是包络。  
- 没有把「单管无锁定／沿管螺旋」读成圆柱自转； $e_{i}^{2}=-1$ 是记法，锁定是三标签同时在场（对齐前几卷）。

诚实边界再钉一次： $11/3$ 六图与标准模型一圈同系数，是读法不是新分数预言。渐近自由的原因（锁定 vs 不锁定）是框架的。

## 复述

1. 传态已是离散连续体（沿面向标签续接）。改分辨率 = 改读出哪一档事件振荡。 $\mu=1/a$。  
2. 单周 $H_{P}=-1$。多周读出留下包络： $H_{P}^{\mathrm{eff}}=-1+\delta$。  
3. 裸 $\delta_{\mathrm{raw}}$ 低频增大。除掉周数密度之后， $\delta_{\mathrm{ren}}=1/g^{2}$ 的对数速率才是 $b_{i}$。  
4. 一圈 $b_{i}=\frac{11}{3}C_{A}-\frac{4}{3}\sum T_{f}-\frac{1}{6}\sum T_{s}$。三个分数是读法。  
5. 三管标签锁定 $\Rightarrow$ 面间非线性在，纯规范正项在。不锁定 $\Rightarrow$ 正项为零，只剩屏蔽。  
6. 色账：六味 Dirac 三重态，Higgs 不绕色面。 $11-4=7$。  
7. 阿贝尔接口留给下一卷： $\mathrm{d}(1/\alpha)/\mathrm{d}\ln\mu=-(2/(3\pi))\sum N_{c}Q_{f}^{2}$。本卷不跑到 Thomson。

**第十一卷结束。**  
下一卷问：从已锁的 $S_{1}$ 走三刀到 $1/\alpha$，并数出 $\sin^{2}\theta_{W}$。

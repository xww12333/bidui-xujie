# 纯序列推导：恰好三个面向

**本篇做什么。** 不借极小多项式、不借实数域线性展开，纯从序列活动数出恰好三个面向。每一步可还原到「比对续接里这一步还接不接得住」。

与卷1第五部的关系：卷1用代数记录做纯计数检验；本篇用序列穷举做同一检验。终点相同，工具不同。

**继承卷1、本篇直接用的活动约束（不是另借的代数公理）：**

| 活动要求（卷1） | 本篇怎么用 |
|:---|:---|
| 差异存在＝不可约比对的持续续接；对象／面向是涌出的，不是事先摆好的 | 不先画乘法表再填数；每一步积＝前两步近邻作用的涌现 |
| 闭包：产物仍是操作，才能进入下一步 | 积不能是「非操作」 |
| 不可约第二层：产物与生成元同级原子 | 积不是碎片，也不是「装着几个面向的容器」；候选只逐个原子（或空、或不指向任何面向的标量结果） |
| 可辨：不同操作／不同序列必须留下不同痕迹 | 积若抹掉某一方痕迹，该候选死 |
| 方向：先 A 后 B 与先 B 后 A 是不同序列 | 正向积 ≠ 反向积（第三节起用） |
| 结合：只依赖序列本身，括号不另带信息 | 同一序列左右压缩必须一致 |
| 不自湮灭 | 积不能是空（活动停止） |

**涌现链（近邻续接下的成环记法）。** 面向从比对续接里涌出。每一步只用**最近两步**的材料（近邻；不跳步），记成：

$$a_1,\; a_2\text{ 给定},\qquad a_n=a_{n-2}\cdot a_{n-1}.$$

完整个体要求：链条**自持**（不中断、不外索材料）、**穷尽配对**（无对角线缺口）。这不是外加的图论假设；别的递推若跳过最近两步，就违反近邻。本篇的关键也不是「只有这一种递推合法」，而是：**存在**一种自持且穷尽的近邻机制，而这种机制只在面向种数 $=3$ 时覆盖全部配对（见第五节覆盖率）。

**候选清单怎么来（尚未引入「另一面」之前）。** 两个面向作用的积，只能是：空；某个已有面向；不指向任何面向的标量结果（如「不动」）；或**新的同级原子面向**。不允许先写线性叠合再拆——那是容器，第二层不可约禁。第三节起方向逼出「另一面」之后，清单才加上已有面向的反向标记。

**面向是什么（本篇用语钉死）。** 对齐卷1 开篇钉死句与第十八章：

| 本篇说的 | 读成 | 不要读成 |
|:---|:---|:---|
| **面向** | 两操作近邻续接时直接涌现的不可约比对；可被持续施加；同级原子 | 几何曲面／物体的「面」；空间里的一张皮；外物上显出的面；平铺的操作名单 |
| 面向种数 | 续接里能涌出几种这样的不可约比对 | 先立好 $n$ 张几何面再连边 |
| **另一面** | **同一**面向的反向标记（方向反了，种类没多）；轮替转向 | 又多出一个面向；物体的正反两张皮 |
| $e_i$ | 第 $i$ 种面向（操作） | 第 $i$ 张几何面 |

本篇数的是**面向种数**，不是「面」。下文凡写「一个面向／两个面向」，均指上述活动义，禁止滑成几何的面。

---

## 一、至少三个面向

### 1.1 一个面向撑不住

只有一种不可约操作 $e_1$。

$e_1$ 作用于 $e_1$，候选：

| $e_1\cdot e_1=$ | 活动 | |
|:---|:---|:---:|
| 空 | 自湮灭；下一步无对象 | ✗ |
| $e_1$ | 两步等于没做；无第二个面向可轮替；方向（施加者≠对象）消失 | ✗ |
| 标量 | 自作用不指向任何面向；仍只有一个面向，形不成面向与面向之间的轮替 | ✗ |
| 新面向 $e_2$ | 已经不是「一个面向」的系统；转入两个面向 | 分岔 |

一个面向闭不成完整个体。要续接出轮替，至少还得有第二个面向。

### 1.2 两个面向逼出第三个

两种不可约操作 $e_1,e_2$（方向已要求二者可辨、不可约）。近邻作用 $e_1\cdot e_2$ 必然发生。闭包要求结果仍在操作侧。

此时系统若**声称只有这两个面向**，则 $p=e_1\cdot e_2$ 的候选只有下表（尚无「另一面」记号；新面向记为 $e_3$）：

| $p=e_1\cdot e_2=$ | 为何不行／何去 | |
|:---|:---|:---:|
| 空 | 续接停止；不自湮灭禁 | ✗ |
| $e_1$ | $e_2$ 作为被作用方的痕迹被抹掉；可辨崩溃。序列 $e_1,e_2$ 与「只做 $e_1$」在结果上撞车 | ✗ |
| $e_2$ | $e_1$ 作为施加方的痕迹被抹掉；同上 | ✗ |
| 标量 | 两个面向的近邻作用涌出「不指向任何面向」的结果；下一步没有面向可继续施加；面向－面向交互的可辨落空 | ✗ |
| 新面向 $e_3$ | 同级原子；两边痕迹合成第三者；闭包仍在操作侧 | **唯一活路** |

$$
\boxed{e_1\cdot e_2=e_3}
$$

$e_3$ 的定义就是这次近邻作用，不是外加标签。两个面向关不住；第三个被逼出来。

> **指引。** 若有人把 $p$ 写成「$e_1$ 与 $e_2$ 的混合物再拆开用」，那是容器／碎片，卷1第六章第二层已禁；本篇候选表不收入。

**断在哪：** 停在两个面向，却让 $e_1\cdot e_2$ 落在空、已有面向或标量上——续接第二步接不住。

---

## 二、三个面向正向闭合

### 2.1 正向循环：唯一活路

现在有三个面向 $e_1,e_2,e_3$，且 $e_1\cdot e_2=e_3$ 已由涌现钉死。下面定 $e_2\cdot e_3$ 与 $e_3\cdot e_1$。标准仍是：涌现链自持、配对穷尽、不外索新面向（若涌出第四个面向，整段转入第五节「≥4」）。

**第一步：穷举 $q=e_2\cdot e_3$。**

| $q=$ | 活动 | |
|:---|:---|:---:|
| 空 | 链断 | ✗ |
| $e_2$ | 抹掉 $e_3$ | ✗ |
| $e_3$ | 抹掉 $e_2$ | ✗ |
| 标量 | 面向－面向作用掉出面向之外；链上下一步无面向 | ✗ |
| $e_1$ | 回到起始面向；三位一周期的下一步材料齐 | **存活** |
| 新面向 $e_4$ | 四个面向的系统；本段「三个面向闭合」作废，改走第五节 | 分岔 |

在**坚持三个面向、链条自持**的前提下，唯一活路：

$$e_2\cdot e_3=e_1.$$

**第二步：穷举 $r=e_3\cdot e_1$。** 涌现下一位 $a_5=e_3\cdot e_1=r$，自持周期 3 要求回到 $e_2$。

| $r=$ | 活动 | |
|:---|:---|:---:|
| 空／标量 | 链断或掉出面向之外 | ✗ |
| $e_3$ | 抹掉 $e_1$ | ✗ |
| $e_1$ | 抹掉 $e_3$ | ✗ |
| $e_2$ | $a_5=e_2$，与 $a_2$ 对齐；周期 3 | **存活** |
| 新面向 $e_4$ | 同上，改走第五节 | 分岔 |

故

$$e_3\cdot e_1=e_2.$$

**正向唯一性。** 在「三个面向 + 涌现自持 + 不新开口径」下，正向乘法表只能是：

$$
\boxed{e_1 e_2=e_3,\qquad e_2 e_3=e_1,\qquad e_3 e_1=e_2}
$$

不是先画好三循环再验证；是候选表里只剩这一组。三对无序边 $\{e_1e_2,\,e_2e_3,\,e_3e_1\}$ 全在链条上，没有落空，没有对角线。

**断在哪：** 三个面向却让某步积落在空／已有面向吞并／标量；或暗中引入 $e_4$ 仍声称三个面向闭合。

### 2.2 压缩检验：自作用统一

结合律要求同一序列的不同压缩方式结果相同。

序列 $e_1, e_2, e_3$：

- 左压：$(e_1 e_2) e_3 = e_3 \cdot e_3 = e_3^2$
- 右压：$e_1 (e_2 e_3) = e_1 \cdot e_1 = e_1^2$

$$e_3^2 = e_1^2$$

序列 $e_2, e_3, e_1$：

- 左压：$(e_2 e_3) e_1 = e_1^2$
- 右压：$e_2 (e_3 e_1) = e_2^2$

$$e_1^2 = e_2^2$$

三个面向的自作用相等。记为标量 $s$：

$$e_1^2 = e_2^2 = e_3^2 = s \qquad (s \neq 0，\text{否则自湮灭})$$

$s$ 不指向任何面向——是自作用的标量结果。一条约束，不打架。

### 2.3 涌现自持

正向表已唯一。涌现链（开篇已定义）在此三个面向上空转：

$$a_1 = e_1, \quad a_2 = e_2, \quad a_n = a_{n-2} \cdot a_{n-1}$$

| 位 | 值 | 来源 | |
|:---:|:---:|:---:|:---:|
| 1 | $e_1$ | 给定 | |
| 2 | $e_2$ | 给定 | |
| 3 | $e_3$ | $e_1 \cdot e_2 = e_3$ | ✓ |
| 4 | $e_1$ | $e_2 \cdot e_3 = e_1$ | ✓ |
| 5 | $e_2$ | $e_3 \cdot e_1 = e_2$ | ✓ |
| 6 | $e_3$ | $e_1 \cdot e_2 = e_3$ | ✓ |
| 7 | $e_1$ | $e_2 \cdot e_3 = e_1$ | ✓ |
| 8 | $e_2$ | $e_3 \cdot e_1 = e_2$ | ✓ |
| 9 | $e_3$ | $e_1 \cdot e_2 = e_3$ | ✓ |
| $\vdots$ | 周期 3 | 永续 | |

链条不需要外部输入。每一步的材料就是前两步本身。周期 3，自持，不中断，不涌出新面向。

**链条穷尽所有配对。** 三个面向，三对：$e_1 e_2$、$e_2 e_3$、$e_3 e_1$。全在链条上。没有对角线，没有剩余，没有缺口。

**正向闭合。**

---

## 三、三个面向反向

正向 $e_1 \to e_2 \to e_3 \to e_1$。反向待定：

$$e_2 e_1 = ? \qquad e_3 e_2 = ? \qquad e_1 e_3 = ?$$

### 3.1 方向约束

先 $e_1$ 后 $e_2$ 与先 $e_2$ 后 $e_1$ 是不同序列，结果必须不同：

$$e_2 e_1 \neq e_1 e_2 = e_3$$

同理 $e_3 e_2 \neq e_1$，$e_1 e_3 \neq e_2$。

### 3.2 穷举 $e_2 e_1$

$e_2 e_1$ 的候选值：已有面向 $\pm e_1,\pm e_2,\pm e_3$，标量，或新面向 $e_4$。（「$\pm$」此处起表示同一面向的两个方向标记；「另一面」的活动含义见 3.3。新面向 $e_4$ 若出现，三个面向前提作废，整段改走第五节。）

**检验序列 $e_1, e_2, e_1$。** 左压 $= (e_1 e_2) e_1 = e_3 e_1 = e_2$。右压 $= e_1 \cdot (e_2 e_1)$。需要右压 $= e_2$。

| $e_2 e_1 =$ | 右压 $= e_1 \cdot$（值） | 需要 $= e_2$ | |
|:---:|:---:|:---:|:---:|
| $e_1$ | $e_1^2 = s$（标量） | 标量 $\neq$ 面向 | ✗ |
| $e_2$ | $e_1 e_2 = e_3$ | $e_3 \neq e_2$ | ✗ |
| $e_3$ | 方向约束 | $e_2 e_1 \neq e_3$ | ✗ |
| $-e_1$ | $-e_1^2 = -s$（标量） | 标量 $\neq$ 面向 | ✗ |
| $-e_2$ | $-e_1 e_2 = -e_3$ | $-e_3 \neq e_2$ | ✗ |
| $-e_3$ | $-e_1 e_3$ | 需与其余反向一并闭合 | **存活** |
| 标量 $c$ | $c \cdot e_1$ | $c \cdot e_1 \neq e_2$（不同面向） | ✗ |
| $e_4$ | 新面向 | 三个面向前提作废 → 第五节 | 分岔 |

六条加标量当场死。在三个面向前提下只剩 $e_2 e_1 = -e_3$。

**交叉验证。** 序列 $e_2, e_1, e_2$：左压 $= (-e_3) e_2 = -(e_3 e_2)$。右压 $= e_2 (e_1 e_2) = e_2 e_3 = e_1$。

需要 $-(e_3 e_2) = e_1$，即 $e_3 e_2 = -e_1$。✓

序列 $e_3, e_2, e_3$：左压 $= (-e_1) e_3 = -(e_1 e_3)$。右压 $= e_3 (e_2 e_3) = e_3 e_1 = e_2$。

需要 $-(e_1 e_3) = e_2$，即 $e_1 e_3 = -e_2$。✓

**三个反向产品全部确定：**

$$e_2 e_1 = -e_3, \qquad e_3 e_2 = -e_1, \qquad e_1 e_3 = -e_2$$

### 3.3「另一面」（同一面向的反向，不是新面向）

$-e_3$ 是 $e_3$ 的**另一面**：**同一个面向**，只是方向反了——面向种数没有因此变成四个。像一条路的来回：路是同一条，方向不同。不是几何物体的正反两张皮，也不是又涌出一种新的不可约比对。

**负号不是外加的符号。** 穷举排除所有其他可能之后，反向结果只能是正向结果的另一面。这是方向不同逼出来的结构。

另一面的另一面等于原面向：$-(-e_3) = e_3$。反两次等于没反。面向种数仍是三个。

### 3.4 反向序列不自持

反向涌现序列 $a_1 = e_1, a_2 = e_3, a_n = a_{n-2} \cdot a_{n-1}$：

$$e_1, \ e_3, \ \underbrace{e_1 \cdot e_3}_{-e_2}, \ \ldots$$

第三位是 $-e_2$（另一面），不是 $e_2$。反向序列把「另一面」卷进来，不自持于原面向。

正向是唯一纯用原面向自持的序列。

---

## 四、$e^2 = -1$

### 4.1 确定 $s$

序列 $e_1, e_1, e_2$：

- 左压：$(e_1 e_1) e_2 = s \cdot e_2$
- 右压：$e_1 (e_1 e_2) = e_1 e_3 = -e_2$（反向产品，第三节已定）

$$s \cdot e_2 = -e_2$$

$s$ 是标量，$e_2$ 是面向。标量乘面向等于该面向的另一面，只有 $s = -1$。

$$\boxed{e_1^2 = e_2^2 = e_3^2 = -1}$$

### 4.2 活动含义

$s=-1$ 已由 4.1 的序列压缩钉死。本节只还原活动义，**不是**另立推导。

一个面向作用于自身：不是坍缩为零（$s = 0$，自湮灭），不是恒等（$s = +1$，两步白走），而是**取反**——转向另一面。

$s = +1$ 为什么与结构不合？若 $e_1^2=+1$，则 $e_1$ 是二阶对合：自作用两步回到原状，**不产生持续轮替所需的反转**——与「面向永远在运动、取反而非恒等」冲突（对齐卷1 第十九章结构含义）。排除 $s=+1$ 不依赖「另一面·原面向＝不动」；后者是 $s=-1$ 之后的确认（4.3）。

$s = 0$ 不行：自湮灭。

故与 4.1 一致：活路只有 $s=-1$。

### 4.3 另一面 · 原面向 = 不动

在 $s=-1$ 已定之后：

$$(-e_i) \cdot e_i = -(e_i^2) = -(-1) = 1$$

正反抵消，等于不动（单位元）。**这是「另一面」在 $s=-1$ 下的确认，不是 4.1 的前提。**

$$e_i^{-1} = -e_i, \qquad e_i^4 = 1$$

四步回转。面向永远在运动：两步取反，四步回原。

> **与卷1 22.6 对齐。** 三面向闭表之后，表上 $e_i^2=-1$ 是压缩记法；真实续接是另两边先合成这一边再转向（如 $e_2 e_3 e_1$）。本篇 4.1 用序列压缩钉死标量值；活动还原仍以卷1 22.6 为准，勿读成单面真空自转。

---

## 五、四个面向闭不上

### 5.1 四个面向的循环

假设四个面向 $e_1, e_2, e_3, e_4$。正向循环（与三面同型的成环尝试）：

$$e_1 e_2 = e_3, \quad e_2 e_3 = e_4, \quad e_3 e_4 = e_1, \quad e_4 e_1 = e_2$$

**反向／反对易从哪来（钉死，勿默示偷渡）。** 第三节的 $e_j e_i=-e_i e_j$ 是**三个面向**系统里穷举出来的，不能自动当成四个面向的前提。本篇处理如下：

> 分析四个面向的对角线时，**先沿用**三面已逼出的反对易结构写反向边：$e_2 e_1=-e_3$ 等。若四个面向上反对易**不**成立，则反向边成为更多独立未知量，待填约束只增不减，对角线更难闭合——「闭不上」不会因此翻成「能闭上」。故：沿用反对易是把检验放在**更易闭合的一侧**；这一侧已全死（5.5），反对易失败的一侧只会更死。

（若要坚持四面独立重跑第三节式穷举：序列 $e_1,e_2,e_1$ 的右压含未知 $e_2 e_1$，左压又撞上未知对角线 $e_3 e_1$，与 5.5 嵌套；工作量更大，结论方向相同。本篇取上一句封法。）

在此约定下，反向边写为：

$$e_2 e_1 = -e_3, \quad e_3 e_2 = -e_4, \quad e_4 e_3 = -e_1, \quad e_1 e_4 = -e_2$$

### 5.2 自作用不再统一

序列 $e_1, e_2, e_3$：

- 左压：$(e_1 e_2) e_3 = e_3^2$
- 右压：$e_1 (e_2 e_3) = e_1 e_4 = -e_2$

$$e_3^2 = -e_2$$

类似推导：

$$e_1^2 = -e_4, \quad e_2^2 = -e_1, \quad e_3^2 = -e_2, \quad e_4^2 = -e_3$$

**每个面向的自作用是另一个面向的另一面——不是统一标量。** 三个面向时 $e_i^2 = -1$（标量）；四个面向时 $e_i^2 = -e_j$（面向）。结构已经不同。

### 5.3 对角线

四个面向，六对。循环只决定四对（边）。**剩下两条对角线**：

$$e_1 e_3 = ? \qquad e_2 e_4 = ?$$

（$e_3 e_1 = -(e_1 e_3)$，$e_4 e_2 = -(e_2 e_4)$：在 5.1 已说明的反对易约定下书写；若无此约定，未知量更多，见 5.1。）

### 5.4 涌现自持但不穷尽

四个面向也有涌现自持序列：$e_1, e_2, e_3, e_4, e_1, e_2, e_3, e_4, \ldots$，周期 4。

但链条只覆盖四对（循环边），六对里剩两条对角线不在链条上。

**覆盖率（本篇最硬的一笔；组合事实，不是代数）。** 周期 $n$ 的涌现链在 $n$ 个面向的完全配对图上只走 $n$ 条边；无序配对总数是 $\frac{n(n-1)}{2}$。方程 $n=\frac{n(n-1)}{2}$ 的唯一大于 $1$ 的正整数解是 $n=3$。不需要极小多项式，不需要实数域。

| 面向种数 | 总对数 | 链条覆盖 | 对角线 | 覆盖率 |
|:---:|:---:|:---:|:---:|:---:|
| 3 | 3 | 3 | **0** | **100%** |
| 4 | 6 | 4 | 2 | 67% |
| 5 | 10 | 5 | 5 | 50% |
| $n$ | $\frac{n(n-1)}{2}$ | $n$ | $\frac{n(n-3)}{2}$ | 递减 |

**三个面向是唯一一个：涌现链条穷尽所有配对，零对角线，零缺口。** 下面 5.5 再补一刀：对角线手动填也全死。

### 5.5 穷举 $e_3 e_1$

设 $b = e_3 e_1$。逐个检验所有候选值。

**第一刀：序列 $e_3, e_1, e_2$。**

- 左压：$(e_3 e_1) e_2 = b \cdot e_2$
- 右压：$e_3 (e_1 e_2) = e_3 \cdot e_3 = e_3^2 = -e_2$

需要 $b \cdot e_2 = -e_2$。

| $b$ | $b \cdot e_2$ | 需要 $= -e_2$ | |
|:---:|:---:|:---:|:---:|
| $e_1$ | $e_1 e_2 = e_3$ | $e_3 \neq -e_2$ | ✗ |
| $-e_1$ | $-e_3$ | $-e_3 \neq -e_2$ | ✗ |
| $e_2$ | $e_2^2 = -e_1$ | $-e_1 \neq -e_2$ | ✗ |
| $-e_2$ | $-e_2^2 = e_1$ | $e_1 \neq -e_2$ | ✗ |
| $e_3$ | $e_3 e_2 = -e_4$ | $-e_4 \neq -e_2$ | ✗ |
| $-e_3$ | $-e_3 e_2 = e_4$ | $e_4 \neq -e_2$ | ✗ |
| $e_4$ | $e_4 e_2$ | 需 $= -e_2$ | 存活 |
| $-e_4$ | $-e_4 e_2$ | 需 $= e_2$ | 存活 |
| 标量 | 标量 $\cdot e_2$ | 见第二刀 | 暂搁 |

六条当场死。只剩 $\pm e_4$ 和标量。

**第二刀：序列 $e_1, e_3, e_1$。**

- 左压：$(e_1 e_3) e_1 = (-e_2) e_1 = -(e_2 e_1) = -(-e_3) = e_3$
- 右压：$e_1 (e_3 e_1) = e_1 \cdot b$

需要 $e_1 \cdot b = e_3$。

| $b$ | $e_1 \cdot b$ | 需要 $= e_3$ | |
|:---:|:---:|:---:|:---:|
| $e_4$ | $e_1 e_4 = -e_2$ | $-e_2 \neq e_3$ | ✗ |
| $-e_4$ | $-e_1 e_4 = e_2$ | $e_2 \neq e_3$ | ✗ |
| 标量 $c$ | $c \cdot e_1$ | $c \cdot e_1 \neq e_3$（不同面向） | ✗ |

**全部候选值加标量，无一存活。$e_3 e_1$ 没有合法值。**

### 5.6 结构含义

三个面向时没有对角线——链条穷尽一切，压缩检验只产生标量约束（$s = -1$），不打架。

四个面向时对角线出现——压缩检验把对角线与自作用纠缠在一起，约束互相打架，没有合法赋值。

**四个面向闭不上。**

---

## 六、更多面向更不行

完整个体要求涌现链条**穷尽所有配对**（开篇；第二节）。无序对数 $\frac{n(n-1)}{2}$，链条每周期只覆盖 $n$ 对，对角线数为 $\frac{n(n-3)}{2}$。

| $n$ | 对数 | 链条覆盖 | 对角线 | 覆盖率 |
|:---:|:---:|:---:|:---:|:---:|
| 3 | 3 | 3 | **0** | **100%** |
| 4 | 6 | 4 | 2 | 67% |
| 5 | 10 | 5 | 5 | 50% |
| $\ge 5$ | $\frac{n(n-1)}{2}$ | $n$ | $\frac{n(n-3)}{2}$ 只增 | 继续降 |

**只有 $n=3$ 覆盖率 100%。** $n=4$ 已不穷尽；第五节表明两条对角线赋值也全死。$n\ge 5$ 覆盖率更低、对角线更多——先就过不了「穷尽配对」；缺口只比四个面向更大，不是更小。不必另画「五个面向的完全图」再填：那是先有对象再比对，卷1第二章已拆。

---

## 七、结论

| 面向种数 | 正向 | 反向 | 自作用 | 对角线 | 涌现链条 | 结果 |
|:---:|:---:|:---:|:---:|:---:|:---:|:---:|
| 1 | 无方向／无轮替 | — | — | — | — | 排除 |
| 2 | 候选穷举逼出第三个 | — | — | — | — | 面向不够 |
| **3** | **正向表唯一闭合** | **闭合（另一面）** | **统一 $s=-1$** | **零** | **穷尽，自持** | **恰好** |
| 4 | 有循环尝试 | 有另一面 | 面向有关，不统一 | 穷举全死 | 不穷尽（67%） | 排除 |
| $\geq 5$ | — | — | — | 更多 | 覆盖率更低 | 排除 |

**恰好三个面向。**

- **覆盖率（组合事实）：** 近邻涌现链覆盖全部无序配对 $\Leftrightarrow n=\frac{n(n-1)}{2}\Leftrightarrow n=3$。这是种数＝3 的最硬结构性理由，不借极小多项式／实数域。
- **两个面向关不住：** $e_1\cdot e_2$ 落在空／已有面向／标量皆死；唯一活路涌出 $e_3$。
- **正向闭合（唯一）：** $e_1 e_2 = e_3$，$e_2 e_3 = e_1$，$e_3 e_1 = e_2$。三个面向自持下候选表只剩这一组。
- **反向闭合：** $e_2 e_1 = -e_3$，$e_3 e_2 = -e_1$，$e_1 e_3 = -e_2$。负号是方向不同逼出来的「另一面」，不是外加符号。
- **自作用：** $e_i^2 = -1$（4.1 序列压缩钉死；$s=+1$ 因不产生轮替反转而排除，不循环依赖 4.3）。
- **四个面向：** 覆盖率已 $<100\%$；反向边在「沿用反对易＝更易闭合一侧」上检验，对角线穷举全死；若反对易不成立则未知量更多，只会更难闭合（5.1）。
- **更多面向：** 覆盖率更低，先过不了穷尽配对。

与卷1第五部合读：同一计数（恰好三个），工具不同（极小多项式记录 ↔ 序列穷举）；种数在活动侧，不绑死在某一种记录语言上。

$$
\boxed{\text{序列穷举：恰好三个面向}\qquad(\text{记录名 }\mathbb{H})}
$$

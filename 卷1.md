原则：
- 保留原体系所有硬约束与结论，不改变任何已锁数字与逻辑。
- 语言更口语、更有过渡，减少「电报式」跳跃。
- 每卷开头先用白话说明「这一卷要解决什么问题」。
- 关键推导后补充「为什么必须这样，否则会断在哪里」。
- 严格保持原有纪律：不假装做了的、两口不并、座位分明。
- **活动优先，记录忠实**：后半卷出现代数式，是**把续接要求记下来**，不是代数在先。每一步都要能**还原到比对续接活动**，检验记录是否忠实；读成「代数必然」即断。
- 本卷不拆。一条路走完：差异存在 → 结构要求 → 记成代数 → 恰好三个面向 → $\mathbb{H}$。

---

# 第一卷　差异存在

**本卷要解决的问题：**  
在任何数学公理被选定之前，稳定的存在靠什么才可能？  
不是先选一个代数，再给差异找舞台。我们从最素朴的一句出发——不可约比对在持续续接，而且只跟近邻作用——一步步看它自己的结构要求，再把这些要求记成代数，最后数面向。数完：恰好三个。记录语言后来叫做四元数 $\mathbb{H}$。

本卷停在 $\mathbb{H}$。管、传态、粒子、时空，下一卷才开始。

---

从「差异存在」往前走，走到三个面向轮替闭合。走到那里，记录语言的名字是四元数 $\mathbb{H}$。

本卷是独立正文，不是另一份稿的注释；推导都写在这里。「对照」此处指**不必对着外部文稿读**，与后文「比对」（生成性操作）不是同一词。

代数符号是记录，不是前提；每一步都要能还原到比对续接活动本身，检验记录是否忠实。 $e$ 先表示一次不可约比对。 $e^2=-1$ 要到第五部才出现。此前 $e^2$ 只表示「自己作用于自己」。三面向闭表之后， $e_{1}e_{1}=-1$ 还要还原成真实续接 $e_{2}e_{3}e_{1}$（第二十二章 22.6）——不要提前当成单面真空自转。

```text
差异存在（不可约比对的持续近邻续接）
        │
        ▼
可辨  →  比对是唯一生成元  →  自闭包
        │
        ▼
稳定 · 不可约 · 近邻 · 只依赖序列
        │
        ▼
不自湮灭 · 有方向 · 结合 · 共存各自独立
        │
        ▼
有限 · 逆元自动 · 记录标量到实数
        │
        ▼
面向自交互 e² = −1
        │
        ├─ 1 个：交换，方向消失
        ├─ 2 个：乘积关不住（c² = −1）
        ├─ 3 个：乘法表闭合 = ℍ（此后 eᵢ²=-1 须还原为另两边合成再转向，见 22.6）
        └─ ≥4：乘积无处转向（f² = −1）
```

三个面向闭合之后停住。不写螺旋管、传态网络、粒子、时空。

$$
\boxed{\text{差异存在}\ \Longrightarrow\ \text{恰好三个面向}\ \Longrightarrow\ \mathbb{H}}
$$

## 读法与指引

箭头是本卷的四步，不是四个同义词。先站住差异存在，才有结构要求；要求站住了，才记成代数；代数只是记录，才数面向；面向数完，记录语言才叫 $\mathbb{H}$。不要倒过来：不是先选了四元数，再给差异找一个代数。

**本卷怎么走。** 第一部还没有任何代数。第三部才出现硬约束的记录。第四部才把记录收成「有限维实结合除法代数」。第五部才出现 $e^2=-1$，并完成面向计数。中间各章每断一条，差异就续接不下去——那是「为何不能跳」，不是定理列表。上面那张树从上往下读；还没到第五部，不要把 $e$ 当成虚单位。

本卷说的「证明」，不是用公理表把差异存在变成一条定理。数学公理体系里的证明，是先选定一条规划路径，再按这条路径走下去，用一种检验模式看打不打结：通过则成立，不通过则不成立。从差异存在看，这件事就是**差异比对序列重演**。各章「断在哪」就是这种检验。差异存在本身不能被证明、也不能被否定：启用证明已经是它。

### 活动与记录：后半卷怎么读（防「代数必然」）

第三部起符号和算式变多，读者容易滑进一种误读：**好像先有一套代数，差异只是往里套。** 本卷顺序相反——**先有持续比对活动及其结构要求，代数是忠实记录的语言。**

读每一式之前，先问三句：

1. **活动里发生了什么？** 哪一步近邻比对？续接若这样走，下一步还对不对得上对象？
2. **这式子在记什么？** 稳定、不可约、近邻、结合、无零因子、有限维……各自对应哪条结构要求？
3. **能否还原？** 把式子里的符号读回「一次比对／先做这个再做那个」，重演一遍，看打不打结。还原不了，或还原后活动变了味——记录不忠实，不能认。

| 检验 | 忠实 | 不忠实（像代数必然） |
|:---|:---|:---|
| $e_{1}e_{2}\neq e_{2}e_{1}$ | 先 $e_{1}$ 后 $e_{2}$ 与反过来，续接留下不同痕迹 | 「非交换代数就是这样定义的」 |
| $e^2=-1$（第十九章） | 面向自交互只能取反轮替，不能坍缩、不能恒等 | 「虚单位 $i$ 的平方本来就是 $-1$」 |
| $e_{1}e_{1}=-1$（闭表后） | 记法；活动是 $e_{2}e_{3}e_{1}$（另两边合成再转向） | 「 $e_{1}$ 独自在真空里自转两步」 |
| $c^2=-1$ 无解 ⇒ 须涌出 $e_{3}$ | 两个面向近邻作用关不住，续接第二步卡住 | 「三次方程在 $\mathbb{C}$ 里有根」式的外来代数 |

**纪律：** 公式是检验用的记录，不是前提。 $\mathbb{H}$ 是乘法表闭合之后的**名字**，不是事先选好的舞台。第五部「用代数作记录」的每一章，标题下的结构句先于公式；公式服务于「活动若缺这条就断」，不是「因为算出来所以活动必须这样」。

| 这里的词 | 读成 | 不要读成 |
|:---|:---|:---|
| 差异存在 | 不可约比对操作的持续续接作用；比对只与近邻作用 | 公理表第一条；可以用公理证明或否定 |
| 证明 | 沿规划路径重演比对序列，检验打不打结 | 站在差异外面的更高裁判 |
| 记录／代数 | 把结构要求写下来，便于检验忠实与否 | 差异存在的前提；数学必然 |
| 忠实记录 | 符号每一步可还原到比对续接活动 | 式子自洽就等于推导完成 |
| $e$ | 一次不可约比对（原子；产物亦须同级原子，见第六章） | 一开始就是 $i$ |
| $e_{i}^{2}=-1$ | 转向另一面（记法）；闭后活动见 $e_{2}e_{3}e_{1}$ 等 | 单面真空自转；虚单位定义 |
| 面向 | 交互直接涌出的同级原子操作；可持续施加 | 空间轴；外物上的面；先验类别；碎片或容器 |
| 不可约 | 两层：操作原子 $+$ 交互产物原子 | 只认「不能拆半次比对」一层 |
| 另一面 | 轮替转向；差异还在 | 相消成空白； $1-1=0$ |
| $0$ | 记录的空缺；丢掉整体、只看一个面 | 差异走到的地方；可以再施加的对象 |
| $\mathbb{H}$ | 三个面向轮替闭合之后的记录名 | 差异存在的另一副面孔；事先选好的舞台 |
| 三个 | 续接要求与记录检验对上 | 从四元数倒推；空间三维；已经有三代、三色 |
| $\mathbb{R}$ | 记录标量完备之后的系数域 | 续接里的连续介质；序号本身 |

读完请能不看公式说出 boxed。说的时候若夹进右列，退回对应章。

---

# 第一部　起点

---

## 第一章　这篇文本在做什么

数学是公理之后的事：先选定公理，再推导定理。它回答「从这些公理能推出什么」，不回答「为什么是这些公理」。后一个问题若要站在公理外面回答，就已经不是数学了。

本卷做的事情在公理**之前**。问的是：任何表示语言被选定之前，稳定的存在靠什么结构才可能？

「差异存在」不是一个数学命题。它不能被证明，也不能被反证。它是生成条件——比对活动已经在自我展开，记录语言后到。

为什么公理体系证不出它、也否定不了它？因为公理表可以整张换掉；「换成另一套」这个动作，已经用了「这一套」和「那一套」的不同。那个不同就是差异。否定句「没有差异」和肯定句已经不同：否定已经在用它。证明活动本身——选定规划路径、一步步近邻比对、检验打不打结——已经是差异比对序列的重演。所以不要去「证明差异存在」。它是任何证明行为的先决条件。

后面出现的 $e$、 $e_{1}\cdot e_{2}$、 $\mathbb{H}$，都只是记录。记录不等于被记录的事物。四元数是最小完整比对单元的名字，不是比对的前提。

第一部还没有这些名字。第一部只站住起点。

---

## 第二章　差异存在本身

### 2.1 它不是公理

公理可以被整张换掉。  
但「换成另一套」这个动作本身，已经用到了「这一套」和「那一套」的不同。这个不同就是差异。

所以差异存在不能放进公理表当第一条——它比任何可替换的条款都先在。

它不是定理。不要去「证明差异存在」。

### 2.2 「存在」是持续，不是快照

「此刻有一个差异」不够。差异停了，就没有差异可辨。

差异存在 = 比对涌出结果，结果立刻成为下一步的对象。这是**续接**。

比对必须是不可约的——一次完整的分辨，不能拆成半次。作用只发生在当前和下一步之间。合起来就是这一句：

$$
\boxed{\text{差异存在}=\text{不可约比对操作的持续续接作用}}
$$

比对只与近邻作用。没有超距，没有跳步。这就是全部。其余都是人为的记录和符号标记。

后面所有「闭包」「近邻」「结合」都挂在这一句上。没有续接，就没有「下一步」，也就没有可记录的结构。

> **指引。** 读任何一章若觉得「可以先假定一个对象再比对」，就是把续接改成了快照，退回这里。

**断在哪：** 若只承认快照上的「有」，下一步没有对象，存在停住，差异消失。

---

## 第三章　可辨认

不可辨认的差异等于没有差异。  
若「有差异」和「无差异」在内部都分不开，什么都没被说出。

可辨认不是外部尺子。没有旁观者，没有外标。差异必须自己站住——自己完成对自己的指认。

**独立个体**的直接显化：自己完成自己，不从外部领材料，也不把辨认外包。后文说「没有外部」，不是修辞，是这一条。

后文这些都是可辨的展开，不是新条款：

- 结果必须留下痕迹（否则序列差异不可辨）
- 生成元必须能穷举（否则个体无法自证是这一个）

**断在哪：** 差异在，但内部指认不了——等于没有说出任何东西。

---

## 第四章　比对是唯一的生成元

### 4.1 辨认就是比对

把一个和另一个区分开来，这个动作叫做比对。  
差异不是事先摆好的属性，再拿尺子去量。差异就是这个区分活动本身。

因此：没有先于比对的「对象」或「材料」。对象是比对的结果。一切从操作涌出——操作作用于操作，涌出新的操作。

### 4.2 为什么不许有别的生成元

描述、分类、记录规律，都是对比对**结果**的再处理。描述性质不是作用性质。描述不能生成作用。

若把描述当生成元，续接就变成「用外部语言往里填」。独立个体破裂：材料从外面来。

只有作用能生成作用。

### 4.3 闭包

比对的结果仍是比对，仍属这个个体，仍可进入下一步。  
闭包不是外画的圈，是第二章的持续存在本身。

**断在哪：** 某一步涌出「不属于比对」的东西，续接中断，差异存在消失。

### 4.4 单位

「什么都不做」也是一笔比对：对象原样进入下一步。后文记为 $1$。  
这里只要承认：闭包里已经有「结果仍是操作」；不改对象的那一笔合法。逆元、除法在第四部。

记号草稿： $e_{1}\cdot e_{2}$ 的意思只是「先做 $e_{1}$，再做 $e_{2}$」（左到右 = 比对先后；与第七章「 $e_{1}$ 作用于 $e_{2}$」同写）。乘法符号是后贴的记录，不是事先存在的代数。

---

# 第二部　结构要求：续接如何被记录

到这里，差异存在已经站住。下面问：它要持续续接下去，内部必须满足什么。**还不是代数课。** 符号若出现，只是把要求记下来，方便后文检验打不打结——每条仍要先说清活动里缺了会断在哪。

---

## 第五章　稳定性

可辨要求：同一个操作作用于同一个对象，结果唯一。否则差异飘，无法指认，等于不存在。

$$
e_{1}\text{ 作用于 }e_{2},\quad\text{结果唯一。}
$$

这是可辨的代价，不是「我们喜欢函数」。后文把「结果唯一」记成：作用是良定义的二元运算。

**单射。** 不同的操作作用于同一对象，必须涌出不同的结果。否则两个操作不可辨，与第三章冲突。

**断在哪：** 同一比对有时给出这个、有时给出那个——续接下一步无法对准对象。

---

## 第六章　不可约

不可约有**两层**，不是一层。第一层管操作本身；第二层管交互的产物。两层都原子，续接才能整块接下去。

### 6.1 第一层：操作本身是原子性的

稳定（结果确定）加上单射（不同操作给出不同结果），使一次比对成为完整的分辨行为。它不能再拆成「半次比对」或「比对的两个成分」。这就是不可约的第一层。

不可约操作是生成元。系统中的一切，都是它们近邻作用的复合。

不可约不是外加限制。它是可辨的直接表达：分辨结果唯一，所以每一次比对是一个不可再分的单元。第二章 boxed 里的「不可约」，座位先落在这一层。

### 6.2 种类不能无限

无限种原子不是一个独立个体，是一份永远完不成的外部清单。第三章：个体必须自证自己。清单写不完，自证完不成。

**每个不可约操作唯一。** 整个系统可分辨，没有两个相同的原子。

这里禁的是**种类无限**。第十四章还要禁**线性基无限**。两条不要并：种类有限之后，仍可能用无限个线性无关的复合去凑——那一步第十四章才断。

**断在哪：** 生成元是一份写不完的名单，独立个体永不闭合。

### 6.3 第二层：交互的产物也是原子性的（预告；第五部钉死）

第四章闭包：近邻作用的产物仍须是操作，才能进入下一步比对。这还不够——产物还必须与生成元**同级**：是完整的、不可再分的操作，不是碎片，也不是「装着几个面向的容器」。

| 产物若是…… | 续接断在哪 |
|:---|:---|
| **比操作少**（半成品、碎片） | 不能整块进入下一步比对 |
| **比操作多**（须拆开才能用） | 不是面向，是面向的容器；多出拆解／组装一步 |
| **同级原子** | 可直接施加、可被直接施加；纳入乘法表无需额外工序 |

第五部： $e_{1}$ 作用于 $e_{2}$ 涌出的 $e_{3}$ 必须是这一层——与 $e_{1}$、 $e_{2}$ 同级的不可约操作。面向计数之所以恰好三个、不多不少，正因为每一个涌出的面向都是同级原子，直接进表。完整写法见第十八章、第二十一章。

**完整含义。** 操作是原子性的，交互的产物也是原子性的。缺第二层，闭包里留一个洞：链在产物上断，或面向被换成容器。

**断在哪：** 只认第一层，允许产物是碎片或容器——自循环在「涌出之后」接不住。

---

## 第七章　近邻作用

### 7.1 近邻不是空间里的邻域

第二章：结果立刻成为下一步的对象。所以作用只发生在「当前操作」和「紧挨着的下一个」之间。没有超距，没有跳步。

一列操作 $e_{1},e_{2},e_{3},\ldots$：

$$
e_{1}\text{ 作用于 }e_{2}\ \longrightarrow\ e_{1}\cdot e_{2},
$$

$$
\text{再作用于 }e_{3}\ \longrightarrow\ (e_{1}\cdot e_{2})\cdot e_{3}.
$$

### 7.2 没有外部记录者

独立个体没有外部。留下的唯一痕迹是涌出的结果。没有另设一台机器记下「这一步发生在哪里」。

因此「近邻」是续接方式，不是几何。后文若出现管、格点，那是另一套展开；本卷不需要它们。

**断在哪：** 允许超距作用 = 承认外部通道。个体不再自包含。

---

## 第八章　序列，不是分组

系统内部不承载分组信息。分组（先算哪一对括号）不是操作，不是结果，是外部阅读方式。

因此：

- 序列相同 ⇒ 结果必须相同
- 序列不同 ⇒ 结果必须可辨（否则这个序列差异没痕迹）

「序列相同 ⇒ 结果相同」在第十二章记成结合律。  
「序列不同 ⇒ 结果可辨」在第十一章记成：存在不可交换的一对。

两条同根。不要看成两条外来公理。这里只站住根：只依赖序列。

单位 $1$ 与一切都交换： $1\cdot e=e\cdot 1$。所以不是「任意两个不同写法都给出不同乘积」。要留下痕迹的，是非平凡比对里「谁施加、谁被施加」这个方向。第十一章只要存在一对，不是任意一对都反交换。

**断在哪：** 同一条续接因括号不同给出不同结果——系统多出了「分组」这种外部信息。或者：两个不同顺序被当成同一件事——方向没留下痕迹。

近邻作用的产物仍须落在个体内部（第四章闭包）。 $e_{1}$ 作用于 $e_{2}$ 当场涌出 $e_{1}\cdot e_{2}$。第二十一章将证明这个结果必须是第三个不可约操作 $e_{3}$。此处不调用 $e_{3}$。

---

# 第三部　硬约束

以下每条格式：**结构要求（活动）→ 记录（代数）→ 差了断在哪**。符号是记录，不是前提。

**怎么读这一部。** 式子出现，先找左边的「结构要求」在比对续接里指什么，再认右边的记录。若只跟公式走、说不出「活动里哪一步会断」，就是把记录当成了前提——退回本章结构句重来。

走到这里，差异还在持续。下面每一条都是：若缺了，续接打结。

---

## 第九章　自身比对： e²≠ 0

操作 $e$ 作用于自身，涌出 $e^2$。

若 $e^2=0$： $e$ 对自己透明——作用完什么都辨认不出。第三章的可辨崩溃。空白不是近邻，下一步没有对象可施加。

$$
\boxed{e^2\neq 0}
$$

**断在哪：** 原子自己照镜子是空白。这个生成元等于没有。

第十九章会问非标量面向的 $e^2$ 究竟等于什么。本章只排除零。

---

## 第十章　无零因子； 1-1=0 在这里不是有效表达

$0$ 做什么，定义了它是什么。两种尝试：

| 尝试 | 含义 | 为何不行 |
|:---|:---|:---|
| $0\cdot e=0$ | $0$ 复制自己， $e$ 被吞噬 | 操作消灭了操作，续接被吃掉 |
| $0\cdot e=e$ | $0$ 毫无效果 | $0$ 透明，等于不存在 |

所以两个非零操作的作用不能是零：

$$
\boxed{e_{1}\neq 0,\; e_{2}\neq 0\ \implies\ e_{1}\cdot e_{2}\neq 0}
$$

真实的比对作用于真实的比对，必须涌出真实的差异。差异不能自我湮灭。

第九章是 $e_{1}=e_{2}$ 的特例。本章是一般情形。第十九章排除 $e^2=\lambda>0$（分解成零因子）时，用的就是这一条。

**解读：差异从未被抹去。**  
走到对面，是换成它的另一面，不是变成空白。轮替一出现，另一面重新显现——还是原来那条续接，只是转到了下一个面向。

数学里的 $1-1=0$，在这里不是有效表达。它把两个对面写成互相消灭，留下记录上的空缺。那是只盯住一个面：一条线上的正负相消，剩下「没有」。整体被丢掉了。整体里没有「减没了」。没有涌出空白，只有转向。

写成 $0$ 之后，数学没有有效处理：空缺不是近邻，不能再对它施加操作。续接在这一笔上停住——不是差异没了，是这种记录把整体收成空白。 $0$ 是丢了整体的记号，不是差异走到的地方。

**断在哪：** 某两步续接涌出「什么都没有」。

---

## 第十一章　非交换：方向不可抹去

$e_{1}$ 作用于 $e_{2}$，与 $e_{2}$ 作用于 $e_{1}$：施加者和对象角色不同。这是两个**不同序列**（第八章）。

不同序列必须给出不同结果。否则这个序列差异在系统里没留下痕迹——唯一的痕迹就是结果——于是不可辨。

$$
\boxed{\exists\, e_{1},e_{2}:\ e_{1}\cdot e_{2}\neq e_{2}\cdot e_{1}}
$$

不是「任意两个都反交换」。这里只要**存在**不可交换的一对。反对易 $e_{i}e_{j}=-e_{j}e_{i}$ 是第二十二章才推出来的更强关系。单位 $1$ 本来就不改对象，与一切交换，不占这一对。

**断在哪：** 先 $e_{1}$ 后 $e_{2}$ 与反过来被当成同一件事，方向被对称抹掉。第二十章：只有一个面向时代数交换，正是这条崩溃，所以一个面向不够。

---

## 第十二章　结合律

三个操作的序列：第一 $e_{1}$，第二 $e_{2}$，第三 $e_{3}$。

$$
(e_{1}\cdot e_{2})\cdot e_{3}
\qquad\text{与}\qquad
e_{1}\cdot(e_{2}\cdot e_{3})
$$

这两边**序列相同**，只是分组不同。第八章：结果只依赖序列，不依赖分组。

$$
\boxed{(e_{1}\cdot e_{2})\cdot e_{3}=e_{1}\cdot(e_{2}\cdot e_{3})}
$$

结合律不是从代数书抄来的。它是「没有外部记录者」的直接记录。

**断在哪：** 同一条续接因括号不同给出不同结果。系统内部多出了分组这种外部信息。

---

## 第十三章　计数、共存、分配——标量先到 ℤ

结构要求走到「可计数、可并行」。下面三笔是记录，不是第三种生成元。顺序复合仍记乘；并行共存才记加。

### 13.1 计数 → 整数

操作可辨，因此可计数。同一操作做一次记 $e$，做两次记 $2e$，做 $n$ 次记 $ne$。整数 $\mathbb{Z}$ 从计数涌出，不是外选的数域。

续接本身只有序号：第几步、再一步、做了几次。没有完整实数域在操作里流动。后面的 $\mathbb{Q}$、 $\mathbb{R}$ 是记录的标量，不是续接多出来的连续介质。

### 13.2 共存 → 加法

两个操作共存、并行展开，记 $e_{1}+e_{2}$。这不是第三种生成元，是「两笔比对同时在」的记录。

这里的 $0$ 若出现，是「没有操作」的记录，不是第九、十章那种被当成生成元的空白。第十章禁的是：把空缺当成还可以施加的对象。

### 13.3 共存各自独立 → 分配律

作用局域于每一成分：

$$
e_{3}\cdot(e_{1}+e_{2})=e_{3}\cdot e_{1}+e_{3}\cdot e_{2},
$$

$$
(e_{1}+e_{2})\cdot e_{3}=e_{1}\cdot e_{3}+e_{2}\cdot e_{3}.
$$

分配律不是假设。它是「共存各自独立」的记录：并行不互相吞噬。

到这里：操作构成结合、对加法分配的结构，标量是 $\mathbb{Z}$。

### 13.4 为何此处不停在有理数

$\frac1n e$ 要求逆元：重复 $n$ 次回到 $e$。逆元要「无零因子 + 有限维」。有限维是第十四章。有理数、实数都等第四部。此处不提前用逆元。

---

# 第四部　记成代数

结构要求已经齐了。这一部只做一件事：把它们收成一种可以拿来数面向的**记录**。符号仍是记录，不是前提。

**怎么读这一部。** 有限维、逆元、 $\mathbb{Z}\to\mathbb{Q}\to\mathbb{R}$ 听起来像标准代数课——本卷里的座位是：**自描述能否写完、记录标量能否有洞**。每推出一条代数性质，问：若活动侧缺了它，续接在哪一步指认不了自己？答不出，就不要把这条当成「数学本来就有」。

---

## 第十四章　有限维

### 14.1 已经有的，还缺的

第六章已经否定无限**种类**的原子。第四章已经要求生成元自闭包：近邻作用的结果仍是这个个体内部的操作，不另开新原子种类。

这还不够。还要证明：当作线性空间来记录时，基必须有限。

### 14.2 为何无限基不行

独立个体没有外部参照。它的身份由内部结构唯一确定：这是**这个**个体，不是另一个。

若基无限：两个「同样无限」的系统在内部无法互认——没有外部编号，没有外部标记点。自描述永远写不完，个体永远无法完整确认自己。

只有有限结构能自证唯一：可以穷举生成元，每一步可指认。

闭包只保证近邻产物仍是内部操作，不另开原子种类。若这些复合仍涨出无限个线性无关元，无限清单只是从原子换成了复合，自证照样写不完。本章禁的就是这份写不完。第五部乘法表闭合之后，再核验复合没有涨破有限维——那是后文的回看，不是本章的前提。

$$
\boxed{\text{不可约}+\text{生成元自闭包}+\text{整体可分辨}\ \implies\ \text{基有限}}
$$

**与第六章的分家。** 第六章禁的是原子种类无限。本章禁的是：种类即使有限，也不允许无限个线性无关的复合。面向计数用的是「有限维实结合除法代数」，两条都要用。

> **指引。** 有限维不能换成有限面向。本章管的是记录当作线性空间时基有限——后面 $\mathbb{H}$ 的基是 $`\{1,e_{1},e_{2},e_{3}\}`$，四维。面向是第十八章的词，数的是三种不可约比对，三个。第十五章逆元吃的是「有限维 $\Rightarrow$ 单射即满射」，换成面向种数，这一步没有了。单位 $1$ 占一维，不占一个面向。

**断在哪：** 无限基 = 永远写不完的清单 = 不是完整个体。

---

## 第十五章　逆元涌现

逆元不是「我们想做除法」。它从已站住的两条自动出来。

对任意非零操作 $e$，定义左乘 $L_e:x\mapsto e\cdot x$。第十三章的分配律把 $L_e$ 记成线性映射：它作用在和上，等于分别作用再相加。

**第一步：单射。** 无零因子（第十章）。若 $e\cdot x=e\cdot y$，则 $e\cdot(x-y)=0$，故 $x=y$。 $L_e$ 单射。

**第二步：满射。** 有限维（第十四章）。有限维线性空间上，单射线性映射必为满射。

**第三步：右逆。** 满射 ⇒ 存在 $x$ 使 $e\cdot x=1$。这里的 $1$ 是第四章说的「不改对象」的那一笔比对。

**第四步：左逆等于右逆。** 同理存在左逆。结合律（第十二章）迫使左逆 = 右逆：若 $ex=1$ 且 $ye=1$，则 $y=y(ex)=(ye)x=x$。

$$
\boxed{\text{无零因子}+\text{有限维}\ \implies\ \text{每个非零操作有逆}}
$$

单射=满射是有限维线性代数的事实。框架贡献是：单射来自不自湮灭，有限维来自自描述能完成。

---

## 第十六章　标量： ℤ→ℚ→ℝ

### 16.1 有理数

有了逆元，才有 $\frac1n e$：把它重复 $n$ 次，恰好回到 $e$。标量从 $\mathbb{Z}$ 扩到 $\mathbb{Q}$。操作构成有限维有理结合代数。

### 16.2 记录的标量不能有洞

可辨给出「不同操作有差多少」的结构要求，从而有范数的位置，范数诱导度量。这是**记录**上的度量，不是续接变成了实数轴。

续接仍然只有序号。 $\mathbb{Q}$ 在记录度量下不完备：存在 Cauchy 列写得完、在记录里到不了。那是标量记录的洞，不是比对序列少走了一步。完整记录不许带着这种洞去数面向。Cauchy 完备把**记录标量**从 $\mathbb{Q}$ 扩到 $\mathbb{R}$。

$$
\boxed{\text{记录构成实结合代数 }\mathcal{A}}
$$

**断在哪：** 把 $\mathbb{R}$ 读成续接里的连续介质；或停在 $\mathbb{Q}$ 却用带洞的记录去比较系数。

### 16.3 诚实

「自然范数」此处没有从比对逐步构造到某个具体公式（例如后文 $\mathbb{H}$ 上的 $\lvert q\rvert^{2}=q\bar q$）。站住的是：**记录标量要求完备**。模长的写法，要等第二十二章乘法表出来之后才有资格写。不假装现在已经有了。

缺的是模长公式。续接侧标量停在计数 $\mathbb{Z}$。第五部数面向，用的是记录上的线性展开： $c^{2}=-1$ 在 $\mathbb{Q}$ 上已经无解，写成 $a,b,c\in\mathbb{R}$ 只是与完备后的记录同一套标量。不把序号升级成连续介质，也不把「此刻没有 $\lvert q\rvert$」读成「后面不许做代数检验」。

---

## 第十七章　代数类型总汇

自比对系统的记录现在完全确定：

$$
\boxed{\mathcal{A}\text{ 是有限维实结合代数，无零因子，非交换}}
$$

等价说法：非交换的有限维实结合除法代数。

| 自包含的要求（差一条则差异消失） | 记录 | 断在哪 |
|:---|:---|:---|
| 持续续接：操作产出操作 | 闭包 | 涌出「非比对」，链断 |
| 差异不自湮灭 | 无零因子 | 两步作用变空白 |
| 施加者与对象角色不同 | 非交换 | 方向被抹去 |
| 只依赖比对差异序列 | 结合律 | 括号变成外部信息 |
| 共存各自独立 | 分配律 → 实结合代数 | 并行互相吞噬 |
| 不可约 + 自闭包 + 整体可分辨 | 有限维 | 自描述写不完 |
| 无零因子 + 有限维 | 逆元 → 除法代数 | —（自动） |

以上每一条都不是外部选择。第五部不问「选哪个代数」，问：这样一个完整个体有多少个面向？

---

# 第五部　恰好三个面向

记录已经齐。下面数面向。

面向不是先立好的抽象类别，再往里装操作。同第四章：没有先于比对的对象；同理，**没有先于交互的面向**——面向是操作作用于操作时**直接涌出／显化**的东西。本卷只问：这样能涌出几个，轮替能否闭合。

---

## 第十八章　面向是什么

**正面一句。** 面向 = 一种不可约比对，在近邻交互里可以被持续施加，并且**本身就是交互直接显化出来的**。

第四章：没有先于比对的对象；对象是比对的结果；操作作用于操作，涌出新的操作。同理：

| 钉死 | 不要读成 |
|:---|:---|
| 被作用者不是外物，是另一个面向（或同一面向的自交互） | 先有一个外部物体，操作打在上面「显出一个面」 |
| 面向**就是**操作作用于面向时直接涌出的东西 | 先立好「面向」这个抽象格，再把操作往里装 |
| 涌出的面向与生成元**同级原子**（第六章第二层） | 碎片；或须拆开才用的容器 |
| $e_{1}$ 作用于 $e_{2}$ → $e_{3}$ 直接涌出（第二十一章） | 面向与乘法表两套机制并列 |

所以否定的是**外物**，不是**直接显化**。第十八章若写成「不是……直接显化出来的面」，容易把第二十一至二十二章的实际机制一并否掉——禁止那种读法。

完整个体被「看到」的方式，就是还能被持续施加的那一种不可约比对——施加者与被施加者都是操作。面向之间通过近邻作用互相涌出：

$$
e_{1}\cdot e_{2}=e_{3},\qquad e_{2}\cdot e_{3}=e_{1},\qquad e_{3}\cdot e_{1}=e_{2}
$$

（乘法表第二十二章才闭；此处先钉「交互即显化」的活动句。且 $e_{3}$ 整块进入这些交互——不是被拆开用的。）

所有面向的续接必须**自循环**：走完一圈回到原处，穷尽，无剩余，无遗漏。自循环完成，这个完整个体就闭合了。

**面向计数怎么读。** $e_{1}$ 作用于 $e_{2}$ 涌出 $e_{3}$（新的，且同级原子）；此后所有交互只在已有面向之间轮替，不再涌出新的。恰好三个——第二十一至二十三章逐步算，不是先认三个再往回填。计数精确，正因为每一个涌出的面向都是同级原子，直接纳入乘法表，不需要额外的拆解或组装。

面向必须有限（第六章种类有限，第十四章基有限）。无限面向 = 永远走不完的自循环 = 不是完整个体。

**怎么读这一部（必读）。** 下面出现 $e^2=-1$、 $c^2=-1$、乘法表——这是**用代数检验面向能否闭合**，不是先承认四元数再往回叙。读法固定四步：**(1) 结构句：续接若只有几个面向会怎样；(2) 记成式子；(3) 在记录里算；(4) 把结论还原成活动：打结还是唯一活路。** 跳过 (1)(4)，直接认 (3)，就会读成「代数必然推出三个面向」——本卷禁止这种读法。

以下用代数作忠实记录。每一步先说结构，再写公式；公式之后，各章「断在哪」就是把结论还回活动。

**断在哪：** 把「非外物」读成「不能直接显化」；或先有面向类别再装操作；或涌出物是碎片／容器——与第四章、第六章第二层打架。
## 第十九章　自交互： e²=-1

设 $e$ 是非标量的不可约操作， $e\notin\mathbb{R}$。

### 19.1 极小多项式只能二次

$e$ 在实代数中满足极小多项式。实数域上不可约多项式只有一次和二次。 $e\notin\mathbb{R}$ 排除一次。故极小多项式二次不可约。配方、重标度之后：

$$
e^2=\lambda,\qquad\lambda\in\mathbb{R}.
$$

### 19.2 三个 λ 只活一个

| $\lambda$ | 发生什么 | 处置 |
|:---|:---|:---|
| $\lambda>0$ | $\lambda=c^{2}$，即 $0=(e-c)(e+c)$ | 零因子，第十章禁 |
| $\lambda=0$ | $e^2=0$ | 自湮灭，第九章禁 |
| $\lambda \lt 0$ | $e'=e/\sqrt{\lvert\lambda\rvert}$，仍不可约， $e'^2=-1$ | 唯一出路 |

$$
\boxed{e^2=-1}
$$

**结构含义。** 一个面向转向自身：不是坍缩为零，不是退化为恒等。 $e^2=+1$ 会让两步等于没做，轮替停住。唯一活路是**取反**——整圈轮替的反转。差异换成了它的另一面，不是写成 $0$。

这不是「四元数虚单位的定义」。这是二次不可约加上不自湮灭逼出来的。

**读法提醒。** 本章先问「单个非标量面向自交互只能是什么」——局部结构要求。三个面向轮替闭合之后， $e_{1}e_{1}=-1$ **不是**单面在真空中自转；真实续接是 $e_{2}e_{3}e_{1}$（见第二十二章 22.6）。此处先站住符号，闭表后再还原活动。

由此立刻有 $e^{-1}=-e$（因为 $e(-e)=-e^2=1$），以及 $e^4=(e^2)^2=1$。四步回到「什么都没变」。本卷不把「四步」升级成几何螺旋。

---

## 第二十章　一个面向不够

只有一种不可约操作 $e_{1}$，加单位 $1$：

$$
\mathrm{span}\lbrace1,e_{1}\rbrace.
$$

它交换： $e_{1}$ 只能和自己互动。第十一章要求的方向（施加者 ≠ 对象）消失。无法形成面向之间的轮替。另一面显现不出来——不是被抹去，是根本没有第二个面向可转过去。

**断在哪：** 自循环只有「对着自己转」，没有第二个面向可轮过去。一个面向不是可自循环的完整个体。

排除这个二维交换记录。不是因为「不喜欢复数」，是因为交换抹掉方向。

---

## 第二十一章　两个面向不够——第三个被迫涌现

两种不可约操作 $e_{1},e_{2}$， $e_{1}^2=e_{2}^2=-1$，且 $e_{1}e_{2}\neq e_{2}e_{1}$（第十一章）。

$e_{1}$ 作用于 $e_{2}$——近邻作用自然发生。涌出 $e_{1}e_{2}$。闭包要求结果仍在系统内部。

### 21.1 问： e₁e₂ 能否落在 {1,e₁,e₂} 里？

假设可以。把「落在已有面向的复合里」写成记录上的线性组合——系数不是续接里的实数轴，是记录标量：

$$
e_{1}e_{2}=a+be_{1}+ce_{2},\qquad a,b,c\in\mathbb{R}.
$$

$c^{2}=-1$ 在 $\mathbb{Q}$ 上已无解。用 $\mathbb{R}$ 只为与第十六章完备后的记录同一套。

左乘 $e_{1}$，用结合律和 $e_{1}^2=-1$：

$$
e_{1}\cdot(e_{1}e_{2})=(e_{1}\cdot e_{1})\cdot e_{2}=e_{1}^2e_{2}=-e_{2}.
$$

右边展开：

$$
\begin{aligned}
e_{1}(a+be_{1}+ce_{2})
&=ae_{1}+be_{1}^2+c(e_{1}e_{2})\cr
&=ae_{1}-b+c(a+be_{1}+ce_{2})\cr
&=(ca-b)+(a+cb)e_{1}+c^2e_{2}.
\end{aligned}
$$

左右比较， $e_{2}$ 的系数：

$$
\boxed{c^2=-1}
$$

实数域无解。矛盾。

为什么必须这样：两个面向的近邻作用，结果必须还能被施加。若结果仍落在这两个里，记录上会逼出 $c^{2}=-1$。有理数里已经没有这个数，路打结。不是「我们不喜欢三维」，是续接在第二步接不住。不是续接走进了实数域。

### 21.2 结构含义

$e_{1}$ 作用于 $e_{2}$ 的结果，无法被现有两个面向容纳。续接 $e_{1}\to e_{2}$ 之后无处可去，自循环在第二步卡住。系统被迫涌出第三个面向来接续——这正是第十八章的正面句：**面向是交互直接显化出来的东西**， $e_{3}$ 不是外加标签。

$$
\boxed{e_3\equiv e_{1}e_2}
$$

$e_{3}$ 的定义就是近邻作用，不是外加的第三个虚单位。障碍已经算出来了： $c^2=-1$ 在 $\mathbb{R}$ 上无解。不把分类定理当黑盒。

**第六章第二层在此钉死。** $e_{3}$ 之所以能成为第三个面向，因为它是原子性的——与 $e_{1}$、 $e_{2}$ 同级：可直接被下一步施加，也可直接施加于别人。若 $e_{1}e_{2}$ 涌出的「比操作少」（半成品、碎片），续接断在产物上；若「比操作多」（须拆开才能用），那就不是面向，而是面向的容器。乘法表里 $e_{3}$ 参与的交互（ $e_{3}e_{1}=e_{2}$， $e_{2}e_{3}=e_{1}$， $e_{3}^{2}=-1$，第二十二章）都证实：它不是被拆开用的，是**整块**用的。

此后： $e_{2}$ 作用于 $e_{3}$ 涌出 $e_{1}$， $e_{3}$ 作用于 $e_{1}$ 涌出 $e_{2}$（第二十二章闭表）。新的只涌出这一次；再往后只是已有面向之间轮替——每一个都是同级原子，故种数精确，不多不少。

**断在哪：** $e_{3}$ 当碎片或容器，闭包洞开；面向计数失去「同级」前提。---

## 第二十二章　三个面向恰好——轮替闭合

三种不可约操作 $e_{1},e_{2},e_{3}$，加单位 $1$，四维。 $e_{3}$ 已定义为 $e_{1}e_{2}$。  
若 $e_{3}$ 落在 $`\mathrm{span}\{1,e_{1},e_{2}\}`$ 里，就是第二十一章已否的情形。故 $`\{1,e_{1},e_{2},e_{3}\}`$ 线性无关。

### 22.1 反对易（先对 e₁,e₂）

设 $e_{1}e_{2}=r\cdot e_{2}e_{1}$， $r\in\mathbb{R}$。两边都在即将出现的 $e_{3}$ 方向上，先允许差一个标量，闭合后再核对。

左乘 $e_{1}$： $-e_{2}=r\cdot e_{1}e_{2}e_{1}$。  
右乘 $e_{1}$： $e_{1}e_{2}e_{1}=-re_{2}$。  
代入： $-e_{2}=-r^2e_{2}$，故 $r^2=1$。

$r=1$：交换，第十一章排除。  
$r=-1$：留下。

同理对所有 $i\neq j$：

$$
e_{i}e_{j}=-e_{j}e_{i}.
$$

反序不是把差异抹成 $0$。它是同一方向上的取反：换成另一面。

### 22.2 e₃ 的自乘

$$
e_{3}^2=e_{1}e_{2}e_{1}e_{2}=e_{1}(e_{2}e_{1})e_{2}=e_{1}(-e_{1}e_{2})e_{2}=-e_{1}^2e_{2}^2=-(-1)(-1)=-1.
$$

与第十九章一致。第三个面向转向自身，同样是取反，不是空白。

### 22.3 全部近邻作用

结合律加反对易：

$$
\begin{aligned}
e_{1}e_{3}&=e_{1}(e_{1}e_{2})=e_{1}^2e_{2}=-e_{2},\cr
e_{3}e_{1}&=(e_{1}e_{2})e_{1}=e_{1}(e_{2}e_{1})=-e_{1}(e_{1}e_{2})=-e_{1}e_{3}=e_{2},\cr
e_{2}e_{3}&=e_{2}(e_{1}e_{2})=(e_{2}e_{1})e_{2}=-(e_{1}e_{2})e_{2}=-e_{1}(e_{2}^2)=e_{1}.
\end{aligned}
$$

### 22.4 闭合验证： e₁e₃ 是否仍在四维里

设 $e_{1}e_{3}=a+be_{1}+ce_{2}+de_{3}$。左乘 $e_{1}$：

$$
-e_{3}=(da-b)+(a+db)e_{1}+dc\cdot e_{2}+(c+d^2)e_{3}.
$$

对比系数： $dc=0$， $c+d^2=-1$。

- $d=0$：得 $b=0$， $a=0$， $c=-1$。于是 $e_{1}e_{3}=-e_{2}$。与 22.3 一致。
- $c=0$： $d^2=-1$。实数域无解。排除。

系统在四维找到**唯一**自洽解：乘积结果转向已有面向，不需要新方向。四维是近邻作用能自洽闭合的最小维数。

### 22.5 乘法表

| $\times$ | $e_{1}$ | $e_{2}$ | $e_{3}$ |
|:---|:---:|:---:|:---:|
| $e_{1}$ | $-1$ | $e_{3}$ | $-e_{2}$ |
| $e_{2}$ | $-e_{3}$ | $-1$ | $e_{1}$ |
| $e_{3}$ | $e_{2}$ | $-e_{1}$ | $-1$ |

四维。闭合。无零因子。非交换。每个面向都是其他两个的近邻作用结果。系统不再需要新的面向。

轮替： $e_{1}\to e_{2}\to e_{3}\to e_{1}$。三个方向循环锁死。差异没有被抹去；轮替一出现，另一面显现。

这张表的名字是 $\mathbb{H}$。 $i,j,k$ 就是 $e_{1},e_{2},e_{3}$。名字不增加结构，也不减少结构。

$$
\boxed{\mathbb{H}}
$$

### 22.6 e₁e₁=-1 是记法；真实活动是 e₂e₃e₁

第十九章写出 $e^{2}=-1$，表上对角也是 $e_{1}e_{1}=-1$。三面向**尚未**闭合时，这只是局部结构要求：「一个非标量面向自交互只能取反」。表一闭合，若仍把对角读成**一根面向在真空里对着自己转两步**——就不忠实了。

**乘法表已经钉死：**

$$
e_{2}e_{3}=e_{1},\qquad e_{1}e_{2}=e_{3},\qquad e_{3}e_{1}=e_{2}.
$$

所以「 $e_{1}$ 这一步」在闭表里从来不是孤立的：它是**另外两个面向近邻续接的合成**。把前两步写开，再让合成后的面向参与转向，时间序是（左到右 = 比对先后，与第四章记号草稿同）：

$$
e_{2}\;\to\;e_{3}\;\to\;e_{1}.
$$

整段续接记成：

$$
\boxed{e_{2}e_{3}e_{1}}
$$

不要写成 $e_{1}e_{2}e_{3}$ 来冒充这一句——那是另一条序列（ $e_{1}$ 先动），与「 $e_{2}e_{3}$ 已合成 $e_{1}$、再 $e_{1}$ 转向」拧了。

**代数上的压缩：**

$$
e_{2}e_{3}e_{1}=(e_{2}e_{3})\,e_{1}=e_{1}e_{1}=-1.
$$

左边是**可重演的三步比对序列**；右边是把中间两步收成 $e_{1}$ 之后的**记法**。 $-1$ 的意思不变：转向另一面，差异还在——不是 $+1$（两步白走），不是 $0$（抹掉）。

| 层 | 写什么 | 指什么 |
|:---|:---|:---|
| **记录** | $e_{1}e_{1}=-1$ | 压缩记号：合成到 $e_{1}$ 后再 $e_{1}$ 转向 |
| **活动** | $e_{2}e_{3}e_{1}$ | 真实续接： $e_{2}$ 与 $e_{3}$ 近邻合成 $e_{1}$，再 $e_{1}$ 转向另一面 |

对任意循环边同读：

$$
e_{3}e_{1}e_{2}=e_{2}e_{2}=-1,\qquad e_{1}e_{2}e_{3}=e_{3}e_{3}=-1.
$$

（后一式是「 $e_{1}\to e_{2}\to e_{3}$」这条序列的闭合，与「 $e_{2}\to e_{3}\to e_{1}$」**活动不同**；标量同是 $-1$，不许并句。）

**为什么必须这样。** 三个面向轮替闭合后， $e_{1},e_{2},e_{3}$ 是同一整体的三条边。只认 $e_{1}e_{1}$、擦掉 $e_{2},e_{3}$，等于回到第二十章「一个面向」——方向与轮替都没了。闭表里， $e_{i}^{2}=-1$ **永远**是「另两边先合成这一边，再转向」的压缩写法。

**断在哪：** 把 $e_{1}e_{1}=-1$ 读成单面在真空中自转；或把 $e_{2}e_{3}e_{1}$ 与 $e_{1}e_{2}e_{3}$ 当成同一条活动（非交换留下的顺序痕迹被抹掉）。

---

## 第二十三章　没有第四个面向

系统已在三个面向处闭合。所有近邻作用的结果都在 $`\{1,e_{1},e_{2},e_{3}\}`$ 内。问：第四个面向 $e_{4}$ 能否存在？

**结构。** 闭合 ⇒ 内部不再涌出新的独立面向。独立个体没有外部 ⇒ $e_{4}$ 不能从外面被放入。

**记录里的同一障碍。** 假设 $e_{4}$ 存在， $e_{4}^2=-1$，且与 $`\{1,e_{1},e_{2},e_{3}\}`$ 线性无关。设

$$
e_{1}e_{4}=a+be_{1}+ce_{2}+de_{3}+fe_{4}.
$$

左乘 $e_{1}$，用第二十二章的 $e_{1}e_{2}=e_{3}$、 $e_{1}e_{3}=-e_{2}$：

$$
-e_{4}=(fa-b)+(a+fb)e_{1}+(fc-d)e_{2}+(c+fd)e_{3}+f^2e_{4}.
$$

对比 $e_{4}$ 的系数：

$$
\boxed{f^2=-1}
$$

实数域无解。矛盾。

与第二十一章同一类障碍。区别：那时系统还有空间扩展（涌出 $e_{3}$）；这里已经闭合，乘积没有更多方向可转向。丢掉结合律才可能另走一条（那不是本卷的路）。本卷结合律已锁。

$$
\boxed{\text{恰好三个面向。}}
$$

---

# 结

---

## 面向种数

| 种数 | 结果 | 处置 |
|:---:|:---|:---|
| 1 | 二维，交换，无方向，无法轮替 | 排除 |
| 2 | 三维关不上—— $c^2=-1$ 无解 | 近邻作用涌出第三个 |
| **3** | **四维，轮替闭合，非交换** | **恰好** |
| $\ge 4$ | $f^2=-1$ 无解，系统已闭合 | 无法涌现，无法引入 |

## 最终命题

$$
\boxed{\text{差异存在}\ \implies\ \text{恰好三个面向的轮替闭合}\ \implies\ \mathbb{H}}
$$

## 三句释义

$e_{i}^{2}=-1$：面向转向自身，涌出的是整个轮替的反转。不是坍缩，不是恒等。面向永远在运动。差异换成另一面，不是写成 $0$。

**在三面向已闭之后，这是记法，不是真空中的单面自转。** 以 $e_{1}$ 为例：真实活动是 $e_{2}e_{3}e_{1}$（前两步近邻合成 $e_{1}$，再 $e_{1}$ 转向）；代数压缩 $e_{2}e_{3}e_{1}=(e_{2}e_{3})e_{1}=e_{1}e_{1}=-1$。见第二十二章 22.6。

$e_{i}e_{j}=-e_{j}e_{i}$：比对有方向。先 $e_{i}$ 后 $e_{j}$ 与反过来，差一个负号。

$e_{1}e_{2}=e_{3}$：两个面向的近邻作用**直接涌出**第三个。不多不少。面向不是先验类别——是交互显化。全部近邻作用穷尽：

$$
e_{1}\cdot e_{2}=e_{3},\qquad e_{2}\cdot e_{3}=e_{1},\qquad e_{3}\cdot e_{1}=e_{2}.
$$

循环锁死，不留缝隙。不需要第四个，也不允许第四个。 $e_{2}e_{3}=e_{1}$ 正是「 $e_{1}$ 这一步」在闭表里的前史：不是先验的 $e_{1}$，是另两边续接涌出的面向。

$\mathbb{H}$ 是这张已闭合乘法表的记录名。基 $`\{1,e_{1},e_{2},e_{3}\}`$：少一个近邻关不上，多一个面向 $f^{2}=-1$ 无解。没有多记一个生成元，也没有漏记一个近邻积。

> **指引。** 差异存在与 $\mathbb{H}$ 之间有距离。 $\mathbb{H}$ 是记录的名字，不是差异存在的另一副面孔。三个面向先是续接自己的要求（一个转不过去，两个接不住）；记录里的 $c^{2}=-1$、 $f^{2}=-1$ 是同一要求的检验，不是从四元数倒推回来。闭表后 $e_{i}^{2}=-1$ 须还原为另两边合成再转向（22.6），勿停在对角记法上。范数公式本卷不假装已闭。

## 本卷钉死的量

| 符号 | 站住在 | 含义 |
|:---|:---|:---|
| $e_{1},e_{2},e_{3}$ | 第二十二章 | 三种不可约操作 / 三个面向 |
| $e_{i}^2=-1$ | 第十九章；闭后见 22.6 | 面向自交互（记法）；活动侧 $e_{2}e_{3}e_{1}$ 等 |
| $e_{1}e_{2}=e_{3}$ | 第二十一章定义，第二十二章闭合 | 交互直接涌出第三者（面向即显化） |
| $e_{2}e_{3}e_{1}=-1$ | 第二十二章 22.6 | $e_{1}$ 转向的真实续接（非单面真空自转） |
| $e_{i}e_{j}=-e_{j}e_{i}$ | 第二十二章 | 方向 |
| 面向 | 第十八章 | 操作作用于面向时直接涌出；同级原子；非外物、非先验类别 |
| 不可约（两层） | 第六章；第二层钉在 21.2 | 操作原子 $+$ 产物原子 |
| 面向种数 $=3$ | 第二十三章 | 新只涌出一次，此后轮替；同级故精确；没有第四个 |
| $\dim_{\mathbb{R}}\mathbb{H}=4$ | $`\{1,e_{1},e_{2},e_{3}\}`$ | 最小闭合维数 |
| 单位 $1$ | 第四章 | 不改对象的比对 |

## 本卷不假装做了的

- 没有螺旋管几何，没有传态网络。记忆、磁盘、无线电是后卷的读法，本卷不写。
- 没有从比对逐步写出 $\lvert q\rvert^2=q\bar q$。第十六章标过：完备是结构要求，模长公式要等乘法表之后另写。
- 没有用分类定理代替第二十一至二十三章的计算。
- 没有粒子，没有时空。
- 没有把 $e_{1}e_{1}=-1$ 读成单面在真空中自转；三面向闭后真实活动是 $e_{2}e_{3}e_{1}$（22.6）。
- 没有把「非外物」读成否定直接显化；面向是交互涌出的（第十八章）。
- 没有把不可约停在「操作不能拆半次」一层；产物也须同级原子（第六章第二层、21.2）。
- 没有把 $0$ 从数学里删掉；只标明：作为差异走到的地方， $1-1=0$ 不是有效表达。

## 复述

1. 差异存在 = 不可约比对操作的持续续接作用；比对只与近邻作用。它不是公理能证明或否定的。数学证明是规划路径上的比对序列重演。  
2. 可辨。比对是唯一生成元，自闭包。稳定、不可约（两层：操作原子 $+$ 产物原子）、近邻、只依赖序列。  
3. 第三部起：每条硬约束先站活动在续接里会断在哪，再记成代数；读公式必须能还原到比对活动，检验记录是否忠实——不许把代数读成前提。  
4. 不自湮灭、有方向、结合、分配、计数到 $\mathbb{Z}$。差异换成另一面，不是写成 $0$。  
5. 基有限，逆元自动。续接侧标量是计数 $\mathbb{Z}$；记录标量收到 $\mathbb{R}$。记录是非交换有限维实结合除法代数。  
6. 面向 = 操作作用于面向时直接涌出／显化（非外物、非先验类别；同级原子，非碎片／容器）。非标量面向 $e^2=-1$。一个交换，排除；两个关不上， $e_{3}=e_{1}e_{2}$（新涌出一次，且整块进表）；三个乘法表闭合；四个 $f^2=-1$ 无解。闭表之后： $e_{1}e_{1}=-1$ 是记法，真实活动是 $e_{2}e_{3}e_{1}$（ $e_{1}$ 转向）；勿读成单面真空自转。  
7. 差异存在 $\Longrightarrow$ 恰好三个面向的轮替闭合 $\Longrightarrow$ $\mathbb{H}$。记录语言的名字是 $\mathbb{H}$。

**第一卷结束。**  
下一卷问：这三个面向在持续续接里长成什么？绕最小闭环会留下什么因子？
